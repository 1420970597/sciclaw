# SciClaw Clone Execution Todo

## Status legend
- [x] completed
- [~] in progress
- [ ] pending

## Phase A — 基础初始化
- [x] 初始化 Next.js 16 + React 19 + TypeScript + Tailwind 4 项目骨架
- [x] 补齐 Dockerfile、docker-compose.yml、Vitest、ESLint、README
- [x] 建立 landing 页面首版与基础测试

## Phase B — Landing 高保真复刻（P0）
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：基于 sequential live/local browser QA、fresh-load browser vision 与 live/local DOM 几何对比，确认当前首要剩余问题仍是 left radial cluster 相较 live 过于显眼、局部 label/connector 竞争偏强，而顶部 utility triggers 已接近 subtle-but-readable 目标，因此本轮不扩张新模块，仅将共享 dock trigger 组继续保持在 `gap-[0.62rem]/sm:gap-[0.82rem]` 并轻收边框/阴影、仅微提前景对比，避免顶部 controls 再次偏重；同时把三枚 radial side nodes 微收并更靠近中心 preview 到 `left-[47.65%]/left-[48.1%]/right-[15.95%]`、将 connector widths 放宽到 `11.78%/11.86%/11.58%`，并同步微收 node ring 阴影、统一 inactive/active label 宽度到 `178px/148px`，再把 radial label typography 收敛为 `text-[10.8px]`、`leading-[2.06]`、`tracking-[0.03em]`，以继续削弱 cluster 的 noisy/detached 感并让 ghosted labels 更 calm。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load live/local browser QA 认为顶部 utility triggers 仍保持 subtle/readable 平衡，left radial cluster 也较上一轮更 anchored、稍少拥挤；但 radial cluster 仍是首屏最明显 fidelity hotspot，下一轮继续优先收敛 cluster 的 hierarchy / anchoring，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 landing Phase B fidelity slice：基于既有热点判断不扩张新模块，仅对顶部 utility dock triggers 做更 subtle 的存在感回收——将共享 dock trigger 组从 `gap-[0.62rem]/sm:gap-[0.82rem]` 微收至 `gap-[0.58rem]/sm:gap-[0.76rem]`，并同步轻收边框/阴影强度、仅微提图标前景对比（`border-[rgba(7,25,39,0.38)]`、更浅 shadow、`text-[#1c3d50]`），让顶部 controls 更贴近 live 的 low-emphasis 分组而不改动既已验证的 radial cluster / preview 结构。该里程碑严格按 TDD 先调整 `src/app/page.test.tsx` 对 dock trigger 间距的 focused 断言形成红灯，再完成 `src/components/landing-page.tsx` 实现；随后已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及双独立 reviewer 复审。最新 controller-side 结论：本轮改动未引入新的 clipping/contract 风险，顶部 utility triggers 进一步向 subtle/live 方向收敛；下一轮继续优先观察 landing hero 顶部 trigger 与 radial cluster 的相对权重是否还需微收，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：根据 sequential live/local browser QA、local browser DOM 几何复核与 focused test red→green，优先收敛 live 仍显著弱于中心 preview 的 left radial cluster，同时避免再次把顶部 utility triggers 做得过重——因此将共享 dock trigger 组从 `gap-[0.7rem]/sm:gap-[0.9rem]` 微收至 `gap-[0.62rem]/sm:gap-[0.82rem]` 并轻收边框/阴影对比，保持 subtle；再把三枚 radial side nodes 向中心 preview 微收至 `left-[47.85%] / left-[48.25%] / right-[15.9%]`，同步将 connector widths 收到 `11.42% / 11.5% / 11.24%`，并把 radial labels 统一收至 inactive `max-w-[180px]` / active `max-w-[152px]`、略提文字前景到 `#607988`，以减弱 cluster 的 detached 感与局部 label 碰撞；另外把 preview 底部 micro-label row 扩到 `gap-[4.52rem]/sm:gap-[4.68rem]`、稍增 horizontal padding 并前移 top padding，继续降低 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 认为顶部 utility triggers 比上一轮更不抢主视觉、bottom micro-label row 更稳；当前最明显剩余差距仍是 left radial cluster 本身虽然更靠近中心 preview，但相对 live 仍是首屏最弱 cohesion/readability 热点，下一轮继续优先做 radial cluster 的 label hierarchy / connector anchoring，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一次更聚焦的 landing Phase B 小步 fidelity slice：基于 sequential live/local browser QA、fresh-load browser vision 与本地 DOM 几何复核，确认当前首要剩余问题仍集中在 left radial cluster 的 detached/fragile microcopy，而不是继续放大顶部 utility triggers 或继续放宽底部 micro-label row；因此将共享 dock trigger 组从 `gap-[0.66rem]/sm:gap-[0.86rem]` 微收回 `gap-[0.62rem]/sm:gap-[0.82rem]` 并轻收按钮边框/阴影对比，避免顶部 controls 在空白角落中过度抢眼；再把三枚 radial side nodes 收到 `left-[47.85%] / left-[48.25%] / right-[15.9%]`，同步将 connector widths 收到 `11.42% / 11.5% / 11.24%`，并把 radial labels 统一放宽到 inactive `max-w-[180px]` / active `max-w-[152px]`、略提文字前景到 `#607988`，以加强 cluster→preview 锚定感并减弱 tiny label 的脆弱感；另外把 preview 底部 micro-label row 微收至 `gap-[4.52rem]/sm:gap-[4.68rem]`、稍增横向 padding 并前移 top padding，在不重回拥挤状态的前提下避免底部标签显得过于横向发散。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load live/local browser QA 认为顶部 utility triggers 现在接近 about-right 但仍略微抢眼、left radial cluster 比前一轮更 anchored、底部 micro-label row 也较前一轮更不 cramped；当前单一最明显剩余弱点仍是 left radial cluster 的 tiny text/connector 区域相对中心 preview 依旧偏 fragile/noisy，下一轮继续优先做 radial label hierarchy / connector anchoring / microcopy anti-crowding，而不是扩张新模块。
- [x] 持续推进 hero / utility controls / radial cluster / micro-label 的高保真小步收敛，保留既有执行记录与验证结论
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility controls 仍是低强调圆形控件、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：依据 sequential live/local browser QA，优先修正当前仍最明显的“顶部 controls 略偏过淡 + left radial cluster 文字/连线仍略 detached 且局部拥挤 + preview 底部 micro-label row 仍可再放松”三点，因此仅将共享 dock trigger 组从 `gap-[0.62rem]/sm:gap-[0.82rem]` 微放宽到 `gap-[0.66rem]/sm:gap-[0.86rem]`，并略收边框/阴影厚度同时保留更清晰图标前景，让顶部 controls 仍 subtle 但比上一轮更可辨识；再把三枚 radial side nodes 微向中心与 active preview 收到 `left-[48.2%] / left-[48.58%] / right-[15.45%]`，同步将 connector widths 扩到 `11.86% / 11.94% / 11.68%`，并把 inactive / active label 宽度统一收至 `172px / 146px`、前景提到 `#617987`，以加强 cluster→preview 锚定感并减少 label/connector 的局部拥挤；另外把 preview 底部 micro-label row 扩到 `gap-[4.76rem]/sm:gap-[4.94rem]`、略增横向 padding 并前移 top padding，继续缓解 `Meth. / Find. / Data / Evid.` 的紧张感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及 sequential live/local browser QA。最新 fresh-load local browser QA 认为顶部 utility controls 已比上一轮更 readable 但仍保持克制、preview 底部 labels 更舒展且无明显 overlap；当前最明显剩余差距仍是 left radial cluster 的文字/连线区域虽然更靠近中心 preview，但依旧是首屏最弱 cohesion/readability 热点，下一轮继续优先做 radial label hierarchy / anti-crowding，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：依据 fresh-load live/local browser QA 与 DOM 几何对比，仅对顶部 utility triggers 与 radial microcopy/readability 做收敛——将共享 dock menu triggers 间距从 `gap-[0.5rem] / sm:gap-[0.62rem]` 放宽到 `gap-[0.58rem] / sm:gap-[0.72rem]`，同时轻增边框/阴影/图标对比，让顶部 controls 仍保持 subtle 但不再过轻；并把 radial node labels 统一放宽至 `max-w-[124px]`、active 版 `max-w-[112px]`，同步把标签字号/leading/tracking 调整到 `text-[11px]`、`leading-[1.88]`、更轻 tracking，以减弱 cluster 内部微字发虚感；另外把 preview 底部 micro-label row 的柱图区高度从 `168px` 微收至 `164px`，增加 bar gap / horizontal padding，并将 label wrappers 提到 `min-h-[7.34rem]`、更轻 tracking / 更稳 leading，以继续降低 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为顶部 utility triggers 比 live 的过轻状态更易感知但仍保持克制，preview 底部 labels 较上一轮更可读；当前最明显剩余差距仍是 radial cluster 与中心 preview 的桥接感还略弱，cluster 整体相较中心 card 仍稍显 detached，下一轮继续优先做 cluster→preview cohesion，而不是再扩大顶部按钮存在感。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 仍比中心 preview 更轻、更像注释层而非强绑定模块、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：根据 fresh-load live/local browser QA 与 DOM 几何复核，将顶部 utility triggers 的共享 dock gap 从 `gap-[0.86rem] / sm:gap-[1.12rem]` 微收至 `gap-[0.82rem] / sm:gap-[1.08rem]`，并同步收轻阴影厚度、仅略提边框与图标对比，让顶部 controls 保持 subtle 但不再像悬浮覆盖层；同时把三枚 radial side nodes 进一步向中心 preview 收到 `left-[50.45%] / left-[51.2%] / right-[18.55%]`，将 connector widths 放宽到 `10.12% / 10.18% / 9.92%`，并把 radial labels 放宽到 inactive `max-w-[186px]` / active `max-w-[156px]`，配合 `gap-[0.92rem]` 与更稳的 `#587485` 前景色，以继续减弱 cluster 的 detached 感；另外把 preview 底部 micro-label row 扩到 `gap-[3.88rem] / sm:gap-[4.04rem]`、柱图区微收至 `156px`、label wrappers 提到 `min-h-[8.68rem]` / `max-w-[10.2rem]` / `leading-[1.22]`，继续缓解 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已按 TDD 更新 `src/app/page.test.tsx` 形成红灯后完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新 QA 认为 radial cluster 已较前更靠近中心 preview、utility controls 也更像低强调导航而非漂浮控件；当前最明显剩余差距仍是 radial cluster 的微字标签与底部 micro-label row 虽更稳但仍是首屏最弱可读点，下一轮继续优先收敛 radial label hierarchy / local anti-crowding，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 仍偏轻巧、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.78rem] / sm:gap-[1.02rem]` 微放宽到 `gap-[0.82rem] / sm:gap-[1.08rem]`，并同步收轻边框/阴影厚度、仅保留 subtle 的前景对比；同时把左上/左中/右侧 radial nodes 收到 `left-[56.15%] / left-[56.7%] / right-[17.3%]`，将 connector widths 放宽到 `7.12% / 7.18% / 6.98%`，并把 radial label 宽度提升到 `max-w-[144px]`、active `max-w-[126px]`，同步调整为 `text-[11.2px]`、`leading-[2.02]`、`tracking-[0.102em]` 与更亮的 `#42667b`，以在不打破首屏骨架的前提下提升 radial microcopy readability 与 cluster→preview bridge 感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、3 轮独立评审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 比上一轮更轻、更不抢主视觉，cluster 与中心 preview 的桥接感继续改善；当前最明显剩余差距仍是 radial cluster 的小字标签虽更清楚但依旧是首屏最弱可读点，下一轮继续优先做 radial label anti-crowding / local offset 微调，而不是扩张新模块。
- [~] 继续对齐头部、细节间距、轮播表现、图像/GIF 占位与 hover/active 状态；本轮已完成一个更小步的 landing hero fidelity slice：在不改动 `/root/nexus-mail` 任意服务/端口的前提下，仅于 `/root/sciclaw` 收紧 top utility triggers 的存在感与间距，轻推 radial cluster 靠近中心 preview 并增强 bridge 感，同时放松 preview 底部 micro labels 与正文下方 `Get started` 的垂直呼吸。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰现有业务端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审与 fresh-load live/local browser QA。最新 QA 认为 top utility triggers 更易感知、`Get started` 层级更干净、micro labels 比上一轮更不拥挤；当前最明显剩余差距收敛为 radial cluster 与主 preview 之间的横向桥接感仍略松，底部 micro labels 也还有轻微偏紧，下一轮继续优先做 cluster→preview cohesion 与 preview 内部微密度收敛，而不是大改整体骨架。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 仍是 subtle/lightweight 圆形 controls、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：基于 fresh-load live/local browser QA 与 DOM 几何复核，将共享 dock menu triggers 间距从 `gap-[0.94rem] / sm:gap-[1.24rem]` 微收到 `gap-[0.86rem] / sm:gap-[1.12rem]`，同步轻收按钮边框/阴影厚度但保留可辨识前景对比，使顶部三枚 utility controls 更接近 live 的 low-emphasis 紧凑分组；同时把三枚 radial side nodes 继续向中心 preview 收到 `left-[51.95%] / left-[52.55%] / right-[19.8%]`，并将 connector widths 放宽到 `9.44% / 9.5% / 9.22%`，再把 radial labels 统一放宽到 `max-w-[176px]`、active `max-w-[150px]`，同步调整为 `leading-[2.22]`、`tracking-[0.056em]` 与更亮的 `#617b8b`，以进一步减弱 detached 感并改善 microcopy readability；另外把 preview 底部 micro-label row 扩到 `gap-[3.5rem] / sm:gap-[3.64rem]`、label wrappers 提到 `min-h-[8.12rem]` / `max-w-[9.56rem]`，继续缓解 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 utility controls DOM 几何对比（local 45.59px vs live 38.25px，说明顶部控件仍略大于 live，但本轮重点已转回 cluster/readability）。最新 QA 认为 radial cluster 相对中心 preview 的 cohesion/readability 较上一轮确有改善、顶部 utility controls 仍保持 subtle；当前最明显剩余差距仍是 radial cluster 标签依旧比 live 更难读、preview 底部 micro labels 仍是最显眼未完全收敛的区域，下一轮继续优先做 bottom micro-label 密度与 radial cluster 标签层级，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 顶部 utility triggers 仍偏轻巧、不放大按钮本体的前提下，本轮对本地 landing hero 做了一次更聚焦的 cluster→preview cohesion 微调：将左上/左中节点分别从 `left-[60.9%]` / `left-[61.55%]` 收到 `left-[58.8%]` / `left-[59.55%]`，把右侧节点从 `right-[14.2%]` 收到 `right-[16.05%]`，并同步缩短三条连接线宽度（`10.55%→9.1%`、`10.9%→9.25%`、`10.72%→8.92%`），让 radial cluster 更靠近中心 preview、桥接感更强，同时维持 top utility triggers 的 subtle 尺寸与 hero→auth gutter 不失衡。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 radial cluster 现在更明显地桥接进中心 preview，未见明显 overlap/clipping，hero→auth gutter 仍可接受，top utility triggers 也仍保持适度克制；下一轮继续优先处理 preview 底部 micro labels 的轻微偏紧与首屏局部密度，而不是继续放大 utility controls。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 首屏 radial cluster 与中心 preview 之间仍略显松散、preview 底部 micro labels 也仍有轻微偏紧的前提下，本轮对本地 landing hero 做了一次更聚焦的 readability / cohesion 微调：仅在 `/root/sciclaw` 内将三条 radial connector 线的宽度进一步从 `9.1% / 9.25% / 8.92%` 收至 `8.42% / 8.54% / 8.28%` 并微调起点到 `50.22% / 50.2% / 50.08%`，以减弱 cluster→preview 之间的松散感；同时将 preview 底部 micro-label wrappers 从 `min-h-[6.48rem]` 提到 `min-h-[6.8rem]`、标签最大宽度从 `7.86rem` 放宽到 `8.34rem`、并轻收 tracking / line-height，让 `Meth. / Find. / Data / Evid.` 更易读。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任何现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中，以及双 reviewer 独立复审。最新 fresh-load local browser QA 认为 cluster→preview 关系与 micro-label 可读性均较上一轮有所改善，但 radial cluster 仍略显 detached、底部 label row 仍有轻微拥挤；下一轮继续优先做更小步的 cluster 锚定感与 micro-label 横向呼吸收敛，而不是改动更大骨架。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 首屏顶部 utility triggers 仍偏 subtle/lightweight、radial cluster 与中心 preview 仍存在可感知横向留白、preview 底部 micro labels 仍略显紧的前提下，本轮仅在 `/root/sciclaw` 内继续推进 landing Phase B 的小步 fidelity slice：把左上/左中/右侧三个 radial nodes 进一步向中心分别收至 `left-[57.7%]` / `left-[58.5%]` / `right-[17.1%]`，并把三条 connector widths 从 `8.42% / 8.54% / 8.28%` 缩到 `7.78% / 7.92% / 7.72%`，以增强 cluster→preview bridge 感而不放大顶部 utility 按钮；同时把 preview 底部 micro-label wrappers 从 `min-h-[6.8rem]` 提到 `min-h-[7.16rem]`、标签最大宽度从 `8.34rem` 放宽到 `8.68rem`，并进一步收紧字号/leading/tracking，减少 `Meth. / Find. / Data / Evid.` 的拥挤感且保持无 overlap。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 仍保持轻巧、cluster 比上一轮更靠近并更像在桥接中心 preview、底部 micro labels 也更少拥挤且未见 overlap；当前剩余最明显差距仍是 cluster 与 preview 之间仍有可见横向留白、micro labels 虽更稳但仍偏小，下一轮继续优先做更小步的 cluster 锚定感与 preview 底部呼吸优化，而不是改动更大骨架。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 顶部 utility triggers 仍偏 subtle、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：仅对顶部 utility trigger 存在感、radial cluster→preview bridge，以及 preview 底部 micro-label breathing room 做收敛——将共享 dock menu trigger 间距从 `gap-[0.58rem] / sm:gap-[0.72rem]` 轻放宽到 `gap-[0.62rem] / sm:gap-[0.8rem]`，并同步轻增边框/阴影/前景对比，让顶部三枚 utility controls 仍保持 subtle 但更易辨识；同时把三枚 radial nodes 进一步向中心 preview 收到 `left-[59.6%] / left-[60.2%] / right-[15.5%]`，并把三条 connector widths 放宽至 `8.52% / 8.58% / 8.44%`，强化 cluster→preview bridge 感；另外把 preview 底部 micro-label row 的列间距从 `2.86rem/2.98rem` 放宽到 `3.04rem/3.18rem`，并将 label wrappers 提到 `min-h-[7.48rem]`、`leading-[1.22]`、`max-w-[8.94rem]`，让 `Meth. / Find. / Data / Evid.` 更易读。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任何现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为顶部 utility triggers 较上一轮更可见、radial cluster 与中心 preview 的桥接感更清楚、底部 micro labels 可读性继续改善且无明显 overlap；当前最明显剩余差距仍是 radial cluster 的小字标签相对 live 仍略显 cramped、cluster→preview 的连接更像 implied bridge 而非强锚定，下一轮继续优先收敛 radial labels 清晰度与 cluster→preview cohesion，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 顶部 utility triggers 仍是 small-to-medium 的轻量圆形 controls、fresh-load live 视觉上 radial cluster 与中心 preview 依旧偏 detached、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu trigger 间距从 `gap-[0.62rem] / sm:gap-[0.8rem]` 微调到 `gap-[0.68rem] / sm:gap-[0.9rem]`，并轻收按钮阴影厚度、细增边框/前景对比，让顶部 utility controls 保持 subtle 但更接近 live 的轻巧分组感；同时把三枚 radial nodes 进一步向中心 preview 收到 `left-[58.9%] / left-[59.45%] / right-[16.35%]`，并将 connector widths 收到 `7.94% / 7.98% / 7.86%`，以减少 cluster 与 preview 的横向断裂感；另外把 radial labels 放宽为 `max-w-[130px]` / active `max-w-[116px]`，并调整到 `leading-[1.94]` + 更轻 tracking，同时把 preview 底部 micro-label row 扩到 `gap-[3.22rem] / sm:gap-[3.34rem]`、chart 柱图区微收至 `162px`、label wrappers 提到 `min-h-[7.72rem]`、`max-w-[9.16rem]`、`leading-[1.18]`，继续改善 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 仍略比 live 更显眼但分组更稳、preview 底部 micro labels 可读性继续改善且无 overlap；当前最明显剩余差距仍是 radial cluster 虽较前更靠近中心 preview，但整体桥接感依旧偏弱、仍更像独立侧边模块，下一轮继续优先收敛 cluster→preview cohesion，而不是放大顶部按钮或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为低强调圆形控件、strictly 只操作 `/root/sciclaw` 且继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：根据 fresh-load live/local browser QA 与 DOM 几何对比，将共享 dock menu triggers 间距从 `gap-[0.68rem] / sm:gap-[0.9rem]` 微放宽到 `gap-[0.74rem] / sm:gap-[0.98rem]`，并轻微提高边框/图标对比、同时收轻阴影厚度，让顶部 utility controls 更接近 live 的 subtle-but-readable 状态；同时将 radial cluster 三枚侧节点进一步向中心 preview 收到 `left-[57.8%] / left-[58.35%] / right-[17.3%]`，并把 connector widths 收到 `7.26% / 7.3% / 7.18%`，以继续减弱 detached 感、加强 cluster→preview 的横向锚定。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为顶部 utility triggers 已从过轻改善到 subtle 但可辨识，cluster 与中心 preview 的锚定感较上一轮更强；当前最明显剩余差距仍是 radial cluster 局部 microcopy / label 与连线区域在浏览器视觉上仍略显拥挤，下一轮继续优先做 radial label readability / anti-crowding 收敛，而不是继续放大顶部按钮或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 相对中心 preview 仍偏 detached、且本轮严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：仅对顶部 utility trigger 的 subtle readability 与 radial cluster→preview cohesion 做收敛——将共享 dock menu triggers 间距从 `gap-[0.74rem] / sm:gap-[0.98rem]` 微放宽到 `gap-[0.78rem] / sm:gap-[1.02rem]`，同时轻收阴影厚度并提升边框/前景对比，让顶部 controls 更接近 live 的 low-emphasis 但可辨识状态；再把三枚 radial side nodes 收到 `left-[56.9%] / left-[57.45%] / right-[18.1%]`，将 connector widths 收到 `6.74% / 6.82% / 6.64%`，并把 node label gap 调到 `0.8rem`、label 宽度放宽到 `max-w-[138px]` / active `max-w-[122px]`，同步将 radial label typography 调为 `leading-[1.98]` + `tracking-[0.112em]`，以减弱 detached 感并提升 `Literature Analysis / Data Mining / Outcome Present` 的可读性。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA 与 DOM 几何复核。最新 QA 认为 radial cluster 已更靠近中心 preview、顶部 utility controls 也仍保持 subtle 但更易辨识；当前最明显剩余差距仍是 radial cluster 整体虽更贴近中心但局部标签依然略显 cramped，下一轮继续优先做 radial microcopy 的 anti-crowding / hierarchy 收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为 readable-but-subtle 的轻量圆形 controls、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.82rem] / sm:gap-[1.08rem]` 放宽到 `gap-[0.88rem] / sm:gap-[1.16rem]`，同步微增边框/图标对比并收轻阴影厚度，让顶部 utility controls 保持 subtle 但更易辨识；同时把三枚 radial side nodes 收到 `left-[55.35%] / left-[55.95%] / right-[18.1%]`，将 connector widths 放宽到 `7.88% / 7.94% / 7.72%`，并把 radial labels 统一放宽至 `max-w-[150px]`、active `max-w-[130px]`，同步调整为 `text-[11.4px]`、`leading-[2.08]`、`tracking-[0.086em]` 与更亮的 `#4f6d82`，以继续减弱 detached 感并改善 cluster microcopy readability。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 已从偏轻提升到 readable-but-subtle，cluster 与中心 preview 的桥接感继续改善；当前最明显剩余差距仍是 radial cluster 微字标签依旧是首屏最弱可读点，且 cluster→preview 仍有可见留白，下一轮继续优先做 radial label anti-crowding 与 cluster→preview cohesion，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 与中心 preview 的关系在视觉上仍是“略 detached 而非强绑定”、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：保持顶部 utility trigger 组仍为 subtle 的轻量圆形 controls，仅轻微收敛边框/阴影到 `border-[rgba(7,25,39,0.5)]`、`text-[#102b3e]` 与更浅 shadow；同时把三枚 radial side nodes 继续向中心 preview 微收至 `left-[54.7%] / left-[55.2%] / right-[18.9%]`，并将 connector widths 放宽到 `8.26% / 8.32% / 8.12%`，配合统一 `gap-[0.76rem]`、inactive `max-w-[156px]` / active `max-w-[136px]` 与 `text-[11.2px]`、`leading-[2.14]`、`tracking-[0.074em]` 的更稳标签排版，以减少 radial microcopy 的 cramped 感并加强 cluster→preview bridge。该里程碑已先用 TDD 将 `src/app/page.test.tsx` 调整为红灯（断言新位置/connector/label 宽度与排版），随后完成实现并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、双独立 reviewer 复审，以及 fresh-load local browser QA 与 DOM 几何复核。最新 QA 认为 radial labels 较上一轮略不拥挤、cluster 与中心 preview 的关系更 cohesive，且未见明显 clipping/overlap；当前最明显剩余差距仍是左侧 radial cluster 作为整页最密区域，距离 live 的“更像环境式导航而非强绑定模块”还有细微拿捏空间，下一轮继续优先做 radial microcopy / connector 的更小步桥接收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为更轻巧的 low-emphasis 圆形控件、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.88rem] / sm:gap-[1.16rem]` 放宽到 `gap-[0.94rem] / sm:gap-[1.24rem]`，同时把 radial side nodes 收到 `left-[53.95%] / left-[54.5%] / right-[19.7%]`、connector widths 放宽到 `8.72% / 8.8% / 8.58%`，并把 radial label 宽度/排版调整到 inactive `max-w-[162px]`、active `max-w-[142px]`、`text-[11.3px]`、`leading-[2.18]`、`tracking-[0.064em]`；另外把 preview 底部 micro-label row 微放宽为 `gap-[3.3rem] / sm:gap-[3.42rem]`、柱图区 `160px`、label wrappers `min-h-[7.92rem]` + `max-w-[9.32rem]`，继续减轻 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为顶部 utility triggers 仍略大于 live 但已更接近 subtle/readable 的轻量状态，preview 底部 labels 更稳且未见明显 overlap；当前最明显剩余差距仍是 radial cluster 与中心 preview 的桥接感虽改善但仍略显 detached，cluster 文案也是首屏最容易显得拥挤的区域，下一轮继续优先做 cluster→preview cohesion 与 radial microcopy anti-crowding，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为 subtle/readable 的轻量圆形控件、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：仅对 radial cluster→preview cohesion 与 radial microcopy anti-crowding 做收敛——将三枚 radial side nodes 进一步向中心 preview 收到 `left-[52.9%] / left-[53.5%] / right-[20.7%]`，同步把 connector widths 放宽到 `9.12% / 9.18% / 8.96%`、微调角度/起点，让 cluster 更像桥接到中心 preview 而非漂浮注释；同时把 radial label 宽度从 inactive `max-w-[162px]` / active `max-w-[142px]` 放宽到 `max-w-[168px]` / `max-w-[146px]`，并将标签前景提升到 `#5b7587` 以减弱小字发虚感。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 仍保持 subtle 且可辨识，中心 preview/右侧 auth card 也未见明显 clipping；当前最明显剩余差距仍是 radial cluster 的局部标签仍略显拥挤，整体与中心 preview 的桥接感虽然更强但依旧是首屏最弱 cohesion 点，下一轮继续优先做 radial label spacing / hierarchy 与 cluster→preview 锚定感收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 相对中心 preview 仍略 detached、而 preview 底部 micro-label row 仍是首屏最显眼微字弱点、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.82rem] / sm:gap-[1.08rem]` 微收至 `gap-[0.78rem] / sm:gap-[1.02rem]`，并同步轻增边框/图标对比、收轻阴影厚度，让顶部 utility controls 保持 subtle 但更靠近 live 的轻量分组；同时把三枚 radial side nodes 继续向中心 preview 收到 `left-[49.35%] / left-[50%] / right-[17.35%]`，并把 connector widths 放宽到 `11.04% / 11.12% / 10.84%`，以进一步减弱 cluster→preview 的 detached 感；另外把 preview 底部 micro-label row 扩到 `gap-[4.12rem] / sm:gap-[4.28rem]`，将柱图区微收至 `152px`，并把 label wrappers 提到 `min-h-[9.12rem]`、`text-[14.16px]`、`leading-[1.18]`、`max-w-[10.8rem]`，继续改善 `Meth. / Find. / Data / Evid.` 的可读性。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为 radial cluster 比上一轮更少 detached、顶部 utility controls 仍保持 subtle 且无明显 clipping/overlap；当前最明显剩余差距仍是 preview 底部 micro-label row 虽已更稳但仍是首屏最显眼微字区，且 radial cluster 局部小字/连线区域仍略显忙，下一轮继续优先做 bottom micro-label 结构与 radial label anti-crowding 收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 相较中心 preview 仍略 detached、preview 底部 micro-label row 仍是首屏最弱可读区、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.78rem] / sm:gap-[1.02rem]` 微收至 `gap-[0.74rem] / sm:gap-[0.98rem]`，保持顶部 utility controls subtle；同时把三枚 radial side nodes 收到 `left-[48.6%] / left-[49.25%] / right-[16.55%]`，并把 connector widths 放宽到 `11.64% / 11.72% / 11.46%`，配合更宽的 inactive `max-w-[198px]` / active `max-w-[168px]` 标签与 `text-[11.2px]`、`leading-[2.16]`、`tracking-[0.048em]` 的更稳排版，以继续增强 cluster→preview bridge 感并减弱微字拥挤；另外把 preview 底部 micro-label row 扩到 `gap-[4.38rem] / sm:gap-[4.56rem]`、柱图区微收至 `150px`、label wrappers 提到 `min-h-[9.46rem]`、`max-w-[11.24rem]`、`leading-[1.2]` 与更轻 tracking，继续放宽 `Meth. / Find. / Data / Evid.` 的呼吸空间。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新 QA 认为顶部 utility triggers 仍保持 subtle、radial cluster 相较前一轮更靠近中心 preview，但整体桥接感依然弱于 live；preview 底部 micro labels 虽更宽松，仍被 fresh-load browser QA 视为略偏紧。下一轮继续优先做 radial cluster 的锚定/降噪，而不是再扩大顶部按钮存在感。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 首屏最明显剩余差距已由 controller-side live/local browser QA 收敛到 radial cluster 本身、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：不扩张新模块，仅针对 left radial cluster 的 anti-crowding / bridge spacing 做收敛——将共享 dock utility triggers 间距从 `gap-[0.74rem] / sm:gap-[0.98rem]` 微收至 `gap-[0.7rem] / sm:gap-[0.9rem]`，同时把三枚 radial side nodes 进一步外移并拉开垂直距离到 `left-[47.1%] top-[11.9%]`、`left-[47.55%] top-[49.85%]`、`right-[15.15%] top-[11.1%]`，配合 connector widths `12.26% / 12.34% / 12.08%`、节点内部 label gap `1.08rem`，以及 inactive `max-w-[188px]` / active `max-w-[160px]` 的标签宽度收敛，优先缓解 radial labels 与 connectors 的视觉碰撞，而不再追逐顶部按钮存在感或 preview 底部 micro-label row。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新结论：bottom micro-label row 仍可继续微调，但本轮之后首要剩余问题已更明确地集中在 radial cluster 作为整页最拥挤区域的 spacing / anchoring；下一轮继续优先做 radial label hierarchy、connector 终点留白与 cluster→preview 锚定感，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-top summary slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并通过 fresh-load local browser QA 确认当前顶部三张 summary cards 的第三卡此前仍像 `Foundry` handoff、导致 center top band 与右侧 output rail 语义过近，因此本轮不扩张新模块，仅把第三张 summary card 从 `Foundry / Answer handoff / Packet staged. / 12 ready` 收敛为更偏 workflow 的 `Tasks / Quieter edge lane / One blocker routed. / 1 blocker`，同步把该 top summary band 的 `xl` 固定列宽从 `10.95rem` 微放宽到 `11.3rem`，并补强 `src/app/chat/page.test.tsx` focused 断言，锁定第三卡已转为 task-lane 语义且顶栏宽度同步更新。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为顶部三张 summary cards 现在更 calm，第三卡已明显读作 task/edge-lane card；当前单一最明显 density hotspot 已转移到右侧 `Session output / Status board / Foundry handoff` 纵向 stack，下一轮继续优先减少该 right rail 的 stacked microcopy / badge 竞争，而不是回到 center top summary band 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Active sessions / task-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并结合 fresh-load local browser vision 确认当前页面整体 coherent、无明显 clipping/overlap，但单一最明显剩余热点已收敛到 center Session timeline 中偏右的 `Active sessions` 行与相邻 task stack，尤其 `12m / Map`、`1 blocker / Queue`、`Ready / Packet` 等 meta pills 与 task meta 在窄轨中仍略显压缩。因此本轮不扩张新模块，仅将 `Active sessions` rows 的 `sm:grid-cols-[minmax(0,1fr)_minmax(4.2rem,auto)]` 微放宽到 `4.6rem`、同步把右侧 meta stack 最大宽度提到 `4.9rem` 并把 row gap 调到 `2.5`，同时把 task rows 的 badge gap 微放宽到 `2.5`、说明文案约束到 `max-w-[26ch]`，以让 center-lane meta tokens 更少争抢横向空间；focused test 也同步补强为逐行断言 `12m/Map`、`1 blocker/Queue`、`Ready/Packet` 和新的 grid class，避免后续密度收敛误伤真实 session-lane contract。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为页面仍 coherent、无明显 breakage；当前单一最明显剩余热点仍是 center Session timeline 偏右的窄 task/meta stack，而 right rail 已退居次级压力区。下一轮继续优先减少 center timeline/task stack 的 stacked badges 与说明竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并经 fresh-load local browser vision 二次复核确认 right rail 仍是单一最明显 density hotspot、但近期收敛已使其与 center lane 更接近，因此本轮不扩张新模块，仅对 `Session output / Status board / Foundry handoff` 做更小步的结构减压——将 `chat-right-rail-grid` 从 `xl:[minmax(0,1fr)_minmax(0,0.98fr)] / 2xl:[minmax(0,1fr)_minmax(288px,0.88fr)]` 调整为 `xl:[minmax(0,1.06fr)_minmax(272px,0.92fr)] / 2xl:[minmax(0,1.08fr)_minmax(284px,0.9fr)]`，同步收短 `Session output` 内卡与 `Status board` / `Foundry handoff` 的垂直内边距、标题说明最大宽度、signal gap 与 export row 包裹宽度，使右栏更像连续 summary rail 而不是三段过厚的窄卡堆叠；focused test 也同步锁定新的 right-rail grid 宽度与 Foundry/export 文案 contract。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为 right rail 仍是 `/chat` 首要 density hotspot，但 `Session output / Status board / Foundry handoff` 已比上一轮更 calm、无明显 clipping/overlap，当前 center lane 虽更接近但尚未反超为更差热点；下一轮继续优先减少 right rail 的 stacked module/label 竞争，而不是回到单纯 widening center lane 或扩张新模块。

## Phase C — Help shell 与公开文档（P1）
- [x] 建立 `/help/[slug]` 文档路由与 docs shell 三栏结构
- [x] 完成已知 help 路由：getting-started / projects / chat / skills / library / tasks / foundry / persona / im / settings
- [x] 加入顶部 search / appearance / language 控件占位与右侧 TOC
- [~] 继续根据公开文案与页面细节补全文档内容、排版层次、滚动状态与交互微调

## Phase D — Privacy / 公共交互细节（P2/P3 部分）
- [x] 完成 `/privacy` 页面公开信息摘要复刻
- [~] 继续补齐公共 auth 卡片的更细粒度状态、表单提示与视觉拟真
- [~] 引入更多 settings / theme / language 菜单交互细节

## Phase E — App shell 占位 IA（P4）
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` tasks-panel density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 确认当前单一最明显剩余热点仍在 center session timeline lane，而 right rail 与 lower checkpoint rail 均次之，因此本轮不扩张新模块，仅对 `Tasks` 面板做更 calm 的 copy/spacing 收敛——将标题限制为更稳的 `max-w-[17ch]` 与 `leading-[1.38]`，同步把 task row 标题/徽标间距从 `gap-2.5` 微收至 `gap-2`、badge tracking 从 `0.12em` 收至 `0.1em`、说明最大宽度从 `26ch` 收至 `22ch`，并把 data 文案收敛为 `12 PDFs`、`Generate compare session`、`Research lead`、`Push verified outline`、`Export`，让 task lane 更像 compact queue 而不是长句说明列表。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、带重试的 Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 fresh-load local browser vision 仍将 center session timeline lane 视为 `/chat` 单一最明显 density hotspot，但确认 tasks-panel copy 与 badge 节奏已较上一轮更 calm、页面无明显 clipping/overlap；下一轮继续优先减少 center timeline lane 内多层卡片/徽标竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 确认当前单一最明显热点仍在左中 `Session timeline` 里靠上的 `Context / Threads / Foundry` 三张窄卡与其下 `Active sessions` 过渡带，因此本轮不扩张新模块，仅将 `Task lane` helper copy 从 `Route one block.` 校准为更自然的 `Route one blocker.`，并把 `Active sessions` helper copy 收敛为 `Next move stays on one quieter edge lane.`、同时将 session row 的右侧 meta 轨宽从 `4rem` 微放宽到 `4.2rem`，让 `12m / Map`、`1 blocker / Queue`、`Ready / Packet` 的双 pill 组合少一点横向挤压。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为页面整体 clean、无明显 clipping/overlap，但当前单一最明显 density hotspot 仍是左中 `Session timeline` 顶部的三张 summary cards 与 `Active sessions` 过渡带；下一轮继续优先减少该上半区的窄卡/徽标同屏竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 先确认当前最明显热点仍落在中心 `Session timeline` 下半部的 lower three-card checkpoint rail，因此本轮不扩张新模块，仅将三张 rail cards 在 `xl/2xl` 下的固定列宽从 `14rem/14.2rem` 微放宽到 `14.6rem/14.8rem`，并把 `Session focus` / `Checkpoint rail` helper copy 分别收短为 `Pin live claim.` 与 `Bridge notes calm.`，继续削减窄卡中的 wrapping 与 chip/text 竞争。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为本轮后 lower checkpoint rail 仍 dense，但不再是唯一最差热点；当前更明显的单一热点已转为整个 central-left timeline stack（top summary cards + `Active sessions` + lower three-card rail）的连续 card/chip 扫描压力。下一轮继续优先减少这条 central-left timeline lane 的整体层级与同屏卡片竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 再次确认当前单一最明显热点已从 right rail 收敛回 center lane，尤其 lower three-card checkpoint rail 比 `Active sessions` 与 right rail 更显拥挤，因此本轮不扩张新模块，仅将 `Task lane` 卡说明从 `Route the blocker.` 收短为 `Route one blocker.`，同步补强 `src/app/chat/page.test.tsx` focused 断言锁定该 copy 变更，以继续降低 lower center strip 的句长与换行压力。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为页面整体 coherent、无明显 overlap/clipping，但 center lane 仍是 `/chat` 单一最明显 density hotspot，且 lower three-card checkpoint rail 比 right rail 更 cramped；下一轮继续优先减少 lower rail 与 `Active sessions` 的同屏内容竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density 收敛 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 再次确认当前单一最明显热点仍是 right rail，尤其 `Session output / Status board / Foundry handoff` 在窄栏中仍承载最多 stacked microcopy / pills。因此本轮不扩张新模块，仅去掉 `Session output lane` 顶部重复的 session meta 徽标，把 `3 live` 并入统计 pills、保留 `Packet ready`，并把说明文案收短为 `Keep answer, risk, and export on one calm rail.`；同时将 `Risk` 说明从 `One review call waits.` 收敛为 `One review wait.`，并将 `Foundry handoff` 文案收敛为 `Bundle memory, sessions, and charts into one export packet.`，以进一步减少窄栏换行与视觉噪音。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 仍将 right rail 视为 `/chat` 单一最明显 density hotspot，但确认其当前首要压力已更集中在整体窄栏层级本身，而不是多余 meta 徽标或过长微文案；下一轮继续优先减少 right rail 的纵向分段与 nested card 感，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density 收敛 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 明确上一轮单一最明显热点仍在 right rail，因此本轮不扩张新模块，仅继续收短并压平 `Session output / Status board / Foundry handoff` 的 microcopy 与 badge 节奏——将 `Session output lane` 说明从 `Keep answer, risks, and export handoff on one quieter rail.` 收敛为 `Keep answer, risk, and export handoff on one calm rail.`，同步收紧其说明宽度、badge 间距与 `Packet ready` pill 尺寸；将 `Status board` signal stack 的间距、说明宽度与字级微收，使 `Sources / Risk / Locked / 1 held` 在窄栏中更少换行竞争；并将 `Foundry handoff` 说明收短为 `Bundle memory, sessions, and verified charts into one export packet.`、导出行说明收短为 `Brief + slides ready.`，同时压低说明宽度与 export row 的 gap。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为 right rail 相较上一轮已明显更 calm、不再是单一首要热点；当前最明显剩余热点已转移到 center `Session timeline / Active sessions / lower checkpoint cards` 区域，下一轮继续优先减少中心 lane 的 stacked cards / badges / microcopy 竞争，而不是回到右栏或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 再次确认当前单一最明显热点仍是 center `Active sessions` lane，因此本轮不扩张新模块，仅将 `Session timeline` 主栅格从 `xl:[minmax(0,0.92fr)_minmax(0,1.08fr)] / 2xl:[minmax(0,0.9fr)_minmax(0,1.1fr)]` 微收为 `xl:[minmax(0,0.88fr)_minmax(0,1.12fr)] / 2xl:[minmax(0,0.86fr)_minmax(0,1.14fr)]`，把 `Active sessions` helper copy 收短为 `Next move stays on one quieter edge lane.`，同时将 row 右侧 meta lane 从 `4.8rem` 再收至 `4.4rem`、row gap 从 `2.5` 收到 `2`、正文最大宽度从 `21ch` 收到 `19ch`，并把 session summary `Cluster map` 收短为 `Map`，再同步将 lower strip 前两张卡标题收短为 `Hold live claim` / `Route blocker`，以进一步压低 center lane 的 chip/title 竞争并验证热点是否仍稳定落在 `Active sessions`。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审与 fresh-load local browser QA。最新 browser vision 认为本轮后整体更 calm、无明显 clipping/overlap，但单一最明显 density hotspot 仍是 `Active sessions` center lane；下一轮继续优先减少该中心窄列的 metadata/badge 同屏竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density slice：基于 sequential live/local browser QA，确认 local `/chat` 当前单一最明显热点仍是 right rail stack，因此本轮不扩张新模块，仅移除 `Status board` 中最末的 `Handoff route / Return to source` 信号行，把状态列表从 `3 signals` 收敛为 `2 signals`，并同步将 Foundry handoff 标题容器、说明段落与 export row 的 `max-width / spacing / row padding` 进一步收紧，让 `Session output / Status board / Foundry handoff` 上半段更像连续的 summary rail 而不是多层窄卡堆叠。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（log 复核 focused suite 继续全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及独立 reviewer 复审。最新 fresh-load local browser vision 仍把 right rail stack 视为 `/chat` 首要 density hotspot，但本轮后其纵向内容竞争已进一步减少；下一轮继续优先压平 right rail 的卡层级与微文案，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` density vertical slice：依据 sequential live/local browser QA，先确认 live `/chat` 仍是 marketing landing，而 local `/chat` 在上一轮后已将首要热点从 center top summary row 转移到 right rail，因此本轮不扩张新模块，仅将顶部三张 summary mini-cards 收短为 `Thread anchor / Live switchboard / Answer handoff` 并微放宽 `xl` 卡宽到 `10.95rem`、收紧标题/说明行高，让 center top row 更像 orientation band；同时把 `Session output` 元信息合并为单个 `Session 07 · 3 live · 5 open` metadata pill，仅保留 `Packet ready` 状态 chip，并把 `Status board` 从 card-within-card 小白卡改成更轻的分隔行，配合收紧 `Foundry handoff` 描述宽度与垂直间距，继续压低 right rail 的 stacked chips / nested cards 扫描负荷。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及独立 reviewer 复审；reviewer 曾发现 lower flow rail 测试断言误指向 summary card 文案，已立即修正并复测通过。最新 fresh-load local browser QA 认为 center top summary row 已明显 calmer，当前单一最明显 density hotspot 已转为 right rail 的 `Session output / Status board / Foundry handoff` 纵向堆叠；下一轮继续优先减少 right rail 上半段的 nested boxes / pills 数量，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` 右栏减压 slice：先用 browser 顺序复核确认 live `/chat` 仍是 marketing landing，而 local `/chat` 的最明显拥挤点此前落在 right rail；因此本轮不扩张新模块，仅将 `chat-right-rail-grid` 从 `xl:[minmax(0,0.94fr)_minmax(0,1.06fr)] / 2xl:[minmax(0,0.96fr)_minmax(296px,0.9fr)]` 收敛为 `xl:[minmax(0,1fr)_minmax(0,0.98fr)] / 2xl:[minmax(0,1fr)_minmax(288px,0.88fr)]`，同时把 `Regulatory memo · current answer` / `Keep the answer and export lane visible in one bridge card.` / `3 exports ready` / `Bundle memory, sessions, and verified charts into one review packet.` / `Brief and slides staged together.` 收敛为更短、更 calm 的 `Regulatory memo · answer lane`、`Keep the answer and export lane in one calmer summary card.`、`Packet ready`、`Bundle memory, sessions, and verified charts into one ready packet.` 与 `Brief and slides staged together.`，并同步更新 `src/app/chat/page.test.tsx` focused 断言以锁定新布局与文案。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 认为 right rail 已比上一轮更不 cramped、无明显 clipping/overlap，当前最明显密度热点已重新回到 `Active sessions` 与 lower center lane，而非 right rail；下一轮继续优先减少 center Session timeline lane 的同屏竞争，而不是重新扩张 right rail 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 与 controller-side browser/DOM 复核，确认当前最明显剩余压力已从 right rail 回到 center `Active sessions` lane，因此本轮不扩张新模块，仅继续压短右侧与下半部 bridge copy，让页面重心更明确地暴露 center-lane hotspot——将 lower checkpoint rail 第三卡从 `Fold into Foundry / Keep the bridge rail calm.` 收敛为 `Foundry fold / Keep bridge notes calm.`，把 `Reg memo final` row 的右侧 token 顺序改为更贴近“状态→产物”的 `Ready / Packet`，同时将 `Session output` 说明从 `Keep the current answer...` 收短为 `Keep the answer...`，并把 `Packet export` 说明从 `Brief, appendix, and slides staged together.` 收短为 `Brief and slides staged together.`。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 认为 lower checkpoint rail 比上一轮更 calm、right rail 也更少 scan-heavy；当前最明显剩余热点仍是 center `Active sessions` 区块，它依旧比 right rail 更 cramped。下一轮继续优先减少 `Active sessions` 与中心窄列的同屏竞争，而不是重新扩张 right rail 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 与 DOM 几何复核确认当前最明显剩余压力仍在 right rail 内部的文字扫描负荷，因此本轮不扩张新模块，仅将 Foundry handoff 主标题从 `Foundry reviewer packet` 收短为 `Foundry packet`、说明从 `Bundle project memory, sessions, and verified charts into one export packet.` 收短为 `Bundle memory, sessions, and verified charts into one review packet.`，并把唯一导出行标签从 `Export summary` 收敛为 `Packet export`，以验证“继续压短 right rail 末端 handoff copy”是否能比再改 center lane 更有效地降低右栏末端的阅读负担。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 仍认为 right rail 是 `/chat` 最明显的 scan-heavy hotspot，但本轮后 Foundry handoff 区块标题/导出项更短、更少抢占窄栏注意力；下一轮继续优先减少 right rail 内 Session output / Status board / handoff 的 stacked copy 竞争，而不是回到单纯放宽 center lane。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load local browser QA 与 controller-side DOM/几何复核，确认当前最明显剩余压力已转向 right rail 本身而非 center lane，因此本轮不扩张新模块，仅将 `chat-right-rail-grid` 从 `xl:1 列 / 2xl:[minmax(0,1fr)_minmax(308px,0.92fr)]` 收敛为 `xl:[minmax(0,0.94fr)_minmax(0,1.06fr)] / 2xl:[minmax(0,0.96fr)_minmax(296px,0.9fr)]`，让 Tasks 提前与 Session output / Status board / Foundry handoff 并排分担纵向堆叠；同时把 Session output 顶部 stats 行增加 `xl:gap-2.5`、将 Foundry export asset rows 的垂直内边距从 `py-3` 收到 `py-2.5` 并把 export pill tracking 从 `0.14em` 收到 `0.12em`，以验证“减少右侧长列堆叠 + 微收 export rows/token 紧张感”是否能比继续压 center lane 更有效地降低 `/chat` 的整体拥挤源。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 local browser QA 仍认为 right rail 是当前最明显 density hotspot，但本轮后其纵向堆叠已较上一轮更可分段扫读、export rows 也更短促；下一轮继续优先减少 right rail 内部的 card-within-card / signal stack 压力，而不是回到单纯 widening center lane。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load live/local 对照与 local browser QA，确认当前 center lane 最明显剩余压力仍集中在 `Active sessions` rows 本身而非 top summary cards 或 lower checkpoint rail，因此本轮不扩张新模块，仅将顶部 Threads 摘要卡说明从 `Branch clean.` 收敛为 `Live state stays surfaced.`，同时把 `Active sessions` helper copy 从 `Signal and next move stay on one calmer edge lane.` 收敛为 `Signal and next move stay on one tighter edge lane.`；再把 session rows 的 `sm` 右侧 meta lane 从 `minmax(5.1rem,auto)` 微收至 `minmax(4.8rem,auto)`、gap 从 `3` 收至 `2.5`、正文最大宽度从 `22ch` 收到 `21ch`，并同步将第二/第三条 session 的 meta-description 重新配对为 `1 blocker + Queue`、`Packet + Ready` 与 `Outline locked.`，以验证“减少 Active sessions 右侧 meta 轨宽竞争 + 让 Threads 摘要卡更直接说明 live state”是否能继续降低 center edge lane 的碎片感而不破坏 lower rail 的稳定性。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部三张 summary cards 仍保持 calm、lower checkpoint rail coherent，而 `Active sessions` 依旧是 center lane 最明显的 cramped hotspot；本轮调整后该区域更像受控 edge lane 而非松散 dashboard，但下一轮仍应优先继续减少 `Active sessions` rows 的同屏竞争，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：fresh-load live/local QA 显示 `/chat` 最明显剩余密度仍在 center top summary cards 与 checkpoint rail 文案竞争，因此本轮不扩张新模块，仅将 `Threads` 摘要卡说明从 `Live state stays surfaced.` 再收短为 `Live state surfaced.`，同时把 `Checkpoint rail` 说明从 `Keep the bridge summary calmer.` 收短为 `Keep the bridge rail calm.`，并将 top summary cards 在 `xl` 下的固定列宽从 `10.35rem` 微放宽到 `10.7rem`、摘要说明最大宽度从 `12ch` 微收至 `11ch`，以验证“再短一层 copy + 轻放宽 top summary 列宽”是否能继续降低 center upper lane 的碎片感，而不重新放大下方 `Active sessions` 与 lower strip 的竞争。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 top summary cards 比上一轮更直接、checkpoint rail 文案更安静，整页未见新的 clipping/overflow；当前最明显剩余压力仍是 `Active sessions` rows 本身与 live 相比仍略显 cramped，下一轮继续优先减少 `Active sessions` 的同屏竞争，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load live/local 对照与 local browser QA，确认当前更显眼的剩余压力仍在 `Active sessions` 左侧窄轨与顶部三张 summary cards 的语义密度，因此本轮不扩张新模块，仅将顶部 summary 第一张卡标题从 `One lane per session.` 收短为 `One lane per thread.`，同时把 `Active sessions` helper copy 从 `Signal and next action stay on one calmer edge lane.` 收短为 `Signal and next move stay on one calmer edge lane.`，并把 session rows 的 `sm` 右侧 meta lane从 `minmax(5.4rem,auto)` 微收至 `minmax(5.1rem,auto)`，以验证“再短一层 thread/move 文案 + 再轻收 meta 轨宽”能否继续减少中心窄列的同屏竞争而不破坏既有 shell 结构。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 仍认为 `Active sessions` 左侧窄轨和顶部三张 summary cards 是 `/chat` 最容易显得紧的区域，但本轮 copy/轨宽收敛后未见新的 clipping/overflow，且中心 lane 的阅读顺序更直接；下一轮继续优先减少 center summary/session lane 的同屏信息竞争，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：基于 fresh-load local browser QA 明确指出 lower three-card checkpoint rail 仍是最明显 cramped hotspot，尤其第三张卡标题/说明/badge/CTA 在窄卡中竞争空间；因此本轮不扩张新模块，仅把第三张卡文案从 `Fold checkpoints into Foundry / Keep the bridge on one calmer rail.` 收敛为 `Fold into Foundry / Keep one calmer bridge rail.`，并把 lower strip 卡宽从 `12.6rem / 13rem` 放宽到 `13.2rem / 13.6rem`，同时给第三张卡增加轻微内边距与 chip/CTA 间距、收短标题/说明最大宽度，以验证“缩短第三卡 copy + 微放宽 rail 宽度”比继续加列或大改结构更能缓解该 hotspot。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 `Fold into Foundry` 第三卡已明显 calmer，lower checkpoint rail 不再是唯一最差热点；当前更明显的密度压力已转向 `Tasks` 与右侧 `Session output / Status board / Foundry handoff` 信息堆叠。下一轮继续优先减少右侧 rail 与 tasks 区块的同屏信息竞争，而不是回到扩大 lower rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 已确认 lower three-card checkpoint rail 仍是最明显 cramped hotspot，而当前最有效的收敛方向不是再压短结构，而是给三卡更多横向呼吸；因此本轮不扩张新模块，仅把 lower center strip 的卡宽从 `13.2rem / 13.6rem` 放宽到 `14rem / 14.2rem`，并将第三张 `Checkpoint rail` 卡说明从 `Keep one calmer bridge rail.` 进一步收敛为 `Keep the bridge summary calmer.`，以验证“轻放宽 rail + 再短一层 helper copy”是否能让三卡更像稳定 summary rail 而非压缩 utility tiles。该里程碑已严格按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及 fresh-load local browser QA。最新 QA 认为 lower checkpoint rail 已较上一轮更 calm、三卡更像分离但相关的 summary steps；当前更明显的剩余密度压力已转向上方那组三张 summary mini-cards（`One lane per session / Switch live threads / Promote the locked answer`）与少量上半部紧凑换行。下一轮继续优先减少这组三张 top summary cards 的压缩感，而不是回到继续挤压 lower rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load local browser QA 确认当前 center Session timeline lane 中最明显 cramped hotspot 已集中在 lower three-card checkpoint rail，尤其 `Checkpoint rail` 卡的标题、说明、badge 与 CTA 在窄卡宽里竞争空间；因此本轮不扩张新模块，仅把该第三张卡的 copy/CTA 收敛为 `Fold checkpoints into Foundry`、`Keep the bridge on one calmer rail.` 与 `Open rail`，保留 `Foundry sync` pill 与 `/help/foundry` 去向不变，以验证“减少该卡内部文本负荷”比继续放宽整条 center lane 更能缓解拥挤。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为 `Active sessions` 相比下半部已不再是最挤区域，当前 center lane 最明显剩余热点仍是 lower checkpoint rail 本身整体仍偏窄，尤其第三张 rail card 虽较上一轮 calmer 但仍是最可见的局部密度点；下一轮继续优先减少 lower center strip 的窄卡信息竞争或进一步重排该 rail，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 与 DOM 几何复核确认当前最明显问题是 `Active sessions` 三条 rows 的右侧 meta rail 过窄、导致 pills/标题竞争宽度，而 lower three-card checkpoint strip 又比 live 更横向铺开；因此本轮不扩张新模块，仅把 `Active sessions` row 的 `sm` 右侧轨道从 `minmax(4.6rem,auto)` 放宽到 `minmax(5.4rem,auto)` 并同步把 `sm` gap 从 `2.5` 回提到 `3`，让 `12m / Cluster map / Queue / 1 blocker / Outline / Ready` 的堆叠更少压缩；同时把 lower center strip 的 `xl/2xl` 固定卡宽从 `13.2rem / 13.6rem` 微收到 `12.6rem / 13rem`，让 `Hold live hypothesis / Assign blocker / Checkpoints fold into Tasks and Foundry.` 三卡更像受控 bridge rail 而非过宽横铺层。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为这一轮后 center lane 的最明显问题已从“右侧 meta rail 被挤爆”收敛为“左侧 Active sessions 窄轨与 lower checkpoint strip 仍比 live 更密一些，但没有明显 clipping/overflow”；下一轮继续优先减少 center lane 的遮挡/叠层感，而不是再单纯 widening 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 fresh-load local browser QA 反复指出 `Active sessions` 仍是整页最明显 cramped hotspot 的问题，本轮不再继续放宽整个中心主栅格，而是直接压缩 `Active sessions` 每条 row 的右侧 meta lane 与文本载荷——将 row 右列从 `minmax(5.4rem,auto)` 收到 `minmax(4.6rem,auto)`、row gap 从 `3` 轻收至 `2.5`，正文最大宽度从 `24ch` 收到 `22ch`，同时把后两条 session 的双 pill 文案改写为更短、更清晰的 `Queue / 1 blocker` 与 `Outline / Ready`，配合 `Replay queued.` 的更短说明，验证“减少同屏 meta token 宽度竞争”比继续 widening 更有效。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为 session meta pills 确实更 calm、未见 clipping/overlap，但 `Active sessions` 仍是整页最明显密度热点；下一轮继续优先减少 `Active sessions` 左侧窄列的同屏卡片/说明竞争，必要时再触及其相邻下半部 center strip，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 fresh-load local browser QA 持续指出 center `Session timeline` lane 尤其 `Active sessions` 与 lower three-card strip 仍是整页最明显 density hotspot 的问题，本轮先做一次更直接的结构验证——将 `Session timeline` 主栅格从 `xl:[0.92fr/616px] / 2xl:[0.9fr/648px]` 继续放宽到 `xl:[0.8fr/724px] / 2xl:[0.78fr/768px]`，同步把 Tasks 前两条 note 收短为 `Ground retrieval before review.` 与 `Keep this lane isolated.`，以区分“宽度不够”与“同屏内容过多”两类根因。该里程碑已按 TDD 先补强 `src/app/chat/page.test.tsx` 对新 tasks microcopy 与当前 lower-strip/Active-sessions 约束的断言，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、连续两次 `docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 结论是：更宽 center lane 仅带来小幅改善，`Active sessions` 与下半部三卡 strip 仍明显偏拥挤，说明下一轮优先级应从“继续放宽列宽”切换为“减少同屏卡片/文案/徽标数量并压平嵌套层级”，而不是继续单纯 widening。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：基于 fresh-load live marketing 对照与 local browser QA 仍指出 lower center strip 偏窄的问题，将原先独立的 `View checkpoints` footer-like 提示卡回收进 center strip 本体，把 `appShellFlowCards` 恢复为三张并行窄卡轨道，同时把第三张卡重构为 `Checkpoint rail`（`Checkpoints fold into Tasks and Foundry.` / `Foundry sync` / `Folded` / `View checkpoints`），并同步将前两张卡标题收短为 `Hold live hypothesis` / `Assign blocker`，让 lower center strip 从“2 张窄卡 + 1 个额外 footer 提示”收敛为更一致的三卡摘要轨道，减少垂直堆叠与碎片感。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 lower center strip 比上一轮更 calm、未见明显 clipping/overlap；当前最明显剩余密度问题已转为其左侧 `Active sessions` 下方的窄 timeline/flow lane 本身仍偏瘦、badge 与短标题仍有压缩感，下一轮继续优先减少该窄 lane 的同屏信息量或进一步放宽其列宽，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 browser QA 指出的中心窄列仍偏拥挤问题，仅收敛 `Active sessions` 与下方 flow-checkpoint CTA——将 session rows 右侧 meta lane从 `8.2rem` 收到 `7.2rem`、row gap 从 `4` 收到 `3`，并把三条 session 文案同步压短为 `Patent landscape · compare / Clinical diligence · replay / Regulatory memo · final` 与更短说明文；同时将 `Active sessions` helper copy 改为更直接的 `One card per live thread...` 提示，减少卡内碎片感；另外把下方 CTA 卡改成 `xl` 下左文右按钮并将 copy 收短为 `More flow checkpoints stay tucked under Tasks and Foundry.` / `Keep the bridge preview on one calmer center scan path.`，按钮标签同步收为 `View all checkpoints`，使该 footer-like 卡更轻、更不挤压中心列。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 功能/安全复审，以及 fresh-load local browser QA。`docker compose up -d --build` 本轮因 Docker Hub `docker/dockerfile:1.7` 远端鉴权/网络不可达失败而未能完成重建，但现有本地服务继续在 `3001` 正常返回 200，且该失败已确认为外部 registry 依赖问题而非当前 diff 逻辑回归。最新 QA 认为当前没有明显 overlap/clipping，CTA 更像 footer-like summary；当前最明显剩余差距已收敛为 `Active sessions` 与其下方两张窄卡仍偏窄、长标题和 pills 仍造成局部 wrapping 压力，下一轮继续优先减少中心窄卡同屏信息量或进一步调整该窄列宽度，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 fresh-load local browser QA 仍指出 center lane 文字卡偏窄的问题，仅对 `Active sessions` 与其下方两张 flow cards 做更轻量的宽度/文案收敛——将 `Active sessions` helper copy 收短为 `Keep the live thread and next move on one quieter edge lane.`，把 session row 右侧 meta lane 从 `6.4rem` 收到 `5.9rem`，同时给 session description 增加 `max-w-[24ch]` 以抑制长行拉伸；并把下方 `Session focus / Task lane` 两张 flow cards 的 `xl/2xl` 宽度从 `13rem / 13.4rem` 放宽到 `13.8rem / 14.2rem`，让下半部更接近 compact bridge strip 而不是过瘦竖卡。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新 QA 认为页面整体 coherent 且无明显 clipping/overflow，但最明显剩余密度问题仍是中心 `Active sessions` 卡和其下方 `Session focus / Task lane / View all checkpoints` 一带：内容已比上一轮更 calm，却依旧因为 center lane 宽度有限而产生明显 wrapping；下一轮继续优先减少 center lane 文本载荷或进一步重排该窄列，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮对本地 `/chat` 再做一次更聚焦的 density pass：将 `Active sessions` 区块说明文案压缩为更短句，并把每条 session row 改为内容/元信息双区布局，让时间戳与 summary pill 不再挤占正文行；同时将 Tasks 三条待办的 label/note 收短、轻收 card 圆角与纵向间距，减少右 rail 的阅读负荷；再把 `Session output / Status board / Foundry handoff` 统一收紧为更低的卡片高度与更紧凑的 summary-grid / signal rows，使 right rail 比上一轮更不 cramped。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任何现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为中心列比上一轮更易扫读、right rail 也更有呼吸感，未见明显 overlap / clipping；当前剩余最明显差距收敛为 center top 三张窄卡与部分 metadata/pill 仍略紧、`View all flow checkpoints` 底部 CTA 锚定感仍可继续优化，下一轮继续优先收敛这些中心窄卡与 CTA 对齐，而不是扩张新模块。
- [x] 建立 `/chat` 公共 app shell 占位页与 project / session / library / tasks / foundry / persona / im / settings 的应用内 IA 占位
- [x] 多轮将 `/chat` 从 docs 风格说明块收敛为 docs→workspace bridge：补齐 sticky shell、Project memory rail、Session timeline、Tasks、Session output、Foundry handoff 与对应路由/测试
- [x] 本轮继续完成一次更偏 `/chat` right-rail breathing-room 与 export copy tightening 的小步收敛：保持 live `https://sciclaw.ai/chat` fresh-load 仍是公开 marketing landing 的前提下，继续将本地 `/chat` 作为 docs→workspace bridge，并把主区栅格扩展为 `xl:grid-cols-[minmax(0,1.04fr)_minmax(500px,0.96fr)]` / `2xl:grid-cols-[minmax(0,1fr)_minmax(528px,1fr)]`，同时把右侧 rail 调整为 `xl:grid-cols-[1.06fr_0.94fr]` / `2xl:grid-cols-[1.04fr_0.96fr]`，让 Tasks 与 Session output 的视觉负载更均衡；再将 `Session output` / `Foundry handoff` 文案收短为更紧凑但仍保留语义的 product copy（如 `Keep the current answer... visible in one bridge card.`、`Foundry reviewer packet`、`Keep citations and checkpoints attached to the draft.`、`Presentation-ready talking points for Foundry.`），并同步收紧 export/summary 文案容器宽度，减少右侧 rail 的 cramped feeling 而不丢失 Foundry destination 与 review/traceability 语义。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审与 fresh-load local browser QA。最新 QA 认为 `/chat` 右侧 rail 比上一轮更不拥挤、Tasks/right-rail balance 更均衡，页面无明显 clipping/overlap；当前剩余最明显密度问题继续收敛为中心下方三张 flow cards 与顶部窄卡仍是整页最密区域，下一轮继续优先减少这些窄卡的同屏说明密度，而不是扩张新模块。
- [~] 让 landing 与 docs 导航正确串联到 app shell 占位；本轮已完成一次更偏 `/chat` 密度治理的小步 vertical slice：在不影响 `/root/nexus-mail` 任何现有服务/端口、继续仅使用 `3001` 的前提下，收短 Tasks / Session output / Foundry handoff 的局部文案，为 task rows 增加可扫描的 meta pills（如 `12 PDFs queued`、`Owner · Research lead`、`Export lane`），放大 right rail 间距与导出行内留白，并把中心两组三列卡片在 `xl` 下改为固定更宽列宽+`justify-between`，同时让卡片顶部 meta badges 使用 `self-start` 避免视觉挤压。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 right rail 继续保持更 breathable、export/handoff 区域较上一轮更不 boxed-in，中心列也比上一轮更不 jammed，且未见明显 overlap/clipping；当前剩余最明显差距收敛为中心窄列中的少数 tiny pills/badges 仍略显紧、整页中心信息密度仍高于 live marketing landing，下一轮继续优先减少中心卡片的同屏文字负荷与 pill 数量，而不是扩张 IA.
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` 右栏语义校准 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，而 fresh-load local `/chat` 的 browser vision 继续判定 right rail `Session output / Status board / Foundry handoff` stack 仍是当前单一最高优先级 density hotspot，因此本轮不扩张新模块、也不再继续 widening center lane，仅将 Foundry handoff copy 从 `Bundle memory and verified charts into one ready packet.` / `Brief + slides staged.` 校准为更贴近 shell 语义与既有 docs→workspace bridge 叙事的 `Bundle memory, sessions, and verified charts into one ready packet.` / `Brief and slides staged together.`，同步更新 `src/app/chat/page.test.tsx` focused 断言锁定该 contract。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 local browser QA / vision 仍认为 right rail 是 `/chat` 首要 density hotspot，而 lower checkpoint rail 相对最 calm；下一轮继续优先减少 right rail stack 的纵向扫描负担或层级数，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail wording slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 local browser vision 确认当前单一最明显剩余热点依旧是 right rail，因此本轮不扩张新模块，只收敛 `Session output / Status board / Foundry handoff` 中最挤的摘要文案与 token 负荷——将 `Evidence chain` / `Open risk` 调整为更短的 `summary↔description` 组合（`Sources locked`、`1 call held`、`Citations pinned.`、`One review call waits.`），并把 `Session output lane` 的说明从 `Keep the answer lane in one calmer summary card.` 收敛为 `Keep the answer, risks, and export handoff on one quieter rail.`，同时把元信息从 `Session 07 · 3 live · 5 open` 收短到 `Session 07 · 3 live`，以在不改动结构的前提下减少 right rail 的窄栏换行压力。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 仍将 right rail 视为 `/chat` 单一最明显 density hotspot，但确认当前没有 clipping / overlap；下一轮继续优先减少 right rail 的卡层级、长标题与多枚小 badge 的同屏竞争，而不是回到 widening center lane 或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Active sessions + help-shell contract 校准 slice：controller 侧先从当前 worktree 与 focused tests 出发复核本轮改动确实只涉及 `/chat` center-lane `Active sessions` 的 meta/token 微调与 help 文档文案契约同步，不扩张新模块；随后根据 `src/app/chat/page.tsx` / `src/app/landing-data.ts` 的当前 diff，将 `Active sessions` helper 文案继续收敛为 `Next move stays on one quieter edge lane.`、session rows 右侧轨宽回调为 `sm:grid-cols-[minmax(0,1fr)_minmax(5rem,auto)]` 并把第二条 meta 从 `1 blocker` 校准为更贴近当前 warn 语义的 `1 hold`，同时把 `src/app/help/help-page.test.tsx` 对 getting-started / chat / settings 三个公开文档页的断言更新为匹配现行 `landing-data.ts` 中已落地的 heroParagraphs 与 section 标题（如 `Use SciClaw to move from a rough research question...`、`This route intentionally feels more product-like...`、`Settings stays intentionally light...`），修复了此前 help focused suite 因测试仍跟旧文案契约而产生的 3 个失败。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check` 与新增行静态安全扫描 0 命中。最新 controller-side 结论：本轮主要完成的是 `/chat` 当前窄轨状态文案与 help shell 测试契约的一致性修复，页面/路由均保持稳定，下一轮可继续回到 `/chat` center/right lane 的 density hotspot 做更小步视觉/信息密度收敛，而不是回退到失配的 help 文案断言。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density 收敛 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 明确当前单一最明显热点仍是 right rail，尤其 `Status board` 与 `Foundry handoff` 在窄栏中的 stacked microcopy / badge 竞争。因此本轮不扩张新模块，仅将 `Status board` 两条信号文案从较长组合进一步压短为 `Sources / Risk` 与 `Locked / 1 held`，把 `Session output lane` 说明从 `Keep the answer, risks, and export handoff on one quieter rail.` 再压短为 `Keep answer, risks, and export handoff on one quieter rail.`，并把导出行说明从 `Brief and slides together.` 收敛为 `Brief + slides staged.`，以减少右栏窄容器中的换行与扫描阻力。该里程碑已对当前 diff 重新执行 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check` 与新增行静态安全扫描 0 命中；controller-side Review 1/2/3 也已按当前 diff 重跑通过。最新 fresh-load local browser vision 仍将 right rail 视为 `/chat` 首要 density hotspot，但确认其首要根因已更集中为窄栏中的 stacked/nested micro-components；下一轮继续优先减少 right rail 的嵌套层级与小 badge 竞争，而不是回到 widening center lane 或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail wording 收敛 slice：controller 侧从当前 worktree 与 focused suite 出发，确认本轮 diff 只涉及 `Session output / Foundry handoff` 右栏文案与窄卡内部 spacing 的小步收敛，不扩张新模块；随后将 `Session output lane` 描述从 `Keep answer, risk, and export on one calm rail.` 收敛为更直接的 `Keep answer, risk, and export aligned.`，将上方摘要卡文本容器微收至 `max-w-[14.5ch]` 并把 stats pills gap 从 `1.5` 放宽到 `2`，同时把 Foundry handoff eyebrow / description / export asset copy 继续压短为 `Ready packet`、`Bundle memory, sessions, and verified charts into one export packet.` 与 `Brief + slides staged.`，再同步更新 `src/app/chat/page.test.tsx` 的 focused contract 断言，修复此前测试仍绑定旧文案而导致的 2 个失败。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审与 fresh-load local browser vision 复核。最新 controller-side 结论：right rail 当前 coherent、无明显 clipping/overlap，且页面单一最明显 density hotspot 仍在 center lane 而非 right rail；下一轮继续优先减少 center timeline / tasks / lower checkpoint rail 的同屏竞争，而不是回到右栏扩张或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` top-mini-card copy tightening slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 继续确认当前单一最明显 density hotspot 落在 center timeline 顶部 mini-card 与其右侧 task-column 交界，而非 right rail；因此本轮不扩张新模块，仅将 `appShellCards` 中 `Live switchboard` 与 `Edge lane` 的描述从 `3 live lanes.` / `1 blocker routed.` 收敛为更短的 `3 live.` / `1 blocker.`，同步把 `src/app/chat/page.test.tsx` 首个 smoke contract 更新为与新文案精确对齐。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审。最新 fresh-load local browser vision 认为 top mini-card row 因短句化而略更 calm、无明显 overlap，但当前单一最明显 hotspot 仍是 center timeline 顶部 mini-card 与右侧 `Queue the next research slice` / `Heatmap-ready` 交界处的 badge+card seam；下一轮继续优先减少该 center seam 与 `Active sessions` 的同屏竞争，而不是回到 right rail 或扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail width rebalance slice：controller 侧先从当前 worktree、live/local browser 对照与本地 DOM 几何复核确认当前 `/chat` 单一最明显 density hotspot 已从 right rail 文案本身收敛为右侧两列之间的整体宽度分配——右栏 `Session output / Status board / Foundry handoff` 虽已较 calm，但 `Tasks` 列仍偏瘦、导致右半区左右两列的扫描节奏不够均衡，因此本轮不扩张新模块，仅把 `chat-right-rail-grid` 从 `xl:[minmax(0,1.06fr)_minmax(272px,0.92fr)] / 2xl:[minmax(0,1.08fr)_minmax(284px,0.9fr)]` 调整为 `xl:[minmax(196px,1.14fr)_minmax(248px,0.86fr)] / 2xl:[minmax(212px,1.12fr)_minmax(260px,0.88fr)]`，让 `Tasks` 提前获得更稳定的阅读宽度、同时轻收 `Session output` 列的横向占比，以验证“更均衡的右半区两列配比”比继续压短文案更能降低 `/chat` 右半区的 scan-heavy seam；并同步更新 `src/app/chat/page.test.tsx` 对新 grid class 的 focused 断言。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：right rail 当前保持 coherent、无明显 clipping/overlap，右半区的最明显压力已不再是 handoff 文案过长，而是后续仍可继续观察 center timeline 与 right-half seam 的整体层级平衡；下一轮继续优先对 `/chat` center/right 交界的 density seam 做更小步收敛，而不是回到大幅 widening 或扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center/right seam 收敛 slice：controller 侧先从当前 worktree、`todo.md` 与 live/local `/chat` 顺序复核出发，确认当前单一最明显 density seam 已从 right rail 进一步收敛为 center timeline 顶部三张 mini-cards 与其右侧 task-column 的扫描交界，因此本轮不扩张新模块，仅把 `src/app/chat/page.tsx` 顶部 `appShellCards` 容器在 `xl` 下的固定列宽从 `11.3rem` 微放宽到 `12rem`，让 `Thread anchor / Live switchboard / Edge lane` 三张 mini-cards 获得更稳定的横向呼吸、减少和右侧 `Tasks` 列交界处的 badge/card seam 压力；并同步更新 `src/app/chat/page.test.tsx` focused 断言以锁定该宽度契约。该里程碑已通过 Review 1 功能/规格复核（确认仅是 top mini-card row 的更小步 breathing-room slice，未扩张模块且 `/chat` 公开桥接语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、独立 reviewer JSON 复审通过）。最新 controller-side 结论：right rail 当前保持 coherent、无明显 clipping/overlap，而 `/chat` 剩余最明显热点仍是 center timeline 顶部 mini-card 与右侧 task seam 的轻微 scan-heavy 感；下一轮继续优先围绕该 center/right seam 与 `Active sessions` 邻接区做更小步密度收敛，而不是回到右栏扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` tasks-panel density slice：controller 侧先从当前 worktree、live/local `/chat` 顺序复核与 browser snapshot 出发，确认本轮热点仍是 center timeline 顶部 mini-cards 与其右侧 Tasks seam，而 Tasks 列内部仍可先做一层更轻量的 copy/line-length 收敛，因此本轮不扩张新模块，仅将 `src/app/chat/page.tsx` 的 Tasks 标题从 `Queue the next research slice` 收短为 `Queue next research slice`，并把 task note 最大宽度从 `22ch` 微收至 `20ch`，让右半区首张 task card 标题与三条 task notes 更快扫描、减少与 top mini-card row 交界处的文本牵引；同步更新 `src/app/chat/page.test.tsx` focused 断言锁定新标题契约。该里程碑已通过 Review 1 功能/规格复核（确认仅是 Tasks 面板 wording/density 的小步收敛，未扩张模块且 `/chat` 公开 bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、独立 reviewer JSON 复审通过）。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap，right rail 也未重新成为主热点；下一轮继续优先围绕 center timeline 顶部 mini-card 与 Tasks/Active sessions 邻接区做更小步密度收敛，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-half seam 收敛 slice：controller 侧先从当前 worktree、focused tests、fresh-load local `/chat` snapshot 与 DOM 几何复核出发，确认当前右半区仍可继续通过更均衡的 Tasks/Session-output 配比来减少 center/right seam 的 scan-heavy 感，而无需扩张新模块；因此本轮仅将 `chat-right-rail-grid` 从 `xl:[minmax(196px,1.14fr)_minmax(248px,0.86fr)] / 2xl:[minmax(212px,1.12fr)_minmax(260px,0.88fr)]` 调整为 `xl:[minmax(212px,1.18fr)_minmax(236px,0.82fr)] / 2xl:[minmax(224px,1.16fr)_minmax(252px,0.84fr)]`，让 Tasks 列获得更稳定宽度、右侧 output rail 略收；同时把 Tasks 标题从 `Queue next research slice` 收短为 `Queue next slice`，并把 task note 最大宽度从 `20ch` 再收至 `18ch`，以进一步降低 top task card 在 center/right 交界处的文本牵引。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap；right rail 本身未重新成为主热点，但 center/right seam 仍可继续通过局部 copy 与列宽微调收敛，下一轮继续优先围绕 `/chat` 的 Tasks/Active sessions 邻接区做更小步密度治理，而不是回到右栏扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Tasks 标题密度收敛 slice：controller 侧先从当前 worktree、live/local `/chat` 顺序复核与 Next.js 本地 page/layout 文档读取出发，确认本轮仍应保持 page 级公开路由不变、仅做一处 center/right seam 的真实 UI 小步收敛，因此不扩张新模块，仅将 Tasks 面板标题从 `Queue next slice` 收短为 `Queue next lane`，并把标题最大宽度从 `15ch` 微收至 `14ch`，以减少 top mini-card row 与右侧 task card 交界处的文本牵引；同时同步更新 `src/app/chat/page.test.tsx` focused 断言，保持页面与测试契约一致。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap，本轮主要验证了通过更短 Tasks 标题即可继续轻收 center/right seam 的扫描压力；下一轮继续优先围绕 `/chat` 的 top mini-card / Tasks / Active sessions 邻接区做更小步密度治理，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Tasks microcopy density slice：controller 侧先从当前 worktree、`todo.md`、local `/chat` fresh-load snapshot 与现行 `landing-data.ts` / `page.test.tsx` 契约出发，确认当前 center/right seam 仍主要由 Tasks 首卡与相邻 mini-card / Active sessions 邻接区的扫描节奏决定，因此本轮不扩张新模块、也不改动更大栅格，仅把 `appShellTodos` 中三条 Tasks microcopy 继续压短为 `Heatmap / Ground retrieval.`、`Research lead / Keep lane isolated.`、`Export / Promote brief.`，同步更新 `src/app/chat/page.test.tsx` focused 断言以锁定新 contract，从而验证“进一步缩短 task meta 与 note 句长”能否在不重做结构的前提下继续降低 center/right seam 的文本牵引。该里程碑已通过 Review 1 功能/规格复核（确认仅是 `/chat` Tasks 面板微文案密度收敛，未扩张模块且公开 bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、独立 reviewer JSON 复审通过）。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap；right rail 未重新成为主热点，而 `/chat` 仍可继续优先围绕 top mini-card / Tasks / Active sessions 邻接区做更小步密度治理，下一轮继续沿该 seam 收敛而不是回到右栏扩张或新增模块。

## Validation / review cadence
- [x] 本地参考资料保留在 `docs/reference-local/` 并通过 `.git/info/exclude` 忽略
- [x] 每个里程碑后执行 docker compose config / tests / build / compose up -d --build
- [x] 每个里程碑后执行 3 轮评审并提交中文 commit + push
