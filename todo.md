# SciClaw Clone Execution Todo

## Status legend
- [x] completed
- [~] in progress
- [ ] pending

## Phase A — 基础初始化
- [x] 初始化 Next.js 16 + React 19 + TypeScript + Tailwind 4 项目骨架
- [x] 补齐 Dockerfile、docker-compose.yml、Vitest、ESLint、README
- [x] 建立 landing 页面首版与基础测试

## Phase B — Landing 高保真复刻（P0）
- [~] 执行检查点（2026-05-19 01:07 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、Next.js 16 AGENTS 约束与本地参考，先完成 repo/path/git/status、`.git/info/exclude`、3001/5173/8080/8081 端口只读核验、`todo.md`/landing 实现与测试读取，以及 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照。systematic-debugging 先用 fresh live/local browser_vision + local DOM 几何锁定 root cause：当前本地最大剩余热点仍是右侧 auth/access module 偏 detached、偏重，local 改动前 `authWidth≈274px`、`gapAuthToBest≈107.19px`、`gapPreviewToAuth≈-240.97px`，而 live 视觉则继续把 hero→Best Cases handoff 视为整体节奏热点；因此本轮只做 auth wrapper 单点微收敛，而不扩散到 Best Cases、left radial cluster、top utility controls 或 `/root/nexus-mail`。具体实现：在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `max-w-[198px] / sm:max-w-[210px] / lg:mr-[0.22rem] / lg:-mt-[15.86rem] / lg:max-w-[254px] / xl:mr-[0.36rem] / xl:-mt-[16.14rem] / xl:max-w-[274px]` 收敛为 `max-w-[192px] / sm:max-w-[204px] / lg:mr-[0.18rem] / lg:-mt-[15.7rem] / lg:max-w-[248px] / xl:mr-[0.32rem] / xl:-mt-[15.96rem] / xl:max-w-[268px]`，让 auth/access module 略微减宽、内收、上提；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同与旧值负断言，保持 `/` 与 `/chat` 继续共用同一 landing shell。三轮评审结果：① 功能/规格——fresh live/local 顺序对照确认当前 local 单一最大剩余热点仍是 auth/access module placement/weight，且本轮改动方向是继续减弱 detached 感；页面无明显 broken、clipped 或 overlapping。② 测试/构建/集成——focused `pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx` 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check`、以及 `/` `/chat` `/help/getting-started` HTTP 200 全绿，继续仅发布到 `3001`。③ 静态/安全/契约——added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON `passed=true`，未发现新增 security concern 或 logic error。当前诚实结论：这是一刀 auth/access wrapper 的轻量减宽 + 内收 + 轻上提测试/实现同步切片；下一轮应继续从最新 worktree 与 fresh live/local 证据出发，优先判断 auth/access module 是否仍为首要热点，或是否可以转向 hero→Best Cases handoff 与 left radial cluster 的剩余视觉差距。
- [~] 执行检查点（2026-05-19 00:46 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-shared-landing-home-chat-parity`、`sciclaw-auth-card-wrapper-micropass` 与 roadmap-doc-edit-safety，先完成 repo/path/git/status、端口只读核验、`todo.md`/README/AGENTS/本地参考读取、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 顺序对照，并用 browser_vision + DOM 几何确认 root cause：当前单一最大残余热点仍是本地首屏右侧 auth/access 模块偏 detached、偏重，live `authWidth≈599.5px / gapAuthToBest≈244.75px`，而本地改动前仅 `authWidth≈282px / gapAuthToBest≈107.19px`，说明本地 access rail 仍过窄且相对 preview 过于浮层化。基于该证据，本轮仅做 wrapper-only 微切片：在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `max-w-[206px] / sm:max-w-[218px] / lg:mr-[0.28rem] / lg:-mt-[16.08rem] / lg:max-w-[262px] / xl:mr-[0.44rem] / xl:-mt-[16.38rem] / xl:max-w-[282px]` 收敛为 `max-w-[198px] / sm:max-w-[210px] / lg:mr-[0.22rem] / lg:-mt-[15.86rem] / lg:max-w-[254px] / xl:mr-[0.36rem] / xl:-mt-[16.14rem] / xl:max-w-[274px]`，不改动 auth-card 内部、Best Cases、left radial cluster 或其它模块；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同与旧值负断言，保持 `/` 与 `/chat` 继续共用同一 landing shell。三轮评审结果：① 功能/规格——fresh local browser_vision 继续把 auth/access 模块判为首要但非破坏性热点，同时明确页面无明显 broken、clipped 或 overlapping；本轮改动方向仍是让 auth rail 更轻、更贴近 preview，而非扩散到其它子系统。② 测试/构建/集成——focused `pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx` 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、以及 `/` `/chat` `/help/getting-started` 的 HTTP 200 全绿；compose 重建后首个 HTTP 探针再次出现 startup race `Connection reset by peer`，已按既有技能要求拆成延后重试并确认非应用回归。③ 静态/安全/契约——`git diff --check` 通过，added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON `passed=true`，未发现新增 security concern 或 logic error。当前诚实结论：这是一刀 auth/access wrapper 的轻量减宽 + 内收 + 轻上提测试/实现同步收口切片，而不是新的大范围视觉重构；下一轮应继续基于 fresh live/local 证据，判断 auth/access 模块是否仍是第一热点，或是否已轮转到 left radial cluster / hero→Best Cases handoff。
- [~] 执行检查点（2026-05-19 00:25 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-shared-landing-home-chat-parity` 先完成 repo/path/git/status、端口只读核验、`todo.md`/README/AGENTS 复核，并对当前未提交 landing 切片做 systematic-debugging 式复现。root cause 证据明确显示这不是组件逻辑回退，而是测试合同残留：`src/components/landing-page.tsx` 已把顶部 utility row、left radial cluster 几何、以及 auth/access wrapper/card 全部回落到更轻的 `gap-[0.48rem] / pr-[1.18rem]`、`left-[45.36%] / left-[45.04%] / w-[12.9%] / w-[13.62%]`、和 `feature-auth-wrapper -mt-[2rem] max-w-[206px] ... lg:-mt-[16.08rem] lg:max-w-[262px] ...` / `landing-auth-card max-w-[204px] ... lg:max-w-[452px] ...` 合同；但 `src/app/page.test.tsx` 仍遗留一组 `keeps the utility triggers subtle...` 断言，继续期待旧值 `left-[44.82%] / left-[44.56%] / w-[13.55%] / w-[14.28%]`，导致 focused Vitest 先失败后被最小补丁修正。基于该证据，本轮仅做最小安全收口：不再改动生产 JSX，只把 `src/app/page.test.tsx` 中该残留断言更新为与当前 landing 真实实现一致的新 geometry/line-width 合同；`src/app/chat/page.test.tsx` 与其余首页合同已保持一致，无需再扩散修改。三轮评审结果：① 功能/规格——当前 diff 仅是对既有 runtime 行为的测试合同回正，不改变 `/` 与 `/chat` 的共享 landing shell；controller 侧复现后确认根因只是首页 focused 测试残留旧断言，生产页面结构与样式未新增回退。② 测试/构建/集成——focused `npm test -- --run src/app/page.test.tsx src/app/chat/page.test.tsx` 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、以及 `/` `/chat` `/help/getting-started` HTTP 200 全绿，继续仅发布到 `3001`。③ 静态/安全/契约——`git diff --check` 通过，added-lines 静态安全扫描对 hardcoded secret / shell / eval-exec / pickle / SQL formatting 均 0 命中，repo-scoped 独立 reviewer JSON `passed=true`，未发现新增 security concern 或 logic error。当前诚实结论：这是一刀 landing 首屏微调切片的首页测试合同收口与验证里程碑，而不是新的视觉实现大改；下一轮应继续从最新 worktree 与 fresh live/local 证据出发，在 auth/access 模块剩余突兀感、left radial cluster 锚定感与 hero→Best Cases handoff 之间选择新的最小安全推进点。

- [~] 执行检查点（2026-05-18 22:27 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-shared-landing-home-chat-parity` 先完成 repo/path/git/status、端口只读核验、`todo.md`/README/AGENTS/.git/info/exclude 复核、focused `/`+`/chat` Vitest、全量 `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描、fresh live/local browser 对照与 repo-scoped 独立 reviewer。systematic-debugging 先以 live/local fresh-load 视觉与 DOM 几何确认 root cause：当前单一最大残余热点仍是本地首屏右侧 auth/access 模块过于突兀，明显强于 live，而 left radial cluster 与 hero→Best Cases handoff 仍是次级问题；本轮 local `browser_vision` 在改动前明确把 auth/access 浮层判为最大热点，controller-side 几何也显示 auth wrapper `312px` 宽、`gapAuthToBest≈107.19px`、且相对 preview 前景感偏强。基于该证据，本轮仅做 auth-wrapper micro-pass：在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[2.34rem] / max-w-[228px] / sm:max-w-[240px] / lg:mr-[0.48rem] / lg:-mt-[17.18rem] / lg:max-w-[292px] / xl:mr-[0.68rem] / xl:-mt-[17.52rem] / xl:max-w-[312px]` 收紧为 `-mt-[2.26rem] / max-w-[222px] / sm:max-w-[234px] / lg:mr-[0.42rem] / lg:-mt-[16.92rem] / lg:max-w-[284px] / xl:mr-[0.62rem] / xl:-mt-[17.24rem] / xl:max-w-[304px]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同与旧值负断言，保持 `/` 与 `/chat` 继续共用同一 landing shell。三轮评审结果：① 功能/规格——fresh local browser_vision 在改动后确认 auth/access 模块“更不突兀、更贴近中心 preview”，当前单一最大残余热点已转回 hero→Best Cases handoff，且页面无明显 clipping、overlap、broken layout 或 absurd spacing；controller-side 几何也确认 auth wrapper 宽度已收至 `304px`。② 测试/构建/集成——focused Vitest 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、以及 `/` `/chat` `/help/getting-started` HTTP 200 全绿，继续仅发布到 `3001`。③ 静态/安全/契约——`git diff --check` 通过，added-lines 静态安全扫描对 hardcoded secret / shell / eval-exec / pickle / SQL formatting 全为 0 命中，repo-scoped 独立 reviewer JSON `passed=true`；无新增 security concern 或 logic error。当前诚实结论：这是一刀 auth/access wrapper 的减宽、减突出、轻微上移/内收收口切片；下一轮最值得继续推进的单一最小切片应转向 hero→Best Cases vertical handoff，而不是再大幅回到 auth card 或触碰 `/root/nexus-mail`。
- [~] 执行检查点（2026-05-18 20:18 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-shared-landing-home-chat-parity` 先完成 repo/path/git/status、端口只读核验、`todo.md` 与本地参考读取、聚焦 `pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描、fresh local browser_vision 与 repo-scoped 独立 reviewer。systematic-debugging 先复现当前未提交切片的真实失败：`src/app/chat/page.test.tsx` 仍保留一组旧的连接线宽度断言（`w-[13.95%]` / `w-[14.7%]`），而 `src/components/landing-page.tsx` 与其余断言已回到新的 live 对齐值 `w-[13.55%]` / `w-[14.28%]`，因此根因是 `/chat` 共享测试合同残留重复旧断言，而不是 landing 组件逻辑回退。基于该证据，本轮仅做最小安全收口：保留当前 radial-cluster 几何微调实现（`Literature Analysis left-[44.82%] + line w-[13.55%]`、`Data Mining left-[44.56%] + line w-[14.28%]`），并删除 `src/app/chat/page.test.tsx` 中两条已失效、且与上方新断言重复冲突的旧宽度检查；同时 `src/app/page.test.tsx` 继续同步锁定首页共享 geometry 合同。三轮评审结果：① 功能/规格——fresh local `browser_vision` 确认页面无明显 clipping、overlap、broken layout 或 absurd spacing；left radial cluster 已不算荒谬错位，但仍略显 under-anchored、轻微偏 floaty，这一剩余视觉差距已如实保留在路线图中。② 测试/构建/集成——聚焦 Vitest 15/15、全量 `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、以及 `/` `/chat` `/help/getting-started` HTTP 200 全绿，继续仅发布到 `3001`。③ 静态/安全/契约——`git diff --check` 通过，added-lines 静态安全扫描 `static_scan=ok`，repo-scoped 独立 reviewer JSON `passed=true`；唯一非阻塞建议仍是后续可减少对精确 Tailwind arbitrary values 的测试耦合。当前诚实结论：这是一刀 radial-cluster 微调切片的测试合同收口与验证里程碑，而不是新的大范围视觉重做；下一轮应继续基于 fresh live/local 证据，在 left cluster 进一步锚定与 hero→Best Cases handoff 之间选择新的最小安全推进点。
- [~] 执行检查点（2026-05-18 18:13 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-shared-landing-home-chat-parity` 先完成 repo/path/git/status、3001/8080/8081 端口只读核验、`todo.md` 状态读取、focused `/`+`/chat` Vitest、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描，以及 repo-scoped 独立 reviewer。root-cause 证据显示当前最小安全切片仍是 left radial cluster 的 inward anchoring，而不是再次扩大到 auth card 或 Best Cases：live fresh browser_vision 继续把 hero→Best Cases handoff 视为公开站更大的共同残余，但 local controller-side 几何与前一轮 checkpoint 已表明本地更直接可控的热点是 left cluster 漂浮感。因此本轮仅在 `src/components/landing-page.tsx` 将 `Literature Analysis` 节点从 `left-[44.2%]` 微收至 `left-[44.55%]`、连接线从 `w-[14.4%]` 收至 `w-[13.95%]`；将 `Data Mining` 节点从 `left-[44.05%]` 微收至 `left-[44.3%]`、连接线从 `w-[15%]` 收至 `w-[14.7%]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 geometry/class 合同，保持 `/` 与 `/chat` 继续共用同一 landing shell。三轮评审结果：① 功能/规格——live fresh browser_vision 仍把 hero→Best Cases 视为公开主热点，而本地 `browser_vision` 本轮 provider 失效，已按降级流程回退到 fresh local snapshot + DOM 几何；controller-side 复核显示 `Literature Analysis x≈343.89`、`Data Mining x≈344.34`，较上一轮继续 inward，且无 clipping、overlap 或明显 broken 状态。② 测试/构建/集成——focused Vitest 15/15、全量 `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全绿，继续仅发布到 `3001`。③ 静态/安全/契约——`git diff --check` 通过，added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON `passed=true`，仅留下一个非阻塞建议：`src/app/chat/page.test.tsx` 中连接线查询断言有轻微重复，可在后续测试去噪时顺手清理。当前诚实结论：这是一刀 left radial cluster inward anchoring 微收敛切片；下一轮应继续依据 fresh live/local 证据，在 hero→Best Cases handoff 与 left cluster 残余漂浮感之间选择新的最小安全推进点，而不是触碰 `/root/nexus-mail`。
- [~] 执行检查点（2026-05-18 17:47 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review 与 systematic-debugging 先完成 repo/path/git/status、3001/5173/8080 端口只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、focused `/`+`/chat` Vitest、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示上一轮 auth/access rail 收窄后，fresh local 最大剩余热点已更明确转向 left radial cluster 的漂浮/锚定感，而不再是右侧 auth/access card：controller-side 几何显示改动前 `feature-auth-wrapper≈312px`、`authBottomToBest≈107.19px` 已稳定，而 `browser_vision` 则把 left cluster 的 orphaned/crowded 感列为更明显问题。因此本轮仅在 `src/components/landing-page.tsx` 做 cluster-only geometry micro-pass：将 `Literature Analysis` 节点从 `left-[43.1%]` 收到 `left-[44.2%]`、连接线宽从 `15.6%` 收到 `14.4%`、标签锚点从 `left-[-1.96rem] sm:left-[-2.04rem]` 收到 `left-[-1.72rem] sm:left-[-1.82rem]`；同时将 `Data Mining` 节点从 `left-[42.9%]` 收到 `left-[44.05%]`、连接线宽从 `16.2%` 收到 `15%`；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 geometry/class 合同，保持 `/` 与 `/chat` 继续共用同一 landing shell。三轮评审结果：① 功能/规格——fresh local browser_vision 确认页面没有明显 broken/clipping，当前视觉上仍以 right auth/access card 为首要视线热点，但 left radial cluster 已被 controller-side 实际向中心收拢；DOM 几何显示 `Literature Analysis` 节点 `x≈342.72`、`Data Mining` 节点 `x≈343.52`，较改动前继续 inward，对应 left 空场略有收敛。② 测试/构建/集成——focused Vitest 15/15、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全绿；compose 重建后首个 HTTP 探针再次出现短暂 `Connection reset by peer`，已按技能要求拆为延后重试并确认属于启动竞态而非应用回归。③ 静态/安全/契约——added-lines 静态安全扫描对 secret、shell/eval/exec、pickle、SQL formatting 均为 0 命中，repo-scoped 独立 reviewer JSON `passed=true`，仅留下“未来可减少精确 Tailwind 几何合同耦合”的非阻塞建议。当前诚实结论：这是一刀 left radial cluster inward anchoring 微收敛切片；下一轮应继续根据 fresh live... [truncated]
- [~] 执行检查点（2026-05-18 17:31 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review 与 systematic-debugging 先完成 repo/path/git/status、3001/现有占用端口只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、focused `/`+`/chat` Vitest、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer 前置要求。root-cause 证据显示当前本地首屏最大剩余热点仍是右侧 auth/access rail 的外挂浮层感：上一轮 local browser_vision 继续把 auth/access card 判为单一最大偏差，而 controller-side 几何也显示改动前 `feature-auth-wrapper` 仍约 `316px` 宽、`previewBottomToAuthTop≈-260.81px`、`authToBest≈107.19px`，说明 access rail 仍稍宽且稍显外凸。基于此，本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做更小的 narrowing + inward + upward micro-pass：`-mt` 从 `2.28/17.04/17.38rem` 调到 `2.34/17.18/17.52rem`，`max-w` 从 `232/244/296/316px` 收到 `228/240/292/312px`，`lg/xl mr` 从 `0.54/0.74rem` 收到 `0.48/0.68rem`；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同与旧值负断言，保持 `/` 与 `/chat` 继续共用同一 landing shell。三轮评审结果：① 功能/规格——fresh local browser_vision 明确确认当前页面没有明显 broken/clipping/overlap/absurd spacing，auth/access card 已不再被判为唯一最大视觉热点，新的最显眼轻微残余转为左侧 radial cluster 的漂浮/锚定感；controller-side 几何显示 rebuild 后 auth wrapper 宽度收至约 `312px`，且 `previewBottomToAuthTop≈-263.05px`，方向与预期一致。② 测试/构建/集成——focused Vitest 15/15、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全绿，继续仅发布到 `3001`。③ 静态/安全/契约——added-lines 安全扫描 `STATIC_SCAN_OK`，当前 diff 未引入 secret、shell/eval/exec、pickle 或 SQL formatting 风险。当前诚实结论：这是一刀 auth/access rail 的更小幅减宽、上提、内收切片；下一轮应优先转向 left radial cluster 的锚定/漂浮感微调，而不是再扩散到无关模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点（2026-05-18 17:10 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review 与 systematic-debugging 先完成 repo/path/git/status、3001/其他占用端口只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、focused `/`+`/chat` Vitest、全量 `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示当前本地首屏最明显残余仍是右侧 auth/access rail 相对中心 preview 的“外挂浮层感”：live 运行态 access band 宽约 `599.5px` 且 `gapAuthToBest≈244.75px`，而本轮改动前 local auth wrapper 仅约 `324px` 宽、`gapAuthToBest≈107.19px`，并被 local browser_vision 继续判为最大热点。因此本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做更小的 inward+narrowing+raising micro-pass：`-mt` 从 `2.18/16.84/17.2rem` 调到 `2.28/17.04/17.38rem`，`max-w` 从 `236/248/304/324px` 收到 `232/244/296/316px`，`lg/xl mr` 从 `0.62/0.82rem` 收到 `0.54/0.74rem`；同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同与旧值负断言，保持 `/` 与 `/chat` 共用同一 landing shell。三轮评审结果：① 功能/规格——fresh local browser_vision 明确确认 auth/access card 仍是首要热点，但已比上一轮“更贴近中心 preview、外挂感减轻”，且 hero→Best Cases 过渡未明显变坏；controller-side 几何显示 rebuild 后 auth wrapper 宽度收至约 `316px`，`previewBottomToAuthTop≈-260.81px`、`gapAuthToBest≈107.19px`，方向与预期一致。② 测试/构建/集成——focused Vitest 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全绿。③ 静态/安全/契约——added-lines 安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON `passed=true`，未发现 secrets、shell/eval/exec、SQL 拼接或测试契约自相矛盾。当前诚实结论：这是一刀 auth/access rail 轻量减宽 + 上提 + 内收的微收敛切片；下一轮仍应优先在 auth rail 的残余独立感与 left radial cluster 的漂浮感之间选更小、更安全的单点推进，而不是扩散到无关模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点（2026-05-18 16:53 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review 与 systematic-debugging 先完成 repo/path/git/status、3001 端口只读核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 顺序对照、focused `/`+`/chat` Vitest、全量 `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示当前最小且真实可落地的下一刀仍是 desktop hero 右侧 auth/access rail 的轻微脱离感：live fresh 视觉仍把 hero→Best Cases 视为更大的公开残余，但 fresh local 则把 auth rail 与 center preview 的拥挤/下压感视为更直接热点，因此本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的 desktop `lg/xl mr` 从 `0.48/0.64rem` 放宽到 `0.62/0.82rem`，并把 `lg/xl -mt` 从 `17.02/17.42rem` 回提到 `16.84/17.2rem`，用更小的 inward-separation + upward-lift 微调减弱 auth card 对 preview 的压迫感；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同与旧值负断言，保持 `/` 与 `/chat` 共用同一 landing shell。三轮评审结果：① 功能/规格——live/local fresh-load 对照与 local browser_vision 均把 auth rail 视为本地当前最值得推进的单点，小改后页面无新 clipping、overlap 或 absurd spacing；② 测试/构建/集成——focused Vitest 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` HTTP 200 全绿；③ 静态/安全/契约——added-lines 安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON `passed=true`，未发现 secrets、shell/eval/exec、SQL 拼接或测试契约漂移。当前诚实结论：这是一刀 auth/access rail spacing/attachment 微收敛切片；下一轮应继续基于 fresh live/local 证据，在 auth rail 残余脱离感、left radial cluster 锚定感与 preview 微标签可读性之间选择新的最小安全切片，而不是触碰 `/root/nexus-mail` 或扩散到无关模块。
- [~] 执行检查点（2026-05-18 16:29 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-shared-landing-home-chat-parity` 先完成 repo/path/git/status、3001/5173/8080 端口只读核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 顺序对照、focused `/`+`/chat` Vitest、全量 `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps`、`git diff --check`、HTTP 200 校验与 added-lines 静态安全扫描、repo-scoped 独立 reviewer。root-cause 证据显示当前 public landing 的单一最小安全切片仍在右侧 auth/access rail：live desktop `VERIFY ACCESS CODE→Best Cases` 间距约 `244.75px`，而本轮改动前 local 仅约 `188.61px` 且 auth wrapper 仅约 `304px` 宽、较 live 更窄更靠右，导致 hero 右下入口相对 preview 仍显拘束。基于此，本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做 geometry-only 微调——将 `max-w` 从 `228/240/292/312px` 放宽到 `236/248/304/324px`，同时将 desktop `mr` 从 `0.62/0.82rem` 左收至 `0.48/0.64rem`，并把 `lg/xl -mt` 轻微回摆到 `17.02/17.42rem`；同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同与旧值负断言，保持 `/` 与 `/chat` 继续共用同一 landing shell。三轮评审结果：① 功能/规格——fresh live/local 顺序对照与 DOM 几何确认方向正确，本地 auth wrapper 已放宽到约 `320px`、`gapAuthWrapToBest` 收敛到约 `105.59px`，页面未出现新的 clipping、overlap 或 blank render；② 测试/构建/集成——`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx` 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps` 与 `/` `/chat` `/help/getting-started` HTTP 200 全绿，其中 compose 重建后首次 HTTP 探针曾出现启动竞态 `Connection reset by peer`，已按技能要求拆分为后续重试并确认非应用回归；③ 静态/安全/契约——`git diff --check` 通过，added-lines 对 hardcoded secret / shell / eval-exec / pickle / SQL formatting 扫描均为 0 命中，repo-scoped 独立 reviewer JSON `passed=true`。当前诚实结论：这是一刀 auth/access rail widening + inward alignment 微收敛切片；下一轮应继续基于 fresh live/local 证据，在 auth rail 残余拘束感... [truncated]
- [~] 执行检查点（2026-05-18 16:11 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review 与 systematic-debugging 先完成 repo/path/git/status、3001/5173/8080 端口只读核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 对照、focused `/`+`/chat` Vitest、全量 `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose ps`、`curl -I /` + `/chat`、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示当前 local 首屏里 hero→`Best Cases` 的垂直过渡已 roughly matching，不再是首要切片；fresh local browser_vision 与 live/local 结构对照均把右侧 auth/access card 重新判为更明显热点。因此本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做轻量 inward+narrowing+raising micro-pass：将 `max-w` 从 `232/244/300/320px` 收到 `228/240/292/312px`，并把 desktop `mr` 从 `0.72/0.92rem` 收到 `0.62/0.82rem`、`-mt` 从 `17.18/17.62rem` 微提到 `17.04/17.46rem`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同；未改动 Best Cases spacing、utility row、help/public routes 或任何 `/root/nexus-mail` 资产。三轮评审结果：①功能/规格——fresh local visual QA 确认页面无 clipping、overlap 或 absurd spacing，auth card 仍是主热点但已更收敛，utility menu 展开与默认 hero/case 状态正常；②测试/构建/集成——focused Vitest 15/15、全量 `npm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose ps`、`curl -I http://127.0.0.1:3001/` 与 `/chat`、`git diff --check` 全绿；③静态/安全/契约——added-lines 安全扫描对 secrets/shell/eval/pickle/SQL format 0 命中，repo-scoped 独立 reviewer JSON `passed=true`。当前诚实结论：这一 diff 是一次 auth/access rail wrapper 微收敛切片；下一轮应继续围绕 auth card 的视觉权重、底部 CTA/底缘节奏与 hero 叠放关系做更小步推进，而不是重新放大 Best Cases handoff。
- [~] 执行检查点（2026-05-18 15:47 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 systematic-debugging 先完成 repo/path/git/status、3001 端口只读核验、live `https://sciclaw.ai/` 结构快照、focused `/`+`/chat` Vitest、全量 `npm run test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose ps`、`curl -I /` + `/chat`、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer；root-cause 证据显示当前最安全且真实落地的最小切片仍是 desktop 首屏 hero→Best Cases 过渡偏紧，因此仅把 `best-cases-section` 的 desktop top margin 从 `lg/xl 5.32/5.64rem` 微放宽到 `5.4/5.74rem`，并将 `feature-auth-wrapper` desktop 负顶距从 `17.22/17.68rem` 轻微回摆到 `17.18/17.62rem`，同步更新 `/` 与 `/chat` focused 合同。三轮评审结果：①功能/规格——fresh live/local 结构仍一致，且本轮只收敛首屏 handoff；②测试/构建/集成——focused Vitest 15/15、full Vitest 23/23、lint/build/compose/HTTP 200 全绿；③静态/安全/契约——无 secrets、eval/exec、shell 注入或 SQL 拼接新增，独立 reviewer JSON 通过。剩余公开热点仍集中在更细粒度的 live/local 视觉 fidelity（尤其 fresh-load 几何与视觉工具缓存漂移之间的差异），下一轮继续围绕 landing 首屏视觉比对做更小步推进。
- [~] 执行检查点（2026-05-18 15:27 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review 与 systematic-debugging 先完成 repo/path/git/status、端口只读核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构/视觉对照、focused `/`+`/chat` Vitest、全量 `pnpm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据继续显示当前最安全且真实可提交的单一切片仍是缩短首屏 hero 到 `Best Cases` 的桌面端纵向停顿：live/local fresh-load `browser_vision` 都把 hero 下方留白判为第一热点，因此本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop top margin 从 `lg/xl 5.56/5.92rem` 收紧到 `5.32/5.64rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同；未改动 auth/access rail、left radial cluster、utility row、help/public routes 或任何 `/root/nexus-mail` 资产。Review 1 功能/规格复核：live/local fresh-load 仍将 hero→`Best Cases` 过渡视为最大共同残余，本轮收紧后未见新的 clipping、overlap 或 absurd spacing。Review 2 测试/构建/集成复核：focused `/`+`/chat` Vitest 15/15、全量 `pnpm test` 23/23、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` + `/chat` HTTP 200 全通过。Review 3 静态/安全/契约复核：当前 diff 仅涉及 `src/components/landing-page.tsx`、`src/app/page.test.tsx`、`src/app/chat/page.test.tsx`，added-lines 静态安全扫描 0 命中，repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security concern 或 logic error。下一轮应继续基于 fresh live/local 证据，在 hero→`Best Cases` handoff、auth/access card 与 left radial cluster 之间选取新的最小安全切片推进。
- [~] 执行检查点（2026-05-18 14:37 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review 与 systematic-debugging 先完成 repo/path/git/status、端口只读核验、focused `/`+`/chat` Vitest、`docker compose config`、`docker compose ps`、`curl -I http://127.0.0.1:3001/` 与 `/chat`、以及 live/local fresh-load 结构对照；root-cause 证据显示当前最安全且真实落地的下一刀仍是收紧 hero→Best Cases desktop handoff，因此本轮仅将 `best-cases-section` 的 desktop top margin 从 `lg/xl 5.9/6.24rem` 收紧到 `5.56/5.92rem`，同步更新 `/` 与 `/chat` focused 合同测试。浏览器视觉工具本轮退化为 provider invalid-response，已按技能回退到顺序 snapshot + DOM 几何核验，本地当前 heroBottom→bestTop 间距约 `94.72px`、未见结构性缺失；接下来进入 3 轮评审、提交并推送。
- [~] 执行检查点（2026-05-18 11:29 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `browser-verification-degradation-fallback` 先完成 repo/path/git/status、`.git/info/exclude`、3001 端口/compose 运行态、AGENTS.md 的 Next.js 16 docs 约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 对照、focused `/`+`/chat` Vitest、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check` 与 added-lines 静态安全扫描。root-cause 证据显示本轮最小安全切片应回到 hero→`Best Cases` vertical handoff：fresh live DOM 几何仍给出 `verifyBottom≈1066.5px`、`bestTop≈1311.25px`、`gap≈244.75px`，而本地上一轮仍只有 `gapVerifyToBest≈177.05px`、`gapAuthToBest≈94.72px`；同时 live/local 顶部 utility controls 与 auth rail 均未重新成为更大的新热点。因此本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` desktop top margin 从 `lg/xl 5.62/5.92rem` 放宽到 `5.9/6.24rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 spacing 合同，不改动 auth/access rail、left radial cluster、utility row、help/public routes 或 `/root/nexus-mail` 任意服务。Review 1 功能/规格复核：live fresh 几何继续表明公开站 hero→`Best Cases` 呼吸带显著更宽；rebuild 后本地 fresh browser snapshot + DOM 读数已提升到 `gapVerifyToBest≈183.16px`、`gapAuthToBest≈99.83px`，方向正确且未出现 clipping、overlap 或 absurd spacing。local `browser_vision` 本轮再次退化为 `Vision analysis returned no content.`，已按 fallback 以 fresh snapshot + DOM/HTTP + build 事实作为本地视觉验收依据，未将工具噪声误判为应用回归。Review 2 测试/构建/集成复核：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3 静态/安全/契约复核：当前 diff 仅涉及 `src/components/landing-page.tsx`、`src/app/page.test.tsx`、`src/app/chat/page.test.tsx`，added-lines 静态安全扫描对 hardcoded secret /... [truncated]
- [~] 执行检查点（2026-05-18 10:59 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 browser-verification-degradation-fallback 先完成 repo/path/git/status、`.git/info/exclude`、3001 端口/compose 运行态、AGENTS.md 的 Next.js 16 docs 约束、live `https://sciclaw.ai/` fresh-load 对照、focused `/`+`/chat` Vitest、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示当前更安全的最小切片重新回到 hero→`Best Cases` vertical handoff：live fresh `browser_vision` 直接把主热点判为“首屏下方留白过大、下个模块起点过低”，而本地 `browser_vision` 本轮退化为白屏/空内容，已按降级流程改用 fresh snapshot + focused tests/build/HTTP/controller-side diff 事实作为本地验收依据；当前运行时代码也确实仍停在上一轮较紧的 `best-cases-section mt-[2.38rem] sm:mt-[2.8rem] lg:mt-[5.22rem] xl:mt-[5.5rem]`。因此本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 放宽回 `mt-[2.52rem] sm:mt-[2.96rem] lg:mt-[5.62rem] xl:mt-[5.92rem]`，并把标题块底边距从 `mb-[2rem]` 回到 `mb-[2.12rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 spacing 合同，不改动 auth/access rail、left radial cluster、utility row、help/public routes 或 `/root/nexus-mail` 任意服务。Review 1 功能/规格复核：live fresh visual 继续把 hero→`Best Cases` 过渡判为首要热点；local `browser_vision` 出现白屏/无内容退化，因此按 fallback 以 fresh local snapshot、当前源码/测试合同与重建后 HTTP 200 作为验收依据，未将工具噪声误判为应用回归。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3 静态/安全/契约复核：当前 diff 仅涉及 `src/components/landing-page.tsx`、`src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 三个文件，added-lines 静态安全扫描 0 命中... [truncated]
- [~] 执行检查点（2026-05-18 09:56 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 browser-verification-degradation-fallback 先完成 repo/path/git/status、`.git/info/exclude`、3001 端口与 compose 只读核验、AGENTS.md 的 Next.js 16 docs 约束、live/local fresh-load 对照、focused `/`+`/chat` Vitest、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示 fresh local 中 left radial cluster 才是更直接的新一轮首要热点：browser_vision 已不再把它判为主问题，转而把 auth/access card 视为更重的残余；对应 controller-side DOM 几何也证明左侧两枚节点已从正文区明显左撤——`Literature Analysis` 标签右缘约 `420.20px`，已落在正文标题左缘 `407.59px` 的轻度近邻而不再深压正文列，`Data Mining` 节点整体也从 `left≈361px` 左移到 `≈336px`。因此本轮仅在 `src/components/landing-page.tsx` 对 left radial cluster 做 cluster-only geometry slice：将 `Literature Analysis` 节点从 `left-[49.45%]` 左撤到 `left-[42.2%]`、连接线宽从 `9.6%` 放到 `16.4%`，其标签锚点同步收至 `left-[-2.12rem] sm:left-[-2.2rem]`；将 `Data Mining` 节点从 `left-[49.34%]` 左撤到 `left-[41.9%]`、连接线宽从 `9.9%` 放到 `17%`；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 geometry/class 合同，不改动 auth rail、Best Cases spacing、utility row、help/public routes 或 `/root/nexus-mail` 任意服务。Review 1 功能/规格复核：fresh local browser_vision 明确确认 hero 现在“整体已基本 coherent”，left node cluster 已不再是最主要 crowding issue，当前更显著残余转为右下 auth/access card 视觉权重；顺序 live/local 对照也表明本轮改动没有引入新的 clipping、overlap 或 absurd spacing。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `/` 与 `git diff --check` 全通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3 静态/安全/契约复核：当前 diff 仅涉及 `src/components/landing-page.tsx`、`src/app/page.test.tsx... [truncated]
- [~] 执行检查点（2026-05-18 09:35 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging、`sciclaw-marketing-landing-parity` 与 browser-verification-degradation-fallback 先完成 repo/path/git/status、`.git/info/exclude`、3001 端口与 compose 只读核验、AGENTS.md 的 Next.js 16 docs 约束、live/local fresh-load 对照、focused `/`+`/chat` Vitest、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示当前 local 首屏 auth/access card 已不再是“明显过低”，而是只剩底部 footer/CTA 略显下坠；因此本轮仅对 `src/components/landing-page.tsx` 的 `feature-auth-wrapper` 做 wrapper-only upward micro-pass：保持宽度与右侧贴合不变，只把 desktop `lg/xl -mt` 从 `17.58/18rem` 提到 `18.08/18.5rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class 合同，不改动 `/chat` 共用壳、utility row、Best Cases spacing、help/public routes 或 `/root/nexus-mail` 任意服务。Review 1 功能/规格复核：fresh local browser_vision 明确确认 auth/access card 现在已“reasonably attached”，最新 upward move 是改进而非回退；当前 single most noticeable hotspot 只剩 card 底部 footer/CTA 略显下坠，而非整体过低或 broken attachment。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3 静态/安全/契约复核：当前 diff 仅涉及 `src/components/landing-page.tsx`、`src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 三个文件，added-lines 静态安全扫描 0 命中，repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security concern 或 logic error。当前诚实结论：这一 diff 是一次 auth/access rail upward-only micro-pass；下一轮应继续优先收敛 auth card 底部 footer/CTA 的下坠感，或在 fresh live/local 证据确认后再切回 left radial cluster，而不是误触其它仓库/端口。
- [~] 执行检查点（2026-05-18 09:15 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 phase-driven-autonomous-execution、requesting-code-review、systematic-debugging 与 `sciclaw-marketing-landing-parity` 先完成 repo/path/git/status、`.git/info/exclude`、端口/compose 只读核验、AGENTS.md 的 Next.js 16 约束、live/local fresh-load 对照、focused `/`+`/chat` Vitest、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`。root-cause 证据显示当前 local 首屏最值得继续推进的最小安全切片仍落在 hero 底部 preview/auth 组与 `Best Cases` 之间的 handoff，但上一轮 widening 后本地更突出的实际残余已转为 auth rail 略低、略宽、对 section handoff 造成轻微下坠感；因此本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做 wrapper-only upward+narrowing micro-pass：保持 mobile 不变，只把 desktop `lg/xl -mt` 从 `17.2/17.58rem` 提到 `17.58/18rem`，并把 desktop `lg/xl max-w` 从 `316/336px` 收到 `312/332px`，不改动端口、`/chat` 共用壳、utility row、radial cluster 或 `/root/nexus-mail` 任意服务；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 shared class 合同。Review 1 功能/规格复核：fresh local browser_vision 明确确认 hero→`Best Cases` handoff 已比改动前更自然、auth card 不再与下方标题竞争，且页面没有新增 clipping、overlap 或 absurd spacing；controller-side DOM 几何显示 `authToBest≈88px`、`previewToBest≈121.06px`，说明 handoff 维持稳定同时 auth rail 更内聚。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 与 `git diff --check` 全通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3 静态/安全/契约复核：当前 diff 仅涉及 `src/components/landing-page.tsx`、`src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 三个文件，added-lines 未引入 secrets、shell/eval/exec、pickle 或 SQL format 风险；接下来以当前 live diff 为准执行 repo-scoped 独立 reviewer。当前诚实结论：这一 diff 是一次 auth/access rail upward+narrowing micro-pass，剩余 primary gap 仍是左侧 radial cluster 略显 stray，以及 hero 底部 `Get started` 引导文案相对 ... [truncated]
- [~] 执行检查点（2026-05-18 08:53 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 systematic-debugging 先完成 repo/path/git/status、`.git/info/exclude`、`ss`/`docker compose ps` 只读核验、AGENTS.md/Next.js 16 约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` sequential fresh-load 对照、focused `/`+`/chat` Vitest、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示当前 fresh local 最明显剩余问题仍是 hero→`Best Cases` 过渡略断裂，而左侧 radial cluster 继续是次级但稳定可见热点；因此本轮仅做一个 hero→`Best Cases` handoff tightening micro-pass：将 `src/components/landing-page.tsx` 中 `best-cases-section` 顶部间距从 `mt/sm/lg/xl 2.52/2.96/5.48/5.78rem` 收到 `2.38/2.8/5.22/5.5rem`，同时把标题块底边距从 `mb-[2.12rem]` 微收至 `mb-[2rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享合同断言，不改动端口、`/chat` 复用结构、auth rail、utility row 或 `/root/nexus-mail` 任意服务。Review 1 功能/规格复核：sequential live/local fresh-load browser_vision 继续把 local handoff 判为“已更自然但仍略断裂”，同时确认左侧 radial cluster 仍是当前最明显热点之一，且未出现新的 broken/clipping/overlap；controller-side browser DOM 几何显示 `gapHeroToBest≈92.47px`、`gapAuthToBest≈92.47px`、`gapPreviewToBest≈132.27px`。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3 静态/安全/契约复核：当前 diff 仅涉及 `src/components/landing-page.tsx`、`src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 三个文件，added-lines 静态安全扫描对 hardcoded secret / shell 注入 / eval/exec / pickle / SQL format 0 命中，repo-scoped 独立 reviewer 将再次以当前 live diff 为准执行；当前诚实结论：这一 diff 是一次 best-cases handoff... [truncated]
- [~] 执行检查点（2026-05-18 08:28 UTC）：本轮继续严格只在 `/root/sc

... [OUTPUT TRUNCATED - 650979 chars omitted out of 700979 total] ...

hat/page.test.tsx` 已同步锁定新的 wrapper / auth-card / inner-shell 合同。Review 1 功能/规格复核：fresh local browser_vision 明确确认 auth/access module 已比上一轮更轻、略更 attached，未出现明显 clipping，当前最大剩余热点诚实保留为 auth module 与 Best Cases 之间的章节竞争；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 与 Best Cases 章节节奏的残余热点继续小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth/access module docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 锁定当前最小安全切片：本地 stacked auth/access module 虽仍是首屏最明显剩余热点，但问题已收敛为相对 preview 仍略偏低、hero→Best Cases 过渡仍稍紧，而非结构错误、宽度失真或新的 clipping/overlap。本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步上提并略放宽（`-mt/max-w` 由 `1.68rem/316-328px/8.62-8.92rem/540-556px` 收敛为 `1.8rem/324-336px/9.04-9.34rem/548-564px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper 合同，保持 `/` 与 `/chat` 继续共用同一 marketing landing shell。Review 1 功能/规格复核：顺序 live/local QA 与 fresh local browser vision 确认 auth/access module 仍是当前单一主热点，但已较上一轮更贴近 preview、宽度更接近 live support band，且页面无新的 broken/clipping/overlap；left radial cluster 与 Best Cases 过渡仍为次级残余问题。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“未来可补更行为化视觉回归以减少精确 class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否仍是主热点，或是否应转向 hero→Best Cases 纵向节奏与左侧 radial cluster anchoring 的剩余微差。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 auth-card 结构合同对齐，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 结构复核，并用 `browser_console` 几何确认 root cause：live 运行态 access-code 输入/VERIFY 按钮整体位于主 preview 下方（`authInput.y≈956`、`authBtn.y≈1019.75`，明显低于 preview `y≈561.67`），而本地仍把 auth-card 作为 preview 右侧并列模块（`authCard.y≈482.36` 与 preview `y≈478.53` 近乎同高），说明当前剩余主偏差是结构层级错误而非单纯 spacing。基于该证据，本轮仅在 `src/components/landing-page.tsx` 将 `FeaturePreview` 从 `lg/xl` 两列 auth-sidecar 结构改为单列堆叠：移除 `lg:grid-cols-[minmax(0,1fr)_218px]` / `gap-x` / `col-start` / `row-start` 契约，使 heading 保持 `order-1`、preview card 改为 `order-2`、auth wrapper 改为 `order-3 mx-auto mt-[0.32rem] max-w-[260px] sm:max-w-[272px] lg:mt-[0.42rem]`，让 access module 回到 preview 下方次级模块；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，将旧的 large-screen sidecar/grid 断言替换为新的 stacked-order / centered-width 合同，保持 `/` 与 `/chat` 共用 landing shell。Review 1 功能/规格复核：live/local fresh-load 结构与 DOM 几何均确认本轮变更方向与 live 层级一致，本地页面未出现新的 clipping、overlap 或 broken content；Review 2 测试/构建/集成复核：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响任何 `/root/nexus-mail` 服务/端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 secret/shell/eval/pickle/sql-injection 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 live/local evidence 出发，评估结构回正后首屏剩余最大 fidelity 热点是否转移到 radial cluster 或 text-stack spacing。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛；已完成 live/local fresh-load、browser vision、Vitest、lint、build、`docker compose config`、HTTP 200 与静态安全扫描，当前 diff 只剩 final reviewer + 文档状态收口。`docker compose up -d --build web` 受外部 `docker/dockerfile:1.7` registry 拉取网络不可达阻塞，已按 fail-closed 记录为外部环境问题而非应用回归，下一轮将继续在同一仓库内推进 review/commit/push。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 live/local fresh-load 顺序复核：live `https://sciclaw.ai/` 结构快照仍显示 access-code 模块位于主 preview 之后，而本地 `http://127.0.0.1:3001/` / `/chat` 继续保持同一 marketing landing shell、仅发布在 `3001`。结合现有 stacked auth-card 结构与 local 几何复核，root cause 已从“层级错误”收敛为“auth-card 仍略窄、略低、与 preview 的衔接仍偏松”，因此本轮只做一个小型 docking slice：在 `src/components/landing-page.tsx` 将 auth host wrapper 从 `mt-[0.08rem] max-w-[272px] sm:max-w-[284px] lg:mt-[0.16rem]` 调整为 `-mt-[0.08rem] max-w-[280px] sm:max-w-[292px] lg:mt-[0.04rem]`，同时把 `landing-auth-card` 自身从 `max-w-[218px]` 放宽到 `224px`，并轻微放松外层 border/background/shadow/blur/padding，让入口卡片更贴近 preview 且不再那么压缩；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 stacked auth wrapper 与 auth shell 的精确类合同，保持 `/` 与 `/chat` 继续同构。Review 1 功能/规格复核：sequential live/local 结构对照确认本轮仍围绕真实剩余热点推进，rebuild 后 local DOM 几何显示 auth wrapper 扩到 `292px`、auth card 扩到 `224px` 且顶部上移约 `1.9px`，页面无新增 clipping / overlap / 失真；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响任何 `/root/nexus-mail` 现有服务/端口；Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描无 secret/shell/eval/pickle/sql-injection 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，无新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth-card 是否已退为次要热点，或是否应转向 radial cluster / hero vertical rhythm 的剩余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量 docking 收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 先确认 root cause：当前本地 stacked auth-card 虽已位于 preview 下方，但仍比 live 公开页 access module 更宽、更低一小步，导致首屏右下入口仍稍显松散。基于该证据，本轮仅在 `src/components/landing-page.tsx` 将 auth wrapper 从 `-mt-[0.12rem]/max-w-[320px]/sm:max-w-[332px]/lg:mt-[0.08rem]/lg:max-w-[620px]/xl:mt-[0.12rem]/xl:max-w-[640px]` 收敛为 `-mt-[0.2rem]/max-w-[304px]/sm:max-w-[316px]/lg:mt-[0.02rem]/lg:max-w-[600px]/xl:mt-[0.06rem]/xl:max-w-[620px]`，并把 `landing-auth-card` 自身从 `max-w-[244px]` / `lg:max-w-[420px]` / `xl:max-w-[436px]` 收窄到 `236px / 404px / 420px`，同时同步内层 shell `max-w-[268px] / lg:max-w-[404px] / xl:max-w-[420px]`，让 access module 在继续保持下置层级的前提下略微上提、收窄、减轻 detached 感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper / auth shell 合同，并保留旧大屏 top-margin 缺失断言。Review 1 功能/规格复核：live browser_vision 连续降级无有效内容，故按降级 fallback 改用 live/local sequential snapshot + DOM 几何作为 controller 事实来源；local rebuild 后几何确认 auth width 已从约 `436px` 收至 `420px`、wrapper 从 `640px` 收至 `620px`、preview 下缘到 auth 顶部 gap 从约 `19.17px` 收至 `18.22px`，且仍保持 preview 下方 stacked 层级、未引入 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I` 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅发布到 `3001` 且未影响 `/root/nexus-mail` 任意现有服务与端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 hardcoded secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅提示清理一个重复测试断言与补齐 `/`/`/chat` 断言对称性，均已在 controller 侧收口处理。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余最大热点是否重新转回 left radial cluster / hero text stack，而不是继续过度迭代 auth-card。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 将当前最小安全切片继续锁定为 auth/access module 仍是首屏最大剩余热点。基于 live/local 对照、browser QA 与本地运行时几何，本轮仅在 `src/components/landing-page.tsx` 对 stacked auth rail 做一次小步收窄与减重：将 `feature-auth-wrapper` 从 `-mt-[0.32rem] / max-w-[352px] / sm:max-w-[364px] / lg:-mt-[4.18rem] / lg:max-w-[624px] / xl:-mt-[4.36rem] / xl:max-w-[640px]` 收敛为 `-mt-[0.18rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[3.7rem] / lg:max-w-[600px] / xl:-mt-[3.88rem] / xl:max-w-[616px]`，同时将 `landing-auth-card` 外壳从 `276px / 612px / 628px` 收窄到 `268px / 588px / 604px`，并轻微收紧 radius、border、background、shadow、blur 与 inner shell `300px / 588px / 604px`，使 access module 相对中心 preview 更轻、更窄、更少 detached；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 wrapper / auth shell / inner-shell 精确类合同，保持 `/` 与 `/chat` 继续同构。Review 1 功能/规格复核：sequential live/local 对照与 local browser QA 继续指向 auth/access module 仍是主热点，但本轮 rebuild 后 local 几何已确认 auth width `628px -> 604px`、wrapper width `640px -> 616px`，且页面无新增 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响 `/root/nexus-mail` 任意现有服务或端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新引入的 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth-card 是否已收敛到次级热点，或是否应转向 left radial cluster / hero composition 的剩余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 先将单一最小安全切片锁定为 auth/access module 仍略显 detached。基于 live/local 结构与 local browser QA，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[0.22rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[3.92rem] / lg:max-w-[600px] / xl:-mt-[4.1rem] / xl:max-w-[616px]` 收紧为 `-mt-[0.34rem] / max-w-[332px] / sm:max-w-[344px] / lg:-mt-[4.22rem] / lg:max-w-[588px] / xl:-mt-[4.42rem] / xl:max-w-[604px]`，在不改动 preview、radial cluster、端口或 `/chat` 共用壳的前提下，进一步上提并轻收 auth wrapper；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper class 合同。Review 1 功能/规格复核：fresh-load local browser_vision 与 DOM 几何确认 auth/access module 仍位于 preview 下方，视觉上比上一轮更紧、更少 detached，且已开始退居为次级热点；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`npm test`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check` 与 added-lines 静态安全扫描通过，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断首屏主热点是否已经转向 left radial cluster 或 hero text/visual rhythm，而不是继续过度迭代 auth-card。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 stacked auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前最小安全切片锁定为 auth/access module 仍是首屏最直接的残余热点。基于 live DOM 几何（`SC-XXXXXXXX` / `VERIFY ACCESS CODE` 仍明显位于 preview 下方）与 local fresh-load 运行时几何（调整前 `authInput.bottom≈1118.20`、`bestHeading.top≈1295.36`，且 wrapper 与 preview 仍有较深叠接）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步上提为 `-mt-[2.08rem] / lg:-mt-[10.08rem] / xl:-mt-[10.42rem]`，保持宽度、preview、radial cluster、Best Cases、help shell、`/chat` 共用 landing shell 与 `3001` 发布边界不变；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper class 合同。Review 1 功能/规格复核：rebuild 后 sequential local fresh-load 与 controller 几何确认 auth module 相对 preview 继续上提（`authInput.bottom≈1106.36`、`bestHeading.top≈1283.52`），页面无新的 clipping / overlap / blank render，当前剩余热点仍诚实保留为 auth/access module 与整体 hero 层级的细微贴合差距。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`npm run lint`、`npm run build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“视觉测试仍较依赖精确 Tailwind class 合同”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否仍是单一主热点，或是否应转向 left radial cluster / hero text rhythm 的残余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 right auth/access module 仍是首屏最大剩余热点，而非 hero→Best Cases handoff 或 left radial cluster。基于 live/local 几何对照（live `authInput.bottom≈1002.75`、`bestHeading.top≈1311.25`；本地改动前 `authWrapper≈564px`、`authCard≈556px`、`gapAuthToBest≈110.72px`，且 local browser QA 仍将 auth module 视为最显著热点）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 stacked `feature-auth-wrapper` 做一次 wrapper-only 微收敛：由 `-mt-[2.32rem] / max-w-[320px] / sm:max-w-[332px] / lg:-mt-[9.74rem] / lg:max-w-[548px] / xl:-mt-[10.08rem] / xl:max-w-[564px]` 调整为 `-mt-[2.12rem] / max-w-[312px] / sm:max-w-[324px] / lg:-mt-[9.22rem] / lg:max-w-[540px] / xl:-mt-[9.56rem] / xl:max-w-[556px]`，在不改动 auth-card shell、preview、Best Cases、help shell 与 `/chat` 共用 landing shell 的前提下，让 auth/access module 略微下沉并收窄，减少“贴在 preview 上方的浮层感”；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper class 合同。Review 1 功能/规格复核：fresh live/local 顺序核验、DOM 几何与 local browser_vision 共同确认 auth/access module 仍是最大剩余热点，但最新 pass 后其相对 preview 已略更 integrated，页面未出现新的 clipping / overlap / blank render，仅保留轻微 preview 边缘 artifact 与 auth overlay 仍稍显独立这一诚实剩余问题。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未... [truncated]
- [~] 执行检查点（2026-05-17 17:40 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/path/git/status、`.git/info/exclude`、3001 端口与 `docker compose ps` 只读核验，再顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load。root-cause 证据显示 live 当前 hero→Best Cases handoff 仍比本地更松，且 live 视觉热点回到左侧 radial cluster；但 fresh local browser QA 与 DOM 几何确认本地更直接的单一剩余热点依旧是 auth/access rail 偏低、把 hero 尾部与 Best Cases 起始区压得更紧（改动前 `previewBottomToAuthTop≈-188.81px`、`gapAuthToBest≈92.16px`）。基于该证据，本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做一次更小的 upward docking micro-pass：`lg/xl -mt` 从 `12.54/12.88rem` 收紧到 `12.82/13.16rem`，不改动 width、Best Cases spacing、left radial cluster、`/chat` 共用 landing shell 或任何非 `3001` 发布边界；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 shared class 合同。Review 1 功能/规格复核：fresh live/local 顺序核验、local browser vision 与 controller-side DOM 几何确认 auth rail 上提约 `4.47px`，`previewBottomToAuthTop` 由 `-188.81px` 收敛到 `-193.28px`，页面未引入新的 clipping / overlap / blank render，当前 remaining gap 诚实保留为 `gapAuthToBest≈92.16px` 仍明显紧于 live 的更松 handoff。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：added-lines 静态安全扫描无 hardcoded secret / shell injection / eval-exec / pickle / SQL-formatting 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断是否继续围绕 auth rail 与 Best Cases handoff 的残余紧张度推进，或转向 left radial cluster 的更高优先级视觉热点。
- [x] 本地参考资料保留在 `docs/reference-local/` 并通过 `.git/info/exclude` 忽略
- [x] 每个里程碑后执行 docker compose config / tests / build / compose up -d --build
- [x] 每个里程碑后执行 3 轮评审并提交中文 commit + push
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live 与 local fresh-load 顺序复核，再按 systematic-debugging 将当前最小安全切片锁定为 hero 下方 auth/access module 与 `Best Cases` 起始区之间的纵向衔接仍略偏紧，而不是 auth-card 自身结构错误。基于 live 几何（auth input `y≈956`、Best Cases heading `y≈1311`）与本地重建后几何（auth wrapper bottom `≈1256`、Best Cases heading top `≈1296.95`）的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 顶部间距从 `mt-[1.34rem] / sm:mt-[1.72rem]` 放宽到 `mt-[2.12rem] / sm:mt-[2.56rem]`，并把标题块底边距从 `mb-5.5` 放宽到 `mb-6.5`，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的相关 spacing 合同，使 hero→Best Cases 过渡更接近 live 的呼吸感。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前更真实的剩余热点是 hero→Best Cases handoff 偏紧，本轮放宽后未引入新的 clipping / overlap / blank render。Review 2 测试/构建/集成复核：focused Vitest（page/chat/help）、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否回到 auth/access module 贴合或 left radial cluster anchoring，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 hero→Best Cases 纵向 handoff 仍略紧，而非 auth/access module 结构或路由问题。基于 live/local 几何对照（live `authInputBottom≈1002.75` 到 `bestHeadingTop≈1311.25`，local `authCardBottom≈1256` 到 `bestHeadingTop≈1357.11`，说明本地过渡仍更紧）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 顶部间距从 `mt-[2.36rem] / sm:mt-[2.88rem] / lg:mt-[5.42rem] / xl:mt-[5.84rem]` 放宽到 `mt-[2.68rem] / sm:mt-[3.1rem] / lg:mt-[5.88rem] / xl:mt-[6.32rem]`，并将标题块底边距从 `mb-7` 放宽到 `mb-[1.9rem]`，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared spacing 合同与首页 Best Cases 默认态断言。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前最真实残余热点是 hero→Best Cases handoff 偏紧，本轮放宽后页面仍 coherent，未引入新的 clipping / overlap / blank render；browser_vision 本轮再次降级为空内容，已按降级 fallback 以 fresh snapshot + DOM/HTTP/controller 事实作为本地视觉验收依据。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、无新增 security concern 或 logic error，仅留下“未来可将一部分 spacing arbitrary values 抽象为共享 token”这一非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否转回 auth/access module 贴合或 left radial cluster anchoring，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 hero→Best Cases 纵向 handoff 仍略紧，而非 auth/access module 结构或路由问题。基于 live/local 几何对照（live `authInputBottom≈1002.75` 到 `bestHeadingTop≈1311.25`，local `authBottom≈1256` 到 `bestHeadingTop≈1296.95`，说明本地过渡仍更紧）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 顶部间距从 `mt-[2.12rem] / sm:mt-[2.56rem]` 小步放宽到 `mt-[2.36rem] / sm:mt-[2.88rem]`，并将标题块底边距从 `mb-6.5` 放宽到 `mb-7`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared spacing 合同与首页默认态断言。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前最真实残余热点是 hero→Best Cases handoff 偏紧，本轮放宽后页面仍 coherent，未引入新的 clipping / overlap / blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否转回 auth/access module 贴合或 left radial cluster anchoring，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 right auth/access module 仍是 fresh-load 首屏最明显剩余热点，而非 left radial cluster、center preview 或 hero→Best Cases handoff。基于 live/local 几何对照（live `authInput.bottom≈1002.75`、`bestHeading.top≈1311.25`；本地改动前 `authInput.bottom≈1106.36`、`bestHeading.top≈1386.88`，auth module 仍更靠近中心主视觉）与 local browser_vision 将 auth/access module 继续判为首要热点这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 stacked auth wrapper 从 `-mt-[2.08rem] / max-w-[316px] / sm:max-w-[328px] / lg:-mt-[10.08rem] / lg:max-w-[532px] / xl:-mt-[10.42rem] / xl:max-w-[548px]` 收敛为 `-mt-[1.92rem] / max-w-[320px] / sm:max-w-[332px] / lg:-mt-[9.52rem] / lg:max-w-[536px] / xl:-mt-[9.84rem] / xl:max-w-[552px]`，只做 auth/access module 的轻微放宽与下沉收口，使其较上一轮更像 preview 下方的附属 access band、而不是过度上提的独立热点；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper 合同。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local browser QA 确认 auth/access module 仍是当前单一主热点，但本轮后已更 integrated、未引入新的 clipping / overlap / blank render；browser_vision 第二次降级为空内容，已按降级 fallback 以 fresh snapshot + DOM/HTTP/controller 事实作为视觉验收依据。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error。下一轮继续从最新 worktree ... [truncated]
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 stacked auth/access module 仍是 fresh-load 首屏最明显剩余热点。基于 live/local 对照、local `browser_vision` 继续将 auth/access module 判为首要热点，以及 controller 侧运行时几何显示 preview→auth 仍有约 `-141.45px` 叠接、auth→Best Cases 仍约 `116.47px` 的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[2.12rem] / lg:-mt-[9.22rem] / xl:-mt-[9.56rem]` 小步上提为 `-mt-[2.36rem] / lg:-mt-[9.58rem] / xl:-mt-[9.92rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper class 合同，在不改动 preview、radial cluster、Best Cases、help shell 与 `/chat` 共用 landing shell 的前提下，让 access module 更贴近中心 preview。Review 1 功能/规格复核：fresh live/local 顺序核验、local browser_vision 与 controller 几何共同确认当前页面无新的 clipping / overlap / blank render，auth/access module 仍是首要剩余热点，但已较上一轮更 attached；左侧 radial cluster 与 hero→Best Cases handoff 继续退居次级问题。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error，仅留下“未来可减少精确 Tailwind class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 的剩余 docking 热点继续做单一视觉切片，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 stacked auth/access module 仍是 fresh-load 首屏最明显剩余热点。基于 live/local 对照：live access band 宽度约 `599.5px`，本地改动前 auth wrapper / card 约 `516px / 516px`，且 `previewBottom_to_authTop≈-147.84px`、`authBottom_to_bestTop≈113.27px`，说明本地模块仍略偏窄、略偏上贴。围绕该 root cause，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步放宽并略微下沉收口：`-mt-[2.32rem] -> -mt-[2.08rem]`、`max-w-[312px]/sm:max-w-[324px] -> 316px/328px`、`lg:-mt-[9.98rem]/lg:max-w-[500px] -> -9.62rem/504px`、`xl:-mt-[10.32rem]/xl:max-w-[516px] -> -9.96rem/520px`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper class 合同，使 auth/access module 相较上一轮更接近 preview 下方的附属 access band，而非过度上提的独立热点。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前页面无新的 clipping / overlap / blank render，auth/access module 仍是首要剩余热点，但本轮后已更 integrated；rebuild 后 `authWrap.width≈520px`、`previewBottom_to_authTop≈-142.09px`，较上一轮略放宽且略下沉。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error，仅保留“未来可减少精确 Tailwind class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 的剩余 docking 热点继续做单一视觉切片，而不是触碰 `/... [truncated]

- [~] 执行检查点（2026-05-18 12:11 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 systematic-debugging 先完成 repo/path/git/status、`.git/info/exclude`、live/local fresh-load、DOM 几何与 browser QA，确认当前最小安全切片仍是首屏 auth-card / utility / preview 的层级关系；随后仅在 `src/components/landing-page.tsx` 收紧顶部 utility row 间距、减轻 auth-card 的宽度/阴影/模糊并提高与 preview 的贴合，同时略增强 preview 外层对比；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新 class 合同。
- [~] 评审检查点（2026-05-18 12:11 UTC）：Round 1 顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/`，确认 auth-card 已比上一轮更不 detached / washed-out，但仍是首屏最大剩余热点；Round 2 已通过 `npx vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat` HTTP 200；Round 3 已完成 controller-side added-line 静态安全扫描（secret/shell/eval/pickle/SQL 注入均 0 命中）以及独立 reviewer JSON 复核，未发现新的安全或逻辑回归。