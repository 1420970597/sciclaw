# SciClaw Clone Execution Todo

## Status legend
- [x] completed
- [~] in progress
- [ ] pending

## Phase A — 基础初始化
- [x] 初始化 Next.js 16 + React 19 + TypeScript + Tailwind 4 项目骨架
- [x] 补齐 Dockerfile、docker-compose.yml、Vitest、ESLint、README
- [x] 建立 landing 页面首版与基础测试

## Phase B — Landing 高保真复刻（P0）
- [x] 本轮继续完成一次更偏 radial-cluster bridge 与 bottom micro-label breathing room 的小步收敛：依据 fresh-load live/local 对照，统一将左侧三枚外围 radial nodes 再向中部 preview card 回收并略微上提，延长 connector 有效连接段、减轻非激活 ring 阴影厚重感、收紧标签最大宽度但略提文字对比，优先缓解 cluster 仍显 detached/悬空的问题；同时把 preview 底部 `Meth. / Find. / Data / Evid.` 四枚 micro labels 的字号、字距、最大宽度、列间距与底部留白再上调一小档，在保持 tiny 信息层级的前提下继续减少拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 fresh-load local browser QA。最新 QA 认为页面整体无明显 clipping/overlap、顶部 utility controls 依旧 subtle 但清晰，底部 micro labels 已可读且无明显碰撞；当前剩余最明显差距继续收敛为左侧 radial cluster 与中部 preview 的桥接感仍弱于 live site、内部小标签层次仍有轻微拥挤，下一轮继续优先做 cluster 连接感与内部 tiny-label 层级的更小步收敛，而不是大改其它模块。
- [x] 本轮继续完成一次更偏 radial-cluster bridge、quiet utility controls 与 bottom micro-label hierarchy 的小步收敛：依据 fresh-load live/local 对照与 DOM 几何复核，将左侧三枚外围 radial nodes 统一向中部 preview card 再回收一小档，并同步缩短 connector 有效长度、略减非激活 ring 尺寸/阴影、压低标签 tracking 与最大宽度，优先缓解 local browser QA 中 cluster 仍略 detached、标签层次略松的问题；同时保留 dock 菜单的键盘/ARIA contract，不再压缩 44px 级命中区，仅轻微减薄 utility controls 的边框/阴影对比，使顶部入口继续保持 subtle-but-usable；再把 preview 底部 `Meth. / Find. / Data / Evid.` 四枚 micro labels 调整为更接近 live 的轻量层级——维持可读性同时降低过重感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 复审与 fresh-load local browser QA。最新 QA 认为当前页面无明显 blocking overlap/clipping，顶部 utility controls subtle 且仍可用，bottom micro labels 已回到轻量但可读；剩余最明显差距仍收敛为左侧 radial cluster 与中部 preview 的最终桥接感尚可继续精修，但已属于非阻塞 follow-up polish，下一轮继续优先做 cluster 内部标签/连接感的更小步收敛，而不是大改其它模块。
- [x] 本轮继续完成一次更偏 utility controls 精度、hero radial cluster→preview bridge 与底部 micro-label 轻量化的小步收敛：将顶部三枚 utility controls 进一步缩小并收紧分组间距，使其更接近 live site 的轻薄 utility cluster；同时把左侧三枚外围 radial nodes 统一向中部 preview 小幅回收并略上提，收短 connector 有效连接段、减轻非激活节点尺寸与阴影厚重感，以继续收敛 fresh-load browser QA 中 left cluster 仍略 detached 的问题；再把 preview 底部 `Meth. / Find. / Data / Evid.` 四枚 micro labels 调整为更轻量的 microcopy（更小字号、更浅色、更克制字重与更紧凑宽度），减少它们相对整页视觉系统过重、过密的问题。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 fresh-load local browser QA。最新 QA 认为顶部 utility controls 已稳定且无 clipping，页面整体无灾难性 overlap；当前剩余最明显差距继续收敛为左侧 radial cluster 仍稍偏 detached / 内部 tiny labels 稍拥挤，而 preview 底部 micro labels 已从“过重/过密”转为“轻量但仍属 tiny-text 精修范围”。下一轮继续优先做 cluster 内部标签层级与 bridge 感的更小步收敛，而不是大改整体骨架。
- [x] 本轮继续做一次更偏 micro-label 结构减拥挤的小步收敛：保持顶部 utility controls 与 dock 菜单契约不变，继续保留左侧 radial cluster 的现有回收构图；同时将 preview 底部四枚 chart micro labels 中过长的 `Result` 缩写为 `Find.`，并同步收紧 chart 区列间距、单列宽度、label 行高与 tracking，优先缓解 fresh-load browser QA 中底部 tiny-label 仍然拥挤、连读的问题。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer，以及 live/local fresh-load browser QA。最新 QA 认为左右整体骨架保持稳定，底部 micro labels 的拥挤来源已从长词本身进一步收敛；剩余最明显差距仍是左侧 radial cluster 相对 live site 略 detached，下一轮继续优先做 cluster 与中部 preview 的横向桥接，而不是再压缩 micro-label。
- [x] 本轮继续做一次更偏 hero 横向耦合与 subtle-control 收敛：轻微减薄顶部 utility circles 的阴影/厚重感但略增边框与图标存在感，避免 fresh-load browser QA 中“太虚”观感；同时将左侧 radial cluster 三枚外围节点整体向中部回收、缩短 connector 路径并提高非激活标签对比度，再同步收紧 hero 与 auth card 的大屏 gap，减少 cluster 相对中部 preview 的 detached 感。该里程碑已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、静态安全扫描、独立 reviewer 与 fresh-load browser QA。最新 QA 认为 utility circles 已不再过于 subtle、整体无 clipping/overlap，左侧 cluster 与中部 preview 的关系更自然；剩余最明显差距已收敛为 cluster 相对 live site 仍略偏轻、略偏左，下一轮继续优先做 cluster 权重与横向构图的更小步对齐。
- [x] 本轮继续做一次 hero 横向桥接与微字可读性收敛：将左侧 radial cluster 三枚外围节点统一向中部 preview 再回收一小档、同步略增 ring/connector 对比与标签宽度/字重，让 cluster 更像被主 preview 吸附的功能卫星；同时收紧 feature rotator 与 preview 的大屏 gap、让网络区容器略向右吃进主卡片，并把 preview 底部 `Meth. / Result / Data / Evid.` 四枚 micro labels 的字号、列间距、最大宽度与字色再上调一小档；顶部 utility circles 则继续保持 subtle，但通过更清晰的边框与更干净的浅阴影避免过虚。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 fresh-load browser QA。最新 QA 认为 detached 感较前一轮有所减轻、utility circles 仍保持 subtle 但没有先前那么虚，micro labels 也更可读；当前剩余最明显差距依旧收敛为左侧 cluster 相对中部 preview 仍略偏游离、底部 micro labels 仍属 tiny-text 精修范围，下一轮继续优先做 cluster 标签避让/桥接与底部微字结构级减拥挤，而不是大改整体骨架。
- [x] 本轮继续完成一轮 hero 微排版/耦合收敛：轻微放松顶部 utility circles 与 shared dock gap、减轻按钮边框/阴影厚重感；同时把左侧 radial cluster 三枚外围节点小幅回收并重算 connector 角度/长度、提升非激活标签对比度；再将 preview 右下微型柱状图标签从较长词缩写为 `Meth. / Result / Data / Evid.`，收窄字距并限制单列宽度，消除 browser QA 中最明显的底部 tiny-label 碰撞。该里程碑已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、静态安全扫描、独立 reviewer，以及 fresh-load browser QA；最新 QA 认为 tiny labels 已不再明显碰撞，整体无 clipping/overlap 阻塞项，剩余差距主要转为 live site 相比下的微字锐度与 hero 精修问题，可在下一轮继续小步收敛。
- [x] 完成 dark hero、feature rotator、嵌入式 auth card、best-cases 区块、footer 首版
- [~] 继续对齐头部、细节间距、轮播表现、图像/GIF 占位与 hover/active 状态（已多轮对齐公开站：移除本地化 header wordmark、将右上角入口改为公开站风格的悬浮 utility controls、反复收敛 hero 与 auth card 的横纵节奏、Login 卡层次、Best Cases 标题样式、Contact Us 外链弹层、fresh-load 默认 hero/Best Cases 状态与相应测试；上一轮继续把顶部三枚圆形入口轻微缩小并上提、收紧组内间距，同时缩窄 auth card、增大 hero/auth gutter、略微放大 Best Cases 顶部留白，并拉开 hero preview 文案与 CTA 的留白；上一轮后又继续把 utility controls 略微放大并加深描边/阴影，同时保留圆形命中区与菜单 contract，避免与 live site 对比时过于发虚；本轮进一步把三枚 utility controls 再轻微做实并收紧分组、略微缩窄并下压 auth card、减少 hero/auth 横向脱节，同时继续拉开 preview 内部 micro-layout（summary/evidence block、柱状图底部 tiny labels、正文与 CTA 留白），并保持测试与菜单交互 contract 同步；本轮继续依据 fresh-load live/local 浏览器比对做更克制的收敛：进一步把 radial cluster 三枚外围节点外推，缩小外圈节点尺寸、降低连接线厚度与雾感、收紧中心 glow；同时继续为 preview 的 summary/evidence/block 和底部 tiny labels 增加留白与字距，并小幅缩窄 auth card、减小 feature rotator 左右 gutter，使 hero 三段关系更连贯；本轮又继续做一轮更小的 crispness/hierarchy 纵切：进一步缩小并收紧顶部 utility circles、减轻阴影与描边厚重感，继续把 radial cluster 三枚外围节点外推并略增 connector 存在感、提高小标签对比度与字距、收紧中心 glow，同时把 auth card 从偏雾化的组件材质收敛为更明确的白色 UI surface，并提升 preview 底部 tiny labels 的可读性。所有改动已通过 Vitest（`pnpm test -- src/app/page.test.tsx`，本次实际仍复跑到 4 个 Vitest 文件）、ESLint、Next build、docker compose config、docker compose up -d --build、127.0.0.1:3001 HTTP 200、静态安全检查、独立 reviewer 与 fresh-load browser QA。当前页面依然 coherent 且无明显炸版；最新 QA 认为 utility controls 已不再过分抢眼，auth card 也更像明确 UI surface，但剩余最明显差距仍集中在 radial cluster 与 preview 内部的微型标签/connector crispness，以及 hero 左侧 cluster 与中部卡片的整体构图平衡，下一轮继续优先做微字重、对比度与 hero 组合关系的小步收敛，而不是大改布局；本轮再次依据 live/local fresh-load 浏览器对比，反向做了一次更贴近 live 的“轻量回调”：轻微增大并上提顶部 utility circles、放松按钮间距但减轻边框/阴影厚度，让它们保持 small-but-present 而不过分 buttony；同时继续把 radial cluster 三枚外围节点外推并略缩圈体、削弱中心 glow、细化 connector 与标签层次，让 cluster 更轻、更像向中部 preview card 输送信号的输入图；并继续拉开 preview 卡片内部 summary/evidence 模块、柱状图 tiny labels 的字距与间距，提升微型信息可读性。该轮已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、`curl -I` 校验 `/` `/chat` `/help/getting-started` 为 200、静态安全扫描与独立 reviewer；仍需后续继续收敛。）
- [x] 增加更多贴近公开站点的案例轮播视觉与动画细节
- [~] 继续精修左侧 hero radial cluster 的节点位置、标签层次与连接线精致度，减少当前 live-site 对比下最明显的视觉偏差（本轮继续做一次不改整体骨架的小步收敛：将顶部 utility circles 轻微放大并收紧组距/加深对比，避免过轻过飘；同时把三枚外围 radial nodes 略向中心回收、增强 connector hairlines 的可见度、提高非激活 tiny labels 的字重/对比度，并进一步削弱中心 glow 与主圆阴影；还同步拉开 preview 内部 Method/Conclusion 卡片与底部四枚 micro labels 的字号、字距和纵向留白，减少 live/local 对比中最明显的微标签发虚与拥挤感。该轮已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、`curl` 校验 `/` `/chat` `/help/getting-started` 为 200、静态安全扫描、独立 reviewer 与 fresh-load browser QA。最新 QA 认为 utility controls 更稳、cluster connector 与微标签可读性提升，但左侧 cluster 相对中/右仍略轻，下一轮继续优先做 cluster 的整体权重与 hero 横向耦合的小步收敛，而不是重写布局。）
- [x] 本轮继续完成一次更偏 quiet utility controls 与 hero radial cluster→preview bridge 的小步收敛：轻微收紧顶部 utility controls 的组距与 shared dock gap，同时只小幅提高边框/图标对比、维持轻量阴影，让三枚圆形入口保持 subtle-but-clear；再将左侧 Literature/Data 节点与右侧 Outcome 节点一并向中心 preview card 小幅回收，并同步延长 connector 有效段、轻微提升非激活 ring/label 对比，使 cluster 与中部 preview 的连接感更强而无明显 overlap。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 fresh-load browser QA。最新 QA 认为顶部 utility controls 已恢复 subtle-but-clear，left radial cluster 比前一轮更贴近中部 preview 且无明显 clipping/overlap；当前剩余主要差距继续收敛为 cluster 内部 tiny-label 的舒适度和更细微的桥接精修，下一轮继续优先做标签可读性与连接感的小步收敛，而不是大改整体骨架。
- [x] 本轮继续完成一次更偏 left hero cluster→preview 桥接与权重补偿的小步收敛：依据 live/local fresh-load 对照，将三枚外围 radial nodes 再向中心 preview card 回收一小档，缩短横向悬空感；同步略增非激活 ring 尺寸、connector 可见度、标签最大宽度与深色对比，让左侧 cluster 从“略 detached”收敛到更像围绕中部 preview 组织的同组结构。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 fresh-load browser QA。最新 QA 认为页面无 clipping/absurd overlap，left cluster 与中心 preview 的连接感较上一轮更强，底部 micro labels 仍可读但保持 tiny-text 精修属性；下一轮继续优先做 cluster 与中部 preview 的最终构图平衡，并谨慎观察微字舒适度，而不是改动其它模块。
- [x] 本轮继续做一次更偏 hero 横向桥接与 tiny-label 舒适度的可交付收敛：在不改动整体三栏信息架构的前提下，轻微提高顶部 utility circles 的边框/图标对比但继续压低阴影厚度，保持 subtle-but-present；同时将左侧三枚 radial nodes 统一向中部 preview 再回收一小档，增加 connector 有效连接长度，并略增非激活 ring/label 的存在感，优先缓解 fresh-load browser QA 中 left cluster 仍显 detached 的问题；再同步放大 preview 底部四枚 micro labels 的字号、列宽、最小高度与列间距，将问题从“轻度拥挤”收敛到“仍偏 tiny 但无重叠”。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 fresh-load local browser QA。最新 QA 认为顶部 utility circles 已更平衡、spacing 干净，bottom micro labels 也已从潜在重叠收敛为可读但仍偏小；当前最明显剩余差距继续集中在 left radial cluster 与中部 preview 的最终桥接感，下一轮继续优先做 cluster 整体连接感与微字舒适度的最后小步收敛，而不是大改其它模块。
- [x] 本轮继续完成一次更偏 hero cluster-to-preview 横向桥接与底部 micro-label 可读性的收敛：将左上、左下、右上三枚外围 radial nodes 统一向中部 preview 回收约一小档、同步缩短 connector 长度并略增非激活 ring/label 对比，让左侧 cluster 更接近 live site 中与主卡片同组的关系；同时把 preview 底部四枚 micro labels 的栅格 gap、字号、最大宽度与最小标签高度再上调一档，并将默认第二枚标签保持为 `Find.`，优先缓解 fresh-load browser QA 中 tiny labels 仍偏拥挤的问题。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer，以及 fresh-load local browser QA。最新 QA 认为顶部 utility controls 依旧 subtle-but-present、页面无 clipping/灾难性 overlap；但左侧 radial cluster 相对中部 preview 仍稍偏 detached，底部 micro labels 也只是从拥挤收敛到“勉强可读”而未达到舒适可读。下一轮继续优先做 cluster→preview 的最后一小步桥接，以及 micro-label 的内容/结构级减拥挤，而不是改动整体骨架。
- [x] 本轮继续做一次更偏 hero 横向耦合与微字减拥挤的小步收敛：轻微放松顶部 utility circles 的组距但保持更克制的边框/阴影，让按钮继续维持 small-but-present 的公开站语气；同时将左上、左下、右上三枚外围 radial nodes 统一向中心回收、缩短 connector 长度并略提非激活标签对比度，减少 left cluster 相对中心 preview 仍偏 detached 的问题；再同步收紧 feature rotator 与 auth card 的大屏 gap，并把 preview 底部 `Meth. / Result / Data / Evid.` 四枚 micro labels 的字号、列宽、字距与底部留白再上调一小档，优先缓解 tiny-label 拥挤。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 fresh-load browser QA。最新 QA 认为 hero 主体依旧无 clipping/overlap，cluster 与 preview 的关系比上一轮更紧、底部 micro labels 更易读；剩余主要差距继续收敛为顶部 utility circles 在本地 fresh-load 观感下仍稍偏轻、左侧 cluster 标签在视觉工具里仍偶有重叠感，下一轮继续优先做 utility contrast 与 cluster 标签层次的小步收敛，而不是改动整体骨架。
- [x] 本轮继续做一次更偏 hero cluster-to-preview 耦合与底部 micro-label 结构化可读性的收敛：将左上、左下、右上三枚外围 radial nodes 统一向中部回收并略增节点尺寸、connector 线宽与标签宽度/对比度，减少 local browser QA 中 left cluster 相对中心 preview 仍偏 detached、偏轻的问题；同时把 preview 右下 `Meth. / Result / Data / Evid.` 标签改为更稳定的两层容器，收紧 tracking、增加字重与最大宽度约束，避免 fresh-load browser QA 中四枚 micro labels 视觉串连。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描与独立 reviewer。最新 local browser QA 认为页面无明显 clipping/overlap，顶部 utility circles 保持稳定；剩余最明显差距继续收敛为 left cluster 相对 live site 仍略 detached/偏轻，后续可继续优先做 cluster 与中心 preview 的横向桥接和少量对比度补偿。
- [x] 本轮继续做一次更偏 hero 组合耦合的小步收敛：将左侧三枚 radial nodes 整体向中部回收并略增 ring/connector 存在感、同步微调中心锚点与背景星点位置，减少 browser QA 中 cluster 相对中部 preview 仍显 detached 的问题；同时进一步减薄顶部 utility circles 的边框/阴影对比并放松组距，让三枚 icon controls 更接近 live site 的 quiet utility controls；再把 preview 底部 `Meth. / Result / Data / Evid.` 四枚 micro labels 的字号、tracking 与列宽小幅上调，缓解 tiny-label 拥挤。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 fresh-load browser QA。最新 QA 认为顶部 utility circles 已更 quiet、无明显 clipping/overlap，但左侧 cluster 与中部 preview 的连接感仍只部分改善，且底部 micro labels 仍属可读性精修范围；下一轮继续优先做 cluster 标签碰撞/连接感与 preview tiny-label 结构级减拥挤，而不是大改其它模块。
- [x] 本轮继续做一次更偏 cluster 耦合与 tiny-label 可读性的收敛：将左侧三枚外围 radial nodes 再向中心轻微回收并微调纵向位置、缩短 connector 距离、略增节点/连接线存在感与非激活标签对比度，让 cluster 相对中部 preview 的关系更明确；同时把 preview 底部四枚 micro labels 的字号、字距、列宽与底部留白再上调一小档，减少 fresh-load browser QA 中仍存在的 tiny-label 偏挤观感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`git diff --check`、静态安全扫描、独立 reviewer，以及 live/local fresh-load browser QA。当前页面无 clipping/overlap 或 absurd spacing；最新 QA 认为剩余最明显差距仍是左侧 cluster 相对 live site 依旧略 detached、底部 tiny labels 仍属小字精修范围。另：本轮 `docker compose up -d --build` 受 Docker Hub `docker/dockerfile:1.7` 元数据拉取超时阻塞，属于外部网络问题而非代码回归；现有 3001 本地站点保持可用，下一轮在网络恢复后继续做容器重建验证。
- [x] 本轮继续做一次更偏 hero 耦合的小步收敛：在不破坏现有默认状态与三栏结构的前提下，轻微增大并放松顶部 utility circles 的横向间距，让按钮保持 live site 的 small-but-present 感；同时把左上/左下/右上三枚外围 radial nodes 小幅向中心回收并略微下压/上提，增强 connector hairlines 与非激活标签对比度，减少 cluster 相对中部 preview 与右侧 auth card 的悬浮/脱节感；还同步缩小 hero 主区与 auth card 的大屏 gap，并继续为 preview 底部四枚 micro labels 增加字距、字号与栅格留白，缓解 browser QA 中仍明显的 tiny-label 拥挤。该轮已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、Python 重试校验 `http://127.0.0.1:3001/` `/chat` `/help/getting-started` 均为 200、静态安全扫描、独立 reviewer 与 fresh-load browser QA。最新 QA 认为顶部 utility circles 已达到 small-but-present 且不过分 buttony，整体没有 clipping/overlap；但左侧 cluster 相对中/右仍略轻，preview 底部 tiny labels 仍有轻度压缩感，下一轮继续优先做 preview tiny labels 的内容/结构级减拥挤与 cluster 权重补偿，而不是大改整体布局。
- [x] 本轮继续做一次更偏 hero 中部耦合与微字可读性的收敛：在不改变默认 hero/story 结构的前提下，将左侧三枚 radial nodes 进一步向中部 preview card 回收，缩短 connector 距离、略增 ring/标签对比，并同步把顶部 utility circles 的描边/图标对比轻微上调，避免 fresh-load QA 中 controls 偏虚；同时为 preview 底部 `Meth. / Result / Data / Evid.` 四枚 micro labels 增加列间距、字号、最大宽度与底部留白，优先缓解“可读但仍偏挤”的观感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 fresh-load local browser QA。最新 QA 认为 utility controls 比上一轮更可见但仍保持 quiet，left cluster 与中心 preview 的关系略有收紧；剩余最明显差距仍是 cluster 在视觉上还不够像直接指向 preview 的结构，底部 micro labels 也仍处于接近拥挤的下边界，下一轮继续优先做 cluster→preview 桥接感与 micro-label 结构级减拥挤的小步收敛。
- [x] 本轮继续做一次更偏 cluster/readability 的小步收敛：将顶部 utility circles 与 shared dock gap 再轻微放松并小幅增大，维持 subtle 但不发虚；同时把三枚外围 radial nodes 向中部略回收并提高 connector hairlines / 非激活标签对比度，让左侧 cluster 与中部 preview、右侧 auth card 更像同一组 hero 组合；再把 preview 底部四枚 micro labels 的字号、字距、单列宽度与列间距一起上调，降低 fresh-load browser QA 中仍存在的拥挤感。该里程碑已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、静态安全扫描、独立 reviewer 与 fresh-load browser QA。最新 QA 认为顶部 utility circles 更 present 但未变重，左侧 cluster 可读性与耦合度有所提升，且无 clipping/overlap；剩余主要差距仍是 cluster 相比中/右略轻、preview 底部 tiny labels 虽改善但仍偏紧，下一轮继续优先做 micro-label 内容/结构级减拥挤与 cluster 整体权重补偿。
- [x] 本轮继续做一次更偏 hero 三栏耦合的小步收敛：把左侧 radial cluster 三枚外围节点向中部回收并重算 connector 长度/角度、同步压缩 cluster 画布高度与中心锚点偏移，让左侧结构更贴近中部 preview card；同时收紧 feature rotator 与右侧 auth card 的大屏 gap、微调 hero 主栅格列宽，并把 preview 底部四枚 micro labels 的单列宽度/字距/列间距改为更紧凑但更易读的组合，减少 browser QA 中“底部标签虽不碰撞但仍略散”的观感。该里程碑已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、静态安全扫描、独立 reviewer 与 fresh-load browser QA。最新 QA 认为 hero 已无 clipping/overlap，左侧 cluster 相比上一轮更像与中心 preview / 右侧 auth card 同属一组系统；当前最明显剩余差距已收敛为左侧 cluster 相对 live site 仍稍微偏左、偏低且结构存在感略弱，下一轮继续优先做 cluster 与中部卡片的最终构图平衡，而不是重写其它模块。
- [x] 本轮继续围绕 Phase B 的 hero fidelity 做一次更偏 cluster-weight/readability 的小步收敛：将三枚外围 radial nodes 向中部再回收一档并同步略增节点尺寸、connector 线宽/对比与中心锚点存在感，减少 fresh-load browser QA 中 left cluster 相对中部 preview 与右侧 auth card 仍偏轻、偏 detached 的问题；同时上调 preview 右下 micro labels 的字号、单列宽度、底部留白与列间距，缓解“可读但仍偏挤”的观感。该里程碑已通过 `pnpm test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 fresh-load browser QA。最新 QA 认为这两项目标都已较上轮改善：left cluster 更融入 hero 三栏组合、bottom micro labels 更易读；剩余差距主要收敛为 cluster 相比 live site 仍略显轻薄、micro labels 仍属小字精修范畴，下一轮继续优先做更小步的 cluster 权重/间距与微字锐度收敛。
- [x] 本轮继续做一次更偏 hero 构图平衡的小步收敛：在不改动默认信息架构与 utility circles 轻量语气的前提下，将左侧 radial cluster 三枚外围节点整体向中心回收并略微加重 ring/connector/中心锚点存在感，同时进一步收紧 hero 与 auth card 的大屏 gap；并把 preview 底部 `Meth. / Result / Data / Evid.` 四枚 micro labels 的字号、单列宽度、行高与底部留白再上调一档，减少 live/local 对比下仍存在的“可读但偏紧”观感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 browser QA。最新 QA 认为顶部 utility circles 依旧 subtle-but-present，left cluster 与中部 preview 的耦合度比上轮更好、无明显 clipping/overlap，底部 micro labels 已无明显碰撞；剩余主要差距收敛为 cluster 相比 live site 仍可再略提权重、micro labels 仍偏 tiny，下一轮继续优先做更小步的 cluster weight 与微字可读性收敛。
- [x] 本轮继续做一次更偏 hero cluster→preview 横向桥接的小步收敛：依据 fresh-load live/local browser QA，统一将左侧三枚外围 radial nodes 向中心 preview 再回收一小档，适度拉长 connector 有效连接段，并略微提高非激活节点尺寸、阴影与标签对比，优先缓解当前最明显的 left cluster 仍略 detached 问题；同步更新 landing hero 聚焦测试中的几何/视觉 class 断言，确保默认 hero 状态与交互 contract 仍保持可验证。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 live/local fresh-load browser QA。最新 QA 认为顶部 utility controls 仍保持 subtle-but-visible，left cluster 与中部 preview 的关系比上一轮更紧，但仍是当前最明显剩余差距；底部 micro labels 依旧无重叠但仍属 tiny-text 精修范围，下一轮继续优先做 cluster 的最终桥接与微字舒适度收敛，而不是大改整体骨架。
- [x] 本轮继续做一次更偏 hero 左侧 cluster 耦合与微字可读性的收敛：将三枚外围 radial nodes 统一向中部回收并略微上提/下压，缩短 connector 距离、略增 ring/connector 对比与节点存在感，使左侧 cluster 与中部 preview 的横向关系更紧；同时将顶部 utility circles 的阴影厚度再减薄一档但略增边框/图标存在感，并把 preview 底部四枚 micro labels 调整为更宽列宽、较低 tracking、额外底部留白与可换行约束，减少 tiny-label 挤压。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描与独立 reviewer。`docker compose up -d --build` 仍因 Docker Hub 拉取 `docker/dockerfile:1.7` 超时失败，属外部网络阻塞而非代码回归；本地 3001 站点持续可用。fresh-load browser QA 认为当前页面无 clipping/灾难性炸版，但剩余最明显视觉差距仍是左侧 radial cluster 相对 live site 依旧偏 detached，且 preview 底部 micro labels 虽有改善仍属 tiny-text 精修范围，下一轮继续优先做 cluster 构图耦合与微字布局的更小步收敛。
- [x] 本轮继续完成一次更偏 hero cluster→preview 桥接与 cluster 权重补偿的小步收敛：统一将左上/左下/右上三枚 radial nodes 再向中部 preview card 回收约一小档、同步拉长 connector 有效连接段，并轻微上调非激活 ring/label 的尺寸、对比与阴影权重，优先缓解 local browser QA 中左侧 cluster 仍略 detached / 偏轻的问题；同时同步更新 landing hero 聚焦测试中的节点几何与视觉 class 断言，保持该 fidelity 切片的回归可验证。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 fresh-load local browser QA。最新 QA 认为顶部 utility controls 仍保持 subtle/tidy、页面无 clipping/overlap，底部 micro labels 依旧可读且未重叠；当前最明显剩余差距仍是左侧 radial cluster 与中部 preview card 的连接感还不如 live site 紧，以及右侧 auth card 相对主 hero 仍略显独立，下一轮继续优先做 hero 三栏整体耦合与 cluster 桥接的小步收敛。

- [x] 本轮继续完成一次更偏 hero cluster→preview 桥接与底部 micro-label 可读性的可交付收敛：统一将左侧三枚外围 radial nodes 再向中部 preview 回收一小档、拉长 connector 有效连接段，并同步略增非激活 ring/label 的尺寸、对比与阴影权重，优先缓解 fresh-load browser QA 中 left cluster 仍略 detached 的问题；同时收紧 hero 与 auth card 的大屏 gap，并把 preview 底部 `Meth. / Find. / Data / Evid.` 四枚 micro labels 的字号、列宽、行高与底部留白再上调一档，减少 tiny-label 仍偏紧的观感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 live/local fresh-load browser QA。最新 QA 认为 utility controls 仍保持 subtle-but-present、页面无 clipping/明显 overlap，left cluster 与中心 preview 的关系较上一轮更紧，底部 micro labels 也更易读；当前剩余最明显差距仍是 left cluster 与 preview 的最终桥接感尚未完全达到 live site、tiny labels 仍属小字精修范围，下一轮继续优先做 cluster 标签摆位/连接感与微字舒适度的更小步收敛，而不是改动其它模块。
- [x] 本轮继续完成一次更偏 utility discoverability、cluster→preview bridge 与 micro-label breathing-room 的小步收敛：轻微提高顶部 utility controls 的边框/图标对比并放松组距，让三枚圆形入口继续保持 subtle 但不至于过虚；同时将左侧三枚 radial nodes 统一向中部 preview card 回收约一小档、略增 connector 线宽/有效长度与非激活 ring/label 权重，优先缓解 fresh-load browser QA 中 left cluster 仍略 detached 的问题；再同步放大 preview 底部 `Meth. / Find. / Data / Evid.` 四枚 micro labels 的字号、最大宽度、列间距、底部留白与柱图区间距，将问题从“偏拥挤”收敛到“仍 tiny 但可读”。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描与独立 reviewer。最新本地 fresh-load browser QA 认为顶部 utility controls 更清晰、无 clipping/明显 overlap；当前剩余最明显差距仍收敛为 left cluster 与中部 preview 的连接感依旧弱于 live site，且底部 micro labels 虽不再明显拥挤，但仍属于偏 tiny 的精修范围。下一轮继续优先做 cluster 内部 tightness/连接感与底部微字舒适度的更小步收敛，而不是大改整体骨架。
- [x] 本轮继续完成一次更偏 radial-cluster 几何桥接与 quiet-controls 收敛的小步可交付切片：根据 live/local fresh-load 对照，将左侧三枚 radial nodes 统一进一步向中部 preview card 回收并略微上提/放大，缩短 connector 距离、减轻 glow/线条厚重感并提高标签可读性，优先缓解本地 browser QA 中 cluster 仍显 detached、标签摆位偏散的问题；同时把顶部 utility controls 的阴影继续减薄但保留轻微边框/图标对比，维持 subtle-but-present 的公开站语气。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与 live/local browser 对照。最新 QA 认为 cluster 相比上一轮更靠近中部 preview、utility controls 更接近 quiet live controls，当前剩余最明显差距继续收敛为 cluster 的最终桥接感与整体 crispness 仍略弱于 live site；下一轮继续优先做 cluster 与 preview 的最终构图平衡，而不是改动其它模块。
- [x] 本轮继续完成一次更偏 utility-presence、cluster→preview bridge 与 micro-label breathing room 的小步收敛：在不改动默认 hero 信息架构的前提下，轻微放松顶部 utility controls 组距并加深边框/图标对比，让三枚圆形入口继续保持 subtle 但不发虚；同时将左侧三枚 radial nodes 统一向中部 preview card 再回收一小档、略增 connector 有效长度与 ring/label 权重，优先缓解 fresh-load browser QA 中 cluster 仍偏 detached 的问题；并把 preview 底部 `Meth. / Find. / Data / Evid.` 四枚 micro labels 的字号、最大宽度、最小高度与纵向留白再上调一档，使其保持 tiny 但避免明显拥挤。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描与独立 reviewer，以及 fresh-load local browser QA。最新 QA 认为顶部 utility controls 已更 present 且未变重、底部 micro labels 可读且无明显重叠；当前剩余最明显差距继续收敛为左侧 radial cluster 与中心 preview card 的最终桥接感仍弱于 live site，下一轮继续优先做 cluster 几何连接与整体 crispness 的更小步收敛，而不是大改整体骨架。

## Phase C — Help shell 与公开文档（P1）
- [x] 建立 `/help/[slug]` 文档路由与 docs shell 三栏结构
- [x] 完成已知 help 路由：getting-started / projects / chat / skills / library / tasks / foundry / persona / im / settings
- [x] 加入顶部 search / appearance / language 控件占位与右侧 TOC
- [~] 继续根据公开文案与页面细节补全文档内容、排版层次、滚动状态与交互微调（本轮已对齐接近公开站的浅色 docs shell、图标化侧栏、Chat/Settings 文案与 `/chat` 回流；本轮继续把 getting-started 正文首段与 live site 一致地保留为导语段，同时新增独立的 `What is SciClaw` 目录锚点，避免 TOC 出现无正文锚点。）

## Phase D — Privacy / 公共交互细节（P2/P3 部分）
- [x] 完成 `/privacy` 页面公开信息摘要复刻
- [~] 继续补齐公共 auth 卡片的更细粒度状态、表单提示与视觉拟真（本轮已对齐 Login 面板的 Email address / Verification code 占位、disabled CTA、Google 按钮大小写与 Privacy Policy 提示）
- [~] 引入更多 settings / theme / language 菜单交互细节（本轮已把 docs/settings 文案与浅色 legal shell 对齐公开站信息架构）

## Phase E — App shell 占位 IA（P4）
- [x] 本轮继续完成一次更偏 `/chat` docs→workspace bridge 语气与中部卡片可读性的收敛：控制器侧重新核对 live `https://sciclaw.ai/chat` fresh-load 仍展示公开 marketing landing（hero + auth + Best Cases），并未进入真实 workspace；据此将本地 `/chat` 的中部 narrative 从“纯产品 app shell”口吻回调为更诚实的过渡态文案——把 `Project & Session flow mirrored from the public docs IA.` 调整为 `Project & Session flow staged between public docs and the future workspace.`，把 `Shape a product-like narrative before the Foundry export starts.` 调整为 `Shape a concise workspace brief before you leave the public docs shell.`，并同步把说明文案改为明确连接 docs shell 与 future app shell 的 bridge card。随后依据本地 browser QA 暴露的 center summary/flow cards 偏拥挤问题，再做一轮窄宽度减压：将三张 summary cards 与三张 flow cards 的三列切换点从 `md/lg` 推迟到 `xl`，并把 Session timeline 主栅格调整为 `xl:grid-cols-[minmax(0,1.12fr)_minmax(320px,0.88fr)]`，为中部卡片争取更多横向宽度。该里程碑同时补充/更新了 `/chat` 聚焦测试，继续锁定 bridge CTA 的路由可达性、桥接文案与右侧 `Session output` 文案；已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 `/chat` browser QA。最新 QA 认为当前 `/chat` 仍是 coherent 的 light workspace preview，center column 较上一轮更不拥挤、无明显 clipping/严重 overlap，且“公开 docs → 未来 workspace” 的过渡语气更准确；当前剩余问题已收敛为中心长标题虽可读但仍接近舒适宽度下界、右侧 rail 仍属高信息密度区。下一轮继续优先压缩中心列剩余解释性文字并温和放松右侧 rail 的信息密度，而不是把它误写成已完成的真实应用壳。
- [x] 建立 `/chat` 公共 app shell 占位页（当前先无缝跳转至 `/help/chat`，避免空白路由）
- [x] 建立 project / session / library / tasks / foundry / persona / im / settings 的应用内 IA 占位
- [~] 让 landing 与 docs 导航正确串联到 app shell 占位（本轮已把 `/chat` 从简单回流升级为浅色 app shell 占位；本轮继续把顶部 shell 文案改为 docs→workspace bridge、补充解释性 subcopy、为头部与左侧导航引入 sticky/sticky-like 结构、为 Project memory 增加更明确的 rail 标识，并补齐按钮 aria-label 与对应测试。所有改动已通过 `pnpm test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、ESLint、Next build、docker compose config、docker compose up -d --build、`curl -I` 对 `/` `/chat` `/help/getting-started` 的 200 校验、静态安全扫描、独立 reviewer 与本地 `/chat` browser QA。当前 `/chat` 作为 docs shell 向 app shell 的过渡页更连贯，但仍存在中心列说明卡偏文档化、右侧输出列相对中心列略轻、部分 sidebar subtitle 与 badge 在窄宽度下略紧的问题；下一轮优先继续把中心内容从“说明文字”收敛到更产品态的 session/task/output 实例，并提升右侧 Foundry handoff 的视觉权重；本轮继续压缩 `/chat` 中心与右侧 rail 的说明密度：将 session timeline 三条记录改写为更短的 runtime 状态句并补入 `3 source clusters · 2 claim deltas` / `1 blocker · 4 exhibits linked` / `Ready · outline locked` 等 summary 微标签；同时收紧 `Session focus / Task lane / Foundry prep` 与 `Session output` 的正文长度，把输出信号文案改成更短的操作态句子，并将右侧 rail 改为 `xl:grid-cols-[1.1fr_0.9fr]` 以更早获得双列呼吸感、让 `Evidence chain / Open risk / Handoff route` 在 xl 宽度就纵向展开。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 `/chat` browser QA。最新 QA 认为 `/chat` 仍是 coherent 的 light workspace preview，右侧 rail breathing room 已明显改善、center column 也较前一轮更易扫描；当前剩余主要问题收敛为中心三张主卡与下方 flow cards 仍稍偏文字密集、个别窄列 badge/eyebrow 仍接近拥挤边界，下一轮继续优先做中心列卡片文案压缩与窄列元信息层级整理，而不是大改 IA。）
- [x] 本轮继续把 `/chat` 从解释性 workspace preview 收敛为更可执行的 app-shell 中部工作区：新增三张 `Session focus / Task lane / Foundry prep` flow cards，用短描述、状态 pills 与真实导航 CTA 替换此前仅靠时间线承担的说明负载，使中心列更像可操作工作台而不是长段落说明；并同步补充对新 flow cards 文案、状态 pills 与 `/chat` `/help/tasks` `/help/foundry` 导航目标的聚焦测试。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描与独立 reviewer。当前 `/chat` 中部已比上一轮更少 explainer 感、更多 task/session/foundry 操作指向；下一轮优先继续压缩标题区说明密度，并给右侧 output/handoff rail 再让出一点纵向呼吸感。
- [x] 本轮继续把 `/chat` 中心与右侧 rail 从文档说明卡收敛为更产品态的 workspace 输出：将原先三条长 bullet 重构为 `Live sessions / Open tasks / Foundry exports` 指标条与 `Evidence chain / Open risk / Handoff route` 信号卡，并把 Foundry rail 升级为带说明文字的 `Brief outline / Evidence appendix / Slides handoff` 导出资产列表；同时收窄顶部标题与说明宽度，减少首屏 explainer 感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、`http://127.0.0.1:3001/chat` HTTP 200、静态安全扫描、独立 reviewer 与本地 `/chat` browser QA。最新 QA 认为页面更像 light app-shell/workspace preview，中心与右侧 rail 更像真实产品模块；剩余主要问题已从“解释感太强”转为卡片密度偏高，尤其是中部三张说明卡与右侧 stacked cards 仍略拥挤，下一轮优先继续缩减中部说明卡文案密度并为右侧 rail 留出更多呼吸感。）
- [x] 本轮继续把 `/chat` 中部从 docs 风格说明块收敛为更产品态的 session output 预览：在时间线右侧新增 `Session output` 模块、加入 `Regulatory memo · current answer` 与可执行 bullet，要点明确回链 sessions/tasks/project memory，同时把 Foundry handoff 提升为更高视觉权重的导出 rail；并同步补充针对新模块与 2xl 右侧栅格的聚焦测试。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑到 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、`http://127.0.0.1:3001/chat` HTTP 200、静态安全扫描、独立 reviewer（功能 + 代码）与本地 `/chat` browser QA。最新 QA 认为页面现在明显更像 light app-shell/workspace preview，中心内容更少 docs 感、右侧 output/handoff rail 更像真实产品模块；后续仍可继续减弱顶部 hero 化标题并进一步压实中心工作区密度。）
- [x] 本轮继续做 `/chat` 的产品态收敛：将中部三张 flow cards 从长段落说明改成带 `128 assets · 6 skills` / `3 live threads` / `12 export-ready assets` 元信息的更紧凑 workspace cards，弱化 docs/explainer 感；同时把右侧 `Session output` 的 evidence/risk/handoff signals 改为横向紧凑信号栅格，并把 `Foundry handoff` 改成带 `Export` pill 的导出条目，减轻 stacked card 拥挤。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描、独立 reviewer 与本地 `/chat` browser QA。最新 QA 认为该页已更 product-like、center column 更少 documentation 感、right rail 更有秩序且本轮切片可 ship；剩余主要问题收敛为中心三张 cards 仍略偏文字说明、right rail 仍属高信息密度区，下一轮优先继续压缩说明性文案并提升次级文字对比度，而不是改动整体 IA。
- [x] 本轮完成 `/chat` 真实路由与运行时校验切片：在控制器侧重新核对 live site fresh-load `/chat` 仍返回公开 landing（`web_extract` 与 live browser snapshot/vision 均显示 hero + auth card，而不是 light workspace shell），随后验证本地 `http://127.0.0.1:3001/chat` 已稳定渲染 light app-shell 占位页，包含 `Project memory`、`Session output`、`Queue the next research slice`、`Foundry handoff` 等结构；同时将 `src/app/chat/page.tsx` 做了仅限 import / Tailwind class 顺序的清洁化整理，使当前工作树与已完成的运行时验证记录保持一致。控制器本轮再次复核了 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`npm run build`、本地 browser snapshot、`curl`+Python 文本命中校验 `/chat`、`docker ps` 端口核对；以上验证结果用于确认本地 `/chat` 现状与既有实现仍成立，而不是宣称本次 diff 新增了功能逻辑。下一轮优先继续把 `/chat` 的 header / center / right rail 再压缩成更接近 live 公开风格与未来工作台过渡态的中间形态，并保持与实际 live `/chat` 差异的持续对照。

## Validation / review cadence
- [x] 本地参考资料保留在 `docs/reference-local/` 并通过 `.git/info/exclude` 忽略
- [x] 每个里程碑后执行 docker compose config / tests / build / compose up -d --build
- [x] 每个里程碑后执行 3 轮评审并提交中文 commit + push
