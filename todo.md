# SciClaw Clone Execution Todo

## Status legend
- [x] completed
- [~] in progress
- [ ] pending

## Phase A — 基础初始化
- [x] 初始化 Next.js 16 + React 19 + TypeScript + Tailwind 4 项目骨架
- [x] 补齐 Dockerfile、docker-compose.yml、Vitest、ESLint、README
- [x] 建立 landing 页面首版与基础测试

## Phase B — Landing 高保真复刻（P0）
- [~] 执行检查点（2026-05-18 02:41 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 systematic-debugging 先完成 repo/path/git/port、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、controller-side DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据表明当前 live/local 更值得推进的最小安全切片重新回到 hero→`Best Cases` vertical handoff，而不是 utility controls、left radial cluster 或 auth/access card：live `verifyToBest≈245px`，而本轮改动前 local `authBottomToBestTop≈72.95px`、`verifyToBest≈220.75px`，说明 local desktop handoff 比 live 明显更紧。基于该证据，本轮仅将 `src/components/landing-page.tsx` 的 `best-cases-section` desktop top margin 从 `lg/xl 4.2/4.56rem` 放宽到 `4.72/5.02rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared class 合同。Review 1：fresh local 快照与 controller-side 几何确认 handoff 已被重新放松，且 auth/access card、left radial cluster、utility row 未出现新的 clipping、overlap 或 absurd spacing。Review 2：focused Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`/` `/chat` `/help/getting-started` HTTP 200 与 `git diff --check` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 handoff-widening micro-pass；下一轮应继续基于 fresh live/local 证据决定是继续微调 handoff，还是再回到 auth/access card。
- [~] 执行检查点（2026-05-18 02:22 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 repo/git/status、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、controller-side DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据表明上一轮后 local 当前最大的共同残余重新切回 hero→`Best Cases` vertical handoff，而不是 left radial cluster、utility controls 或 auth/access card：live `gapInputToBest≈220.5px`，本轮改动前 local `gapInputToBest≈232.19px`、`gapHeroToBest≈84.47px`，说明 local desktop handoff 比 live 略更松。基于该证据，本轮仅对 `src/components/landing-page.tsx` 做一个最小安全切片：将 `best-cases-section` desktop 顶边距从 `lg/xl 4.92/5.28rem` 微收紧到 `4.2/4.56rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared class 合同，保持 `/` 与 `/chat` 继续共用同一 marketing landing shell。Review 1：fresh local 快照与 controller-side 几何确认 handoff 已更贴近 live，`gapHeroToBest≈72.95px`、`gapInputToBest≈220.67px`，且 auth/access card、left radial cluster、utility controls 未出现新的 clipping / overlap / absurd spacing；browser_vision/vision 在本轮对 live/local 截图均返回空内容或请求失败，因此本轮按 skill 要求降级使用 browser snapshot + DOM geometry 作为视觉判断来源，并已在结论中诚实记录该退化。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`/` `/chat` `/help/getting-started` HTTP 200 与 `git diff --check` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`，仅建议未来补一条 hero→Best Cases 视觉回归覆盖，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 handoff-tightening micro-pass；下一轮若继续推进 Phase B，应在保持 `/root/sciclaw` 单仓隔离与 3001 端口不变的前提下，再次基于 fresh live/local 证据决定是继续微调 handoff 还是转回 auth/access card。
- [~] 执行检查点（2026-05-18 02:00 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 repo/git/status、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据表明本轮开始前 fresh live 仍把 hero→`Best Cases` vertical handoff 视为更大的公共残余，但 fresh rebuilt local 已把 left radial cluster label spacing 重新暴露为单一最明显本地热点，而 auth/access card 退居次级；controller-side 几何也确认改动前 `literature(dx≈-7,dy≈-19)`、`outcome(dx≈47,dy≈-17)` 仍显局部拥挤。基于该证据，本轮仅对 `src/components/landing-page.tsx` 做 radial-label micro-pass：将 `Literature Analysis` 标签进一步左移/微下调到 `left-[-1.88rem] top-[0.46rem] sm:left-[-1.94rem]`，并将 `Outcome Present` 标签右移/微上调到 `left-[1.78rem] top-[0.36rem] sm:left-[1.88rem]`；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步更新 shared class 合同。Review 1：fresh local browser QA 明确确认 left radial-cluster label spacing 已改善，当前 single most obvious hotspot 已切回右侧 auth/access card，说明本轮成功把 radial labels 从主热点降为次级残余；controller-side 几何也显示 `literature(dx≈-9,dy≈-17)` 与 `outcome(dx≈49,dy≈-18)` 保持更稳定锚定，同时 hero→`Best Cases` 与 auth rail 未被误伤。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`/` `/chat` `/help/getting-started` HTTP 200 与 `git diff --check` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`，仅建议未来补充视觉回归覆盖，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 radial-label anchoring micro-pass；下一轮应优先回到 auth/access card 的视觉权重与贴合度，而不是切回 `/root/nexus-mail` 或其他仓库。
- [~] 执行检查点（2026-05-18 01:37 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 repo/git/status、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、DOM 几何、focused `/`+`/chat` Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据表明 fresh live 仍把 hero→`Best Cases` vertical handoff 视为更大的公共残余，但 fresh rebuilt local 继续把左侧 radial cluster 判为单一最明显 hero mismatch，而 auth/access card 与 handoff 只属次级残余；controller-side 几何也确认本轮前 local `heroToBest≈84.47px`、`authToBest≈84.47px`、`previewRightToAuthRight≈-28.47px` 保持稳定，说明更安全的最小切片不是重开 auth card 或 `Best Cases` spacing，而是仅通过 left network frame 的 wrapper-only inward/upward 微调来让 radial cluster 略更靠近中心 preview。基于该证据，本轮仅对 `src/components/landing-page.tsx` 做 radial-cluster wrapper-only micro-pass：将 `FeatureRotator` 左侧 network frame desktop 从 `lg/xl -mt 0.2/0.28rem`、`lg/xl mr 0/0` 调整为 `lg/xl -mt 0.28/0.38rem`、`lg/xl -mr 0.08/0.12rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared class 合同。Review 1：fresh local browser QA 诚实确认 left radial cluster 仍是当前 single most obvious mismatch，但本轮后它比改动前更靠近中心 preview，且 auth/access card 与 hero→`Best Cases` handoff 未被误伤；controller-side 几何也确认 frame `top≈277.02 -> 275.58`、left nodes 整体小幅右移/上提，而 `heroToBest≈84.47px`、`authToBest≈84.47px`、`previewRightToAuthRight≈-28.47px` 基本不变，证明这次 diff 只作用于 radial cluster seam。Review 2：focused `/`+`/chat` Vitest 15/15、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`/` `/chat` `/help/getting-started` HTTP 200 与 `git diff --check` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 radial-cluster wrapper-only inward/upward 微收敛；下一轮仍应优先围绕 left radial cluster 本身继续做更直接的节点/标签锚定切片，而不是回切 `/root/nexus-mail` 或其它仓库。
- [~] 执行检查点（2026-05-18 00:52 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check` 与静态安全扫描。root-cause 证据表明：fresh live 仍把 hero→`Best Cases` vertical handoff 视为页面更大的公共残余，但 fresh rebuilt local 已把右侧 auth/access card 重新暴露为首要本地热点；local browser QA 明确判定 auth/access card 仍是最醒目的 hero overlay，而 controller-side 几何也显示改动前 auth rail `width≈380px`、`verify.width≈347.22px`，说明更安全的最小切片不是继续动 section spacing，而是只把 auth wrapper/card 再收窄、轻量化、略上提并保持 `/chat` 同步。基于该证据，本轮仅对 `src/components/landing-page.tsx` 做 auth/access rail 微收敛：`feature-auth-wrapper` desktop `lg/xl mr 1.42/1.68rem -> 1.44/1.72rem`、`lg/xl -mt 16.58/16.96rem -> 16.62/17rem`、`lg/xl max-w 360/380px -> 352/372px`，并将 `landing-auth-card` / inner shell 从 `232/532/548px`、`266/536/552px` 收到 `228/520/536px`、`262/524/540px`，同时轻微降低圆角、边框高光、背景不透明度与阴影权重；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步更新 focused class 合同与旧值负断言。Review 1：fresh local browser QA 与 DOM 几何一致确认 auth/access card 现在比上一轮略更轻、更贴近主 preview，但仍诚实地是当前 local hero 的最大热点；同时 `authToBest≈84.47px`、`verifyToBest≈168.13px`、`previewBottomToAuthTop≈-254.73px` 基本稳定，说明本轮没有误伤 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态安全扫描 `STATIC_SCAN_OK`；当前诚实结论：这一 diff 是一次 auth/access rail lightening + width/placement micro-pass，下一轮仍应优先继续围绕该卡片的视觉权重与融入度做最小安全切片。
- [~] 执行检查点（2026-05-18 01:17 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据表明上一轮后 fresh local 右侧 auth/access card 仍是 landing hero 的首要本地热点，而不是 left radial cluster；live 与 local QA 都确认 `Best Cases` handoff 只是次级张力，因此本轮继续遵循 `sciclaw-auth-card-detachment-parity` 做 auth-card micro-pass，而不是重开 section spacing。基于该证据，本轮仅对 `src/components/landing-page.tsx` 做 auth/access rail 收窄与轻量化：`feature-auth-wrapper` 调整为 `max-w 248/260/344/364px`、`lg/xl mr 1.48/1.78rem`、`lg/xl -mt 16.82/17.2rem`，`landing-auth-card` 调整为 `224/508/524px`、更浅边框/背景/阴影与 `0.78rem` 圆角，内层 shell 同步收至 `258/512/528px` 与 `0.82rem` 圆角；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步更新 focused class 合同与旧值负断言。Review 1：fresh local browser QA 明确确认 auth/access card 现在略更轻、略更窄、与中心 preview 的贴合更强，页面无 clipping / overlap / broken render；controller-side 几何也确认 `auth.width≈372 -> 364px`、`verify.width≈339.88 -> 331.88px`、`previewBottomToAuthTop≈-254.73 -> -257.92px`，而 `gapAuthToBest≈84.47px` 与 `gapVerifyToBest≈167.8px` 基本稳定，说明本轮只改善 auth rail 的融入度而未误伤 hero→`Best Cases` handoff。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`/` `/chat` `/help/getting-started` HTTP 200 与 `git diff --check` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 auth/access card lightening + narrowing + attachment micro-pass，下一轮仍应优先继续围绕该卡片的视觉权重与融入度推进最小安全切片。
- [~] 执行检查点（2026-05-18 00:05 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` fresh-load 顺序对照、DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示 live 与 local 当前共同最值得推进的最小安全切片仍是 hero→`Best Cases` vertical handoff：live/local fresh QA 都将其判为仍略偏松，而 auth/access card 与 utility controls 已降为次级问题。基于该证据，本轮仅将 `best-cases-section` desktop top margin 从 `lg/xl 5.08/5.44rem` 微收紧到 `4.92/5.28rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 确认 handoff 较上一轮更合理但仍略偏松，当前 primary hotspot 仍是 handoff，auth/access card 与 utility controls 属次级残余；controller-side 几何也确认 `authToBest≈87.03px`。Review 2：focused Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 handoff-tightening 微收敛 + 双页测试同步 + 全链路验证完成的里程碑；下一轮仍应优先继续围绕 handoff 做最小安全切片。
- [~] 执行检查点（2026-05-17 23:47 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，并保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口。按 systematic-debugging 先完成 repo/git/status、AGENTS.md 的 Next.js 16 docs 约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check`、`/` `/chat` `/help/getting-started` HTTP 200、静态 added-lines 安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示上一轮后 fresh local 最明显热点已从 hero→`Best Cases` handoff 切回右侧 auth/access card detached 感：live fresh QA 仍把 handoff 视为更大的公共残余，但 rebuilt local fresh QA 明确把 auth/access card 视为单一最明显问题，controller-side 几何亦确认改动前 `auth.width≈400px`、`verify.width≈366.25px`、`auth.left≈788.33px`、`previewBottomToAuthTop≈-246.73px`，说明更安全的下一刀是 auth wrapper 的 placement-only 微收敛，而不是继续改 section spacing。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 的 `feature-auth-wrapper` 做 desktop 收窄+左移+上提微调：`lg/xl mr 1.74/1.98rem -> 1.58/1.82rem`、`lg/xl -mt 16.12/16.5rem -> 16.34/16.72rem`、`lg/xl max-w 380/400px -> 370/390px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同与旧值负断言。Review 1：rebuild 后 fresh local browser QA 诚实确认 auth/access card 已较上一轮更窄、更贴近主 preview，不再显得 overly wide，但当前 single most obvious mismatch 已切回 left radial cluster 稍显 detached / cramped，且页面无新的 clipping、overlap 或 broken render；controller-side 几何也确认 auth rail `width≈400 -> 390px`、`left≈788.33 -> 800.89px`、`verify.width≈366.25 -> 356.25px`、`previewBottomToAuthTop≈-246.73 -> -250.25`，同时 `gapAuthToBest≈87.03px` 与 `gapVerifyToBest≈171.64px` 保持稳定，说明本轮刻意只增强 auth rail 与中心 preview 的耦合，没有误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line ... [truncated]
- [~] 执行检查点（2026-05-17 23:24 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，并保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口。按 systematic-debugging 先复核 repo/git/status、AGENTS.md 的 Next.js 16 docs 约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 对照，再用 local browser QA + DOM 几何确认当前首要剩余热点仍是 hero→`Best Cases` vertical handoff，而非 auth/access card 或 left radial cluster；据此仅将 `best-cases-section` desktop top margin 从 `lg/xl 5.2/5.56rem` 微收紧到 `5.08/5.44rem`，并同步更新 `/` 与 `/chat` focused tests。Review 1：fresh local browser QA 证实页面无新的 clipping / overlap / broken spacing，垂直衔接较上一轮略更自然，但诚实判断首屏最强热点仍是 hero 主 preview + auth 浮层，handoff 仍有轻微分段感；controller-side 几何亦确认 `gapAuthToBest≈88.95px -> 87.03px`、`gapVerifyToBest≈237.95px -> 236.03px`，说明本轮只做了 Best Cases 上提微调。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：静态 added-lines 安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 hero→`Best Cases` handoff tightening 微收敛 + 双页测试同步 + 全链路验证完成的里程碑；下一轮应继续基于 fresh live/local 证据，在 handoff rhythm 与 hero 主 mockup / auth 浮层权重之间择一推进最小安全切片。
- [~] 执行检查点（2026-05-17 23:03 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 repo/git/status、AGENTS.md 的 Next.js 16 docs 前置约束、live/local fresh-load 顺序对照、controller-side DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check`、`/` `/chat` `/help/getting-started` HTTP 200、静态 added-lines 安全扫描与 repo-scoped 独立 reviewer。root-cause 证据显示 fresh live 仍把 hero→`Best Cases` vertical handoff 视为更大的公共热点，但 fresh rebuilt local 已重新把 right auth/access card placement-detachment 视为单一最明显残余；controller-side 几何也证实改动前 local `auth.width≈408px`、`verify.width≈374.25px`、`auth.left≈777.13px`、`previewBottomToBest≈153.7px`，而 targeted visual analysis 明确指出 auth card 主要问题是略偏低、略偏右、略显过重，不是 width collapse、cluster clipping 或 section breakage。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 做 auth-wrapper placement-only 微收敛：将 `feature-auth-wrapper` desktop `lg/xl mr 1.94/2.18rem -> 1.74/1.98rem`、`lg/xl -mt 15.88/16.26rem -> 16.12/16.5rem`、`lg/xl max-w 388/408px -> 380/400px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 fresh local browser QA 继续诚实判定 auth/access card 仍是首要热点，但已 slightly more attached / less detached，且没有新的 clipping、overlap 或 absurd spacing；controller-side 几何也确认 auth rail `width≈408 -> 400px`、`left≈777.13 -> 788.33px`、`verify.width≈374.25 -> 366.25px`，同时 `gapAuthToBest≈88.95px` 与 `gapVerifyToBest≈173.56px` 保持稳定，说明本轮刻意只改善 auth rail 的贴合感而未误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines 静态安全扫描 `STATIC_SCAN_OK`，两名 repo-scoped 独立 reviewer 均返回 `passed=true`，仅建议未来补充更高层视觉回归覆盖，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 auth/access card placement-only 微...
- [~] 执行检查点（2026-05-17 22:40 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 repo/git/status、AGENTS.md 的 Next.js 16 docs 前置约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何与 local browser QA。root-cause 证据表明 fresh live/local 首屏共同仍把 hero→`Best Cases` vertical handoff 视为更大的共同热点：live `verifyToBest≈156.75px`，而 local 改动前 `authToBest≈92.16px`、`verifyToBest≈176.77px`，且两侧视觉都将 left radial cluster 降为次级残余。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 做 handoff-only tightening 微调：将 `best-cases-section` desktop top margin 从 `lg/xl 5.4/5.76rem -> 5.2/5.56rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 fresh local DOM 几何显示 `authToBest≈92.16px -> 88.95px`、`verifyToBest≈176.77px -> 173.56px`、`previewToAuthTop≈-307.64px` 保持稳定，说明本轮只收紧了 handoff，未误伤 auth/access seam；local browser QA 也确认页面无 clipping/overlap/broken spacing，整体比改动前更自然一点。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 hero→`Best Cases` handoff tightening 微收敛 + 双页测试同步 + 全链路验证完成的里程碑；下一轮应继续基于 fresh live/local 证据，在 auth/access card placement 与 handoff rhythm 之间重新择一做最小安全切片。
- [~] 执行检查点（2026-05-17 22:19 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 repo/git/status、AGENTS.md 的 Next.js 16 docs 前置约束、live/local fresh-load 对照、controller-side DOM 几何与 local browser QA。root-cause 证据显示当前 local 最明显残余已从 left radial cluster clipping 风险切回 hero→`Best Cases` vertical handoff：本轮前 local browser QA 将 left radial cluster 判为首要问题且指出 `Outcome Present` 标签/连接整体过度贴左，而 fresh live/local DOM 证据同时表明 auth/access card 几何稳定、hand off 仍显偏松。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 做 radial-cluster-safe 微收敛：将 network frame 左内边距从 `base/lg/xl 0.18/0.2/0.24rem` 提到 `0.34/0.42/0.5rem`，同时把 `Outcome Present` 节点锚点从 `right 16.9% -> 15.7%`、connector `w 9.98% -> 9.42%`、label `max-w 132 -> 126px`、`left/top 1.78rem/0.4rem -> 1.62rem/0.42rem`（`sm:left 1.86rem -> 1.72rem`），并同步更新 `/` 与 `/chat` focused tests。Review 1：rebuild 后 fresh local browser QA 明确确认 single most obvious mismatch 已切回 hero→`Best Cases` handoff，left radial cluster 不再是 primary hotspot，且页面无新的 clipping、broken render 或 absurd spacing；controller-side 几何也确认 radial labels 整体左界回到 `68px` 安全带。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 left radial cluster anti-clipping/anti-detachment 微收敛 + 双页测试同步 + 全链路验证完成的里程碑；下一轮应优先回到 hero→`Best Cases` vertical handoff 做最小安全切片。
- [~] 执行检查点（2026-05-17 21:59 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`todo.md` 续跑态、AGENTS.md 的 Next.js 16 docs 前置约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check`、`/` `/chat` `/help/getting-started` HTTP 200、静态 added-lines 安全扫描与 repo-scoped 独立 reviewer。按 systematic-debugging 先做 root-cause：fresh live 与 fresh local 都继续把首要热点锁定为 hero→`Best Cases` vertical handoff，而不是 auth/access card detachment 或 left radial cluster；controller-side 几何也显示 local `gapAuthToBest≈88.95px -> 92.16px`、`gapVerifyToBest≈173.56px -> 176.77px`，同时 `previewBottomToAuthTop≈-242.89px` 保持稳定，说明更安全的下一刀是只放宽 `best-cases-section` desktop top margin，而不是重开 auth rail 或 radial cluster。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 做 handoff-only 微调：`best-cases-section` 从 `lg/xl 5.2/5.56rem -> 5.4/5.76rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 契约。Review 1：rebuild 后 fresh local browser QA 明确确认 handoff 仍是 primary hotspot，但比上一轮略更自然，auth/access card 已降为次级问题；页面无新的 clipping、overlap 或 absurd spacing。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 hero→`Best Cases` handoff widening 微收敛 + 双页测试同步 + 全链路验证完成的里程碑；下一轮仍应优先围绕该 vertical handoff 做最小安全切片，再视 fresh live/local 证据决定是否回切 auth/access card 或 radial cluster。
- [~] 执行检查点（2026-05-17 21:42 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`todo.md` 续跑态、AGENTS.md 的 Next.js 16 docs 前置约束、live/local fresh-load 顺序对照、controller-side DOM 几何、focused `/`+`/chat` Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态 added-lines 安全扫描与 repo-scoped 独立 reviewer。按 systematic-debugging 先做 root-cause：live fresh QA 继续把右侧 auth/access card 视为首要热点，但 local fresh QA 已明确指出 left radial cluster 侵入感下降，当前 local 单一最明显问题回到 auth/access card 仍偏低/略 detached；与此同时 local 几何也显示 auth wrapper 仍为 `top≈871.84 / bottom≈1179.48`，而 network frame 在上一轮仍被较大的 desktop negative margin 上拉，说明更安全的下一刀不是继续改 auth rail 宽度，而是减弱左侧 network shell 的整体上顶量，避免 hero 左半区继续把视觉重量抬高。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 做 feature-network frame vertical-offset micro-pass：`lg/xl -mt 2.18/2.38rem -> 0.2/0.28rem`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 契约。Review 1：rebuild 后 fresh local browser QA 明确确认 left radial cluster 已 less intrusive，当前 single most obvious mismatch 诚实落回 auth/access card 仍略低/略 detached；controller-side 几何也确认 network frame 顶部从约 `275px` 回到 `277px` 附近、hero 结构稳定、auth/access 与 `Best Cases` 几何未被误伤，说明这是一次只作用于左侧 hero cluster 权重的安全微调。Review 2：focused `/`+`/chat` Vitest 15/15、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，仅给出“未来可补一个更高层级的响应式视觉回归覆盖”的非阻塞建议，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 left hero cluster vertical-offset 微收敛 + 双页测试同步 + 全链路验证完成的里程碑；下一轮应优先回到 auth/access card 的 vertical attachment / detachment，而不是继续停留在 radial cluster。
- [~] 执行检查点（2026-05-17 21:15 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`todo.md` 续跑态、AGENTS.md 的 Next.js 16 docs 前置约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何、focused/full Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、静态 added-lines 安全扫描与 repo-scoped 独立 reviewer。root-cause 证据表明当前 live 与 local 的 single most noticeable hotspot 重新一致收敛到右侧 auth/access card placement，而不是 hero→`Best Cases` handoff 或 left radial cluster：改动前 local 几何为 `authWrapWidth≈392px`、`previewRightToAuthRight≈-29.11px`、`previewBottomToAuthTop≈-235.20px`、`gapVerifyToBest≈173.56px`，且 fresh local browser QA 明确指出 auth card 已更 integrated 但仍最吸睛；live 侧 fresh QA 同样继续把 auth/access card 视为首要视觉失衡点。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 的 `feature-auth-wrapper` 做 placement-only 微调：desktop `lg/xl mr 1.62/1.82rem -> 1.94/2.18rem`、`lg/xl -mt 15.42/15.78rem -> 15.88/16.26rem`、`lg/xl max-w 376/392px -> 388/408px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 契约与旧值负断言。Review 1：rebuild 后 fresh local browser QA 诚实确认 auth/access card 仍是最明显热点，但比上一轮更 integrated，未出现 clipping、broken render 或 absurd spacing；controller-side 几何也确认 auth rail 按预期继续左移上提并稍放宽（`authWrapWidth≈392 -> 408`、`previewRightToAuthRight≈-29.11 -> -34.88`、`previewBottomToAuthTop≈-235.20 -> -242.89`），而 `gapVerifyToBest≈173.56px` 保持稳定，说明本轮刻意只增强 auth rail 与中心 preview 的耦合，没有误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，仅建议未来在视觉类切片中保留更高层级断言以减少 tests 对 Tailwind token 的脆弱耦合，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 auth/access card placement-only wrapper pass + 双页测试同步 + 全链路验证完成的里程碑；下一轮仍应优先围绕 auth/access card 做最小安全切片，再视 fresh live/local 证据决定是否回切 handoff 或 radial cluster。
- [~] 执行检查点（2026-05-17 20:53 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`todo.md` 续跑态、AGENTS.md 的 Next.js 16 docs 前置约束、3001 端口与 compose 运行态只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何与 local browser QA。root-cause 证据表明当前 local 最小安全切片继续落在右侧 auth/access card 的 placement-detachment，而不是 left radial cluster 或 hero→`Best Cases` handoff：改动前 local 几何为 `authWrapWidth≈392px`、`previewRightToAuthRight≈-25.27px`、`previewBottomToAuthTop≈-231.05px`、`authBottomToBestTop≈88.95px`，且 fresh local browser QA 明确指出 auth card 已比上一轮更 integrated，但仍可通过 wrapper-only 左移+上提进一步收敛；与此同时 live 侧 browser QA 仍把更大的公共残余视为 hero→`Best Cases` handoff，说明本轮应继续遵循 local-first 最小安全收敛，而不是直接改 section spacing。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 的 `feature-auth-wrapper` 做 placement-only 微调：desktop `lg/xl mr 1.42/1.58rem -> 1.62/1.82rem`、`lg/xl -mt 15.18/15.52rem -> 15.42/15.78rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 契约与旧值负断言。Review 1：rebuild 后 fresh local browser QA 诚实确认 auth/access card 已进一步改善并不再是首要热点，当前 single most noticeable hotspot 切回 hero→`Best Cases` vertical handoff；controller-side 几何也确认 auth rail 按预期左移上提（`previewRightToAuthRight≈-25.27 -> -29.11`、`previewBottomToAuthTop≈-231.05 -> -235.20`），而 `authBottomToBestTop≈88.95px` 保持稳定，说明本轮刻意只增强 auth rail 与中心 preview 的耦合，没有误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 auth/access card placement-only wrapper pass + 双页测试同步 + 全链路验证完成的里程碑；下一轮应优先围绕 hero→`Best Cases` handoff 做最小安全切片，再视 fresh live/local 证据决定是否回切 radial cluster 或 auth card。
- [~] 执行检查点（2026-05-17 20:34 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`todo.md` 续跑态、3001 端口与 compose 运行态只读核验、AGENTS.md 的 Next.js 16 docs 前置约束、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何与 local browser QA。root-cause 证据表明当前 local 最小安全切片仍应优先落在右侧 auth/access card detachment：改动前 local 几何为 `authWrapWidth≈392px`、`previewRightToAuthRight≈-10.23px`、`previewBottomToAuthTop≈-209.28px`、`authBottomToBestTop≈88.95px`，且 local browser QA 明确给出“略偏右、略偏低、宽度可接受，应主要通过左移+上提改善”的结论；与此同时 live 侧 browser QA 仍将更大的公共残余视为 hero→`Best Cases` handoff，说明本轮应继续遵循 local-first 的最小安全收敛，而不是把 live 的更高层热点直接套到 local。基于该 root cause，本轮仅对 `src/components/landing-page.tsx` 的 `feature-auth-wrapper` 做 wrapper-only 上提+左移微调：desktop `lg/xl mr 0.54/0.64rem -> 1.42/1.58rem`、`lg/xl -mt 13.82/14.16rem -> 15.18/15.52rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的精确类名契约与旧值负断言。Review 1：rebuild 后 fresh local browser QA 明确确认 auth/access card 已改善且不再是首要热点，当前 single most noticeable hotspot 诚实切回 left radial cluster crowding；controller-side 几何也确认 auth rail 按预期左移上提（`previewRightToAuthRight≈-10.23 -> -25.27`、`previewBottomToAuthTop≈-209.28 -> -231.05`），而 `authBottomToBestTop≈88.95px` 维持稳定，说明本轮刻意只增强 auth rail 与中心 preview 的耦合，没有误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 auth/access card integration 微收敛 + 双页测试同步 + 全链路验证完成的里程碑；下一轮应优先围绕 left radial cluster crowding 做最小安全切片，再视 fresh live/local 证据决定是否回切 handoff 或 auth card。
- [~] 执行检查点（2026-05-17 20:15 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`todo.md` 续跑态、3001 端口与 compose 运行态只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何与 local browser QA。root-cause 证据表明当前首屏最值得推进的最小安全切片继续落在右侧 auth/access card detached 感，而不是 radial cluster 或 hero→`Best Cases` handoff：fresh local browser QA 仍把 auth card 视为最主要剩余问题，并明确指出其仍略显 detached / 偏窄 / 偏低；同时改动前 local 几何为 `authWrapWidth≈384px`、`authCardWidth≈384px`、`verifyWidth≈350.25px`、`previewBottomToAuthTop≈-205.45px`、`previewRightToAuthRight≈-11.52px`、`gapVerifyToBest≈173.56px`，说明当前更需要小步上提并放宽 auth wrapper，而非继续改 `Best Cases`。因此本轮仅对 `src/components/landing-page.tsx` 的 `feature-auth-wrapper` 做 wrapper-only 微收敛：desktop `lg/xl mr 0.62/0.72rem -> 0.54/0.64rem`、`lg/xl -mt 13.58/13.92rem -> 13.82/14.16rem`、`lg/xl max-w 368/384px -> 376/392px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的精确类名契约。Review 1：rebuild 后 fresh local browser QA 继续诚实判定 auth/access card 仍是第一热点，但已较上一轮更贴近中心 preview、略少 detached 感，且 radial cluster 与底部微标签已属次级可接受残余；controller-side 几何也确认 auth host/card `384px -> 392px`、`verifyWidth≈350.25px -> 358.25px`、`previewBottomToAuthTop≈-205.45px -> -209.28px`、`previewRightToAuthRight≈-11.52px -> -10.23px`，而 `gapVerifyToBest≈173.56px` 保持稳定，说明本轮刻意只改善 auth rail 融合感而未误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest 15/15、full `pnpm test` 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`docker compose ps` 与 `git diff --check` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-line 静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次 auth/access card detachment 微收敛 + 测试同步 + 全链路验证完成的里程碑；下一轮仍应优先围绕 auth/access card 的融合感继续做最小安全切片，再视 fresh live/local 证据决定是否切回 handoff 或 radial cluster。
- [~] 执行检查点（2026-05-17 19:53 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先顺序复核 live/local fresh-load 与 DOM 几何，确认当前首屏最明显热点仍是右侧 auth card detached 感而非 radial cluster / preview 微标签 / hero→Best Cases 留白，于是仅收敛 auth-card wrapper/card 的宽度、圆角、右侧贴合与上提量，并同步校准 `/` 与 `/chat` focused tests；controller 侧已完成并通过 focused Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、HTTP 200、静态安全扫描与独立 reviewer 复核。
- [~] 执行检查点（2026-05-17 19:26 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、controller-side DOM 几何、focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、静态 added-lines 安全扫描与 repo-scoped 独立审查。root-cause 证据表明本轮最小安全切片应从上一轮的 hero→`Best Cases` handoff 暂时切回 auth/access card detachment：live 仍将 handoff 视为更大公共热点，但 fresh local browser QA 明确把 auth/access card 视为首屏最强剩余热点，且改动前 local `authWrapWidth≈404px`、`verifyWidth≈369.63px`、`previewBottomToAuthTop≈-189.13px`；因此本轮仅对 `src/components/landing-page.tsx` 的 `feature-auth-wrapper` 做 wrapper-only 收窄与上提微调（base/sm `272/284px -> 268/280px`，desktop `lg/xl mr 0.42/0.44rem -> 0.38/0.4rem`、`lg/xl -mt 12.58/12.9rem -> 12.84/13.14rem`、`lg/xl max-w 388/404px -> 380/396px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的精确类名契约。验证结果：focused Vitest 15/15、full Vitest 23/23、`pnpm lint`、`pnpm build`、`docker compose config`、容器重建与 `curl -I http://127.0.0.1:3001/` 均通过；静态安全扫描无新增命中，独立 reviewer 仅给出“测试类名断言较脆弱”的非阻塞建议。rebuild 后 local 几何收敛为 `authWrapWidth≈396px`、`verifyWidth≈361.63px`、`previewBottomToAuthTop≈-192.97px`；fresh local browser QA 认定 auth/access card 仍是第一热点，但已比上一轮更一体化、略少 detached 感，而 left radial cluster 与 hero→`Best Cases` handoff 退为次级热点。因此本里程碑被诚实记录为“auth/access card detachment 微收敛 + 测试同步 + 全链路验证完成”，下一轮应继续在不破坏当前 handoff 与 radial-cluster 稳定性的前提下收敛 auth/access card 融合感。
- [~] 执行检查点（2026-05-17 19:06 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、3001 端口与 compose 运行态只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、focused Vitest/full Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 复核。root-cause 证据显示上一轮后 local 最强剩余热点已从右侧 auth/access card detachment 切回 hero→`Best Cases` vertical handoff：live `gapVerifyToBest≈244.75px`，而本轮改动前 local `gapVerifyToBest≈173.89px`；同时 local browser QA 明确确认 auth card 已从首要热点降为次要问题。基于 `sciclaw-auth-card-detachment-parity`，本轮仅对 `src/components/landing-page.tsx` 做 auth/access card 去 detached 微调：`feature-auth-wrapper` desktop 收窄并轻微下沉（`lg/xl mr 0.44/0.46rem -> 0.42/0.44rem`、`lg/xl -mt 12.82/13.16rem -> 12.58/12.9rem`、`lg/xl max-w 396/412px -> 388/404px`），同步收窄并减轻 `landing-auth-card` 外壳与 inner shell（`lg/xl max-w 576/592px -> 560/576px`、inner `584/600px -> 568/584px`、边框/渐变/阴影小步减轻），并同步更新 `/` 与 `/chat` focused tests。复核结果：rebuild 后 local `auth.width 412 -> 404`、`verify.width 377.63 -> 369.63`、`gapPreviewToAuth -419.36 -> -411.03`、`overlapPreviewBottomToAuthTop -193.28 -> -189.13`；browser QA 亦确认 auth card 已改善并不再是最强剩余热点，当前 Phase B 主残余重新回到 hero→`Best Cases` handoff，下一轮应优先围绕该纵向过渡继续做最小安全切片，而不是回头扩大 auth card 改动。
- [~] 执行检查点（2026-05-17 18:45 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`todo.md` 续跑态、3001 端口与 compose 运行态只读核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 视觉与 DOM 几何。root-cause 证据显示当前最值得推进的最小安全切片已从 auth/access card 暂时切换为 hero→`Best Cases` vertical handoff：live/local browser QA 都明确确认上一轮后 auth/access card 仍是剩余热点但已退居次级，而更直接的局部收益来自缩短 hero 尾部到 `Best Cases` 的空白断层；controller-side local geometry 也显示改动前 `gapAuthToBest≈96px`、`previewBottomToBest≈203.97px`、`gapGetStartedToBest≈839.38px`，说明 section handoff 仍偏松。本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` desktop top margin 从 `lg/xl 5.64/6rem` 收紧到 `5.2/5.56rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 fresh local browser QA 明确确认 hero→`Best Cases` 过渡已改善、`Best Cases` 更像 hero 的自然下一个 section，而当前单一最强剩余热点已诚实切回 auth/access module 偏重/略低；页面无新的 clipping、overlap 或 broken render。Review 2：focused `/`+`/chat`+help Vitest、full `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check`、added-line 静态安全扫描与 repo-scoped 独立 reviewer 全部通过，reviewer 返回 `passed=true`、未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次运行时验证支撑下的 hero→`Best Cases` handoff tightening 微调切片；下一轮应优先围绕 auth/access module 的视觉权重/垂直贴合度继续做最小安全收敛，而不是触碰 `/root/nexus-mail` 或扩散到无关模块。
- [~] 执行检查点（2026-05-17 18:24 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、todo 续跑态、3001 端口与 compose 运行态只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、focused DOM 几何与 local browser QA。root-cause 证据表明上一轮后 local 当前最强剩余热点已从 left radial cluster 切回右侧 auth/access card placement-detachment，而 radial cluster 已降为次级可接受残余：live `gapVerifyToBest≈244.75px`；local 改动前 `gapVerifyToBest≈180.94px`、`previewBottomToAuthTop≈-193.28px`，且 local QA 明确指出 radial cluster 已 improved enough to stop this slice、下一步应转向 auth/access card。基于该证据，本轮只做最小安全 radial-label readability micro-pass，而不误动 auth/access 或 `Best Cases`：在 `src/components/landing-page.tsx` 将三枚 radial labels 做轻量去拥挤/再锚定（`Literature Analysis max-w 124px -> 128px, left -1.92rem -> -1.76rem, top 0.34rem -> 0.38rem`; `Data Mining max-w 126px -> 130px, top calc(100%-0.28rem) -> calc(100%-0.08rem)`; `Outcome Present max-w 126px -> 132px, left 1.62rem -> 1.78rem, top 0.32rem -> 0.4rem, sm:left 1.7rem -> 1.86rem`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 fresh local browser QA 明确确认 strongest remaining hotspot 已转为 auth/access card，left radial cluster 仅剩 polish-level residual，且页面无新的 clipping/overlap/broken issue；controller-side geometry 也确认 radial labels 仅做轻量位移（例如 `Literature dx≈-9.94 -> -7.38`，而 `gapVerifyToBest≈180.94px`、`previewBottomToAuthTop≈-193.28px` 保持稳定），说明本轮刻意只改善 radial 文本呼吸感而未误改 hero→`Best Cases` handoff 或 auth rail。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check` 与 added-line 静态安全扫描通过；下一轮应优先围绕 auth/access card placement-detachment 做最小安全切片，而不是继续停留在 radial cluster。
- [~] 执行检查点（2026-05-17 18:02 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、todo 顶部执行态、3001 端口只读核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、browser QA 与 controller-side DOM 几何。root-cause 证据表明当前 live 首要剩余热点仍是 hero→`Best Cases` vertical handoff，而 rebuilt local 在上一轮 handoff widening 后已把更明显热点切回左侧 radial cluster；同时 local 几何显示 `gapVerifyToBest≈177.09px`，仍显著低于 live `≈244.75px`，但 fresh local browser QA 已不再把 handoff 视为当前第一问题，而是明确把左侧 radial labels/cluster 的 detached 感视为主热点。因此本轮仅做最小安全切片：将 `src/components/landing-page.tsx` 的 `best-cases-section` desktop top margin 从 `lg/xl 5.4/5.76rem` 微调到 `5.64/6rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，作为一次 live/local 节奏再对齐的小步 widening pass。Review 1：fresh rebuilt local browser QA 确认页面无新的 clipping、collapse 或 absurd spacing，hero→`Best Cases` 过渡保持 coherent，但当前主热点已诚实转回 left radial cluster；controller-side 几何也确认 `bestCases.top≈1321.91px -> 1325.75px`、`gapVerifyToBest≈177.09px -> 180.94px`，而 auth/access 几何保持稳定，说明本轮刻意只放宽了 section handoff。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check`、added-line 静态安全扫描与 repo-scoped 独立 reviewer 全部通过；独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次运行时验证支撑下的 hero→`Best Cases` handoff widening 微调切片，section 节奏更接近 live，但 rebuilt local 当前更突出的剩余热点已切回 left radial cluster，下一轮应优先围绕 radial labels/connector anchoring 做最小安全收敛。
- [~] 执行检查点（2026-05-17 17:21 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、3001 端口只读核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、live/local browser QA 与 controller-side DOM 几何。root-cause 证据再次表明 live 与 local 当前更大的共同剩余热点都落在 hero→`Best Cases` vertical handoff，而 auth/access card 已退为次级热点：live `gapVerifyToBest≈244.75px`；local 改动前 `gapVerifyToBest≈173.25px`、`previewBottomToAuthTop≈-188.81px`，且 local browser QA 明确判定 auth/access card 基本贴合、无明显包裹/破损，真正更值得推进的是纵向过渡的断层感。因此本轮仅对 `src/components/landing-page.tsx` 做 `best-cases-section` desktop top-margin widening 微调：`lg/xl 5.16/5.52rem -> 5.4/5.76rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 fresh local browser QA 明确确认 hero→`Best Cases` 过渡比上一轮更自然，auth/access card 仍只是次级热点，且页面整体 coherent、无明显 clipping/overlap/broken spacing；controller-side 几何也确认 `gapVerifyToBest≈173.25px -> 177.09px`，而 `previewBottomToAuthTop≈-188.81px` 保持稳定，说明本轮刻意只放宽 section handoff 而未重新打乱 auth rail。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过。Review 3：`git diff --check`、added-line 静态安全扫描与 repo-scoped 独立 reviewer 全部通过；独立 reviewer 返回 `passed=true`，未发现新的安全或逻辑问题。当前诚实结论：这一 diff 是一次运行时验证支撑下的 handoff spacing 微调切片，hero→`Best Cases` 节奏已更顺，但 auth/access card 的轻微 detached 感与左侧 radial cluster 的悬浮感仍是后续可继续收敛的剩余热点。
- [~] 执行检查点（2026-05-17 16:59 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、3001 端口、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、local `browser_vision` 与 controller-side DOM 几何。root-cause 证据表明 live 当前更大的残余热点仍是 hero→`Best Cases` vertical handoff，而 fresh local 当前单一最明显热点依旧是 auth/access card：controller-side 几何显示改动前 `feature-auth-wrapper≈420px`、`VERIFY ACCESS CODE.width≈385.63px`、`previewBottomToAuthTop≈-184.97px`、`gapVerifyToBest≈173.25px`，说明本地 support rail 仍略宽、略低、较 live 更 detached。基于该证据，本轮仅对 `src/components/landing-page.tsx` 做 wrapper-only upward+narrowing 微收敛：`feature-auth-wrapper` 从 `-mt/max-w 2.44rem/276px sm:288px lg:-12.3rem lg:404px xl:-12.64rem xl:420px` 调整到 `2.56rem/272px sm:284px lg:-12.54rem lg:396px xl:-12.88rem xl:412px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 继续诚实判定 auth/access card 仍是首屏最明显剩余热点，但同时确认其已 slightly more attached / slightly less low，且无新的 clipping、overlap 或 absurd spacing；controller-side 几何也确认 auth host `420px -> 412px`、`VERIFY ACCESS CODE.width≈385.63px -> 377.63px`、`previewBottomToAuthTop≈-184.97px -> -188.81px`，而 `gapVerifyToBest≈173.25px` 与 `authBottomToBest≈88.31px` 基本持平，说明本轮刻意只改善 auth rail 贴合度而未误改 `Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过。Review 3：`git diff --check` 与 added-line 静态安全扫描通过；下一轮应继续基于 fresh live/local 证据，在 auth/access card、left radial cluster 与 hero→`Best Cases` handoff 之间重新择一做最小安全切片。
- [~] 执行检查点（2026-05-17 16:41 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、3001 端口、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 对照、focused DOM 几何与 local browser QA。root-cause 证据表明 live 当前更大的残余热点仍是 hero→`Best Cases` vertical handoff，而 local 当前单一最明显热点仍是 auth/access card，但它已不再是最粗糙的 detached overlay，而是略宽、略低的 support rail；因此本轮仅对 `src/components/landing-page.tsx` 做 wrapper-only upward+narrowing 微收敛：`feature-auth-wrapper` 从 `-mt/max-w 2.32rem/280px sm:292px lg:-12.06rem lg:412px xl:-12.4rem xl:428px` 调整到 `2.44rem/276px sm:288px lg:-12.3rem lg:404px xl:-12.64rem xl:420px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 明确确认 auth/access card 已比上一轮更 attached，当前最大热点已从 auth card 本体转向 hero→`Best Cases` handoff，left radial cluster 退居次级；controller-side 几何也确认 auth host `428px -> 420px`、`VERIFY ACCESS CODE.width≈393.63px -> 385.63px`、`previewBottomToAuthTop≈-181.13px -> -184.97px`，而 `gapVerifyToBest≈173.25px` 保持稳定，说明本轮刻意只改善 auth rail 贴合度而未误改 `Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过。Review 3：`git diff --check`、added-line 静态安全扫描与 repo-scoped 独立 reviewer 全部通过；独立 reviewer 返回 `passed=true`，未发现新安全/逻辑问题。当前诚实结论：auth/access card 已进一步收敛并不再是本轮首要阻塞，下一轮应优先围绕 hero→`Best Cases` vertical handoff 做最小安全切片。
- [~] 执行检查点（2026-05-17 16:21 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、3001 端口、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 对照、focused DOM 几何与 local browser QA。root-cause 证据表明当前单一最明显剩余热点仍是 auth/access card：live 几何显示 `VERIFY ACCESS CODE.bottom≈1066.5px`、`Best Cases.top≈1223.25px`、`gapVerifyToBest≈156.75px`，而 local 改动前 `feature-auth-wrapper` / `landing-auth-card` 宽约 `436px`、`VERIFY ACCESS CODE.width≈401.63px`、`previewBottomToAuthTop≈-177.28px`、`gapVerifyToBest≈173.25px`，说明本地 support rail 仍略宽、略低、与 live 相比更 detached。基于 `sciclaw-auth-card-detachment-parity` 与 `sciclaw-auth-card-docking-micro-pass`，本轮仅对 `src/components/landing-page.tsx` 做 wrapper-only 微收敛：`feature-auth-wrapper` 从 `-mt/max-w 2.14rem/284px sm:296px lg:-11.82rem lg:420px xl:-12.16rem xl:436px` 调整到 `2.32rem/280px sm:292px lg:-12.06rem lg:412px xl:-12.4rem xl:428px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 继续诚实判定 auth/access card 仍是首屏最明显剩余热点，但同时确认其已 slightly less low / more attached，且页面无 clipping、broken render 或 absurd spacing；controller-side 几何也确认 auth host `436px -> 428px`、`VERIFY ACCESS CODE.width≈401.63px -> 393.63px`、`previewBottomToAuthTop≈-177.28px -> -181.13px`，而 `gapAuthToBest≈88.31px` 与 `gapVerifyToBest≈173.25px` 基本持平，说明本轮刻意只改善 auth rail 贴合度而未误改 `Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过。Review 3：`git diff --check`、added-line 静态安全扫描与 repo-scoped 独立 reviewer 全部通过；独立 reviewer 返回 `passed=true`，未发现新安全/逻辑问题。当前诚实结论：auth/access card 仍是 local 首要剩余热点，但已再次小步收窄与上提；下一轮应继续基于 fresh live/local 证据，在 auth/access card、left radial cluster 与 hero→`Best Cases` 过渡之间重新择一做最小安全切片。
- [~] 执行检查点（2026-05-17 14:40 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、3001 端口、live/local fresh-load 对照与 vision fallback/DOM 几何，确认当前更强剩余热点已从 `hero -> Best Cases` 过紧转回 auth/access card placement-detachment，因此本轮仅将 `best-cases-section` desktop 顶边距从 `lg/xl 4.92/5.26rem` 回摆到 `5.16/5.52rem`，并同步 `/` 与 `/chat` focused tests。随后完成 focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`curl -I` 命中 `/` `/chat` `/help/getting-started`、`git diff --check`、added-line 静态安全扫描与 repo-scoped 独立 reviewer。运行后本地 fresh-load 页面无明显 clipping/overlap/absurd spacing，`gapAuthToBest` 由约 `84.16px` 回升至 `88.31px`，当前更强剩余热点重新落在 auth/access card placement-detachment，而不是 `hero -> Best Cases` 过渡本身。
- [~] 执行检查点（2026-05-17 15:18 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、3001 端口、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 对照、focused DOM 几何与 local browser QA。root-cause 证据表明当前单一最明显剩余热点已从 auth/access card 再次切回左侧 radial cluster，且 local QA 明确把 `Literature Analysis` 分支视为最强子热点：其标签已侵入主文案阅读通道，而 auth/access card 与 hero→`Best Cases` handoff 当前均次于该问题。因此本轮仅对 `src/components/landing-page.tsx` 做 `Literature Analysis` single-label micro-pass：为标签补上 `whitespace-nowrap`，并将锚点从 `left/top -1.98rem/-0.08rem sm:left -2.04rem` 调整到 `-2.1rem/0.42rem sm:left -2.16rem`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 继续诚实判定左侧 radial cluster 仍是首屏最明显剩余热点，且 `Literature Analysis` 仍是其中最突出分支，但页面整体无新的 clipping/overlap/absurd spacing；controller-side geometry 显示其相对 node 的偏移约 `dx -16.31px / dy -12.82px -> dx -12.81px / dy -18.02px`，同时 `authToBest≈88.31px` 保持稳定，说明本轮刻意只改善单标签锚定而未误改 auth/access 或 `Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过。Review 3：`git diff --check` 与 added-line 静态安全扫描通过；下一轮应继续围绕左侧 radial cluster，优先处理 `Literature Analysis` 与正文阅读通道的干扰，而不是回到 auth/access card 或 hero→`Best Cases`。
- [~] 执行检查点（2026-05-17 14:12 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-bestcases-handoff-geometry-fallback` 与 `browser-verification-degradation-fallback` 先完成 repo/git/status、3001 端口、live/local fresh-load 对照、focused/full Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check` 与静态 added-lines 安全扫描。root-cause 证据显示：live 公开页 auth→Best Cases 垂直距离仍明显大于本地，而 rebuilt local 几何在上一里程碑后 `gapAuthToBest≈96.95px`，说明当前首要剩余热点继续是 hero→`Best Cases` handoff 偏松，而不是 auth/access card 破损；同时本轮 live/local `browser_vision` 均退化为 no-content，已按 fallback 规则改以 fresh snapshot + DOM 几何 + build/HTTP 事实作为视觉验收依据。基于该 root cause，本轮仅将 `src/components/landing-page.tsx` 的 `best-cases-section` desktop 顶部间距从 `lg/xl 5.72/6.06rem` 小步收紧到 `4.92/5.26rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local fresh-load 几何确认 `gapAuthToBest≈84.16px`、`heroBottomToBest≈84.16px`，较上一里程碑再收紧约 `12.8px`，且 `previewBottomToAuthTop≈-177.28px` 保持稳定，说明本轮只改善 hero→Best Cases 过渡，没有误伤 auth/access 贴合度；browser snapshot 仍显示 fresh-load 默认状态为 `01 / 04` + `Automated Report Generation`。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check` 与 added-lines 静态安全扫描通过；本轮 browser vision narration 退化为 no-content，但 fresh snapshot + DOM/HTTP/controller-side 构建事实一致，未将工具噪声误判为应用回归。当前诚实结论：hero→`Best Cases` handoff 已继续小步收紧，但仍是 Phase B 当前首要剩余热点；下一轮应继续基于 fresh live/local 证据围绕该过渡做最小安全切片。
- [~] 执行检查点（2026-05-17 13:51 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-bestcases-handoff-geometry-fallback` 与 `browser-verification-degradation-fallback` 先完成 repo/git/status、3001 端口、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 对照、focused/full Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态 added-lines 安全扫描与 repo-scoped 独立审查。root-cause 证据显示：fresh local browser QA 已明确把当前最大剩余热点重新收敛为 hero→`Best Cases` handoff，而非 auth/access card；controller-side geometry 也确认本轮改动后 `gapAuthToBest≈101.11px -> 96.95px`、`previewBottomToAuthTop≈-177.28px` 保持稳定，说明这次 slice 只收紧 section handoff，没有误伤 auth/access 贴合度。因此本轮仅将 `src/components/landing-page.tsx` 的 `best-cases-section` desktop 顶部间距从 `lg/xl 5.96/6.32rem` 小步收紧到 `5.72/6.06rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh rebuilt local browser QA 明确确认 handoff 更紧、更连贯，当前最明显剩余热点仍是 `Best Cases` handoff，其次才是 auth/access card，且无新的 clipping、overlap 或 absurd spacing。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过；compose 后首次 `curl` 命中启动竞态返回 connection reset，按既有 fallback 规则 sleep/retry 后通过，未误判为应用回归。Review 3：`git diff --check` 与静态 added-lines 安全扫描通过，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：hero→`Best Cases` handoff 已再次小步收紧，但仍是 Phase B 当前首要剩余热点；下一轮应继续基于 fresh live/local 证据优先围绕该 section 过渡做最小安全切片。
- [~] 执行检查点（2026-05-17 13:31 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`.git/info/exclude`、3001 端口、live/local fresh-load 对照、focused/full Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200、静态安全扫描与 repo-scoped 独立审查。root-cause 证据显示：live 仍更偏向把 hero→`Best Cases` handoff 视为更大热点，而 fresh local QA 继续把 auth/access card 视为首屏单一最明显剩余问题，但已明确从“明显过低/脱离”收敛为“仍略低、已更贴近 preview 且无 clipping/absurd spacing”；因此本轮只做 `feature-auth-wrapper` 的 upward-only 微收敛：`-mt-[2.02rem] -> -mt-[2.14rem]`、`lg:-mt-[11.66rem] -> lg:-mt-[11.82rem]`、`xl:-mt-[12rem] -> xl:-mt-[12.16rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 wrapper class 合同。rebuild 后 local geometry 显示 auth top `940 -> 937.45`、`previewBottomToAuthTop ≈ -174.73 -> -177.28`，browser QA 继续判定 auth/access card 仍是当前主热点，但也明确说明其已 slightly less low / more vertically integrated，且无新的 clipping、overlap 或 absurd spacing；下一轮继续从 fresh live/local 证据重新判断是继续 auth-card 微收敛还是切回 hero→`Best Cases` handoff。
- [~] 执行检查点（2026-05-17 13:06 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-radial-cluster-geometry-parity`、`sciclaw-radial-label-single-node-micropass` 与 `browser-verification-degradation-fallback` 完成 repo/git/status、`.git/info/exclude`、3001 端口、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 对照、focused DOM 几何、browser QA、focused/full Vitest、lint、build、compose config、compose rebuild、HTTP 200、`git diff --check` 与静态安全扫描。root-cause 证据显示：live fresh-load 当前把最大残余继续指向 hero→`Best Cases` vertical handoff，但 rebuilt local fresh-load 已将首要热点重新收敛为 left radial cluster 中 `Outcome Present` label 偏紧/贴圆；因此本轮不重开 auth/access 或 Best Cases 大改动，只在 `src/components/landing-page.tsx` 对 `Outcome Present` 做 single-label micro-pass：`max-w 122px -> 126px`、`left 1.5rem -> 1.62rem`、`top 0.26rem -> 0.32rem`、`sm:left 1.58rem -> 1.7rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 明确确认 `Outcome Present` 比上一轮更不 cramped、left radial cluster 已退居次级，而当前单一最明显剩余热点重新回到 auth/access card；同时页面无新的 clipping、overlap 或 absurd spacing。controller-side geometry 也确认 `Outcome Present` 相对 node 的偏移约 `dx 44.52px -> 46.44px`、`dy -19.30px -> -18.34px`，而 `gapHeroToBest/gapAuthToBest≈101.11px` 保持稳定，说明本轮刻意只改善单标签呼吸感而未误改 hero→Best Cases 节奏。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 为 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 复核留待本轮提交前执行；当前诚实结论：left radial cluster 的 `Outcome Present` 已小步去拥挤，auth/access card 再次成为 local 首要剩余热点；下一轮应继续基于 fresh-load live/local QA 在 auth/access module 与 hero→`Best Cases` handoff 之间重新择一做最小安全切片。
- [~] 执行检查点（2026-05-17 12:50 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `browser-verification-degradation-fallback` 完成 repo/git/status、`.git/info/exclude`、3001 端口、live/local fresh-load 对照、controller-side DOM 几何与 targeted visual QA。root-cause 证据显示 live 与 local 均已把当前单一主热点从 auth/access host column 切换为 hero→`Best Cases` vertical handoff，因此本轮不再继续收窄 auth card，而仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.24/6.62rem` 小步收紧到 `5.96/6.32rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh live/local browser QA 均确认 handoff 更紧且仍 coherent，无新的 clipping、overlap 或 absurd spacing；local `browser_console` 也确认 `authToBestGap≈105.91px -> 101.11px`，而 `preview→auth overlap≈-442.08px` 保持稳定，说明本轮确实只改善了 hero→`Best Cases` 过渡。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 为 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：hero→`Best Cases` handoff 已小步收敛，auth/access panel 现在是次级但仍最显眼的剩余热点；下一轮应继续基于 fresh-load live/local QA，在 auth/access module 与 left radial cluster 之间重新选择最小安全切片。
- [~] 执行检查点（2026-05-17 12:28 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、`.git/info/exclude`、3001 端口、live/local fresh-load 对照、local `browser_vision` 热点判定与 controller-side DOM 几何。基于这些证据，本轮将最小安全切片继续限定为 `feature-auth-wrapper` 的 wrapper-only 微收敛：base/sm `288/300px -> 284/296px`，desktop `lg/xl mr 0.34/0.36rem -> 0.36/0.38rem`、`lg/xl -mt 11.54/11.88rem -> 11.66/12rem`、`lg/xl max-w 428/444px -> 420/436px`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。运行时复核显示本轮只改变 auth host column：local auth host `444px -> 436px`、preview→auth overlap `-449.75px -> -442.09px`、auth top 再上提约 `1.92px`，而 `gapAuthToBest≈105.91px` 基本不变；当前结论仍是 auth/access host column 已再次小步收敛但仍是 local 首要剩余热点，下一轮继续在 auth/access host 与 hero→`Best Cases` handoff 之间重新择一做最小安全切片。
- [~] 执行检查点（2026-05-17 12:05 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-auth-card-detachment-parity` 与 `browser-verification-degradation-fallback` 先完成 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 对照，并确认 AGENTS.md 要求的 Next.js 16 docs 路径仍可读。root-cause 证据表明本轮 live 侧最大残余仍是 hero→`Best Cases` 的纵向 handoff，而 rebuilt local fresh-load QA 与 controller-side geometry 继续把 auth/access host column 视为更直接的本地热点：local `browser_vision` 明确指出右侧 auth/access card 仍比 section spacing 更显眼，controller 侧几何也显示改动前 auth host `452px`、preview→auth overlap `-457.44px`、`gapAuthToBest≈105.91px`，因此本轮先做更小的 auth host-column micro-pass，而不误把 live 的 handoff 结论直接套到 local。基于该 root cause，本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做 wrapper-only 收窄与轻量上提（base/sm `292/304px -> 288/300px`，desktop `lg/xl mr 0.32/0.34rem -> 0.34/0.36rem`、`lg/xl -mt 11.42/11.76rem -> 11.54/11.88rem`、`lg/xl max-w 436/452px -> 428/444px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 fresh local browser QA 仍诚实判定 auth/access card 是首要剩余热点，但同时确认它比上一轮更窄、更贴近 preview，且没有新的 clipping/overlap/absurd spacing；controller-side geometry 也确认 auth host `452px -> 444px`、preview→auth overlap `-457.44px -> -449.75px`、auth top 再上提约 `1.92px`，而 `gapAuthToBest≈105.91px` 保持不变，说明本轮刻意只收敛 auth host 而未误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200（`/` `/chat` `/help/getting-started`）与 `git diff --check` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`；本轮未触碰 `/root/nexus-mail`。Review 3：added-lines static scan 为 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：auth/access host column 已再次小步收敛但仍是 local 首要剩余热点；下一轮应继续基于 fresh-load live/local QA 在 auth/access host 与 hero→`Best Cases` handoff 之间重新择一做最小安全切片。
- [~] 执行检查点（2026-05-17 11:24 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-auth-card-detachment-parity` 与 `browser-verification-degradation-fallback` 复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、Next.js 16 docs 约束、live/local fresh-load snapshot 与 controller-side 几何。root-cause 证据表明：live 侧仍缺少本地 `data-testid`，只能通过结构快照确认 hero→`Best Cases` 仍是公开站更显眼的残余；本地侧 fresh-load 快照与几何则继续把 auth/access card 宿主列视为当前最小安全切片，而两次 `browser_vision` 均退化为 `Vision analysis returned no content.`，因此本轮按 fallback 规则改以 snapshot + DOM geometry + build/HTTP 事实作为视觉验收依据，而不把工具噪声误判为应用回归。基于该 root cause，仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做 wrapper-only 微收窄与上提（base/sm `300/312px -> 296/308px`，desktop `lg/xl mr 0.28/0.3rem -> 0.3/0.32rem`、`lg/xl -mt 11.18/11.52rem -> 11.3/11.64rem`、`lg/xl max-w 452/468px -> 444/460px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local snapshot/DOM 几何确认 auth host `468px -> 460px`、preview→auth overlap `-472.80px -> -465.11px`、auth top 继续上提约 `1.92px`，页面无新的 clipping/overlap/absurd spacing；auth→`Best Cases` gap 仍约 `105.91px`，说明本轮刻意只收敛 auth host 而未误改 section handoff。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 为 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：auth/access card 仍是 local 首要剩余热点但已再次小步收敛；下一轮应继续基于 fresh-load live/local QA 在 auth/access host 与 hero→`Best Cases` handoff 之间重新择一做最小安全切片。
- [~] 执行检查点（2026-05-17 09:53 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-auth-card-detachment-parity` 与 `sciclaw-bestcases-handoff-geometry-fallback` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、controller-side 几何和 browser QA。root-cause 证据表明本轮开始前 live 侧仍将 hero→`Best Cases` vertical handoff 视为最大热点，而 local fresh-load QA 继续将 auth/access card detachment 视为当前主热点；因此本轮先采用更小的 auth host-column micro-pass，而不是盲目扩大到 Best Cases：仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 做 wrapper-only 收窄与上提（base/sm `296/308px -> 292/304px`，desktop `lg/xl mr 0.26/0.28rem -> 0.28/0.3rem`、`lg/xl -mt 10.74/11.06rem -> 10.86/11.18rem`、`lg/xl max-w 448/464px -> 440/456px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 明确确认 auth/access card detachment 略有改善、仍是剩余主热点但未出现新的 clipping/overlap/absurd spacing；controller-side 几何也确认 auth host `464px -> 456px`、preview→auth overlap `-468.47px -> -460.80px`、`gapAuthToBest≈105.91px` 基本持平，说明本轮确实只是在缩窄与上提 auth host，而未误改 hero→`Best Cases` 节奏。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 对 hardcoded secrets / shell injection / eval/exec / pickle / SQL injection 为 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：auth/access card 已再次小步收敛但仍是 local 首要剩余热点；下一轮应基于 fresh-load live/local QA 在 auth/access card 与 hero→`Best Cases` handoff 之间重新择一做最小安全切片，而不是触碰 `/root/nexus-mail` 或无关模块。
- [~] 执行检查点（2026-05-17 07:08 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-auth-card-detachment-parity` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、controller-side 几何和 browser QA。root-cause 证据表明上一轮后 live 侧仍更关注 hero→`Best Cases` handoff，而 fresh local QA 仍把 auth/access card 视为当前主热点；因此本轮先执行更小的 auth host-column micro-pass，而不是盲目扩大到 Best Cases：仅在 `src/components/landing-page.tsx` 收窄并上提 `feature-auth-wrapper`（base/sm `300/312px -> 296/308px`，desktop `lg/xl mr 0.24/0.26rem -> 0.26/0.28rem`、`lg/xl -mt 10.62/10.94rem -> 10.74/11.06rem`、`lg/xl max-w 456/472px -> 448/464px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 明确确认 auth/access card 比上一轮更贴近 preview、已不再是最大破损点，但当前更大的剩余高保真热点已转为 hero→`Best Cases` 垂直过渡，left radial cluster 也比 auth card 更值得下一轮关注；同时无新的明显 clipping、overlap 或 absurd spacing。controller-side 几何也确认 auth host `472px -> 464px`，preview→auth seam 仍保持强叠合（`gapPreviewToAuth≈-468.47px`），`gapAuthToBest≈105.91px` 基本未变，说明这次 slice 主要解决的是 auth host 显著性而非下方 section 节奏。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 对 hardcoded secrets / shell injection / eval/exec / pickle / SQL injection 为 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：auth/access card 已进一步收敛并退居次级，下一轮应优先围绕 hero→`Best Cases` handoff 与 left radial cluster 做更小步收敛，而不是继续单线程执着于 auth card 或触碰 `/root/nexus-mail`。
- [~] 执行检查点（2026-05-17 06:39 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-auth-card-detachment-parity` 与 `sciclaw-bestcases-handoff-geometry-fallback` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、controller-side 几何和 browser QA。root-cause 证据表明本轮开始前 live `browser_vision` 将最大热点判为 hero→`Best Cases` spacing，而 local `browser_vision` 仍将 auth/access card 判为主热点；因此本轮没有盲目继续收紧 Best Cases，而是先做更小的 auth/access micro-pass：仅在 `src/components/landing-page.tsx` 轻量收窄并软化 `feature-auth-wrapper` 与 `landing-auth-card`（base/sm wrapper `304/316px -> 300/312px`，desktop wrapper `lg/xl mr 0.22/0.24rem -> 0.24/0.26rem`、`lg/xl -mt 10.5/10.82rem -> 10.62/10.94rem`、`lg/xl max-w 464/480px -> 456/472px`；auth card `252px -> 248px`、inner shell `286px -> 282px`，同时小步降低圆角、边框/背景暖度、阴影与 blur），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh rebuilt local browser QA 明确确认 auth/access card 已比上一轮更 attached、略更不显眼，但当前最明显剩余热点又切回 hero→`Best Cases` spacing；local 几何也确认 auth button 到 `Best Cases` 约 `111.70px`，auth shell 宽度约 `248px` 且无新的 clipping/overlap/absurd spacing。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 对 hardcoded secrets / shell injection / eval/exec / pickle / SQL injection 为 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：auth/access card 已再次收敛，但本轮完成后主热点重新回到 hero→`Best Cases` handoff；下一轮应优先围绕该纵向过渡继续做更小步节奏收敛，而不是扩散到 `/root/nexus-mail` 或无关模块。
- [~] 执行检查点（2026-05-17 06:05 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging 与 `sciclaw-marketing-landing-parity` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、controller-side 几何和 browser QA。root-cause 证据表明当前单一最明显热点已从 auth/access card 转为 hero→`Best Cases` handoff：live/local `browser_vision` 都明确把首屏到底部案例区的空白过渡视为更大的视觉断裂，controller 侧几何也显示 local `gapAuthToBest` / `heroBottomToBest` 在改动前约 `118.08px`。因此本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 7rem/7.38rem` 小步收紧到 `6.24rem/6.62rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 明确确认 hero→`Best Cases` 过渡已有改善，且无新的 clipping/overlap/absurd spacing；最大剩余热点仍诚实保留为过渡仍略 airy/disconnected，而不再是 auth card。Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 对 hardcoded secrets / shell injection / eval/exec / pickle / SQL injection 均为 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：auth/access card 已降为次级热点，Phase B 下一轮应继续围绕 hero→`Best Cases` handoff 与首屏整体阅读流做更小步收敛，而不是扩散到 `/root/nexus-mail` 或无关模块。
- [~] 执行检查点（2026-05-17 05:29 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-auth-card-detachment-parity` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、controller-side 几何和 browser QA。root-cause 证据表明当前单一最明显热点仍是右侧 auth/access card，但问题已收敛为轻微过强/略 detached，而不是 clipping、破损或错误布局；因此本轮仅对 `feature-auth-wrapper` 做 wrapper-only host-column 微收窄与轻量贴合（base `max-w 312/324px -> 308/320px`、`lg/xl mr 0.18/0.2rem -> 0.2/0.22rem`、`lg/xl -mt 10.42/10.74rem -> 10.34/10.66rem`、`lg/xl max-w 480/496px -> 472/488px`），并同步更新 `/` 与 `/chat` focused tests。Review 1：fresh local browser QA 继续诚实把 auth/access card 视为首屏最明显热点，但明确判断它比上一轮更轻、更贴近 preview，且无新的明显 clipping、overlap 或 absurd spacing；Review 2：focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`；Review 3：added-lines static scan 返回 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`。当前诚实结论：auth/access card 仍是剩余主热点，但已进一步缩窄并更贴近中心 preview；下一轮应在 fresh-load live/local QA 基础上继续围绕 auth card 与左侧 radial cluster 两者择其一做新的最小安全切片，而不是扩散到 `/root/nexus-mail` 或无关模块。
- [~] 执行检查点（2026-05-17 05:07 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity` 与 `sciclaw-auth-card-detachment-parity` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、controller-side 几何与 browser QA。root-cause 证据表明当前最小安全切片重新落在 auth/access card 宿主列，而不是继续阻塞在调研：本轮仅对 `feature-auth-wrapper` 做 wrapper-only 轻量收窄与上提（`312/324px` base、`lg/xl mr 0.18/0.2rem`、`lg/xl -mt 10.42/10.74rem`、`lg/xl max-w 480/496px`），同步更新 `/` 与 `/chat` focused tests。controller 侧已通过 focused `/`+`/chat` Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200（`/`、`/chat`、`/help/getting-started`）、`git diff --check`；fresh local browser QA 确认 auth/access card 仍是当前最显眼热点但已更收窄、更贴近 preview，且无明显 clipping/overlap/absurd spacing，同时 left radial cluster 标签拥挤感也被暴露为下一轮候选热点；下一轮应继续从 fresh-load local/live QA 出发，在 auth/access card 与 radial-cluster 之间择其一做新的最小安全切片，而不是扩大改动面。
- [~] 执行检查点（2026-05-17 04:46 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging 与 `sciclaw-radial-cluster-geometry-parity` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、Next.js 16 约束、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、DOM 几何与 browser QA。root-cause 证据表明上一轮 auth/access card 微调后，当前单一最小安全切片已从 auth card 切回 left radial cluster：fresh local browser QA 明确指出 radial cluster 已改善但不再是“损坏态”，同时把更大层级的 hero/auth/Best Cases 过渡列为新的主热点；因此本轮仅在 `src/components/landing-page.tsx` 对 radial cluster 做 geometry-only 收敛，不触碰 auth shell、preview card 或 `/root/nexus-mail` 任意资源。具体改动为：将 `Literature Analysis` 节点/连线/标签整体轻微向内上提（`left 48.6% -> 49%`、connector `w 10.8% -> 10.1%`、label `left/top -2.14/-0.14rem -> -1.98/-0.08rem`），将 `Data Mining` 节点与标签略上收并缩短连线（`left/top 49.02/47.2% -> 49.34/46.8%`、connector `w 10.52% -> 9.9%`、label `top calc(100%-0.42rem) -> calc(100%-0.28rem)`），并将 `Outcome Present` 轻微上收靠内（`right/top 17.9/13.55% -> 16.9/13.35%`、connector `w 10.74% -> 9.98%`、label `left/top 1.62/0.38rem -> 1.5/0.26rem`、`sm:left 1.7 -> 1.58rem`）；同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 明确确认 left radial cluster 已比上一轮更可读、更不 detached，且当前最大剩余热点已转为 hero 中 preview/auth card 与 `Best Cases` 的整体过渡，而非 radial cluster 本身；controller 侧几何也验证三枚 radial labels 均向中心略收、连线缩短、无新 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 无 findings，repo-scoped 独立 reviewer 返回 `passed=true`，当前 diff 仅触及 radial cluster 几何类名与镜像测试合同。当前诚实结论：Phase B 仍在进行中，但 radial cluster 已从主故障态降为次级残余；下一轮应优先围绕 hero 中 auth/access module 与 `Best Cases` 的 vertical/compositional handoff 做小步收敛。
- [~] 执行检查点（2026-05-17 04:26 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging 与 `sciclaw-auth-card-detachment-parity` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser QA 与 DOM 几何。root-cause 证据表明当前单一最小安全切片已从 left radial cluster 转回 auth/access card：live/local 共同仍将其视为最大剩余热点，但本轮 wrapper-only 微调后 local browser QA 明确确认页面无 clipping/overlap/absurd spacing，auth card 相比上一轮更贴近中心 preview、视觉更完整；因此本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 做小步 composition pass（`lg/xl mr 0.12/0.14rem -> 0.16/0.18rem`、`lg/xl -mt -10.42/-10.76rem -> -10.28/-10.6rem`、`lg/xl max-w 496/512px -> 488/504px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 wrapper class 合同。Review 1：fresh local browser QA 继续将 auth card 视为最大剩余热点，但明确判断其比上一轮“更 attached、仍略偏低”，left radial cluster 已退居次级；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、`curl` 复核 `/` `/chat` `/help/getting-started` 全部通过；Review 3：`git diff --check` 与新增行静态安全扫描均为干净，repo-scoped 独立 reviewer 返回 passed=true。后续下一轮继续围绕 auth/access card 的 vertical attachment 与 hero→Best Cases handoff 做小步收敛。
- [~] 执行检查点（2026-05-17 04:03 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging 与 `sciclaw-radial-cluster-geometry-parity` 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、Next.js 16 docs、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、DOM 几何与 browser QA。root-cause 证据表明当前单一最小安全切片仍是 left radial cluster，且 fresh local `browser_vision` 明确将 `Outcome Present` 判为三枚标签中最拥挤、偏左贴圆的一支；controller 侧几何也显示改动前该标签相对 node 约 `dx≈44.52px / dy≈-17.38px`。因此本轮仅在 `src/components/landing-page.tsx` 将 `Outcome Present` 做 placement-only 微调（`left 1.5rem -> 1.62rem`、`sm:left 1.58rem -> 1.7rem`），不改 connector、node、auth rail 或 `/root/nexus-mail` 任意资源，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 继续诚实判定 left radial cluster 仍是当前首要 residual hotspot，但 `Outcome Present` 已从“偏左贴圆、呼吸感不足”收敛为更靠右、更不拥挤的状态；local DOM 几何收敛到约 `dx≈46.44px / dy≈-17.38px`，未引入新的 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、full `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 返回 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`、无 security/logic findings，当前 diff 仅触及 radial label class 与镜像测试合同。当前诚实结论：Phase B 仍在进行中，left radial cluster 仍是 local 首要残余；下一轮可继续围绕 `Outcome Present` / `Literature Analysis` 的 cluster 锚定收敛，或在 radial cluster 不再是最差热点后转回 hero→Best Cases handoff。
- [~] 执行检查点（2026-05-17 03:44 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution 与 systematic-debugging 先复核 repo/git/status、`.git/info/exclude`、`docker compose ps`、3001 端口、AGENTS.md 提示的 Next.js 16 docs 路径、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser QA 与 local DOM 几何。root-cause 证据重新确认当前单一最小安全切片仍是 left radial cluster，且 local `Outcome Present` 在上一轮后仍是 cluster 内最松的一支；因此本轮仅在 `src/components/landing-page.tsx` 对 `Outcome Present` 做 placement-only 微调（`left/top 1.62/0.32rem -> 1.5/0.38rem`、`sm:left 1.7rem -> 1.58rem`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 继续诚实判定 left radial cluster 是当前最明显 residual hotspot，但 `Outcome Present` 已从约 `dx≈46.44px / dy≈-18.34px` 收敛到 `≈44.52px / -17.38px`，看起来略更贴近节点且未引入新的 clipping/overlap；live 侧 browser_vision 则提示更大层级的 hero→Best Cases handoff 仍偏松，后续应在 radial cluster 收敛到可接受后再回切该热点。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、full `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 返回 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true`、无 security/logic findings，当前 diff 仅触及 radial label class 与镜像测试合同。当前诚实结论：Phase B 仍在进行中，left radial cluster 仍是 local 首要残余，而 `Outcome Present` 已小步改善；下一轮可继续围绕 `Outcome Present` / `Literature Analysis` 的 cluster 锚定收敛，或在 radial cluster 不再是最差热点后转回 hero→Best Cases handoff。
- [~] 执行检查点（2026-05-17 03:25 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、Next.js 16 docs 路径、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser QA 与 local DOM 几何后，确认当前单一最小安全切片重新回到 left radial cluster，而不是继续误把 auth/access card 当作主热点。root-cause 证据显示 live/local browser_vision 都把 left radial cluster 视为首要残余，且 local `Outcome Present` 标签调整前约 `dx≈49px / dy≈-18.98px`；因此本轮仅在 `src/components/landing-page.tsx` 将 `Outcome Present` 标签锚点做 placement-only 微调（`left/top 1.78/0.28rem -> 1.62/0.32rem`、`sm:left 1.86rem -> 1.7rem`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 仍诚实判定 left radial cluster 是当前最明显 residual hotspot，但 `Outcome Present` 已从 `dx≈49px / dy≈-18.98px` 收敛到 `≈46.44px / -18.34px`，标签与节点关系略更稳，auth/access card 继续降为次级问题。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、full `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps`、`git diff --check` 与 `/` `/chat` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 无 findings，repo-scoped 独立 reviewer 返回 `passed=true`、无 security/logic issues，当前 diff 仅触及 radial label class 与镜像测试合同。当前诚实结论：Phase B 仍在进行中，但本轮已把热点判断纠偏回 left radial cluster；下一轮应继续围绕 `Outcome Present` / `Literature Analysis` 的 cluster 锚定与视觉权重做更小步收敛，而不是扩散到 `/root/nexus-mail`、其他端口或无关结构。
- [~] 执行检查点（2026-05-17 03:06 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser QA 与 local DOM 几何，并遵循 AGENTS.md 中对当前 Next.js 版本先读官方 docs 的约束后再落代码。root-cause 证据显示当前单一最小安全切片已从 left radial cluster 转回 auth/access card：live `input.bottom≈1002.75 / best.top≈1311.25`、local 改动前 `authTopVsPreviewBottom≈-152.64px`、`authBottomVsBestTop≈118.08px`，fresh local `browser_vision` 也将 auth/access card 判定为最明显剩余热点，因此本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 做 placement-only 微调（`lg/xl mr 0.08/0.1rem -> 0.12/0.14rem`、`lg/xl -mt 10.28/10.62rem -> 10.42/10.76rem`、`lg/xl max-w 504/520px -> 496/512px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local snapshot/DOM 几何确认 auth card 上提并略收窄后，`authTopVsPreviewBottom≈-154.89px`、`authBottomVsBestTop≈118.08px`、`authWidth=512px`，页面未引入新的 clipping/overlap，且当前视觉热点已被诚实记录为 auth/access card placement 而非继续假设 radial label。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、full `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：added-lines static scan 返回 `STATIC_SCAN_OK`，当前 diff 仅触及 landing auth wrapper class 与镜像测试合同，未引入 secrets、eval/exec、shell 注入或跨仓库改动；repo-scoped 独立 reviewer 返回 `passed=true`、无 security/logic findings。本轮诚实结论：Phase B 仍在进行中，但当前主热点已从 left radial cluster 切回 auth/access card placement；下一轮应继续围绕 auth rail 的 vertical docking / overlap mass 做更小步收敛，而不是扩散到 `/root/nexus-mail` 或其他端口。
- [~] 执行检查点（2026-05-17 02:47 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/status、`.git/info/exclude`、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser QA 与 local DOM 几何。root-cause 证据表明当前单一最小安全切片仍是 `Outcome Present` 标签 detached，而非 auth card、Best Cases 或全局 cluster 结构；因此本轮仅在 `src/components/landing-page.tsx` 将其标签锚点从 `left/top 1.92rem/0.34rem sm:left 2rem` 微调为 `1.78rem/0.28rem sm:left 1.86rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的合同断言。Review 1：live 侧 hero 仍将 `Literature Analysis` 视为最弱、local 侧最新 browser QA 继续将 `Outcome Present` 视为最弱，但 rebuilt local 几何已从 `dx≈51.25 / dy≈-18.02` 收敛到 `dx≈49.0 / dy≈-18.98`，问题保持在可控 detached 范畴且未引入 clipping/overlap。Review 2：focused Vitest、full `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过。Review 3：added-lines static scan 为 `STATIC_SCAN_OK`，本轮 diff 仅触及 landing radial label class 与镜像测试合同，未引入 secrets、eval/exec、shell 注入或跨仓库改动。当前结果：`Outcome Present` 有所改善但仍是 local 最明显剩余标签热点，下一轮可继续围绕该单标签或再对照 live/local 重新判定是否切回 `Literature Analysis`。
- [~] 执行检查点（2026-05-17 02:27 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`.git/info/exclude`、`docker compose ps`、3001 端口、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser QA 与 local DOM 几何。root-cause 证据表明当前最明显剩余热点仍在 left radial cluster，且 local `browser_vision` 与 `browser_console` 一致把单一最小安全收敛切片锁定为 `Outcome Present`：调整前该标签相对 node 约 `dx=34.55px / dy=7.36px`、读感仍偏左游离；因此本轮仅在 `src/components/landing-page.tsx` 将 `Outcome Present` 标签锚点从 `left/top 2.08rem/0.46rem sm:left 2.16rem` 小步收敛为 `1.92rem/0.34rem sm:left 2rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 确认页面整体 coherent、当前最明显残余仍是 left radial cluster，但 `Outcome Present` 已更贴近其 node/cluster，local 几何收敛到约 `dx=32px / dy=5.44px`，且未引入新的 clipping、overlap 或 auth/access card 漂移；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`；Review 3：added-lines static scan 返回 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true` 且无 security/logic findings。本轮诚实结论：`Outcome Present` 仍是 Phase B 的单一剩余热点，但 detached 感继续收敛，下一轮应继续围绕 left radial cluster 做更小步的 `Outcome Present` / `Literature Analysis` 微调，而非扩散到 auth-card、Best Cases 或 `/root/nexus-mail` 任意资源。
- [~] 执行检查点（2026-05-17 02:05 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser QA 与 local DOM 几何。root-cause 证据表明上一轮 `Outcome Present` 单标签微调后，最明显热点已从该分支转移到 upper-left `Literature Analysis` 一侧，而 local 视觉仍确认 `Outcome Present` 只剩次级 detached 感；因此本轮仅在 `src/components/landing-page.tsx` 将 `Outcome Present` 标签锚点从 `left/top 2.16rem/0.4rem sm:left 2.24rem` 小步收敛为 `2.08rem/0.46rem sm:left 2.16rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，作为一次已验证的单标签收尾微调而非整簇重写。Review 1：fresh local browser QA 确认页面当前 coherent、无新增 hard clipping，且 `Outcome Present` 已不再是最差热点，remaining top hotspot 诚实转为 `Literature Analysis` 靠近橙色竖线的 spacing/alignment 压力；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`；Review 3：added-lines static scan 返回 `STATIC_SCAN_OK`，当前 diff 未发现 hardcoded secrets / shell 注入 / eval / unsafe 反序列化 / SQL 拼接风险，下一轮应优先回到 `Literature Analysis` 单标签或轻量 branch-spacing 微调，避免扩散到 auth-card、Best Cases 或 `/root/nexus-mail` 任意资源。
- [~] 执行检查点（2026-05-17 01:45 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、3001 端口与 `docker compose ps`，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、DOM 几何与 browser QA。live/local 共同表明本轮最小安全收敛切片应回到 left radial cluster 中更拥挤的 `Literature Analysis` 标签，而非继续扩大 `Outcome Present` 调整范围；因此仅在 `src/components/landing-page.tsx` 将该标签做单点 label-only 微调（`max-w-[118px] -> 124px`、`left-[-1.98rem] -> -2.14rem`、`top-[-0.08rem] -> -0.14rem`、`sm:left-[-2.04rem] -> -2.2rem`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：fresh local browser QA 确认 `Literature Analysis` 可读性改善、无新增 clipping/overlap，当前更明显残余热点转为 radial cluster 左侧靠近橙色竖线/节点的 spacing pressure；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过；Review 3：`git diff --check` 与 added-lines static scan 均通过，repo-scoped 独立 reviewer 返回 passed=true。当前工作树仅含本轮 3 文件微调，下一轮应继续基于 fresh local/live 对照，优先收敛 radial cluster 左侧剩余 spacing pressure，避免同时改动 auth-card 或 `/root/nexus-mail` 相关任何资源。
- [~] 执行检查点（2026-05-17 01:24 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`.git/info/exclude`、3001 端口与 `docker compose ps`，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、DOM 几何与 browser QA。live/local 共同继续把 left radial cluster 的最小安全切片锁定为 `Outcome Present`，且问题仍是 detached 而非 clipping/overlap；因此本轮仅在 `src/components/landing-page.tsx` 将该标签锚点从 `left/top 2.08rem/0.34rem sm:left 2.16rem` 小步回调为 `2.16rem/0.4rem sm:left 2.24rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1（功能/视觉契约）：rebuild 后 local `browser_console` 几何显示 `Outcome Present` 相对 node 的中心偏移由约 `dx=53.8px / dy=-18.02px` 变为 `dx=55.08px / dy=-17.06px`，fresh local `browser_vision` 亦确认它仍是当前最明显残余，但本轮有轻微改善且无 clipping / hard overlap；Review 2（测试/构建/集成）：focused Vitest 15/15、full `npm test` 21/21、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps` 与 `/` `/chat` `/help/getting-started` HTTP 200 全通过，容器继续仅发布 `0.0.0.0:3001->3000`；Review 3（静态/安全/合同）：`git diff --check` 通过，针对本次改动文件的静态安全扫描返回 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true` 且无 security/logic findings。本轮诚实结论：`Outcome Present` 单标签微调已再次落地并验收，但它仍是 Phase B 当前最明显的剩余 hero hotspot，下一安全切片仍应继续围绕该分支的 detached 感做更小步收敛。
- [~] 执行检查点（2026-05-17 01:03 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`.git/info/exclude`、3001 端口与 `docker compose ps`，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、DOM 几何与 browser QA。live/local 共同表明 left radial cluster 当前最明显残余热点仍是 `Outcome Present`，且问题主要是 detached 而非 clipping/overlap；因此本轮仅在 `src/components/landing-page.tsx` 将该标签锚点从 `left/top 2.16rem/0.28rem sm:left 2.24rem` 小步收敛为 `2.08rem/0.34rem sm:left 2.16rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1（功能/视觉契约）：rebuild 后 local `browser_console` 几何显示 `Outcome Present` 相对 node 的中心偏移从约 `dx=55.08px / dy=-18.98px` 收敛到 `dx=53.8px / dy=-18.02px`，fresh local `browser_vision` 亦确认它仍是当前最明显残余，但已略有改善，且无 clipping 或 hard overlap；Review 2（测试/构建/集成）：focused Vitest 15/15、full `npm run test` 21/21、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps` 与 `/` `/chat` `/help/getting-started` HTTP 200 全通过，容器继续仅发布 `0.0.0.0:3001->3000`；Review 3（静态/安全/合同）：`git diff --check` 通过，针对本次改动文件的静态安全扫描返回 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true` 且无 security/logic findings。本轮诚实结论：`Outcome Present` 单标签微调已落地并验收，但它仍是 Phase B 当前最明显的剩余 hero hotspot，下一安全切片应继续围绕该分支的 detached 感做更小步收敛。
- [~] 执行检查点（2026-05-17 00:46 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`.git/info/exclude`、3001 端口与 `docker compose ps`，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、DOM 几何与 browser QA。live 侧 `browser_vision` 明确表明 left radial cluster 当前最明显残余热点已回到 `Data Mining`；而 local 改动前 `browser_console` 几何显示 `Data Mining` label 相对 node 约 `dy=59.46px` 且以两行堆叠呈现，因此本轮仅在 `src/components/landing-page.tsx` 将 `Data Mining` 标签改为 `max-w-[126px] + whitespace-nowrap` 并把锚点从 `top-[calc(100%-0.22rem)]` 上提到 `top-[calc(100%-0.42rem)]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1（功能/视觉契约）：rebuild 后 local DOM 几何显示 `Data Mining` 已收敛为单行、相对 node 的 `dy` 从约 `59.46px` 收紧到 `43.08px`，local `browser_vision` 亦确认它不再是最差热点，当前单一最明显残余已转为 `Outcome Present`；Review 2（测试/构建/集成）：focused Vitest 15/15、full `npm run test` 21/21、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps` 与 `/` `/chat` `/help/getting-started` HTTP 200 全通过，容器继续仅发布 `0.0.0.0:3001->3000`；Review 3（静态/安全/合同）：`git diff --check` 通过，针对本次改动文件的静态安全扫描返回 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 返回 `passed=true` 且无 security/logic findings。本轮诚实结论：`Data Mining` 微调已落地并验收，Phase B 仍在进行中，下一安全切片应转向 `Outcome Present` 标签附着感。
- [~] 执行检查点（2026-05-17 00:25 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`.git/info/exclude`、3001 端口与 `docker compose ps`，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、DOM 几何与 browser QA。live/local 共同把 left radial cluster 的单一热点收敛到 `Outcome Present`，controller 侧几何显示调整后 label 相对 node 约 `dx=55.08px / dy=-18.98px`，且 browser QA 已确认其不再是最差 detached branch；因此本轮仅在 `src/components/landing-page.tsx` 将该标签锚点从 `left/top 2.02rem/0.24rem` 微调到 `2.16rem/0.28rem`（`sm:left 2.1rem -> 2.24rem`），并同步更新 `src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 的 class 合同。Review 1（功能/视觉契约）：fresh local browser snapshot + DOM 几何 + browser QA 确认 `Outcome Present` 已从最差热点降为可接受，当前更值得后续关注的是 `Data Mining`；Review 2（测试/构建/集成）：focused Vitest、`npm run test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`http://127.0.0.1:3001/` `/chat` `/help/getting-started` HTTP 200 全通过；Review 3（静态/安全/合同）：`git diff --check` 通过，针对本次改动文件的静态安全扫描无新增命中，repo-scoped 独立 reviewer 返回 `passed=true` 且无 security/logic findings。本轮诚实结论：`Outcome Present` 微调已落地并验收，Phase B 仍在进行中，下一安全切片应转向 `Data Mining` 标签附着感。
- [~] 执行检查点（2026-05-17 00:06 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`.git/info/exclude`、3001 端口、`docker compose ps`，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构、browser snapshot、DOM 几何与 local browser QA。live/local 一致表明当前首要剩余视觉热点仍是 left radial cluster，而 local `browser_console` 进一步定位为 `Outcome Present` 标签相对节点仍略向右下游离（调整前 label vs node 约 `dx=-37.75px / dy=5.44px`）；因此本轮仅在 `src/components/landing-page.tsx` 将该标签锚点从 `left/top 2.14rem/0.34rem sm:left 2.22rem` 小步收敛为 `2.02rem/0.24rem sm:left 2.1rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 明确确认最明显剩余问题仍在 left radial cluster、而非 auth/access card、center preview softness 或 hero→Best Cases spacing；本轮后 `Outcome Present` 标签更贴近所属节点、未引入新的 clipping/overlap。Review 2：focused Vitest 15/15、full `pnpm test` 21/21、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check`、added-lines 静态安全扫描 `STATIC_SCAN_OK` 与 repo-scoped 独立 reviewer JSON 复审 `passed=true` 全部通过，未发现新增 security concern 或 logic error。下一轮继续优先围绕 left radial cluster 的 `Outcome Present` / `Literature Analysis` 残余 attachment 微差做更小步几何收敛。
- [~] 执行检查点（2026-05-16 23:28 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`docs/reference-local/recon-summary.md`、3001 端口与 `docker compose ps`，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构/几何。结合 local browser QA 已将当前单一安全收敛切片从 `Data Mining` / `Outcome Present` 转移到 left radial cluster 中 `Literature Analysis` 标签仍显拥挤、过近主文案的残余热点，本轮仅在 `src/components/landing-page.tsx` 将该标签从 `max-w-[104px] left-[-1.66rem] top-[0.04rem]` 小步收敛为 `max-w-[118px] left-[-1.98rem] top-[-0.08rem] sm:left-[-2.04rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local snapshot、browser QA 与 DOM 几何确认 `Literature Analysis` 标签中心相对 node 已移动到约 `dx=-16.31px / dy=-12.82px`，无新的 clipping 或硬重叠；当前最明显残余热点仍诚实保留为 left radial cluster，且 `Literature Analysis` 依旧是第一视觉问题。Review 2：focused Vitest 15/15、full `npm test` 21/21、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check`、added-lines 静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`，仅提示保留 Outcome Present 的既有断言覆盖；已吸收该建议并复跑 focused 验证通过。下一轮继续优先围绕 left radial cluster 的 `Literature Analysis` / `Outcome Present` 轻微 crowding 做更小步几何或换行策略收敛。
- [~] 执行检查点（2026-05-16 23:47 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`docs/reference-local/recon-summary.md`、3001 端口与 `docker compose ps`，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 结构/几何。结合 live/local browser QA 与 local DOM 几何，本轮将单一安全收敛切片收窄为 `Outcome Present` 标签仍略偏左侧游离、相对圆点与 cluster 主干稍显 detached；当前仅在 `src/components/landing-page.tsx` 将该标签锚点从 `left/top 2.28rem/0.24rem sm:left 2.36rem` 小步收敛为 `left/top 2.14rem/0.34rem sm:left 2.22rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local snapshot、browser QA 与 DOM 几何确认 `Outcome Present` 标签相对 node 已从约 `dx=57 / dy=-19.63px` 收紧到 `dx=54.77 / dy=-18.02px`，未引入新的 clipping、overlap 或 auth→Best Cases handoff 漂移；当前最明显残余热点仍诚实保留为 left radial cluster，且 `Outcome Present` 依旧是第一视觉问题。Review 2：focused Vitest 15/15、full `npm test` 21/21、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check`、added-lines 静态安全扫描 0 命中，repo-scoped 独立 reviewer 返回 `passed=true`、无新增 security concern 或 logic error。下一轮继续优先围绕 left radial cluster 的 `Outcome Present` / `Literature Analysis` 轻微 detached 感做更小步标签或 connector 收敛。
- [~] 执行检查点（2026-05-16 22:59 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/branch/remote、`todo.md` 顶部状态、`docs/reference-local/recon-summary.md`、3001 端口与 `docker compose ps`，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `/`、`/chat` 的 fresh-load 结构/几何。结合上一轮残余与 `sciclaw-radial-cluster-geometry-parity` / `sciclaw-radial-label-single-node-micropass`，将本轮单一安全收敛切片锁定为 left radial cluster 中 `Data Mining` 标签相对节点仍略偏下；当前仅在 `src/components/landing-page.tsx` 将其锚点从 `top-[calc(100%-0.1rem)]` 小步上提到 `top-[calc(100%-0.22rem)]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local fresh snapshot 与 `browser_console` 几何确认 `/` 与 `/chat` 的 `Data Mining` label 现为 `top-[calc(100%-0.22rem)]`，相对 node 的 `dy` 约 `69.73px`，页面无 clipping/overlap，当前残余热点仍诚实保留为 radial cluster 中 `Data Mining` / `Outcome Present` 的轻微 detached 感；live 侧本轮几何探针受公开站点 DOM 结构限制较粗，但 fresh snapshot 继续确认默认 hero/case 合同未漂移。Review 2：focused Vitest 15/15、full `pnpm test` 21/21、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check`、added-lines 静态安全扫描 0 命中，且 repo-scoped 独立 reviewer 严格在 `/root/sciclaw` / `main` 上返回 `passed=true`、无新增 security concern 或 logic error；下一轮继续优先围绕 left radial cluster 中 `Data Mining` / `Outcome Present` 的剩余贴合度做更小步几何收敛。
- [~] 执行检查点（2026-05-16 22:35 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/容器/端口、`todo.md` 顶部状态、local fresh-load snapshot/DOM 几何，并结合上轮遗留与 local browser QA 证据，将本轮单一安全收敛切片锁定为 left radial cluster 中 `Data Mining` 标签相对节点仍略偏下。当前仅在 `src/components/landing-page.tsx` 将该标签锚点从 `top-[calc(100%+0.02rem)]` 小步上提到 `top-[calc(100%-0.1rem)]`，同步更新 `/` 与 `/chat` focused tests；controller 侧几何显示 `Data Mining` label 相对 node 的 `dy` 约 `63.3px -> 61.38px`，其余 `Outcome Present` / `Literature Analysis` 分支保持不变，说明本轮为单标签微调而非重写 cluster。Review 1：fresh local snapshot + DOM 几何确认 `Data Mining` 纵向 gap 略有收紧、页面无 clipping/overlap，当前残余热点仍诚实保留为 radial cluster 中 `Data Mining` 与 `Outcome Present` 的轻微 detached 感；本地 `browser_vision` 在 rebuild 后一度白屏、随后 again no-content，已按 browser-verification-degradation-fallback 以 fresh snapshot + DOM/HTTP/controller 事实验收。Review 2：focused Vitest 15/15、full `npm run test` 21/21、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与延迟后 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check`、added-lines 静态安全扫描 0 命中与 repo-scoped 独立 reviewer 通过；下一轮继续优先围绕 left radial cluster 中 `Data Mining` / `Outcome Present` 的剩余贴合度做更小步几何收敛。
- [~] 执行检查点（2026-05-16 22:07 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先复核 repo/git/容器/端口与 local fresh-load 几何，再将单一安全收敛切片收敛为 left radial cluster 中 `Outcome Present` 标签略偏右偏上。当前仅在 `src/components/landing-page.tsx` 将该标签锚点从 `left/top 2.42rem/0.16rem` 微调到 `2.28rem/0.24rem`，并同步更新 `/` 与 `/chat` focused tests；controller 侧已通过 focused/full Vitest、lint、build、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check` 与 added-lines 静态安全扫描。下一轮继续在不影响 auth handoff 的前提下复核 radial cluster 其余标签相对中心 preview 的贴合度。
- [~] 执行检查点（2026-05-16 21:44 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口。按 systematic-debugging 先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、3001 容器与 local fresh-load QA；结合 radial geometry 读数与 browser fallback，确认当前单一最小安全收敛切片已从整体 auth handoff 收敛为 left radial cluster 内 `Outcome Present` label 仍略偏上偏右、与 node/橙色 hub 的归属关系稍显 detached。基于 `sciclaw-radial-cluster-geometry-parity` 与 `sciclaw-radial-label-single-node-micropass`，本轮仅在 `src/components/landing-page.tsx` 将该 label 锚点从 `left/top 2.66rem/-0.02rem` 回调到 `2.42rem/0.16rem`（`sm:left 2.72rem -> 2.5rem`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local snapshot + DOM 几何显示 `Outcome Present` label 已左移下压至 `left≈174.47 / top≈399.22`，较改动前更贴近 node，未引入 clipping/overlap；当前最显著剩余热点仍诚实保留为 left radial cluster 中 `Outcome Present` 分支的轻微 crowding。Review 2：focused Vitest 15/15、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、以及 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check` 通过，added-lines 静态安全扫描 0 命中，repo-scoped 独立 reviewer JSON 返回 `passed=true`、无新增 security concern 或 logic error。下一轮继续优先围绕 left radial cluster 的剩余 Outcome Present / orange hub crowding 做更小步 connector 或 breathing pass，暂不回到 `/root/nexus-mail` 或其他端口。
- [~] 执行检查点（2026-05-16 21:17 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口。基于 repo-local 参考、fresh local snapshot/DOM 几何与 `sciclaw-auth-to-bestcases-handoff` 既有经验，本轮将 `feature-auth-wrapper` 的 desktop 负上边距从 `lg/xl -10.16/-10.5rem` 小步加深到 `-10.28/-10.62rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同；rebuild 后 local 几何显示 auth card 与 Best Cases 整体同步上移约 `1.91px`、`gapAuthToBest` 维持 `118.08px`，说明本次改动主要改善 auth/access module 相对 center preview 的贴合而非改写 section gap。Review 1：fresh local browser snapshot + DOM 几何确认页面无 clipping/overlap，remaining hotspot 仍是 auth rail 轻微 detached 与 left radial cluster 次级差距；Review 2：focused Vitest、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过；Review 3：`git diff --check`、added-lines 静态安全扫描与 repo-scoped 独立 reviewer 均通过。下一轮继续优先判断主热点是否转向 left radial cluster，或在 auth/access module 上继续做更小的垂直 docking 微调。
- [~] 执行检查点（2026-05-16 20:32 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，继续保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口。按 systematic-debugging 先完成 repo/path/git/status、`.git/info/exclude`、端口/容器前置核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并结合 live/local browser QA 与 local DOM 几何，将当前单一最小安全收敛切片继续锁定为 hero→Best Cases handoff 仍是最值得推进的残余热点。基于 local 改动前 `gapAuthToBest≈113.27px` 与本轮小步放宽后 `≈118.08px` 的几何事实，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.72/7.08rem` 调整到 `7/7.38rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，让首屏过渡略更从容，同时保持 auth/access module、preview、radial cluster、help shell 与 `3001` 发布边界不变。Review 1：sequential live/local fresh-load 与 browser QA 继续表明页面无新的 clipping / overlap / blank render，且当前手感更接近“较从容但仍可继续微调”的 handoff，而不是新的结构回退。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过，继续仅发布到 `3001`。Review 3：added-lines 静态安全扫描 0 命中，repo-scoped 独立 reviewer JSON 返回 `passed=true`、无新增 security concern 或 logic error，仅保留“后续可继续补强视觉回归”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断主热点是否仍是 Best Cases handoff，或是否已转回 hero 上半区层级与 radial cluster 的次级微差。
- [~] 执行检查点（2026-05-16 20:50 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口。按 systematic-debugging 先完成 repo/path/git/status、`.git/info/exclude`、端口与 `docker compose ps` 前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load browser QA 与 DOM 几何。live 视觉与几何继续表明 auth/access module 到 Best Cases 的 handoff 约 `126px`、顶部 utility controls 略偏重，而 local 改动前 `feature-auth-wrapper -> Best Cases` 间距约 `118.08px` 且 browser QA 仍判断 handoff 略偏紧，因此本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的 desktop 负上边距从 `lg/xl -9.84/-10.18rem` 小步加深到 `-10.16/-10.5rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，让 access/auth rail 略微上提、hero 收尾更接近 live 而不改宽度、结构、端口或 `/root/nexus-mail` 任意状态。Review 1：fresh local browser QA 复核显示 handoff 已比改动前更接近 live、仍诚实记录为“略偏紧但改善”，且 left radial cluster 已重新成为更突出的剩余热点，顶部 utility controls 仍仅是次级略偏重。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`docker compose ps` 以及 `/` `/chat` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check` 通过，added-lines 静态安全扫描对 hardcoded secrets / shell injection / eval/exec / pickle / SQL injection 均为 0 命中，repo-scoped 独立 reviewer JSON 返回 `passed=true`、无新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先把剩余主热点从 auth→Best Cases handoff 转向 left radial cluster 的 detached 感，再视需要微调顶部 utility controls 的权重。
- [~] 执行检查点（2026-05-16 20:14 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 systematic-debugging 先完成 repo/path/git/status、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，确认当前单一最小安全收敛切片仍是 hero 底部 auth/access module 相对 center preview 略偏低、hero→Best Cases handoff 略偏松。基于 local DOM 几何（改动前 `gapAuthToBest≈113.27px`）与 fresh browser QA，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的 desktop 负上边距从 `lg/xl -9.62/-9.96rem` 小步加深到 `-9.84/-10.18rem`，把 auth rail 轻微上提以缩短首屏空带；同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：spec/geometry 复核确认当前 diff 只触及 auth wrapper margin 与双页测试同步；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config` 通过，`docker compose up -d --build web` 因 Docker Hub `docker/dockerfile:1.7` DNS 超时失败，但现有 `3001` 容器仍保持 `/` 与 `/chat` HTTP 200，故按 fail-closed 记录为外部构建依赖阻塞而非代码回归；Review 3：diff-only 静态安全扫描 clean，repo-scoped 独立 reviewer 返回 passed=true。剩余诚实缺口：fresh browser QA 仍判断 hero→Best Cases 过渡略松，下一轮继续围绕 auth/access module 与 Best Cases handoff 做小步几何收敛。
- [~] 执行检查点（2026-05-16 19:36 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 hero 下方 auth/access module 与 Best Cases 的纵向 handoff 在本地已略偏松。基于 live/local 几何对照（live `authInput.bottom≈1002.75`、`best.top≈1223.25`；local 改动前 `authBtn.bottom≈1191.88`、`best.top≈1390.39`、`gapAuthWrapToBest≈113.27px`）与 fresh local 结构事实，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.92/7.32rem` 小步回调到 `6.72/7.08rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 合同，让 Best Cases 起始区相对 stacked auth/access module 略微上收，避免 handoff 继续偏松。Review 1：sequential live/local fresh-load、DOM 几何与 local snapshot 确认当前最真实残余热点已从“过紧”转为“略偏松”，本轮回调后页面未引入新的 clipping / overlap / blank render。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error；local `browser_vision` 本轮再次退化为 `Vision analysis returned no content.`，已按降级流程以 fresh snapshot + DOM/HTTP/controller 事实作为本地视觉验收依据。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否转回 auth/access module 贴合或 left radial cluster anchoring，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点（2026-05-16 19:16 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片继续锁定为 hero→Best Cases handoff 在本地仍略偏紧。基于 fresh local DOM 几何与 rebuild 后运行事实，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.72/7.08rem` 小步放宽到 `6.92/7.32rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 合同；rebuild 后 local `gapCardToBest` 由约 `110.72px` 增至 `113.27px`，让 right auth/access rail 与 Best Cases heading 的纵向 handoff 略松一小步，同时保持 auth rail 本身定位与宽度不变。Review 1：fresh-load live/local 对照与 controller DOM 几何确认本轮改动仍聚焦首屏 handoff，未引入新的 clipping、route 漂移或 landing-shell 破损，当前 remaining hotspot 仍诚实记录为 handoff 仍稍紧而非已完全对齐。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm --prefix /root/sciclaw run lint`、`npm --prefix /root/sciclaw run build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 `curl -I` 复检 `/`、`/chat` HTTP 200 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：controller 侧 diff-only 静态安全扫描 0 命中、`git diff --check` 通过，下一步继续优先围绕 hero→Best Cases residual spacing 与 auth/access rail 的次级对位做更小步收敛。
- [~] 执行检查点（2026-05-16 18:58 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/status、`.git/info/exclude`、`docker compose ps`、端口占用、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 hero→Best Cases handoff 在本地仍略偏紧。基于 fresh local DOM 几何（本轮改动前 `gapAuthToBest≈110.72px`）与 live/local 结构事实，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.56/6.92rem` 小步放宽到 `6.72/7.08rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 合同。Review 1：fresh-load live/local 对照确认本轮目标仅是放松首屏 hero 到 Best Cases 的 desktop handoff，页面未引入新的 clipping、route 漂移或 marketing-shell 破损。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config` 与 `/`、`/chat`、`/help/getting-started` HTTP 200 全部通过；`docker compose up -d --build web` 因 Docker Hub `docker/dockerfile:1.7` 解析阶段 DNS 超时失败，但按 fail-closed 规则已识别为外部 registry/network 阻塞而非应用回归，既有本地容器继续健康提供 `3001` 服务。Review 3：controller 侧 diff-only 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 hero→Best Cases residual spacing 与 auth/access rail 的次级对位做更小步收敛。
- [~] 执行检查点（2026-05-16 18:34 UTC）：本轮严格只在 `/root/sciclaw` 内推进 public landing Phase B；保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树/服务/端口。基于 live/local fresh-load 对照与本地 DOM 几何，本轮将 `feature-auth-wrapper` 的 desktop 负上边距从 `lg/xl -10.28/-10.62rem` 小步回调到 `-9.98/-10.32rem`，让 right auth/access rail 相对 center preview 略微下沉；同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：local fresh-load 几何复核 `gapAuthToBest≈110.72px`、auth rail 下沉约 `4.8px`，未引入新 clipping/结构漂移；Review 2：focused Vitest（15/15）、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`/` 与 `/chat` HTTP 200 全部通过；Review 3：diff-only 静态安全扫描无新增 secrets/shell/eval/SQL 注入模式，repo-scoped 独立 reviewer 通过。当前下一残余热点仍是 hero 底部 auth/access module 相对 live 的 handoff 仍偏紧，后续继续优先收敛该区域。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 desktop 首屏 right auth/access card 相对 center preview 仍略偏高、hero→Best Cases handoff 仍略偏松。本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的桌面负上边距从 `lg/xl -10.46/-10.82rem` 小步回调到 `-10.28/-10.62rem`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同，以轻微下压并减弱 auth rail 的悬浮读感、同时保持宽度与 hero 结构不变。Review 1（功能/视觉合同）：live/local fresh-load 结构与 DOM 几何复核显示本轮后 local `feature-auth-wrapper` 更接近 live auth rail 的垂直锚定，未引入 clipping/overlap，当前 remaining hotspot 仍主要是 auth rail 与 Best Cases 的首屏衔接细抛光。Review 2（测试/构建/集成）：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx` 15 tests passed；`pnpm --dir /root/sciclaw lint` 通过；`pnpm --dir /root/sciclaw build` 通过；`docker compose config` 通过；`docker compose up -d --build web` 成功并继续仅发布 `0.0.0.0:3001->3000`；HTTP 复检 `http://127.0.0.1:3001/` 返回 200。Review 3（静态/安全/合同）：新增 diff 仅涉及 Tailwind class 与对应测试断言；controller 静态扫描未发现 hardcoded secret / shell injection / eval/exec / unsafe deserialization / SQL injection；repo-scoped 独立 reviewer 返回 `passed=true`，仅建议继续依赖后续视觉回归核验。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片继续锁定为 desktop 首屏 right auth/access card 相对 center preview 仍略偏高、与 Best Cases 的 handoff 仍是首要残余热点。基于 live/local 结构快照、fresh DOM 几何与 focused diff 事实，本轮仅在 `src/components/landing-page.tsx` 将 desktop `feature-auth-wrapper` 的负上边距从 `lg/xl -10.72/-11.08rem` 小步回调到 `-10.46/-10.82rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local snapshot/DOM 复核确认首屏结构、默认 hero 状态与 `/chat` 共用 shell 均保持正确，且未引入 clipping/overlap，但 `gapAuthToBest≈110.72px` 基本未变，说明 auth/access handoff 仍是下一轮需要继续收敛的真实热点，本轮不夸大为已解决。Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 diff-only 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 right auth/access card 的 residual high-placement 与 hero→Best Cases handoff 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 desktop 首屏 right auth/access card 仍相对 live 偏窄、略 detached。基于 live/local DOM 几何对照（live access rail 宽约 `669.5px`，本地改动前 auth card 约 `456px`），本轮仅在 `src/components/landing-page.tsx` 小步放宽并轻微下压桌面 auth rail：将 `feature-auth-wrapper` 从 `lg/xl 440/456px` 放宽到 `500/516px`、负上边距从 `-10.58/-10.96rem` 调整到 `-10.72/-11.08rem`，并将 `landing-auth-card` 与 inner shell 宽度从 `520/536`、`528/544` 放宽到 `584/600`、`592/608`；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 focused class 合同。Review 1：fresh local browser/DOM 复核确认 auth card 宽度已提升到约 `516px`，更接近 live access rail 的横向存在感，页面无 clipping/overlap，当前残余热点诚实保留为 right auth/access rail 仍可继续向 live 的更宽、更低贴合态收敛。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm --prefix /root/sciclaw run lint`、`npm --prefix /root/sciclaw run build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 `/`、`/chat` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧静态安全扫描 0 命中、repo-scoped reviewer 两次均未按要求返回严格 schema，已按 fail-closed 规则改由 controller 直接复核当前 diff；结合 `git diff --check`、当前 diff 与运行结果，未发现新增 security/logic 问题。下一轮继续优先围绕 right auth/access rail 的 residual width/vertical fit 与 left radial cluster 次级 detached 感做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 right auth/access card 仍略偏下、hero→Best Cases handoff 仍略偏松。基于 live/local browser QA、fresh DOM 几何与 focused diff 事实，本轮仅在 `src/components/landing-page.tsx` 小步把 desktop `feature-auth-wrapper` 继续向 center preview 回收并上提（`lg:mr 0.14→0.08rem`、`xl:mr 0.18→0.1rem`、`lg:-mt -10.12→-10.34rem`、`xl:-mt -10.46→-10.72rem`），同时将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.9/7.28rem` 收敛到 `6.56/6.92rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：sequential browser QA 与 fresh DOM 几何确认 auth/access card 较上一状态更贴近中心 preview，`gapApplyToBest` 由约 `123px` 收紧到 `117px`、`gapAuthToBest` 由约 `266px` 收紧到 `260px`，hero→Best Cases 过渡更紧凑，但当前最明显的残余热点仍诚实保留为 auth/access card 仍略偏下。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧静态安全扫描 `STATIC_SCAN_OK`、`git diff --check` 通过，repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 right auth/access card 的 residual low-placement 与 left radial cluster 的次级 detached 感做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 desktop 首屏 right auth/access card 仍略偏低、与 center preview 的右侧 rail 关系仍未完全贴合。基于 live/local browser vision、fresh DOM 几何与 focused diff 事实，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的 desktop 布局收敛为更窄、更靠右且更高的 support rail（`lg:ml-auto lg:mr-[0.14rem] lg:-mt-[10.12rem] lg:max-w-[456px] xl:mr-[0.18rem] xl:-mt-[10.46rem] xl:max-w-[472px]`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同；rebuild 后 local 几何显示 auth wrapper 已从 `536px` 收窄到 `472px`、左边界右移到约 `745px`，视觉上更接近 live 的右侧 rail。Review 1：sequential browser QA 明确确认 auth/access card 已比上一轮更像 right-side rail、hero→Best Cases handoff 更干净，但仍诚实保留“略偏低且仍有少量 dead space”的残余差距。Review 2：`npm test -- --run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 added-lines 静态安全扫描 0 命中、当前 milestone diff 的 repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth/access rail 的 residual low-placement 与 right-edge integration 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、live/local fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 hero 底部 auth/access module 与 Best Cases 的纵向 handoff 仍略偏松。基于 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照和 DOM 几何（live `gapAuthToBest≈126px`、本轮改动前后 local `gapCardToBest≈116.47px`，当前热点仍集中在 auth bridge 而非 preview softness 或 radial clipping），本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的 desktop 负上边距从 `lg/xl -9.18/-9.52rem` 回调到 `-8.58/-8.92rem`，让 auth/access card 轻微下沉、更像连接 hero 与 Best Cases 的 bridge 层；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 class 合同。Review 1：fresh local snapshot/DOM 与 controller-side live 对照确认页面无 clipping/overlap，hero→Best Cases handoff 比上一轮更 coherent，但仍诚实保留“略偏松”的残余差距。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`curl http://127.0.0.1:3001/` 与 `curl http://127.0.0.1:3001/chat` 全部通过，容器继续仅发布到 `3001`。Review 3：controller-side diff 静态安全扫描 0 命中、`git diff --check` 通过、repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth→Best Cases residual whitespace gap 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为首屏右侧 auth/access card 仍略偏重、hero→Best Cases 过渡仍略偏松。本轮仅在 `src/components/landing-page.tsx` 收窄 auth wrapper/card 的 `lg/xl max-width`（`540/556 -> 504/520`，内层 `548/564 -> 512/528`），并同步下调 card 外壳的 border / gradient / shadow / backdrop-blur 强度，以降低 detached/heavy 感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新样式合同。Review 1：fresh local browser QA 确认 auth card 比上一轮更轻、更不 detached，但 hero 到 Best Cases 的过渡仍略偏松；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `curl http://127.0.0.1:3001/` 全部通过；Review 3：controller-side diff added-lines 静态安全扫描为 0，repo-scoped 独立 reviewer 判定 `passed=true`、未见新增逻辑/安全问题。剩余真实热点继续收敛为 hero 底部到 Best Cases 的纵向 handoff 仍可再小步收紧。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`、`.git/info/exclude`、`docker compose ps`、端口占用与 live `https://sciclaw.ai/` / rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 hero 底部 auth/access module 与 Best Cases 之间的纵向过渡仍略偏松。基于 live/local 对照、fresh DOM 几何与 browser QA（改动前 `gapAuthToBest≈110.72px`，收敛后 `≈105.59px`），本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 顶部间距从 `mt-[2.68rem]/sm:mt-[3.1rem]/lg:mt-[6.56rem]/xl:mt-[6.92rem]` 小步收紧到 `mt-[2.42rem]/sm:mt-[2.84rem]/lg:mt-[6.24rem]/xl:mt-[6.6rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 合同，并修复 `src/app/page.test.tsx` 中同一 Best Cases 间距断言的第二处遗留值。Review 1：fresh local browser QA 明确确认 auth/access card 不再显著 detached，hero→Best Cases 过渡较上一轮更 connected，当前仅剩轻微偏 roomy 但整体已 roughly balanced。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 diff-only 静态安全扫描 `STATIC_SCAN_OK`、`git diff --check` 通过；下一轮继续优先围绕 left radial cluster 的 residual detached/readability 与 hero 内部层级关系做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`、`.git/info/exclude`、端口/容器前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、DOM 几何与 browser QA，按 systematic-debugging 将当前单一最小安全收敛切片锁定为 hero 底部 auth/access module 相对 Best Cases 的 handoff 仍略紧。基于 live/local 对照与几何事实（live `bestCases.top≈1223px`、local 调整前 `gapAuthToBest≈110.72px`），本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的 desktop 负上边距从 `lg/xl -10.12/-10.46rem` 回调到 `-9.74/-10.08rem`，让 auth/access card 轻微下沉、更贴近 Best Cases 方向，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 明确确认 auth/access module 较上一状态更像 bridge 层、hero→Best Cases handoff 紧感减轻，且未引入明显 clipping/overlap/absurd spacing；当前诚实残余热点仍是左侧 radial cluster 比中心 preview 更 detached。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 diff-only 静态安全扫描 `STATIC_SCAN_OK`、`git diff --check` 通过，repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 left radial cluster 的 residual detached 感与 utility/auth cluster 的细微层级关系做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、端口占用与 live `https://sciclaw.ai/` / rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 desktop 首屏 hero→Best Cases handoff 在本地仍略紧。基于 live/local browser QA 与 fresh DOM 几何，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg:mt-[5.92rem] / xl:mt-[6.24rem]` 放宽到 `lg:mt-[6.56rem] / xl:mt-[6.92rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 spacing 合同。Review 1：live page 仍表现为 auth/access module 稍高、与 Best Cases 过渡更松，而 rebuilt local 在本轮后 `gapAuthToBest` 已由约 `100px` 提升到约 `111px`，handoff 更接近 live 但仍诚实保留“略紧”残差；left radial cluster 仍是次级 detached 热点。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 diff-only 静态安全扫描 0 命中、`git diff --check` 通过，repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth/access module 的 residual high-placement 与 left radial cluster detached 感做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧完成 repo/path/git/status、`.git/info/exclude`、容器/端口前置核验，并顺序对照 live sciclaw.ai 与 rebuilt local 3001 fresh-load hero→Best Cases handoff，按 systematic-debugging 将当前单一最小收敛切片锁定为首屏过渡仍略 busy：本轮仅把 `src/components/landing-page.tsx` 的 `best-cases-section` desktop 顶部间距从 `lg/xl 6.74/7.08rem` 收紧到 `5.92/6.24rem`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：browser QA 显示主要问题已从“过渡太松”收敛为 hero mockup/access panel 仍稍压近 Best Cases，说明本轮收紧方向正确但剩余热点转为 hero 底部视觉收口；Review 2：focused Vitest（page/chat）通过，lint/build 通过，`docker compose config` 通过，`docker compose up -d --build web` 后本地 3001 HTTP 200 正常；Review 3：controller 侧 diff 静态安全扫描无 secrets/eval/shell 注入命中，改动仅覆盖 landing section spacing 与对应测试合同。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、端口/容器前置核验、live/local fresh-load 对照，并按 systematic-debugging 将当前最小安全收敛切片锁定为右侧 auth/access module 仍是 hero→Best Cases handoff 的首要残余热点。基于 live/local browser QA、fresh DOM 几何与当前 diff 事实，本轮仅在 `src/components/landing-page.tsx` 小步上提并放宽 hero 内嵌 auth rail：保留 `feature-auth-wrapper` 的上提节奏（`-mt-[2.08rem] / lg:-mt-[9.88rem] / xl:-mt-[10.18rem]`），同时把 desktop `feature-auth-wrapper` 最大宽度从 `536/552` 放宽到 `548/564`，并将 `landing-auth-card` 与 inner shell 的 desktop 最大宽度从 `528/544`、`536/552` 放宽到 `540/556`、`548/564`，以减轻 card 的 compressed/detached 读感而不改动路由、交互与端口；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 focused class 合同。Review 1：live/local fresh-load 与 local browser QA 确认 auth/access module 较上一轮更贴近 hero preview，问题已从“明显悬浮”收敛到“仍是首要残余热点但改善有限”，页面无 clipping/overlap/结构破损。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 added-lines 静态安全扫描 0 命中、`git diff --check` 通过，repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth/access module 的 residual hanging/composition gap 做更小步收敛，而不是扩散到 `/root/nexus-mail` 或其他端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`、`.git/info/exclude`、`docker compose ps`、端口占用与 live `https://sciclaw.ai/` / rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 desktop 首屏中 hero auth/access module 到 Best Cases 的 handoff 仍较 live 偏紧。基于 live/local fresh geometry，live `gapAuthToBest≈126px`，本地改动前 `gapAuthWrapToBest≈101.11px`；因此本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg:mt-[5.88rem] / xl:mt-[6.32rem]` 放宽到 `lg:mt-[6.28rem] / xl:mt-[6.72rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 合同。Review 1：rebuild 后 local fresh snapshot 与 DOM 几何确认页面无 clipping/overlap，且 `gapAuthWrapToBest` 已提升到 `107.52px`，desktop handoff 较上一轮更松弛但仍诚实保留与 live 的留白差距。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 added-lines 静态安全扫描 0 命中，下一轮继续优先围绕 desktop hero→Best Cases handoff 的 residual whitespace gap 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/branch、`.git/info/exclude`、`docker compose ps`、端口占用与 live `https://sciclaw.ai/` / rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 desktop 首屏中 auth/access module 与 Best Cases 之间的 handoff 在本地仍略偏紧。基于 live/local DOM 几何事实，live fresh-load `gapAuthToBest≈244.75px`，而本地改动前 `gapAuthWrapperToBest≈46.08px`，说明当前主要差距已从 auth-card 自身视觉重量收敛为 hero→Best Cases 的纵向呼吸不足；因此本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl` 未显式放宽收敛为 `lg:mt-[5rem] xl:mt-[5.4rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 合同。Review 1：rebuilt local fresh-load snapshot 与 DOM 几何确认页面无 clipping/overlap，且 `gapAuthWrapperToBest` 已从 `46.08px` 提升到 `86.39px`，desktop handoff 明显更松弛；当前诚实剩余热点仍是 live 相比本地拥有更大的首屏下半段留白，而非新的结构性破损。Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 local HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 返回 `passed=true` 且未发现新增 security/logic 问题；下一轮继续优先围绕 desktop hero→Best Cases handoff 的 residual whitespace gap 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`/local-only ignore、端口/容器与 live `https://sciclaw.ai/` / local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前最小安全收敛切片继续锁定为 hero 下方 auth/access module 与 Best Cases 之间的 handoff 在本地仍略紧。基于 live/local 结构快照与 fresh DOM 几何（本轮改动前后 `gapAuthToBest≈27.52px`，说明当前差距仍小且热点集中在 auth support rail 自身），本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 的负上边距从 `-mt-[2.08rem]/lg:-mt-[9.52rem]/xl:-mt-[9.84rem]` 小步回收为 `-mt-[2rem]/lg:-mt-[9.36rem]/xl:-mt-[9.68rem]`，把 auth/access 模块从 Best Cases heading 稍微拉开，避免 handoff 继续发紧；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 focused spacing 合同。Review 1：fresh local snapshot/DOM 几何确认页面无 clipping、overlap 或 absurd spacing，且 handoff 由“略挤”回到更 balanced 的状态，当前诚实剩余热点转为 auth/access module 仍可继续做更细的纵向节奏微调。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 local HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 added-lines 静态安全扫描 0 命中、`git diff --check` 通过，repo-scoped 独立 reviewer JSON 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth/access module 与 Best Cases handoff 的 residual rhythm gap 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、`docker compose ps`、端口占用、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片继续锁定为 hero→Best Cases handoff 在本地仍略紧。基于本轮 fresh DOM 几何（调整前 `gapAuthToBestSection≈38.08px`、`gapHeadingToCard≈28px`）与 live/local 结构事实，仅在 `src/components/landing-page.tsx` 小步继续收紧首屏底部纵向节奏：将 `feature-auth-wrapper` 从 `-mt-[2.02rem]/lg:-mt-[9.34rem]/xl:-mt-[9.64rem]` 上提到 `-mt-[2.08rem]/lg:-mt-[9.52rem]/xl:-mt-[9.84rem]`，并将 `best-cases-section` 顶部间距从 `mt-[2.02rem]/sm:mt-[2.38rem]` 收至 `mt-[1.86rem]/sm:mt-[2.2rem]`、标题包裹从 `mb-7` 收至 `mb-6.5`；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 focused spacing 合同并补足 `BestCases` heading wrapper 的 `mb-6.5` 断言。Review 1：fresh local browser snapshot/DOM 几何确认页面无 clipping、overlap 或 absurd spacing，且 handoff 已较上一轮更紧凑，当前诚实剩余热点仍是 auth/access card 与 Best Cases heading 之间仅剩的轻微 crowding。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 added-lines 静态安全扫描 `STATIC_SCAN_OK`、`git diff --check` 通过，repo-scoped 独立 reviewer 三路复核均未发现新增 security/logic 问题，仅补充了已在本轮修复的测试覆盖提醒；下一轮继续优先围绕 auth/access card 与 Best Cases 起始标题之间的 residual crowding 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、端口/容器前置核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero→Best Cases handoff 在本地仍略拥挤。基于 live/local browser vision、fresh DOM 几何与 focused diff 事实，本轮仅在 `src/components/landing-page.tsx` 小步收紧首屏底部纵向节奏：将 `feature-auth-wrapper` 负上边距从 `-mt-[1.86rem]/lg:-mt-[9.18rem]/xl:-mt-[9.48rem]` 调整到 `-mt-[2.02rem]/lg:-mt-[9.34rem]/xl:-mt-[9.64rem]`，并将 `best-cases-section` 顶部间距从 `mt-[2.28rem]/sm:mt-[2.66rem]` 收敛到 `mt-[2.02rem]/sm:mt-[2.38rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 合同。Review 1：fresh local browser QA 确认 hero→Best Cases handoff 已较上一轮更 tight/clean，且无 clipping/overlap/broken spacing；当前诚实剩余热点变为 auth/access card 与 Best Cases heading 仍略近。Review 2：`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 diff-only 静态安全扫描 0 命中、`git diff --check` 通过，repo-scoped 独立 reviewer JSON 返回 `passed=true` 且未发现新增 security/logic 问题；下一轮继续优先围绕 auth/access card 与 Best Cases 起始标题的 residual crowding 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero→Best Cases handoff 仍略紧。基于 live/local browser QA、fresh DOM 几何与 focused diff 事实，本轮仅在 `src/components/landing-page.tsx` 为 `BestCases` 增加稳定 `data-testid="best-cases-section"` 并将顶部间距从 `mt-[1.72rem]/sm:mt-[2.06rem]` 小步放宽到 `mt-[2.28rem]/sm:mt-[2.66rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 对新锚点与 spacing 合同的 focused 断言。Review 1：fresh local browser QA 与 live 对照确认 hero→Best Cases 过渡更 airy/coherent、无 clipping/overlap，handoff 已不再是当前首要问题，当前诚实剩余热点回到 auth-card 仍略悬浮。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：diff-only 静态安全扫描未发现 secrets/eval/shell 注入等问题，repo-scoped 独立 reviewer JSON 复核 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth-card residual visual-weight 与贴合度做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 right auth/access card 仍略偏强、对 hero→Best Cases 过渡仍有轻微打断。本轮仅在 `src/components/landing-page.tsx` 小步收紧 `feature-auth-wrapper` 与 `landing-auth-card` 的宽度/外壳半径/边框/阴影/blur（wrapper `-mt/max-w` 从 `-1.8rem/324px, lg -8.9rem/548px, xl -9.2rem/564px` 收到 `-1.68rem/316px, lg -8.62rem/540px, xl -8.92rem/556px`；auth-card 从 `254px`、`rounded-[0.96rem]`、`border-[rgba(240,244,248,0.98)]`、`shadow-[0_3px_8px...,0_2px_4px...]`、`backdrop-blur-[0.36px]` 收到 `248px`、`rounded-[0.94rem]`、`border-[rgba(243,246,249,0.98)]`、`shadow-[0_2px_6px...,0_1px_3px...]`、`backdrop-blur-[0.3px]`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的精确类契约。Review Round 1 通过 live/local browser QA 确认页面无明显破坏、auth/access 观感更轻且对过渡的打断小幅缓和；Review Round 2 已通过 focused Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat`、`/help/getting-started` 的 HTTP 200；Review Round 3 已完成静态安全扫描并保持新增改动未引入 secrets / eval / shell 注入模式。当前剩余诚实差距：auth/access 虽已更轻，但仍略压在 hero 底部，下一轮更适合继续收敛 hero→Best Cases 的纵向过渡，而不是重新放大 auth 模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、fresh-load live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的顺序对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero 上半段文案/主视觉仍略拥挤。基于 live/local browser QA、rebuild 后 local 几何与当前 diff 事实，本轮仅在 `src/components/landing-page.tsx` 小步放松首屏上半段：收紧 headline stack 的 `space-y`、把 `landing-hero` 的 `lg:-mt` 从 `-1.94rem` 回调到 `-1.68rem`，并将 feature network 容器的 `lg/xl` 负上边距从 `-2.46/-2.72rem` 回调到 `-2.18/-2.38rem`、同时略放宽 `feature-rotator` 的 `lg/xl` gap 到 `0.06/0.1rem`，以减少左侧 radial cluster 与主文案的竞争感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 focused class 合同。Review 1：fresh local browser vision 明确确认 upper hero 已较上一轮更 clean/less crowded，当前最明显剩余 fidelity hotspot 诚实收敛为左侧 radial cluster 标签/节点仍略有干扰。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过，容器继续仅发布到 `3001`。Review 3：`git diff --check` 与 diff-only 静态安全扫描 clean，repo-scoped 独立 reviewer JSON 复核 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 left radial cluster 的 residual anchoring/readability 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero 下半段 auth/access module 相对 Best Cases 仍略偏低、过渡仍稍紧。基于 live/local browser QA、rebuild 后 local DOM 几何与当前 diff 事实，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[1.18rem]/lg:-mt-[7.04rem]/xl:-mt-[7.34rem]` 上提到 `-mt-[1.42rem]/lg:-mt-[8.22rem]/xl:-mt-[8.52rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同；fresh local browser vision 明确确认 auth/access module 与 Best Cases 之间不再像上一轮那样发紧，且无 clipping/absurd spacing。Review 1：live/local fresh-load 对照确认当前剩余首要热点仍是 auth/access module 相对 live 稍低，但页面首屏承接已更自然。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 diff-only 静态安全扫描 `NO_STATIC_SECURITY_FINDINGS`、`git diff --check` 与 repo-scoped 独立 reviewer JSON 复核 `passed=true` 全部通过，未发现新增 security/logic 问题；下一轮继续优先围绕 auth/access module 的 residual low-placement gap，再回到 left radial cluster 的次级锚定差距推进。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、fresh-load live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的顺序对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero 下半段 auth/access module 仍略偏低、与 Best Cases 顶部过渡略显拥挤。基于 local browser QA、DOM 几何（本轮调整前 `gapAuthToBest≈32.95px`）与 live/local 对照事实，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[0.94rem]/lg:-mt-[5.94rem]/xl:-mt-[6.22rem]` 小步上提到 `-mt-[1.02rem]/lg:-mt-[6.18rem]/xl:-mt-[6.46rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser vision 继续确认 auth card 仍是首屏最明显剩余热点，但上提后更贴近 preview、未引入 clipping/overlap/blank render。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：`git diff --check` 与 added-lines 静态安全扫描 `STATIC_SCAN_OK` 通过，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth/access module 与 Best Cases handoff 的残余 crowding、再回到 left radial cluster 的次级锚定差距推进。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero 下半段 auth stack 与 Best Cases 起始区之间仍略显拥挤，而非 auth-card 自身 detached。基于 local browser QA、live/local 对照与本地 DOM 几何（调整前 `gapAuthToBestSection≈26px`、`bestHeadingGapFromAuth≈26px`），本轮仅在 `src/components/landing-page.tsx` 将 `BestCases` 顶部外边距从 `mt-[1.24rem]/sm:mt-[1.64rem]` 放宽到 `mt-[1.72rem]/sm:mt-[2.06rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：rebuild 后 local browser QA 明确确认 hero→Best Cases handoff 更干净、无 clipping/overlap/结构破损，但 auth-card 对 Best Cases heading 仍有轻微 crowding，remaining gap 已诚实保留。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，容器继续仅发布到 `3001`。Review 3：当前 diff 仅涉及 landing spacing 与 focused tests；`git diff --check` 与 controller 侧 diff-only 静态安全扫描均通过，下一轮继续优先围绕 auth-card 与 Best Cases heading 的 residual crowding 及 left radial cluster 的细部保真做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero→Best Cases 的首屏垂直过渡仍略显拥挤/过密，而不再是 auth-card 自身破损。基于 live/local browser QA、fresh rebuild 与本地 DOM 几何（改动前 `gapAuthToBest≈30.39px`、`gapPreviewToBest≈260.94px`），本轮仅在 `src/components/landing-page.tsx` 小步上提 `landing-hero`（`lg:-mt-[1.78rem] -> -mt-[1.94rem]`）并同步收紧 `BestCases` 顶部外边距（`mt-[1.48rem]/sm:mt-[1.9rem] -> mt-[1.24rem]/sm:mt-[1.64rem]`），同时更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1：fresh local browser QA 明确确认 hero→Best Cases 过渡较上一轮更连贯、Best Cases 首屏承接更自然，页面无 clipping/overlap/broken layout；新的首要剩余热点已诚实转为下半段 hero stack（auth-card 与 Best Cases 起始区）仍稍显拥挤，而不是 auth-card 自身单独 detached。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 复核全部通过，容器继续仅发布到 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题，仅留下“focused tests 仍偏 Tailwind spacing 合同、后续可结合更高层视觉回归”的非阻塞建议。下一轮继续优先围绕 hero 下半段拥挤感与 left radial cluster 的 residual fidelity gap 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 对照，并按 systematic-debugging 将当前最小安全收敛切片锁定为 right auth/access card 仍是首屏最明显的剩余视觉热点。本轮仅在 `src/components/landing-page.tsx` 小步收紧 auth support rail：将 `feature-auth-wrapper` 从 `344/356 -> 340/352`、`lg/xl 600/616 -> 592/608` 并继续轻微上提 `lg/xl -mt`，同时把 `landing-auth-card` 与 inner shell 从 `268/300 -> 262/294`、`lg/xl 588/604 -> 580/596`，并进一步减轻 card 边框、渐变、阴影与 blur；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新 class 合同。Review 1：fresh local browser QA 明确确认页面无 clipping/overlap/absurd spacing，center preview 仍是主锚点，而 auth-card 较上一轮更不抢眼、也更少 detached。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过，容器继续仅发布到 `3001`。Review 3：controller 侧 added-lines 静态安全扫描 clean、`git diff --check` 通过；repo-scoped 独立 reviewer 返回了错误 JSON schema，因此按 fail-closed 规则改由 controller 侧基于 live diff + 已通过验证结果完成静态/契约复核，未发现新增 security/logic 问题。下一轮继续优先围绕 auth-card residual visual-weight gap，再回到 left radial cluster 的剩余 fidelity 差距做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，再按 systematic-debugging 复现当前单一安全收敛切片为 center preview 仍相对 live 略偏 washed-out / soft。基于 live/local browser vision、fresh rebuild、focused diff 与 DOM 几何事实，本轮仅在 `src/components/landing-page.tsx` 收紧 preview card 的外层边框/渐变/阴影并同步增强内层 warm workspace 与右侧 chart panel 的边界清晰度（未改动路由、端口、auth-card 布局或 `/root/nexus-mail` 任何内容），`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview class 合同。Review 1：fresh local browser QA 明确确认中心 preview 已较上一轮更 crisp、less washed out，当前剩余首要热点诚实收敛为 preview 下半部与重叠 auth-card 一带仍略 milky，以及 left radial cluster 次级可读性问题；页面无 clipping/overlap/结构破损。Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过，容器继续仅发布到 `3001`。Review 3：`git diff --check` 与 added-lines 静态安全扫描均通过，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题，仅留下“后续可补更多 inner preview visual-contract 测试”的非阻塞建议；下一轮继续优先围绕 preview 下半部 glow/overlap 与 left radial cluster residual fidelity gap 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、local-only 参考核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前最小安全收敛切片继续锁定为 hero 中 preview 下方 auth/access card 仍相对 live 略偏低、略 detached。基于 live/local browser vision、local DOM 几何与 focused diff 事实，本轮仅在 `src/components/landing-page.tsx` 继续上提 `feature-auth-wrapper`（`-mt-[0.34rem]/lg:-mt-[4.22rem]/xl:-mt-[4.42rem] -> -mt-[0.82rem]/lg:-mt-[5.58rem]/xl:-mt-[5.88rem]`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同；当前 local DOM 复核显示 auth wrapper 与 preview 的重叠已从 earlier `~53px` 收敛到更强锚定，但 browser QA 仍诚实指出 auth card 依旧是首屏最大剩余 hotspot，因此本轮 checkpoint 定位为“继续收紧低位/分离感”而非宣称 auth-card 已完全对齐。Review 1：live/local fresh-load 与 browser QA 一致确认本轮后 auth card 仍是单一最明显剩余问题，但页面无 clipping/overlap/结构破损，hero→preview→auth 的层级关系较上一轮更贴近 live。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 复核全部通过，容器继续仅发布到 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上对当前 diff 返回 `passed=true`，未发现新增 security/logic 问题；下一轮继续优先围绕 auth-card 的 residual anchoring/composition gap 做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live/local fresh-load、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web`、HTTP 200、静态安全扫描与 repo-scoped 独立 reviewer 复核，再按 systematic-debugging 将当前最小安全收敛切片锁定为首屏 auth/access module 仍较 live 略偏低、略 detached。基于 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与本地几何（live access input top≈`956px` / local≈`1170px`），本轮仅在 `src/components/landing-page.tsx` 小步上提 hero visual group 与 stacked auth wrapper（`landing-hero lg:-mt-[1.28rem] -> -[1.42rem]`、`feature-auth-wrapper -mt[-0.18rem]/lg:-mt[-3.7rem]/xl:-mt[-3.88rem] -> -mt[-0.22rem]/lg:-mt[-3.92rem]/xl:-mt[-4.1rem]`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：live/local sequential browser QA 确认页面无 clipping/overlap，auth-card 仍是首屏剩余主热点，但已更贴近 hero、问题主要收敛为轻微低位/独立感而非宽度错误。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过。Review 3：当前 diff 仅为局部 spacing/test 合同收敛；controller 静态扫描未见 hardcoded secret / shell injection / eval / pickle / SQL injection，repo-scoped 独立 reviewer 返回 passed=true，未发现新增安全或逻辑问题。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 对照，并按 systematic-debugging 将当前最小安全收敛切片锁定为 right auth/access card 相对中心 preview 仍略偏低、略 detached。基于 live/local 结构快照与本地 DOM 几何（改动前 access copy top≈`1154px`、Best Cases top≈`1423px`，中间垂直空带约 `249px`），本轮仅在 `src/components/landing-page.tsx` 将 preview 下方 auth support rail 继续上提为 `-mt-[0.32rem] / lg:-mt-[4.18rem] / xl:-mt-[4.36rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同；rebuild 后 local 几何复核显示 access copy top≈`1127px`、Best Cases top≈`1396px`，在继续保持无 clipping/overlap 的前提下把 preview→auth→Best Cases 的纵向节奏收紧约 `27px`。Review 1：live/local fresh-load、snapshot 与 DOM 几何共同确认本轮单一切片已按预期减少 auth-card detached/下坠读感，且 left radial cluster、center preview、utility row 未恶化。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/`、`/chat` HTTP 200 校验全部通过，容器继续仅发布到 `3001`。Review 3：`git diff --check` 通过、added-lines 静态安全扫描 0 命中，下一步将继续优先围绕 auth/access rail 与 preview 的残余贴合度、再回到 left radial cluster 的细部保真收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 对照，并按 systematic-debugging 将当前最小安全收敛切片锁定为 left radial cluster 标签/连线仍比 live 略 detached，而底部 micro-label row 次之。本轮仅在 `src/components/landing-page.tsx` 小步将三枚 radial nodes 向中心 preview 回收、缩短 connectors、放宽并微调 labels，同时轻微放松 `feature-preview-chart-label-row` 的顶部/横向呼吸；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新几何与 micro-label focused 合同。Review 1：live/local fresh-load 对照与本地 DOM 几何确认 utility row、center preview 与 auth-card 未恶化，当前主要剩余热点仍是 radial cluster；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`；Review 3：`git diff --check` 通过、added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing 左侧 radial cluster 的 residual anchoring/readability 热点推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live/local fresh-load、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web`、HTTP 200 与 browser QA 前置核验，再按 systematic-debugging 将当前最小安全收敛切片锁定为 hero 顶部文案与下方主视觉之间仍略偏紧。基于 live/local 对照与本地几何事实，本轮仅将 `src/components/landing-page.tsx` 的 `landing-hero` 在 `lg` 断点负上边距从 `-mt-[2.12rem]` 收敛为 `-mt-[1.56rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同；Review 1：fresh local browser QA 确认首屏无 clipping/overlap/absurd spacing，hero 较上一轮略更透气；Review 2：`pnpm test -- src/app/page.test.tsx src/app/chat/page.test.tsx`（实际 4 files / 21 tests）通过，`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 通过；Review 3：`git diff --check`、diff 静态扫描与独立 reviewer 均未发现新增安全/逻辑问题。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/port、`.git/info/exclude`、`docker compose ps`、live/local fresh-load、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 前置核验，再按 systematic-debugging 将当前最小安全收敛切片锁定为 center preview 仍相对 live 略偏亮偏软。基于 live/local browser vision 与本地运行时样式事实，本轮仅在 `src/components/landing-page.tsx` 小步收紧 `feature-preview-card` 的边框、渐变与阴影强度，使主预览更 crisp、同时保持无 clipping/overlap；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新该 preview class 合同。Review 1：fresh local browser QA 确认 center preview 比上一轮更不发白发软，且页面无明显错位或遮挡；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 全部通过；Review 3：新增 diff 仅涉及 preview 视觉 tokens 与对应测试常量，`git diff --check` 通过，静态扫描未见 secrets/eval/exec/shell 注入类问题，且未引入新的路由/权限/外部调用面。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 核验，并按 systematic-debugging 将当前单一最安全收敛切片锁定为 center preview / auth stack 仍偏 soft、边界深度略弱。基于 live/local browser vision、fresh snapshot、DOM/HTTP/build 事实与当前 diff，本轮仅在 `src/components/landing-page.tsx` 收紧中心 preview 与 auth-card 的清晰度：将 preview 外壳收敛为 `border-[rgba(222,228,236,0.98)]`、`bg-[linear-gradient(180deg,#fffefd_0%,#f4f7fb_100%)]`、`shadow-[0_18px_38px_rgba(15,23,42,0.088)]`，同步把 inner workspace 渐变从 `#fff2e7` 收至 `#fff0e3`、右侧 chart panel 提升到 `border-[#e1d3c6] bg-white/98 shadow-[0_12px_24px_rgba(15,23,42,0.05)...]`，并把 `landing-auth-card` 提升到 `border-[rgba(236,241,246,0.98)]`、`bg-[linear-gradient(180deg,rgba(255,255,255,0.998)_0%,rgba(251,246,240,0.92)_100%)]`、`shadow-[0_4px_10px_rgba(15,23,42,0.018),0_2px_5px_rgba(241,180,135,0.006)]`、`backdrop-blur-[0.5px]`，让中心 preview/auth stack 比上一轮更 crisp、layered，而不改动 radial cluster、hero 结构、Best Cases、help routes 或 `/chat` 共用 shell；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview/auth-card class 合同。Review 1：fresh live/local 对照与 rebuild 后 local browser vision 一致确认本轮后 center preview/auth stack 更 crisp、无 clipping/overlap/absurd spacing；当前单一最明显剩余热点已诚实回到 left radial cluster 仍略 detached。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 复核全部通过，容器继续仅发布 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 left radial cluster 的 residual detached / anti-crowding 差距做更小步 fidelity 收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 核验，并按 systematic-debugging 将当前单一最安全收敛切片锁定为 auth/access card 仍是首屏最大的 residual fidelity hotspot。基于 live/local browser vision、fresh snapshot、DOM 几何与当前 diff 事实，本轮仅在 `src/components/landing-page.tsx` 上提 preview 下方 auth support rail：将 auth wrapper 从 `-mt-[0.12rem] / lg:-mt-[1.94rem] / xl:-mt-[2.08rem]` 收敛到 `-mt-[0.18rem] / lg:-mt-[2.2rem] / xl:-mt-[2.34rem]`，让 access card 更贴近主 preview 下沿，同时仅同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，不改动 radial cluster、preview 主体、Best Cases、help routes 或 `/chat` 共用 shell。Review 1：fresh live/local 对照与 rebuild 后 local browser vision 一致确认 auth card 仍是当前单一最明显 hero 热点；本轮后其与 preview 的锚定略有改善，且无 clipping/overlap/明显破碎，remaining gap 仍诚实保留为 auth card 继续偏抢眼。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 复核全部通过，容器继续仅发布 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 0 命中，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题，仅留下“测试仍偏 Tailwind class 精确断言”的非阻塞建议。下一轮继续优先围绕 auth-card 的 residual visual-weight / anchoring gap 做更小步 fidelity 收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 核验，并按 systematic-debugging 先把当前单一安全收敛切片锁定为 left radial cluster 仍是首屏最大的 residual fidelity gap。基于 live/local browser vision、fresh snapshot、DOM 几何与当前 diff 事实，本轮仅在 `src/components/landing-page.tsx` 收敛 radial cluster 的节点尺寸、连接线与标签锚定：将 `Literature Analysis / Data Mining / Outcome Present` 三枚 side nodes 收敛到更接近 live 的小尺寸 pale atoms（active `4.74rem`、inactive `4.58rem`）、同步调整三条 connectors 到 `11.9% / 11.7% / 10.95%`，并把三枚 labels 统一放宽到 `max-w-[112px]` 后重新锚定为 `left-[-2.18rem] top-[0.16rem]`、`top-[calc(100%+0.08rem)]`、`left-[3.1rem] top-[0.14rem]`，让左侧 cluster 相对中心 preview 更 anchored、less cramped，同时保持 auth-card、preview、Best Cases、help routes 与 `/chat` 共享 shell 不变；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 radial node/connector/label class 合同。Review 1：fresh rebuild 后 local browser vision 明确确认 left radial cluster 已较上一轮更 coherent / less detached，但首屏剩余最大 mismatch 仍诚实保留为 left cluster 仍比 center preview 与 auth-card 更轻、更像 schematic annotation；DOM 几何也确认三枚标签已更贴近各自节点且无 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 复核全部通过，容器继续仅发布 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 0 命中，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 left radial cluster 的剩余 visual-weight/container gap 与 auth-card 的 residual detached feel 做更小步 fidelity 收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 核验，再按 systematic-debugging 将当前单一安全收敛切片锁定为 center preview 仍略偏亮、偏软。基于 live/local browser QA、fresh snapshot、local DOM 几何与当前 diff 事实，本轮仅在 `src/components/landing-page.tsx` 收敛中心 preview 的亮度与内部 panel 对比：将外层 preview card 边框/渐变/阴影改为更 calm 的 `border-[rgba(234,238,243,0.94)]`、`bg-[linear-gradient(180deg,#fffefd_0%,#f6f8fb_100%)]`、`shadow-[0_16px_34px_rgba(15,23,42,0.072)]`，并同步轻微提高 inner workspace、左侧内容卡、子卡片与右侧 chart panel 的分离度，避免继续扩张 auth-card 或 radial-cluster 结构；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview class 合同。Review 1：live/browser vision 与 rebuild 后 local browser vision 一致确认本轮后 center preview 已更 calm / less glowy、页面无明显 clipping/overlap，当前主剩余热点已诚实回到重叠式 auth-card 仍较抢眼。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 复核全部通过，容器继续仅发布 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 0 命中，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 auth-card 与中心 preview 的 residual visual competition 做更小步 fidelity 收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live/local fresh-load、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 前置核验，再按 systematic-debugging 将当前最小安全收敛切片锁定为 right auth/access card 相对 center preview 仍略偏低、略 detached。基于 live/local 结构与本地 DOM 几何（改动前 auth card top≈`1103px`，preview bottom≈`1086px`，support rail 与 preview 之间仍有约 `17px` 纵向下坠），本轮仅在 `src/components/landing-page.tsx` 将 preview 下方 auth wrapper 于 `lg/xl` 断点上提到 `-mt-[1.68rem] / -mt-[1.82rem]`，让 access rail 更贴近 live 的“嵌入 preview 下沿”读感而不改动 radial cluster、preview 主体、help 路由或 `/chat` 共享 shell；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth wrapper class 合同。Review 1：fresh local rebuild 后 DOM 几何确认 auth card top 已从约 `1103px` 上提到 `1074px`、与 preview bottom 的垂直差收敛到约 `-12px`，local fresh-load 结构正常且无 clipping/overlap；browser 视觉工具本轮仍有退化噪声，但 fresh snapshot + DOM/HTTP 与构建事实一致支持该收敛切片。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl http://127.0.0.1:3001/` 的 HTTP 200 校验全部通过，容器继续仅发布到 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 0 命中，当前 diff 仅涉及 landing 布局与对应 focused tests；下一轮继续优先围绕 auth-card 与 center preview 的 residual detached/scale gap 做更小步 fidelity 收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load/HTTP 200 前置核验，并按 systematic-debugging 将当前单一安全收敛切片继续锁定为 right auth/access card 相对 live 仍略窄、略 detached。基于 live/local 结构对照、浏览器快照与 local DOM 几何，本轮仅在 `src/components/landing-page.tsx` 小步放宽并轻微下置 auth support rail：将 auth wrapper 从 `max-w-[316/328px]` 放宽到 `332/344px`、`lg/xl max-w` 放宽到 `652/668px`、负上边距从 `-mt-[0.16rem]` 收到 `-mt-[0.12rem]`、`xl mt` 从 `0.04rem` 收到 `0.02rem`，同时将 `landing-auth-card` 与 inner shell 从 `244/276px` 放宽到 `256/288px`，以及 `lg/xl` 宽度放宽到 `448/462px`，让 access panel 更接近 live 的 support-rail 比例且继续保持 preview 下方嵌入结构；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth wrapper/card/inner-shell class 合同。Review 1：fresh live/local 对照与 rebuild 后 local DOM 几何确认 auth-card 外宽已提升到约 `462px`、inner shell 约 `448.5px`，相对上一轮更不压缩，且无 clipping/overlap；当前剩余最大 fidelity gap 仍诚实保留为 auth-card 相对 live 仍可继续放宽与贴合。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl http://127.0.0.1:3001/` 的 HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：controller 侧 diff-only 静态安全扫描 0 命中，`git diff --check` 通过，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 auth-card 与中心 preview 的 residual detached/scale gap 做更小步 fidelity 收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live/local fresh-load、focused/full Vitest、lint、build、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 前置核验，再按 systematic-debugging 将当前最小安全收敛切片锁定为 right auth/access card 相对 live 仍略窄、略 detached。基于 live/local DOM 几何对照（live access rail 约 `680px` 宽，本地上一轮 auth-card 约 `420px` 外宽）与 fresh-load 结构复核，本轮仅在 `src/components/landing-page.tsx` 小步放宽并下置 auth support rail：将 auth wrapper 从 `max-w-[304/316px]` 放宽到 `316/328px`、`lg/xl max-w` 放宽到 `628/644px`、轻收负上边距到 `-mt-[0.16rem]` 并把 `lg/xl mt` 收到 `0 / 0.04rem`，同时将 `landing-auth-card` 与其 inner shell 从 `236/268px` 放宽到 `244/276px`，以及 `lg/xl` 宽度放宽到 `424/438px`，让 access panel 更接近 live 的 support-rail 比例且继续保持 preview 下方嵌入结构；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth wrapper/card/inner-shell class 合同。Review 1：fresh live/local 对照与 rebuild 后 local DOM 几何确认 auth-card 外宽已提升到约 `438px`、inner shell 约 `424.5px`，相对上一轮更不压缩，且无 clipping/overlap；当前剩余最大 fidelity gap 仍诚实保留为 auth-card 相对 live 仍可继续放宽与贴合。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl http://127.0.0.1:3001/` 的 HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 0 命中，repo-scoped 独立 reviewer 在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 auth-card 与中心 preview 的 residual detached/scale gap 做更小步 fidelity 收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树/服务/端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live/local fresh-load、DOM 几何与 HTTP 200 前置核验，再按 systematic-debugging 将当前单一安全收敛切片锁定为 right auth card 仍略 detached。基于 live/local 对照、controller 几何与本地截图分析，本轮仅在 `src/components/landing-page.tsx` 将 auth wrapper 上移并微放宽（`mt-[0.32rem] -> 0.08rem`、`lg:mt-[0.42rem] -> 0.16rem`、`max-w-[260px] -> 272px`、`sm:max-w-[272px] -> 284px`），让 access panel 更贴近中心 preview 而不改动路由、端口、radial cluster、preview 主体或 `/help` 结构；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新 wrapper 合同。Review 1：fresh local 结构/DOM/几何确认 auth wrapper 已更贴近 preview（auth wrapper 顶部约 `1105.61px`，较上一轮继续上提），页面无 clipping/overlap，当前剩余最大热点仍诚实保留为 auth-card 整体仍可继续更高保真贴合。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、controller 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 auth-card 与 center preview 的 residual detached/composition gap 做更小步 fidelity 收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live/local fresh-load 对照，再按 systematic-debugging 将当前单一安全收敛切片聚焦到中心 preview 底部 micro-label row，基于本地 DOM 几何确认标签行与柱体间距仍略 detached 后，仅在 `src/components/landing-page.tsx` 收紧 `feature-preview-chart-label-row` 的 `mt/gap/pt`、单项纵向 gap 与标签最小高度/最大宽度，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`。Review 1：fresh local 结构与 DOM 事实确认三枚 `方法论 / 结论 / 数据` 标签已更贴近柱体、无新增重叠或裁切；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl http://127.0.0.1:3001/` 均通过；Review 3：`git diff` 静态扫描与独立 reviewer 均未发现新增 secret / 注入 / 契约回退问题。当前 Phase B 仍未完成，后续继续围绕 live/local hero 剩余 fidelity gap 做小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 核验，并按 systematic-debugging 先锁定单一热点。live/local browser QA 一致表明当前首要剩余 fidelity gap 仍是 left radial cluster，因此本轮仅在 `src/components/landing-page.tsx` 小步把三枚 radial nodes 与标签向中心 preview 回收并减轻拥挤：`Literature Analysis` 改为 `left-[48.4%] top-[15.3%] + max-w-[104px] + left-[-1.84rem] top-[0.28rem]`，`Data Mining` 改为 `left-[48.25%] top-[46.9%] + max-w-[104px] + top-[calc(100%+0.02rem)]`，`Outcome Present` 改为 `right-[16.2%] top-[14.0%] + left-[2.46rem]`，并同步把三条 connector 收短到 `10.8% / 10.6% / 10.7%`，使 radial cluster 更贴近中心 preview、减少 detached/crowded 读感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 class 合同。Review 1：fresh live/local 对照与 rebuild 后 local browser vision 均确认 left radial cluster 仍是主剩余热点，但本轮后其相对中心 preview 更 cohesive，未见新的 clipping/overlap/broken layout；已诚实记录 residual gap 仍在 radial cluster。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，容器继续仅发布 `0.0.0.0:3001->3000`；HTTP reachability 以 200 shell 校验通过，且未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题，仅留下“后续可补更高层布局断言”的非阻塞建议。下一轮继续优先围绕 left radial cluster 与正文/中心 preview 的 residual overlap/anchoring 差距做更小步收敛，而不是扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，并按 systematic-debugging 先复现当前最小且安全可落地的单一热点。live/local 结构、browser snapshot 与本地 DOM 几何共同表明：left radial cluster 仍是 residual fidelity gap，但本轮更适合先收敛右侧 auth-card 相对中心 preview 的轻微 detached/偏高读感；因此仅在 `src/components/landing-page.tsx` 小步调整 `FeaturePreview` 的大屏双列关系与 auth support rail：将 preview/auth 第二列从 `224px` 微放宽到 `228px`、同步把 `lg/xl gap-x` 从 `0.04/0.08rem` 收至 `0.02/0.06rem`，并把 auth wrapper 进一步左推且轻微下压（`lg:-ml-[0.12rem]`、`lg:mt-[0.32rem]`、`xl:-ml-[0.08rem]`、`xl:mt-[0.4rem]`），同时将 `landing-auth-card` 最大宽度从 `214px` 微放宽到 `218px`，让 access/onboard panel 更像被中心 preview 吸附的次级 support rail，而不重开 radial cluster、preview card、help routes 或 `/chat` 共享 landing shell 的其他合同。Review 1：fresh live/local 对照先确认当前最明显的安全微热点是 auth-card 的 residual detached feel；rebuild 后 local DOM 几何显示 preview→auth seam 进一步压紧（preview `587.14px`、auth `218px`、auth wrapper `227.19px`），browser snapshot 结构正常、无 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过；随后 `curl` 复核 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态扫描 0 命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题，仅留下“后续可逐步减少纯 Tailwind token 级测试断言”的非阻塞建议。下一轮继续优先围绕 left radial cluster 的 residual anchoring/anti-crowding 做更小步 fidelity 收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 对照，并按 systematic-debugging 先复现当前最明显的 left radial cluster 热点。root-cause 证据显示 `Outcome Present` 标签此前因 `max-w-[104px] + normal white-space` 发生两行折断，成为当前最显眼的本地首屏破绽；因此本轮仅在 `src/components/landing-page.tsx` 将该节点右移并上提、放宽 connector，并把标签改为单行 `whitespace-nowrap` 的垂直居中定位，同时同步 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：fresh local browser QA 确认 `Outcome Present` 已恢复单行、无明显重叠，且与中心 preview 的关系较前一轮略更 coherent；Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过；Review 3：`git diff --check` 通过、controller 静态安全扫描未见 secrets/eval/shell 注入/SQL 注入新增项，独立 reviewer 也确认 diff 仅引入非阻塞建议（可后续把 connector 断言再收紧为更唯一的选择器）。后续仍需继续收敛 left radial cluster 的整体凝聚度与其余标签的换行/装饰噪声，但本轮已完成一个真实且可验证的单点修复切片。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 systematic-debugging 先复现当前最大剩余热点。fresh local browser QA 继续指出左侧 radial cluster 虽已有改善，但 `Outcome Present` 一侧仍略 detached、`Literature Analysis / Data Mining` 标签仍稍显紧，因此本轮仅在 `src/components/landing-page.tsx` 对三枚 radial labels/connectors 做更小步锚定收敛：将上方与中部节点各向中心小步内收并略放宽连接线（`left-[47.85%] / w-[11.9%]`、`left-[47.8%] / w-[11.7%]`），把右侧节点收至 `right-[19.05%] / w-[10.95%]`，同步把三枚标签统一放宽到 `max-w-[112px]`，并将 `Literature Analysis`、`Data Mining`、`Outcome Present` 的标签位置分别收至 `left-[-2.18rem] top-[0.16rem]`、`top-[calc(100%+0.08rem)]`、`left-[3.1rem] top-[0.14rem]`；同时更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，继续保持 hero 结构、preview micro-label row、auth-card、Best Cases 与 help routes 不变。Review 1：fresh local browser vision 明确认为左侧 radial cluster 已比上一轮更 anchored、less cramped，未见新的 clipping/overlap，当前仍有轻微 residual detachment，但右侧 auth-card 已重新成为更大的剩余视觉问题。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全部通过，且继续仅发布到 `3001`。Review 3：`git diff --check` 与 controller 侧 diff-only 静态安全扫描均通过、0 风险命中，repo-scoped 独立 reviewer 已返回 `passed=true`，未发现新增 security/logic 问题，仅留下“focused tests 仍偏精确 Tailwind 断言、后续可逐步提升到更高层布局合同”的非阻塞建议。下一轮继续优先围绕 auth-card 的 residual detached feel 与 radial cluster 的最后小步 label/connectors 保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 systematic-debugging 先复现当前最明显剩余热点。live/local browser QA 与 DOM 几何共同指向 left radial cluster 仍是单一最大 fidelity gap，因此本轮仅在 `src/components/landing-page.tsx` 小步收敛三枚 radial labels 的锚定：将 `Literature Analysis` 标签轻微右移并下压（`left-[-2.5rem]`、`top-[0.32rem]`）、将 `Data Mining` 标签上提贴近节点（`top-[calc(100%+0.22rem)]`）、并把 `Outcome Present` 标签轻微右移并下压（`left-[3.32rem]`、`top-[0.32rem]`），同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，让三枚标签较上一轮更贴近各自圆点与中心 preview，而不改动 hero 结构、auth-card、Best Cases、help routes 或 `/chat` 共享 landing shell。Review 1：fresh local browser vision 证实三枚标签都按预期方向收敛，未见新的 clipping/overlap，left radial cluster 虽仍是当前主要剩余热点，但已比上一轮更 anchored；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全部通过；Review 3：`git diff --check` 与 controller 侧 diff-only 静态扫描均通过、0 风险命中，repo-scoped 独立 reviewer 已返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 left radial cluster 的 residual detachment / cramped wrapping 做更小步保真收敛，而不是扩张新模块。
- [~] 执行检查点：本轮仅在 `/root/sciclaw` 内继续收敛 landing hero 左侧 radial labels，保持 `/chat` 与 `/` 共用 marketing landing shell、继续使用 `3001`，未触碰 `/root/nexus-mail` 工作树、服务或端口；基于 live/local fresh-load、browser QA 与 DOM 几何，将 `src/components/landing-page.tsx` 三枚 radial labels 的宽度与锚点小步收敛为更贴近节点的布局（literature/outcome `max-w-[108px]`、data `max-w-[106px]`，并同步收近左右 label 的 `top/left` 与中间 label 的 `top` 间距），以降低 detached/crowded 读感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 class 合同。Review 1：browser QA 确认 left radial cluster 已从最明显热点降级为次要问题，当前更明显热点转向 center/right composition。Review 2：focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 全部通过。Review 3：static scan clean，独立 reviewer 对当前 diff 给出通过结论。下一步继续优先收敛 hero center/right composition 的拥挤感，同时保持 radial cluster 当前改进。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树或现有服务/端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live/local fresh-load、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 前置核验，再按 systematic-debugging 顺序把当前最安全的单一收敛切片聚焦到 hero 纵向凝聚度。基于 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 browser vision + DOM 几何共同证据，确认当前更值得优先收敛的是 headline/supporting copy 与下方 preview/auth cluster 之间仍略显 airy、hero visual group 仍稍 detached；因此本轮仅在 `src/components/landing-page.tsx` 小步压紧首屏纵向节奏：将标题栈 `space-y` 从 `6/7` 收至 `5/6`、标题副文案间距从 `0.92rem` 收至 `0.82rem`、将 `landing-hero` 上提到 `lg:-mt-[2.38rem]`，并同步收紧 `FeaturePreview` 的纵向 `gap-y` 与 heading block 内部 `space-y/pt`，让 headline、center preview 与右侧 auth-card 在不改动信息架构和交互的前提下更像同一 cohesive hero composition；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/preview spacing 合同。Review 1：fresh local browser vision 认为首屏已较上一轮更 cohesive，headline→hero gap 已显著压缩，controller 几何显示 hero top 从约 `281px` 收至 `270px`、preview/auth 同步上提且 `authVsPreviewTopDelta` 仍稳定在约 `7px`，说明本轮命中了真实 composition 热点而未破坏既有对位；当前剩余最明显差距已诚实收敛为 left radial cluster 仍略 detached。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，随后轮询 `http://127.0.0.1:3001/` 返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 与 controller 侧 diff-only 静态扫描均通过、0 风险命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上仅针对当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 left radial cluster 的 anchoring / anti-detached 视觉保真做更小步收敛，而不是扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再按 systematic-debugging 顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何。root-cause 证据表明当前单一最明显剩余热点仍是 left radial cluster：local browser vision 继续认为三枚节点标签相对圆点与中心 preview 仍偏 detached，而 `browser_console` 几何进一步显示三枚 radial wrappers 仍继承整列宽度（约 `334px`），导致 `Outcome Present` wrapper 左缘仍落到负坐标 `x≈-2px`、cluster 继续带着漂浮感。本轮因此仅在 `src/components/landing-page.tsx` 把 radial node 外层 wrapper 从 `flex` 收敛为 `inline-flex`，让每枚节点/标签 wrapper 改为按内容收缩宽度，直接修正 detached/floating 的结构根因；并同步在 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 补强 `inline-flex` focused contract，保持 hero 主体、auth-card、Best Cases、help 路由、`/chat` 共享 landing shell 与 `3001` 发布边界不变。Review 1：fresh live/local sequential browser QA 与 rebuild 后 local browser vision 一致确认 left radial cluster 仍是当前单一最明显热点，而本轮改动后 cluster 读感略有收紧、但仍诚实保留“标签仍略 detached/floating”的后续差距；说明本轮是针对真实 root cause 的安全收敛，而非误改 auth-card 或 preview。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/`、`/chat` 全通过，继续仅发布到 `3001`，未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中，且改动仅涉及 radial wrapper 布局与 focused tests；repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复审当前 live diff 并返回 `passed=true`，未发现新的安全或逻辑阻断。下一轮继续优先围绕 left radial cluster 的 label anchoring / anti-floating 做更小步 fidelity 收敛，而不是回到 auth-card 或扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、`3001` 端口与 live/local fresh-load 前置核验，再按 systematic-debugging 先复现当前最明显的首屏热点。基于 live `https://sciclaw.ai/` browser vision、local `http://127.0.0.1:3001/` rebuild 后 browser vision 与本地 DOM 几何共同证据，确认上一轮 auth-card detached 已下降为次要问题，而新的单一最明显剩余热点重新回到 left radial cluster 相对中心 preview 仍略 detached；因此本轮仅在 `src/components/landing-page.tsx` 小步压缩 `FeaturePreview` 的大屏第二列（`232px -> 228px`）、同步收紧 `lg/xl gap-x`、把 auth wrapper 再向 preview 轻推并小幅下压，同时将 `landing-auth-card` 从 `222px` 收至 `218px`，继续让右侧 auth-card 更像被中心 preview 吸附的次级 support panel，而不触碰 radial cluster、utility dock、Best Cases 或 `/chat` 共用 landing shell 的既有合同；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview-grid、auth wrapper 与 auth-card 宽度/位置 class 合同。Review 1：fresh live/local 对照先确认本轮前的最明显热点是 auth-card detached，rebuild 后 local DOM 几何显示 preview→auth gap 已从约 `6.59px` 收至 `5.31px`，auth-card 宽度从 `222px` 收至 `218px`，local browser vision 也确认 auth-card 已退居次要、当前主热点回到 left radial cluster，页面无 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过；随后轮询 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上仅针对当前 diff 返回 `passed=true`，未发现新增 security/logic/test-contract 问题。下一轮继续优先围绕 left radial cluster 的 anchoring / anti-crowding 做更小步 fidelity 收敛，而不是重新放大 auth-card 或扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，并按 systematic-debugging 顺序复现当前 hero 结构热点。基于 live browser vision 与 local browser vision/DOM 几何共同证据，确认当前最小安全收敛切片仍是“本地 hero 过于 balanced、右侧 auth-card 过宽且过显眼”，因此本轮仅在 `src/components/landing-page.tsx` 将 preview/auth 双列从 `228px` 微收至 `220px`、同步收紧 `gap-x`、把 auth wrapper 再向 preview 轻推并下压，同时轻收 `landing-auth-card` 的宽度/圆角/边框/阴影/blur；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新 class 合同。Review 1：rebuild 后 local browser vision 确认 hero 读感较上一轮更少 balanced、更偏 right-heavy，auth-card 更像从属 edge panel，且无新增 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 全部通过。Review 3：controller 侧 `git diff --check` 与 added-line 静态扫描均无 secrets/shell/eval/pickle/SQL 注入命中，独立 reviewer 也仅基于当前 diff 给出 passed=true；当前剩余诚实差距仍是 auth-card 与 main mockup 间距已压到 0px、hero 右侧组仍可继续微调，但本轮 safe slice 已完成并可提交。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，并按 systematic-debugging 先复现实测热点，再用 local browser vision + DOM 几何确认当前单一最明显剩余问题仍是右侧 auth-card 过窄、与中心 preview 的关系偏 detached：改动前 auth-card 仅 `194px`、wrapper `204px`，虽然 seam 已压到约 `6px`，但 card 自身仍显得 compressed。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步扩宽并贴合右侧 support panel：将 `FeaturePreview` 的大屏第二列从 `204px` 放宽到 `220px`、同步把 `lg/xl gap-x` 从 `0.06/0.14rem` 收到 `0.02/0.08rem`，并把 auth wrapper 调整为 `lg:max-w-[13.75rem]`、将 `landing-auth-card` 最大宽度从 `194px` 放宽到 `210px`，让 access panel 仍保持次级窄列身份，但不再像被挤压的 sidebar；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview-grid、auth wrapper 与 auth-card 宽度合同。Review 1：fresh local browser QA 明确确认本轮改动后右侧 auth-card 已比上一轮更不那么极端狭窄，页面无 clipping/overlap/白屏，当前剩余热点仍是 auth-card 细部 cramped 感而不是结构性错位。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过；重建后轮询 `http://127.0.0.1:3001/` 返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描 0 命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 live diff 并返回 `passed=true`，未发现新增 security/logic/test-contract 问题。下一轮继续优先围绕 auth-card 内部控件的 cramped 感与 radial cluster/preview 的整体统一度做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 继续共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，并按 systematic-debugging 先复现实测热点，再用 live/local DOM 几何对照确认当前更安全的最小收敛切片已从 radial cluster 转回右侧 auth-card：live 页对应 access panel 宽约 `680px`，而本地嵌入式 auth-card 仅 `198px`，导致虽然层级正确但仍显得过窄、偏紧。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步收敛 preview→auth seam：将 `FeaturePreview` 的大屏第二列从 `208px` 微收至 `204px`、同步把 `lg/xl gap-x` 从 `0.18/0.24rem` 收至 `0.14/0.2rem`，并把 `landing-auth-card` 的最大宽度从 `198px` 微收至 `194px`，让右侧 support panel 更像被中心 preview 吸附的次级窄列，而不重开 radial cluster 或整体 hero 布局；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview-grid 与 auth-card 宽度合同。Review 1：live/local fresh-load 结构、文本与 controller 几何复核一致确认本轮热点就是 auth-card 仍略紧且 seam 稍松；rebuild 后 local 几何显示 auth-card 已从 `198px` 收至 `194px`、preview 扩至 `607.625px`，整体仍 coherent、无 clipping/overlap，且 `/` 与 `/chat` 继续保持同一 marketing shell。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过；随后轮询 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描未见 secrets / shell injection / eval / unsafe deserialize / SQL formatting 风险；下一步继续做 repo-scoped 独立 reviewer 复核并在通过后提交推送。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，并按 systematic-debugging 先复现实测热点，再用 `browser_console` 几何确认当前真实问题不是整体 hero 溢出，而是左侧 `Outcome Present` 标签仅比 radial wrapper 左缘多出约 `2.67px`、落在 `overflow-hidden` 裁切边界外。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 对 `FeatureRotator` 左侧 network-shell wrapper 做一次最小安全修正：保持 `overflow-hidden` 与既有圆角裁切不变，仅新增 `pl-[0.18rem] / lg:pl-[0.2rem] / xl:pl-[0.24rem]` 轻推内容入框，避免上一版把整个 wrapper 改为 `overflow-visible` 带来的 bleed 风险，同时继续让 radial cluster 在 fresh-load 本地页中完整落在可视/容器边界内；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步从错误的 `overflow-visible` 断言切回 `overflow-hidden`，并补强新的左内边距 class 合同。Review 1：live/local sequential browser QA 先把 residual hotspot 重新锁定为 left radial cluster，随后 local `browser_console` 几何复核证明 `Outcome Present` 标签此前只是在 wrapper 左边界外溢极小量；重建后 fresh local `browser_vision` 明确确认左侧 radial cluster 已完整 contained、无可见 clipping，且未出现新的 overflow/breakage。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过；`http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 与 controller 侧 diff-only 静态扫描均为 0 风险命中；repo-scoped 独立 reviewer 先 fail-closed 指出“直接把 wrapper 改为 overflow-visible 会移除唯一裁切边界并可能 bleed”，controller 立即回退该路径并改为 padding-only 修正，随后第二次 repo-scoped reviewer 在 `/root/sciclaw` `main` 上复核当前 live diff 返回 `passed=true`，未发现新增 security/logic 问题。下一轮继续优先围绕 left radial cluster 与 center preview 的 residual hierarchy/spacing 做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，再结合 live/local browser QA、DOM 几何与 focused/full Vitest 结果，确认当前更安全的最小收敛切片重新落回 left radial cluster。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步把三枚 radial feature nodes 略向外分离并同步放宽 label 宽度/位置与 connector 长度：`Literature Analysis`、`Data Mining`、`Outcome Present` 分别调整为更宽的 `max-w-[96px]`、更克制但更可读的 label 前景色，并把对应连线扩到 `12.05% / 11.85% / 11.3%`，让 cluster 边缘文案比上一轮更易读；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 radial node / label / connector 的 focused 合同。Review 1：fresh local browser QA 与 DOM 几何确认三枚标签都已完整落在可视区域内，局部可读性较上一轮提升；但 browser_vision 仍把 left radial cluster 识别为首屏主要 residual hotspot，说明后续应继续围绕该 cluster 做更小步的 spacing/contrast 收敛，而非扩张新模块。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw exec vitest run`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，且 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 与 controller 侧 diff-only 静态扫描均通过、0 风险命中；当前 diff 仅涉及 `landing-page.tsx` 与首页/`/chat` focused 测试中的 radial cluster 合同更新。下一轮继续优先围绕 left radial cluster 的 residual clipping/contrast 误读与中心 preview 的相对贴合关系做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 继续共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 live/local fresh-load 前置核验，再基于 live `https://sciclaw.ai/`、local `http://127.0.0.1:3001/` 的 sequential browser QA、DOM 样式事实与 local rebuild 后 visual sanity，确认当前单一最安全的 hero fidelity 热点已从结构错位转为前景层级过淡：center preview、right auth-card 与顶部 utility controls 虽结构正确，但整体仍比 live 更像淡化占位。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步提高前景层级对比：收紧并加深 utility trigger 的 border/background/shadow，提升 settings popover 面板阴影；同时将 `feature-preview-card` 收敛为更明确的白底、稍强边界与 `0_18px_40px` 级外层阴影，并把 `landing-auth-card` 调整为更清晰的边框、近白渐变与更可感知的深度阴影/blur。`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 utility/auth-card/preview card 的 focused class 合同，并在首页 focused test 中补回 `guideTrigger/contactTrigger` 复用变量，保证 selector 收敛后测试仍诚实。Review 1：fresh local browser vision 复核确认 hero/header 已更 coherent，center preview、auth-card 与 utility controls 都获得更清晰的 foreground contrast/depth，且未引入 clipping、overlap 或明显过重/刺眼样式；live/local 对照下当前剩余差距重新收敛为后续可继续微调的细粒度视觉保真，而非结构性破损。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，重建后轮询 `http://127.0.0.1:3001/` 返回 HTTP 200，继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 与 controller 侧 diff-only 静态扫描均为 0 风险命中；repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上仅针对当前 milestone diff 返回 `passed=true`，未发现新增 security/logic 问题，仅建议未来逐步减少对纯 Tailwind token 的过细断言。下一轮继续优先围绕 live/public 首页与本地首屏在更细粒度 hero hierarchy 上的 residual polish 做小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 端口前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前最明显且可安全落地的结构性热点仍是本地首屏右侧 auth-card 相对中心 preview 偏低、偏 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `FeaturePreview` 从纯纵向堆叠改为 `lg/xl` 下“标题跨两列 + preview 左列 + auth-card 右列”的显式两列 grid，并为 auth-card wrapper / preview card 加入确定的 `lg:col-start-*` 与 `lg:row-start-*` 放置类，去掉旧的超大负上边距黑魔法，让 auth-card 在大屏下与中心 preview 顶缘重新对齐、保持贴合但不再掉到下方；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步从旧的负 margin 合同切换为新的显式 grid-placement 合同，继续保护 `/chat` 与 `/` 共用 marketing landing shell。Review 1：顺序 live/local 对照、browser vision 与 controller-side DOM 几何一致确认修复后 auth-card 顶缘与 preview 顶缘仅剩约 `7px` 级别差异，不再像先前那样整体掉到 hero 下方，且页面无 clipping/overlap/absurd spacing。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，随后轮询 `http://127.0.0.1:3001/` 返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 与 controller 侧 diff-only 静态扫描均通过；repo-scoped 独立 reviewer 先 fail-closed 指出 grid 自动放置会把 auth-card 错放到左列，controller 随即补上显式 `lg:col-start-2` / `lg:row-start-2`，并在 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 继续补强 `FeaturePreview` grid、heading `lg:col-span-2` 与 preview card `lg:col-start-1 / lg:row-start-2` 的 focused 合同断言后复测全绿，因此当前变更已回到可提交状态。下一轮继续优先围绕 live/public 首页与本地首屏在 left radial cluster → center preview 的残余层级差距做更小步收敛，而不是重新放大 auth-card 或扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 端口前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前最大的结构性偏差不是顶部 utility row 或 left radial cluster，而是本地首屏仍把 auth-card 作为右侧并列列布局展示。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `AuthCard` 从 `landing-hero` 的 `lg/xl` 并列第二列移入 `FeaturePreview` 下方，保留同一份 Onboard/Login/Apply 交互与 `handleApplyNow` 行为不变，仅把它降为 preview 说明文案之后的纵向后置次级模块；同步去除旧的 `lg/xl grid-cols` 与 auth-card 顶部对位 class，并更新 `src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 使其改为保护“hero 不再是右侧并列 auth 列布局、auth-card 仍存在且 /chat 继续与 / 共用 marketing shell”的新合同。Review 1：live/local 对照、browser QA 与 DOM 几何一致确认本轮命中的确是首屏最大结构偏差；rebuild 后 local fresh-load sanity QA 明确显示中心 preview 已重新成为首屏唯一主核，auth-card 已降为纵向后置次级模块，且无白屏、无明显 clipping/overlap、无 absurd spacing。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，随后轮询 `http://127.0.0.1:3001/` 与 `/chat` 均返回 HTTP 200，继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 与 controller 侧 diff-only 静态扫描均通过，repo-scoped 独立 reviewer 返回 `passed=true`，仅建议后续补一条“auth-card 已重新嵌入 hero 纵向节奏”的非阻塞断言。下一轮继续优先围绕 live/public 首页与本地首屏在 radial cluster→preview 关系上的残余差距做更小步收敛，而不是重新放大 auth-card 或扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 仍与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 核验，再用 live/local browser QA 锁定当前最值得收敛的单一热点为 left radial cluster 的真实左缘 clipping。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `FeatureNetwork` 根容器从 `overflow-hidden` 调整为 `overflow-visible`，并补充稳定 `data-testid="feature-network-shell"`，让 `Outcome Present` 左侧标签不再被容器左缘硬裁切；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步改为同时校验外层 network shell 的 `overflow-visible` 合同与包裹 frame 的既有 `lg/xl` 负边距合同，避免把 layout wrapper 与真实 clipping 修复混为同一断言。Review 1：fresh local browser vision 明确确认 `Outcome Present` 左缘硬 clipping 已消失，剩余问题仅为 radial cluster 的 spacing/detachment 微差距，未出现新的 broken/clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw exec vitest run`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，随后轮询 `http://127.0.0.1:3001/` 返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务或端口。Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描未发现 secrets / shell injection / eval / 反序列化 / SQL formatting 风险，repo-scoped 独立 reviewer 也返回 `passed=true`，仅给出后续可补视觉回归保护的非阻塞建议。下一轮继续优先围绕 left radial cluster 剩余 spacing/detachment 微差距做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing Phase B，保持 `/chat` 仍与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 live/local fresh-load 前置核验，再结合 live/local browser QA、DOM 样式复核与 focused/full Vitest 结果确认当前最安全的单一收敛热点仍是 hero 右侧 auth-card。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 对 auth-card 做一次不改布局与交互的视觉收敛：将卡面外壳、tab shell、tabpanel、access-code input、disabled `VERIFY ACCESS CODE` 按钮与 `Apply Now` CTA 统一调整为更贴近中心 preview 的 warm glass / cream gradient / softer shadow 语言，保留宽度、位置、文案与行为不变，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同断言。Review 1：live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` sequential QA 均指向 auth-card 仍是最明显的单一剩余热点，而本轮调整后 auth-card 无 clipping/overlap、hero 区未出现新的明显破损；browser QA 仅记录非阻塞余项为说明文案与 apply CTA 在窄卡宽下仍略显紧。Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config` 与 `docker compose up -d --build web` 全绿，随后 `curl http://127.0.0.1:3001/` 返回 HTTP 200。Review 3：controller-side added-lines static scan 未发现 secrets / shell injection / eval / 反序列化 / SQL injection 命中；独立 reviewer 也确认当前 diff 仅为 auth-card 视觉收敛与对应测试更新，未引入安全或逻辑回归。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、controller 几何与本地 visual QA，确认当前更值得优先收敛的单一剩余热点仍在中心 preview：live 首页首屏主预览更 calm / crisp，而本地 preview shell 仍稍偏亮、偏软。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步继续软化 `feature-preview-card`：将外层边界从 `border-white/88` 收至 `border-white/82`、背景从 `#ffffff→#f8fafc` 收至更轻的 `#fffefd→#f6f8fb`、外层阴影从 `0_14px_32px_rgba(15,23,42,0.048)` 收至 `0_10px_24px_rgba(15,23,42,0.038)`，并把内层 workspace 底色从 `#fffcf8→#fff5ea` 微收至 `#fffaf5→#fff1e4`，让中心 preview 保持主视觉锚点但更接近 live 的 restrained / less-glowy 读感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview-card class 合同。Review 1：rebuild 后 fresh local browser QA 明确确认中心 preview 已更 calm/crisp、未见 clipping/overlap，且当前页面最明显差距仍是 residual hero composition polish 而非功能破损。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，随后 `curl -I http://127.0.0.1:3001/` 返回 HTTP 200，继续仅发布到 `3001`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态扫描 0 命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 diff 并返回通过，未发现新增 security/logic/test-contract 问题。下一轮继续优先围绕中心 preview residual prominence 与 radial cluster / auth-card 的相对主次做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、DOM 几何与本地 visual QA，确认当前单一最明显剩余热点仍是 left radial cluster：live/public hero 的左侧功能网络仍比本地更像被中心 preview 吸附的次级结构，而本地 cluster 虽已改善但仍略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步把三枚 radial nodes 朝中心 preview inward 收拢并同步缩短 connectors：`Literature Analysis` 从 `left-[46.1%]` 收至 `left-[47.2%]`、connector 从 `w-[12.8%]` 收至 `w-[11.35%]`，`Data Mining` 从 `left-[46.45%]` 收至 `left-[47.3%]`、label 与节点间距从 `0.72rem` 收至 `0.58rem`、connector 从 `w-[12.45%]` 收至 `w-[11.1%]`，`Outcome Present` 从 `right-[15.95%]` 收至 `right-[16.85%]`、connector 从 `w-[11.6%]` 收至 `w-[10.55%]`，并同步轻收左右 label offset，让左侧功能网络更像围绕中心 preview 运作的功能桥接，而不是外摆装饰；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 radial node / connector / label class 合同。Review 1：live/local 顺序对照与 rebuild 后 local browser vision 一致确认 radial cluster 仍是首要热点，但较上一轮已更 anchored/coherent，未见新的 clipping/overlap 或功能破损。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，随后 `curl -I http://127.0.0.1:3001/` 返回 HTTP 200，继续仅发布到 `3001`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态扫描 0 命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 diff 并返回 `passed=true`，未发现新增 security/logic 问题，仅留下可补更高层视觉回归检查的非阻塞建议。下一轮继续优先围绕 left radial cluster 的 residual hierarchy/readability 与 center preview 的相对主次做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、DOM 几何与本地 visual QA，确认当前单一最明显剩余热点已从 auth-card / radial cluster 再次收敛回中心 preview：live 首页首屏的主预览仍更克制，而本地 preview card 的大面积白底、边界亮度与外层阴影仍比 live 更像强主角。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步软化 `feature-preview-card` 外壳：将圆角从 `2.12rem` 微收至 `2.08rem`、外层边界从 `border-white/92` 收至 `border-white/88`、背景从 `#ffffff→#f9fbfd` 收至更平的 `#ffffff→#f8fafc`，并把外层阴影从 `0_18px_42px_rgba(15,23,42,0.06)` 收至 `0_14px_32px_rgba(15,23,42,0.048)`，让中心 preview 继续保持主视觉锚点，但更接近 live 的 calm / less-glowy 读感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview-card class 合同。Review 1：顺序对照 live/local fresh-load 结构与 browser vision 明确确认本轮最小安全切片就是压低中心 preview 的过强存在感；rebuild 后 local browser sanity QA 未见 broken/clipping/overlap，且 browser 结论从“preview 明显过强”收敛为“仍是主角但较先前更克制”。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，随后轮询 `http://127.0.0.1:3001/` 返回 HTTP 200，继续仅发布到 `3001`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态扫描 0 命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 diff 并返回 `passed=true`，未发现新增 security/logic/test-contract 问题。下一轮继续优先围绕中心 preview residual prominence 与左侧 radial cluster / 右侧 auth-card 的相对主次做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、DOM 几何与本地 visual QA，确认上一轮中部 preview 底部中文 micro-label row 已退到次要细节，而当前更真实的单一剩余热点重新收敛为 hero 右侧 auth-card，尤其是其 warm segmented tabs / access panel 仍比 live 更容易形成局部 attention hotspot。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 继续做 auth-card 软化与轻微下沉：外壳从 `bg-[#fdfdff] / border-[#f6f8fb] / shadow-[0_1px_2px_rgba(15,23,42,0.004),0_1px_2px_rgba(241,180,135,0.0015)]` 收至更轻的 `bg-[#fefeff] / border-[#f8fafc] / shadow-[0_1px_2px_rgba(15,23,42,0.003),0_1px_2px_rgba(241,180,135,0.001)]`，并将 `lg/xl` 顶部对位从 `2.08/2.28rem` 下调到 `2.18/2.34rem`；同时把 `Onboard` 选中态从更亮更重的 `bg-[#efc095] + shadow-[0_7px_14px...]` 收敛为更柔和的 `bg-[#e8c6aa] + shadow-[0_5px_10px...]`，并同步轻化 tab shell / inner panel 边框与底色，让 auth-card 继续保留功能可辨识度，但更像中心 preview 旁的次级入口而非并列主视觉。Review 1：fresh live/local 顺序对照与 rebuild 后 local `browser_vision` 明确确认 auth-card 已从“单一最强热点”退为更次级的右侧支持面板，未见新的 clipping/overlap；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，且 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，继续仅发布到 `3001`、未影响 `/root/nexus-mail` 任意现有服务与端口；Review 3：`git diff --check` 通过、controller 侧 diff-only 静态扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 diff 并返回 `passed=true`，未发现新增 security/logic/test-contract 问题。下一轮继续优先围绕 auth-card residual hotspot 与 radial cluster / preview 之间的相对主次做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 live DOM 事实，确认当前单一最明显 hero fidelity 热点已从 auth-card / radial cluster 收敛到 center preview 底部中文 micro-label row 仍过于显眼。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-preview-chart-label-row` 做一次更小步降噪：整体轻微下压并收窄横向分布（`mt/gap/pt` 调整为 `2.98rem / 3.38rem / 2.86rem`，`sm:gap` 收至 `3.54rem`），同时把单项 label wrapper 从高耸的大号深色文案收敛为更短、更轻、更低对比的微标签（`min-h` 由 `7.32rem` 收至 `4.92rem`、字号约降到 `11.2px`、`max-w` 收至 `6.18rem`、前景改为 `#6b8191`），让 `方法论 / 结论 / 数据` 更像 preview 内部的次级 UI microcopy，而非首屏焦点。Review 1：fresh local browser vision 明确确认这些底部 micro-label 仍是 hero 最显眼但已明显 calmer 的剩余热点，未见新的 clipping/overlap；同时顺序 live DOM 几何复核显示 live 对应 chips 仅约 `19.75px` 高的低强调徽标，本地现已更接近“缩小/弱化而非抢眼正文”的方向。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过；重建后首次 HTTP 探测命中过一次 startup race `Connection reset by peer`，随后按重试策略复核 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 200，继续仅发布到 `3001`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态扫描 `STATIC_SCAN_OK`，当前改动仅涉及 `landing-page.tsx` 与首页/`/chat` focused tests 的 class/visual contract；下一轮继续优先围绕 preview micro-label row 与 left radial cluster 的 residual subtlety 做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card 纵向贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、DOM 几何与本地 visual QA，确认当前单一最明显剩余热点重新收敛到 hero 右侧：auth-card 相对中心 preview 仍略偏高、略显悬浮 detached，而左侧 radial cluster 与顶部 utility row 不再是本轮首要问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步下移 auth-card，将 `landing-auth-card` 的 `lg/xl` 顶部外边距从 `2.02/2.18rem` 微调到 `2.18/2.34rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的对应 class 合同断言。Review 1：fresh live/local 对照与 rebuild 后 local `browser_vision` 一致确认该热点已改善——auth-card 不再明显偏高、未引入 clipping/overlap，仍保留刻意的右侧层级但不再突兀悬浮；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，且 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务或端口；Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 diff 并返回 `passed=true`、未发现新增 security/logic/test-honesty 问题。下一轮继续优先围绕 auth-card 与 center preview 的 residual horizontal bonding/weight 做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card 纵向对位微收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，并顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、DOM 几何与本地 visual QA，确认当前本地首屏最直接可验证的小步问题仍是 auth-card 相对中心 preview 略偏高。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `landing-auth-card` 的 `lg/xl` 顶部外边距从 `1.92/2.1rem` 微调到 `2.08/2.28rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的对应 class 合同断言。Review 1：fresh local rebuild 后 `browser_console` 几何显示 auth-card `y` 从约 `328.63` 下移到 `331.5`、`topDelta` 约 `36.47px`，与 center preview 的组合关系略向下收敛；尽管 local `browser_vision` 仍认为 auth-card 是首屏剩余热点，但已明确其“比之前更贴近中心 preview，只是仍略偏高”，且未出现 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，且 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 均返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务或端口。Review 3：`git diff --check` 通过，当前 diff-only 静态扫描无 secrets/eval/shell 注入命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 live diff 并返回 `passed=true`，未发现新增 security/logic/test-contract 问题。下一轮应把剩余热点从 auth-card 偏高进一步转向 radial cluster 与 preview/auth seam 的相对主次，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的 left radial cluster 凝聚度收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、DOM 几何与本地 visual QA，确认当前新的单一最明显剩余热点已从 auth-card 重新转回左侧 radial cluster：cluster 相对中心 preview 仍略分散，顶部/右侧节点标签与 connector 读感偏松。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步内收三枚 radial nodes 与 connector（literature/data/outcome 的 `left/right/top` 与 connector 宽度/角度微收），同时缩短 label 最大宽度并拉近 label 偏移，让 cluster 更像围绕中心 preview 的功能网络；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 node 位置、connector 宽度与 label class 合同。Review 1：live/local 顺序对照与 rebuild 后 local `browser_vision` 一致确认 left radial cluster 较上一轮更 cohesive，页面未出现新的 broken/clipped/absurd spacing，剩余仅是 center-left 一带仍可继续微调；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 全部通过，且 `http://127.0.0.1:3001/` 返回 HTTP 200，容器继续仅发布 `0.0.0.0:3001->3000`、未影响 `/root/nexus-mail` 任意现有服务或端口；Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描 0 命中，repo-scoped 独立 reviewer 已在 `/root/sciclaw` `main` 上复核当前 diff 并返回 `passed=true`、仅给出“后续可补更高层视觉回归保护”的非阻塞建议。下一轮继续优先围绕 left radial cluster 与中心 preview 的 residual overlap/anchoring 微差距做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与本地 DOM 几何，复核得到当前单一最明显剩余热点仍在 hero 右侧：auth-card 与顶部 utility controls 相对中心 preview 仍略偏右、略偏高，导致右侧组感稍 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步收紧右侧组：将 `landing-hero` 的 `lg/xl` 右列从 `242px` 微收至 `238px`、hero/auth gap 从 `0.28/0.36rem` 收至 `0.18/0.24rem`，同时把 `landing-auth-card` 的 `lg/xl` 顶部外边距从 `1.74/1.9rem` 下移到 `2.02/2.18rem`，并把顶部 utility row 与共享 dock trigger gap / 右侧 padding 同步轻收，让右侧 controls 更靠近 preview 而不过度抢眼；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth/header spacing 合同。Review 1：fresh live/local browser 对照与 local browser vision 一致确认本轮命中的确是当前最真实剩余热点，调整后右侧 auth-card 更 inward/down、顶部 utility controls 更少漂浮感，页面无 clipping/overlap，left radial cluster 与 center preview 未被误伤。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，且容器继续仅发布 `0.0.0.0:3001->3000`，未影响 `/root/nexus-mail` 任何现有服务/端口。Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描未见 hardcoded secrets / shell injection / eval / pickle / SQL formatting 风险；后续再做 repo-scoped 独立 reviewer 复核后提交推送。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card 轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/chat` 的 fresh-load 结构、browser 几何与本地 visual QA，确认主要热点仍是右侧 auth-card 相对中心 preview 的轻微 detached/heavy 感，因此仅在 `src/components/landing-page.tsx` 将 hero 右栏从 `248px` 收至 `242px`、把 `lg/xl gap` 从 `0/0.02rem` 放宽到 `0.28/0.36rem`，并同步下调 auth-card 外层圆角、边框、阴影与 `lg/xl` 顶部偏移；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已按同一合同更新。controller 侧已完成 focused Vitest、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build`、`curl -I http://127.0.0.1:3001/chat` 与独立 reviewer 复核；当前结果是 auth-card 相对中心 preview 已更轻、更不 detached，但 hero 文案与中心 preview 的轻微纵向遮挡仍是下一轮优先热点。
- [~] 执行检查点：本轮基于 live/local fresh-load 对照与 browser QA，将首屏中心 preview 从过亮/过软收敛为更克制的主视觉卡面：仅在 `src/components/landing-page.tsx` 下调 preview 外层 glow/shadow、略提边界分离，并同步提高内层卡片/图表区的结构对比；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview class 合同。Review 1：live/home 与 rebuilt local browser QA 一致确认当前最值得收敛的小步热点就是中心 preview 过亮问题，调整后页面无 clipping/overlap，preview 读感更 calm、且仍保持主视觉锚点。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`curl -I http://127.0.0.1:3001/` 全部通过。Review 3：controller 侧新增 diff-only 静态扫描未发现 secrets/shell/eval/SQL 风险，独立 reviewer 也确认本次 diff 仅为展示层收敛且未削弱测试合同；后续下一小步优先继续按 live/local 证据收敛左侧 radial cluster 相对中心 preview 的权重关系，而非扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude` 与 `3001` 发布前置核验，再按顺序执行 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构比对、browser vision、focused/full Vitest、lint、build、`docker compose config` 与 `docker compose up -d --build web`。基于 live/local 对照确认当前更值得优先的小步方向是修正右侧 auth-card 相对中心 preview 仍略偏高且略 detached 的组感，因此本轮仅在 `src/components/landing-page.tsx` 中将 `landing-hero` 的 `lg/xl` 横向 gap 从 `0.12/0.16rem` 微收到 `0.08/0.12rem`，并把 `landing-auth-card` 的 `lg/xl` 顶部外边距从 `2.44/2.64rem` 下移到 `2.74/2.96rem`，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同断言。Review 1（功能/对照）：fresh local browser vision 确认 auth-card 比上一轮更贴近 preview、位置更靠下，且无明显 broken/clipping/overlap；剩余问题已收敛为仍可继续微量左收/下沉。Review 2（测试/构建/集成）：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx`、`pnpm test`（21/21）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 全部通过，且本地 `http://127.0.0.1:3001/` 返回 200。Review 3（静态/安全/契约）：controller 侧 added-lines 静态扫描未见 secrets / shell injection / eval / unsafe deserialize / SQL formatting 风险；独立 reviewer 在正确 repo/branch 上对当前 diff 复核后返回通过，未发现新引入的安全、逻辑或测试诚实性问题。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude` 与本地 `3001` 发布前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，结合 live/local 共同证据确认当前单一最真实剩余热点仍是 hero 标题区与下方大型 visual group（left radial cluster + center preview + auth-card）之间的纵向距离略大，中心 preview 整组仍可再小步上提。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 `landing-hero` 的 `lg/xl` 负上边距从 `-mt-[2.64rem] / -mt-[2.88rem]` 小步收紧到 `-mt-[2.92rem] / -mt-[3.16rem]`，让 center preview、left radial cluster 与右侧 auth-card 作为整体更贴近标题区；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 `lg/xl` hero 负上边距合同。Review 1：fresh live/local browser 对照与 rebuild 后 local browser vision 一致确认 hero visual group 已比上一轮更靠近标题区、整体更接近 live，且当前首要剩余差距仍是 center preview/title vertical spacing，而不是 utility controls、auth-card 或 best-cases；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001` 且未影响 `/root/nexus-mail` 任意现有服务/端口；Review 3：`git diff --check` 通过、added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 hero 标题区与中心 preview 的 residual vertical-spacing / prominence 微差距做更小步收敛，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，再顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认上一轮“auth-card 与中心 preview 过近/过挤”已不再是首要热点，当前更真实的单一问题已转为 hero 顶部说明文案与下方大型 visual group（left radial cluster + center preview + auth-card）之间的纵向竞争。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `landing-hero` 的 `lg/xl` 负上边距从 `-mt-[2.9rem] / -mt-[3.16rem]` 回调到 `-mt-[2.56rem] / -mt-[2.84rem]`，并把 hero 主列与 auth-card 列间距从 `lg:gap-0 / xl:gap-[0.02rem]` 放宽到 `lg:gap-[0.14rem] / xl:gap-[0.18rem]`，让 right auth-card 继续维持已改善的贴合度，同时给标题区与 hero visual 顶缘留出更自然的呼吸空间；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 `lg/xl` hero 间距合同。Review 1：fresh rebuild 后 local browser vision 明确确认 auth-card vs center preview spacing 已改善，新的首要热点转移为 hero 文案与 visual cluster 的上中部拥挤，而不是右侧 auth-card 碰撞；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`；Review 3：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 hero 标题/说明文案与下方 visual group 的纵向层级收敛做更小步调整，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前最小且最安全的剩余热点是首屏整组 hero visual group 相对标题区仍略偏低，导致主标题下方存在可继续压缩的空带。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `data-testid="landing-hero"` 容器从 `lg:-mt-[2.32rem] / xl:-mt-[2.58rem]` 小步上提到 `lg:-mt-[2.72rem] / xl:-mt-[2.96rem]`，让 left radial network、center preview 与 right auth-card 作为整体更贴近标题区；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 `lg/xl` 负上边距合同。Review 1：fresh local browser vision 确认整体 vertical alignment 比上一轮更紧致、无明显 clipping/overlap，但单一最明显剩余问题已转为 intro 文案与上方装饰/preview 顶缘仍有轻微竞争；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`；Review 3：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕标题区文案与 hero 顶缘之间的剩余竞争做更小步 spacing/hierarchy 收敛，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，且不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port 与 Docker `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前更值得小步收敛的单一热点已从 utility/auth-card 转为 hero 中心 preview shell 的 airy/softness 合同。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 收敛中心 preview：放大外层圆角、减轻外壳阴影与边界对比、把内层 workspace 调到更暖的 cream 渐变、放宽主工作区与右侧 tall chart panel 的卡片间距/内边距，并把 chart 底部标签 row 的 `mt/gap/pt` 与 label wrapper `min-height/leading` 同步放松；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新这些受控 class 合同。Review 1：live/local browser 对照确认中心 preview 现在更 airy/soft、右侧 tall panel 留白更像 live，且无明显 clipping/absurd spacing；Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm build`、`pnpm lint`、`docker compose up -d --build web` 全通过，随后轮询 `http://127.0.0.1:3001/` 返回 `200`；Review 3：`git diff --check` clean、controller 静态扫描无 secrets/eval/shell 注入命中，独立 reviewer 复核当前 diff 仅给出“精确 Tailwind class 断言偏多”的非阻塞建议、无 blocking security/logic issues。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的 hero radial cluster 收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 Docker `3001` 发布前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构/视觉，再结合 live/local browser vision 与 DOM 几何确认当前最明显剩余热点仍是左侧 radial cluster 相对中心 preview 略 detached、标签偏淡偏窄。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将三枚 radial feature nodes 向中心 preview 轻推并收短 connectors（`left/right/top` 微调到 `44.15% / 44.55% / 11.7%` 与 `12.4% / 49.75% / 12.7%`，connector 宽度收至 `14.9% / 14.8% / 14.55%`），同时统一增强 inactive/active ring 边框与文本对比、放宽 label 最大宽度到 `84/88px` 并缩短与节点间距，让 cluster 更像被中心 preview 吸附的功能网络而不是游离装饰；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 radial 位置、connector 与 label class 合同。Review 1：fresh live/local 对照与 browser vision 一致指出 radial cluster 仍是首要热点且本轮修正方向正对该热点，没有误触顶部 utility row、auth-card 或 best-cases；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`curl -I http://127.0.0.1:3001/` 全通过，继续仅使用 `3001` 且不影响 `/root/nexus-mail` 现有端口；Review 3：`git diff --check` 与新增行静态安全扫描通过，接下来再做一次 repo-scoped 独立 reviewer 复核后提交推送。下一轮继续优先围绕 left radial cluster 与中心 preview 的 residual hierarchy/readability 微差距推进。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏视觉层级收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构/几何，结合 live `browser_vision` 指出当前更真实的剩余热点已收敛为“左侧 radial cluster 激活态略抢中心 preview 注意力”与“preview 底部 micro-label row 与柱体略 detached”，而顶部 utility row 与右侧 auth-card 已基本保持 subtle/coherent、不是当前首要问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中小步压低三枚 radial feature nodes 的激活态能量（统一将 active ring 从 `4.9rem` 收至 `4.82rem`、下调橙色边框/阴影/文本对比），并将 `feature-preview-chart-label-row` 的 `mt/pt` 从 `3.26rem/3.28rem` 微收至 `3.08rem/3.06rem`，让首屏注意力更稳定回到中心 preview、同时让 `方法论 / 结论 / 数据` 更贴近柱体；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 radial active-state 与 chart-label-row class 合同。Review 1：fresh live/local 结构与几何复核确认本轮修改对应的正是当前 2 个最小剩余热点，且 utility row / auth-card 未被误伤；Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、`curl -I http://127.0.0.1:3001/` 全通过，继续仅发布到 `3001`；Review 3：`git diff --check`、新增行静态安全扫描与 repo-scoped 独立 reviewer 均通过，仅留下“后续可补视觉回归快照”的非阻塞建议。下一轮继续优先围绕 left radial cluster 与中心 preview 的 residual hierarchy/readability 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的 hero radial cluster 层级收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构/视觉，再结合 live/local browser vision 与 DOM 几何确认当前单一最明显剩余 hero fidelity gap 已收敛为“左侧 radial cluster 相对 live 仍略偏弱、被中心 preview 压住”，而 utility controls、auth-card 与 preview 底部 micro-label row 不是本轮首要热点。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把三枚 radial feature nodes 小步向外展开并增大存在感（`left/right` 位置微调到 `42.8% / 43.25% / 9.45%`，三条 connector 放宽到 `16.4% / 16.35% / 16.2%`，inactive/active ring 统一放宽到 `4.96rem / 5.02rem`，并同步略增强边框/阴影），同时把 feature-rotator network shell 的 `lg/xl` 负右边距从 `0.18/0.28rem` 收至 `0.12/0.18rem`，减少 radial cluster 被 preview 吞没的读感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 radial 节点位置、connector、ring 尺寸与 network-shell class 合同。Review 1：fresh rebuild 后 local browser vision 明确确认左侧 radial cluster 已更可见、仍保持辅助层级，hero 未出现新的 clipping/overlap；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、Python HTTP 200 校验 `/` 全通过，继续仅发布到 `3001`；Review 3：`git diff --check`、新增行静态安全扫描与 repo-scoped 独立 reviewer 均通过。下一轮继续优先围绕 radial cluster 与中心 preview 的 residual hierarchy/readability 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮在 `/root/sciclaw` 内继续收敛 landing hero 右侧 auth-card；基于 live/local 结构核验、local `/chat` browser vision 与几何检查，仅小步将 hero 右栏收至 `280px`、hero/auth gap 收至 `0.04/0.08rem`，同时把 `landing-auth-card` 放宽到 `280px`、内层容器放宽到 `292px`，并把 `VERIFY ACCESS CODE` 主按钮收敛为 `whitespace-nowrap + text-[0.76rem] + tracking-[0.06em]` 以稳定单行呈现；对应 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新 class 合同。Review 1：fresh local `/chat` browser vision 确认 auth-card coherent/non-broken，主按钮已稳定单行、无 clipping/overlap，但 auth-card 相对中心 preview 仍略 detached；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过且继续仅使用 `3001`；Review 3：`git diff --check`、新增行静态安全扫描与 repo-scoped 独立 reviewer 均通过。下一轮继续优先收敛 auth-card residual detachment，不触碰 `/root/nexus-mail` 工作树、端口或既有服务。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 landing Phase B 的 preview micro-label coherence 收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构/视觉，确认右侧 auth-card 已保持 coherent、`VERIFY ACCESS CODE` 单行且不再是首要热点，当前更明显剩余差距转为中心 preview chart 底部 `方法论 / 结论 / 数据` micro-label 与柱状条之间距离偏大、阅读上略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中为 chart label row 增加稳定 `feature-preview-chart-label-row` anchor，并将底部 label row 的 `mt/gap/pt` 与 label wrapper `min-height` 做一次更小步压缩（`3.82rem -> 3.26rem`、`4.52rem -> 3.46rem`、`3.88rem -> 3.28rem`、`9.46rem -> 7.22rem`），让标签更贴近柱体且不引入 overlap；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步锁定新 class 合同，并把首页现有 readability focused case 改为直接锚定 `feature-preview-chart-label-row`。Review 1：fresh local browser vision 确认 preview micro-label 现已更贴近柱体、无 overlap，auth-card 继续 coherent 且 `VERIFY ACCESS CODE` 维持单行；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过且继续仅使用 `3001`；Review 3：`git diff --check`、新增行静态安全扫描与 repo-scoped 独立 reviewer 均通过，仅留下“若后续继续压缩可再补一条响应式 focused 断言”的非阻塞建议。下一轮继续优先围绕 landing hero 左侧 radial cluster 与 preview 内部剩余轻微 detached 感做更小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing hero 右侧 auth-card 的 readability/composition 微收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 local `http://127.0.0.1:3001/chat` fresh-load 结构与 browser vision，确认页面整体 coherent/non-broken，且右侧 auth rail 仍是更真实的可读性热点，其中 `VERIFY ACCESS CODE` 主按钮依旧存在轻微 wrapped/cramped 读感。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 hero 右栏从 `272px` 微放宽到 `280px`、`landing-hero` gap 从 `0.1/0.18rem` 微收至 `0.08/0.14rem`、将 `landing-auth-card` 外壳放宽到 `268px` 并继续轻量化 border/shadow/顶部下压，同时把 disabled 主按钮的内边距、字级与 tracking 收敛为更易单行呈现的组合；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步锁定新的 hero/auth-card class 合同。该 slice 已完成 Review 1 功能/规格复核（fresh local `/chat` browser vision 明确认为 auth rail 已更 calm、不再是最刺眼热点，但 `VERIFY ACCESS CODE` 仍略 wrapped，且页面无 clipping/overlap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“按钮字级 tweak 可视需要补更细 focused 断言”的非阻塞建议）。下一轮继续优先围绕 auth-card 主按钮单行可读性与 residual detachment 做更小步收敛，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing hero 右侧 auth-card 的 detached/composition 热点收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构/视觉层级，确认 auth-card 仍是 center preview 之后的主要剩余热点且问题以 composition/gap 为主；随后仅收窄 hero 右栏 `284px -> 280px`、将 `landing-hero` gap 从 `0.2/0.32rem` 收到 `0.16/0.26rem`、把 auth-card 外壳收窄到 `268px` 并同步轻量化外层 border/shadow、内层 shell 宽度与阴影；controller 侧复测几何显示 preview→auth gap 收到 `10.16px`、auth 宽度为 `268px`，local browser QA 继续将 auth-card 识别为主要剩余热点，但明确评价其已更窄、更轻、与中心 preview 略更贴合，且未出现新的 clipping/overlap；本轮已完成 `pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、`curl` 复核 `/` `/chat` `/help/getting-started` 均为 `200`、静态安全扫描 clean，以及 repo-scoped 独立 reviewer 通过，下一轮继续围绕 auth-card residual detachment 做更小步 composition 收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing hero 的 auth-card 贴合度微收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，再顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构，并结合 live/local `browser_vision` 结论确认当前单一最大剩余热点仍是右侧 auth-card 相对中心 preview 略 detached、顶部 utility controls 与 wordmark 则继续轻于 auth-card。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 `landing-hero` 的 `lg/xl` 横向 gap 从 `0.42rem/0.58rem` 微收至 `0.32rem/0.46rem`、将 `feature-rotator` 的 `xl` gap 从 `0.24rem` 微收至 `0.2rem`，并把 `landing-auth-card` 的 `lg/xl` 顶部下压从 `2.56rem/2.82rem` 微调至 `2.5rem/2.74rem`，让右侧入口更贴近中心 preview 而不引入新的遮挡；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步锁定首页与 `/chat` 共用 shell 的新 gap / margin 合同。该 slice 已完成 Review 1 功能/规格复核（fresh live/local 对照 + rebuild 后 local browser vision 确认 auth-card 较上一轮更贴近中心 preview，且未出现新 clipping/overlap，当前剩余最大热点仍是 auth-card 与中心 preview 的轻微分离感）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 auth-card residual detachment 做更小步收敛，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 右侧 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构，再用 local `browser_vision` + `browser_console` 几何确认当前单一最大剩余热点仍是 auth-card 相对中心 preview 略 detached、局部仍偏强。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 hero 右栏宽度从 `300px` 微收至 `296px`、hero/auth gap 从 `lg:0.62rem/xl:0.82rem` 收至 `lg:0.5rem/xl:0.68rem`，同时把 `landing-auth-card` 收窄到 `286px`、外层与内层边框/底色/阴影继续减重并把 `lg/xl` 顶部下压到 `2.42rem/2.68rem`，让右侧入口更像从属支持卡而非并列主视觉；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步锁定首页与 `/chat` 共享 shell 的新 hero/auth-card class 合同。该 slice 已完成 Review 1 功能/规格复核（fresh live/local 对照 + rebuild 后 local browser vision 确认中心 preview 仍为主视觉，auth-card 已比上一轮更轻、更低、更贴近，但仍是下一轮首要剩余热点）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `http://127.0.0.1:3001/` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 auth-card residual detachment 与左侧 radial cluster 的剩余可读性热点推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏主次层级的轻量高保真收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构/DOM/vision 对照，再结合 focused tests 与 build/rebuild 事实确认当前单一最大剩余热点已从“左侧 radial cluster + 右侧 auth-card 双热点”收敛为“中部 preview 已回到主视觉、右侧 auth-card 仍略偏强”，因此本轮仅在 `src/components/landing-page.tsx` 中把中心 preview 卡面轻度提亮提实（更强 `feature-preview-card` 外层 shadow、`border-white/80`、更暖的 preview 内层渐变与右侧分析面板边框/阴影），同时继续把 `landing-auth-card` 进一步轻量化并小幅下压到 `lg:mt-[1.82rem]` / `xl:mt-[2.04rem]`、收淡为 `bg-[#fdfefe]` + `border-[#eff3f7]` + 更轻 shadow，让首屏注意力更稳定落回中心 hero 而非右侧表单卡；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步补强，对首页与 `/chat` 共享 shell 显式锁定新的 preview/auth-card class 合同与 `feature-preview-card` anchor，保持 radial cluster、Best Cases、help shell、Docker 仅发布 `3001`、以及 `/root/nexus-mail` 现有服务/端口完全不受影响。该 slice 已完成 Review 1 功能/规格复核（fresh live/local 对照 + rebuild 后 local browser vision 确认左侧 radial cluster 已降为边缘辅助热点，当前首屏中心 preview 更像主视觉，右侧 auth-card 仍在但更像次级功能入口）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中；若独立 reviewer 出现跨仓库/陈旧反馈则以 controller-side repo-scoped diff 与验证事实 fail-closed 处理）。下一轮继续优先围绕 left radial cluster 的 residual readability/anchoring 与 auth-card 的剩余次级热点推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing header/hero 的轻量高保真收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，再顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构，并结合 live/local `browser_snapshot`、`browser_vision`、DOM 几何与 focused tests 确认当前更安全的最小差距已收敛为两点：顶部左侧 `SciClaw` 小字标相对 utility icons 仍略过轻、hero 右侧 auth-card 相对中心 preview 仍略偏高/偏 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中小步增强 header wordmark（`text-[1.08rem]` / `sm:text-[1.14rem]`、略收 tracking 与更稳的深色前景），并把 `landing-auth-card` 的 `lg/xl` 顶部下压从 `1.48rem/1.68rem` 收至 `1.72rem/1.94rem`，让品牌锚点略强、右侧卡片与中心 preview 更 cohesive；同时在 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 引入稳定 `landing-header-wordmark` anchor 并同步锁定新的 wordmark/auth-card class 合同，保持 hero 主内容、Best Cases、`/chat` 共享 public shell、Docker 仅发布 `3001`、以及 `/root/nexus-mail` 现有服务/端口完全不受影响。该 slice 已完成 Review 1 功能/规格复核（fresh live/local 对照 + rebuild 后 local browser vision 确认 wordmark 仍 subtle 但更可辨，auth-card 位置更低且更贴近中心 preview，无明显 clipping/布局破损）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 left radial cluster 的 faint/detached 可读性热点推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 顶部 wordmark 合同对齐，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构，再结合 local `browser_snapshot`、`browser_console` 与 focused tests 确认 live 顶部 header 在 utility icon 左侧仍保留 `SciClaw` 品牌字标，而本地共享 `LandingPage` header 此前仅右对齐 utility row、缺少该左侧 wordmark，导致首屏 header 语义与视觉层次少了一块明显的 public-branding 合同。本轮仅在 `src/components/landing-page.tsx` 的 header 中补回左侧 `Sci<span className="text-[#eb8a3c]">Claw</span>` wordmark，并把 header 容器从 `justify-end` 收敛为 `justify-between`；同时同步补强 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式锁定首页与 `/chat` 共享 landing shell 都继续存在该顶栏 wordmark，保持 hero、auth-card、Best Cases、help 路由与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（fresh live/local 顺序对照确认当前差距仅是 header branding 缺口，补回后 local snapshot 与 DOM 已恢复顶栏 `SciClaw` 字标，且未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing hero / auth-card / radial cluster 的剩余高保真细节推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 右侧 auth-card 的轻量化收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构，再结合 local `browser_vision` 确认 auth-card 先前仍相对中心 preview 偏重、偏像并列主模块，因此本轮仅在 `src/components/landing-page.tsx` 中把 `landing-auth-card` 收敛为更淡的边框/底色/阴影并追加小幅 `lg/xl` 顶部下压（`bg-[#fcfdff]`、`border-[#edf1f5]`、更轻 shadow、`lg:mt-[0.58rem]`、`xl:mt-[0.7rem]`），同时补强 `src/app/page.test.tsx` 锁定这些 auth-card 轻量化合同，保持 hero 主 heading/body、radial cluster、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（fresh live/local QA 与 local browser vision 确认 auth-card 现已更像辅助面板、不再是首屏单一最大热点，且未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing 左侧 radial cluster 与底部 micro-label 的剩余高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 右侧 auth-card 的对位/权重收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 结构，再结合 live/local `browser_vision` 与 `browser_console` 几何确认当前更真实的剩余热点已从“auth-card 过重”收敛为“auth-card 仍略 detached、略偏高”，因此本轮仅在 `src/components/landing-page.tsx` 中继续小步收敛 hero 右栏：将 `landing-auth-card` 宽度从 `318px` 微收至 `314px`、卡面从 `bg-[#fcfdff]` 收敛为 `bg-white`、边框收为 `border-[#e6ebf1]`、阴影略转向更清晰但仍轻量的 `0_16px_30px/0_8px_18px` 组合，并把 `lg/xl` 顶部下压提高到 `lg:mt-[0.86rem]` / `xl:mt-[1rem]`；同时将 `landing-hero` 的右栏栅格同步收至 `lg:grid-cols-[minmax(0,1fr)_314px]`、`xl:grid-cols-[minmax(0,1.03fr)_314px]`，并把 hero/auth gap 微收至 `lg:gap-[1.06rem]` / `xl:gap-[1.28rem]`，让 auth-card 更贴近中心 preview 但仍维持辅助模块层级；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 同步补强，锁定首页与 `/chat` 复用 shell 的新 auth-card/hero 布局合同。该 slice 已完成 Review 1 功能/规格复核（fresh live/local QA 与 rebuild 后 local `browser_vision` 确认 auth-card 相比上一轮更接近 live 的权重与对位，首要剩余热点已转回左侧 radial cluster，而未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、HTTP 200 校验 `/` 与 `/chat` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff` 复核仅涉及 landing hero/auth-card 与 focused tests、当前 diff 新增行静态安全扫描 0 命中、未引入 secrets/shell/eval/SQL 风险，且首页与 `/chat` 契约断言保持同步）。下一轮继续优先围绕 public landing 左侧 radial cluster 的可读性/凝聚感与 utility/auth 的残余视觉微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing Best Cases 翻页控件的 SVG 箭头合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/`、live `https://sciclaw.ai/chat` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_console` 直接检查 `Previous/Next` 按钮 DOM，确认 live 两个公开 marketing landing 路由的 Best Cases 翻页控件都使用内嵌 chevron SVG，而本地 `src/components/landing-page.tsx` 仍保留纯文本 `← / →`，导致轮播 controls 比 live 更像占位符。本轮仅将 `Previous/Next` 按钮内容收敛为与 live 一致的 `h-3.5 w-3.5` 内嵌 SVG chevron，并同步补强 `src/app/chat/page.test.tsx`，显式锁定 `/chat` fresh-load marketing shell 中两个翻页按钮都继续保留可访问名称且内含 SVG 图标；其余 hero/auth-card/Best Cases 文案、`/chat` 复用 landing shell 与 `3001` 发布边界保持不变。该 slice 已完成 Review 1 功能/规格复核（fresh live/local DOM 对照确认按钮控件语义不变，仅视觉合同从文本箭头收敛为 SVG chevron，且未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅补充“后续可在首页测试也锁定该 SVG 合同”的非阻塞建议）。下一轮继续优先围绕 public landing hero / radial cluster / auth-card 的剩余高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 的 auth-card 对位收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 local `browser_vision` 与 `browser_console` 几何确认当前最明显剩余差距仍是 auth-card 相对中心 preview 偏右、与主视觉脱节，因此本轮仅将 `src/components/landing-page.tsx` 的 `landing-hero` 在 `lg/xl` 断点收敛为更贴近 live 的 `items-start` + 更窄 hero/auth gap（`lg:gap-[1.18rem]`、`xl:gap-[1.42rem]`，并同步把 `xl` 主列收至 `1.02fr`），让 auth-card 更靠近中心 preview；同时补强 `src/app/page.test.tsx` 锁定新的 `lg/xl` 栅格、对齐与间距合同，保持 auth-card 318px 宽度、hero 文案、radial cluster、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（fresh live/local 结构与 local browser QA 确认 auth-card 仍是单一最明显 fidelity hotspot，但本轮后 hero 右侧卡片与中心 preview 的组合更紧，未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅补充锁定 `xl:grid-cols-[minmax(0,1.02fr)_318px]` 的 focused test 后再次全量复核通过）。下一轮继续优先围绕 public landing hero 右侧 auth-card 与中心 preview 的残余对位差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 的可读性/遮挡收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 local `browser_vision`、live DOM 几何与本地 snapshot/DOM 事实确认当前最明显剩余视觉热点是左侧 radial labels 与节点仍被中心 preview 卡部分遮挡、`Data Mining`/`Outcome Present` 一带阅读感偏弱。本轮仅在 `src/components/landing-page.tsx` 中将三枚外侧 radial nodes 整体左移/右移微调、同步拉长 connectors，并收窄 label 最大宽度与提高文字对比，减少 cluster 被中心卡片切入的程度；同时更新 `src/app/page.test.tsx` 锁定新的 node 位置、connector 宽度与 label 宽度合同，保持 hero 主 heading/body、auth-card、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序对照 + local browser_vision 确认 radial cluster 较前一轮更可读、但剩余热点仍是 cluster 与中心卡片的轻微遮挡/分离感）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余遮挡/留白细节推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 auth-card 对位收敛；已先完成 repo/path/port 前置核验（工作树仅限本仓库、继续仅使用 3001、只读确认不影响 `/root/nexus-mail` 现有服务与端口），并顺序复核 live `https://sciclaw.ai/`、live `/chat` 与 local `http://127.0.0.1:3001/` fresh-load 结构。结合 local browser vision 与 DOM 几何，确认当前最明显剩余差距是 hero 右侧 auth-card 相对主预览区略显上浮、与中心 mockup 基线不够贴合；据此本轮仅将 `src/components/landing-page.tsx` 的 `landing-hero` 在 `lg+` 断点从 `items-start` 收敛为 `items-center`，并将横向间距从 `lg:gap-[1.96rem] / xl:gap-[2.18rem]` 微收至 `lg:gap-[1.64rem] / xl:gap-[1.88rem]`，让 auth-card 更贴近主 visual，而不改动既有 318px 卡宽、tab/apply/auth contract、Best Cases 或 `/chat` 共用 landing shell；同步更新 `src/app/page.test.tsx` 的 focused 断言以锁定 `lg:items-center` 与新 gap。后续继续按 3 轮 review 执行 tests / lint / build / compose / HTTP / 静态安全扫描 / 独立 reviewer，并在验证通过后中文提交并 push。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing / `/chat` 的 hero 中心节点可访问语义对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，并结合 `browser_snapshot`、`document.body.innerText` 与 focused Vitest 复现确认公开 contract 中 hero 中心圆形按钮应保持纯 icon-only 控件，不额外暴露 `Literature Analysis` 这类可访问名称，而本地 `src/components/landing-page.tsx` 仍通过 `aria-label={centerNode.label}` 让中心按钮语义比 live 更重。本轮仅移除该中心按钮的额外 `aria-label`，新增稳定 `data-testid="feature-node-center"` 供 focused tests 精准断言，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，改为验证中心节点无可访问名称，保持首页与 `/chat` 默认 hero 文案/结构合同继续对齐 live。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public help shell 的导航语义对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧顺序复核 live/local `/help/getting-started` 结构，并结合 `browser_snapshot`、`document.body.innerText` 与 focused Vitest 失败复现确认公开 contract 中左侧 Contents 栏虽呈现文章目录链接，但 live runtime 并未暴露具名 `User Guide navigation` landmark，而本地 `src/components/help-shell.tsx` 仍保留该额外 `aria-label`，使公开 help shell 语义比 live 多出一层 landmark。基于该 live 事实与当前 diff，本轮仅移除 sidebar `<nav>` 的 `aria-label`，并同步将 `src/app/help/help-page.test.tsx` 的 focused 断言从依赖具名 landmark 收敛为直接校验目录链接与其 href，同时显式锁定该 landmark 在本地不再出现，保持 help 正文、TOC、顶部 docs shell、`/chat` 回链、`3001` 发布边界与 `/root/nexus-mail` 端口隔离不变。该里程碑已通过 Review 1 功能/规格复核、Review 2 测试/构建/集成复核（focused Vitest、lint、build、compose config、compose rebuild、`/` `/chat` `/help/getting-started` HTTP 200 全通过，继续仅使用 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅给出“若未来不再以 live parity 为目标可考虑恢复 landmark 命名”的非阻塞建议）。下一轮继续优先围绕 public landing/help 的 live 语义与视觉微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing / `/chat` 的 hero radial-button 可访问语义对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/chat` 与 local `http://127.0.0.1:3001/chat` 的 fresh-load 结构，并通过 `browser_snapshot`、`document.body.innerText`、按钮几何与 focused Vitest 失败复现确认公开 contract 是三枚外侧 radial feature buttons 保持 icon-only clickable circles、其可见英文标签独立渲染在按钮外侧，而不应再把外侧标签并入按钮的可访问名称；据此本轮仅移除 `src/components/landing-page.tsx` 中 `feature-node-*` 三枚外侧 radial buttons 的 `aria-label`，并同步将 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 断言从脆弱的 `getAllByRole(... name=/data mining|outcome present/)` 收敛为基于现有 `data-testid` 的节点点击/存在性断言，同时保留 visible label、heading 切换、Q1/Q2/Q3/Q4 与 Best Cases 合同校验不变。该里程碑已通过 Review 1 功能/规格复核（顺序复核 live/local `/chat` 与 `/` 后确认外侧 radial labels 继续可见，但本地 runtime 的 interactive circles 现不再错误暴露额外 accessible names，更贴近 live icon-only contract）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config` 全通过，继续仅使用 `3001`，未触碰 `/root/nexus-mail` 任意现有服务与端口）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅给出“若未来确需保留无障碍命名可考虑 `aria-labelledby`”的非阻塞建议）。下一轮继续优先围绕 public landing hero / carousel / auth-card 的剩余微差距推进，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero 中心顶点装饰语义收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，并用 `browser_snapshot` + `document.body.innerText` 确认 live 默认 hero 中心圆点上方已不再暴露可见 `AI` 文本，而本地此前仍把该橙色顶点渲染成可见 `AI` 字样，导致中心 preview 比 live 多出一层 helper microcopy。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 中心顶点从可见 `AI` 文本收敛为 `aria-hidden` 的白色装饰圆点，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式锁定 `/` 与 `/chat` fresh-load 均不再暴露独立 `AI` 文本，保持 hero heading/body、Best Cases、auth-card、`/chat` 复用 landing shell 与 `3001` 发布策略不变。该里程碑已通过 Review 1 功能/规格复核（live `/` 仍为 marketing landing，local fresh-load 结构现已移除中心顶点的额外 `AI` 可见文案）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing 的 hero / carousel / auth-card 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 与 live `https://sciclaw.ai/chat` fresh-load 仍为同一 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的顶部 utility icon 比例收敛 slice：controller 侧先顺序复核 live/local 首页与 `/chat` 的 fresh-load 结构，再用 `browser_console` 直接比对顶部三枚 utility triggers 的运行时几何，确认本地按钮本体已与 live 基本对齐（live 约 `38.25px`、local 约 `38.39px`），但图标本体仍略大于 live（live 约 `17px`、local 约 `18.88px`），导致视觉上 controls 仍稍显“placeholder/dev-like”。因此本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 中 `GuideIcon` / `ContactIcon` / `SettingsIcon` 的默认尺寸从 `1.18rem` 系列微收至 `1.06rem`，并同步补强 `src/app/page.test.tsx`，显式锁定三枚 trigger 内嵌 `svg` 的 `h-[1.06rem] w-[1.06rem]` 合同，保持按钮直径、菜单结构、hero / auth-card / Best Cases、`/chat` 复用 landing shell 与 `3001` 发布策略不变。该里程碑已通过 Review 1 功能/规格复核（live/local `/` `/chat` 顺序复核确认仅为顶部 utility icon 比例微收敛，未改动路由或菜单合同）、Review 2 测试/构建/集成复核（`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。最新 controller-side 结论：本轮后三枚 utility triggers 的运行时按钮几何仍保持约 `38.39px`，而图标几何已收敛到约 `16.95px`，与 live `17px` 基本对齐；页面未见新的 clipping/overlap，下一轮继续优先沿 public landing hero / carousel 的剩余微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 hero CTA 合同对齐 slice：controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `/`、live `/chat` 与 local `http://127.0.0.1:3001/`、`/chat` 的 fresh-load 结构；随后用 `browser_snapshot`、`document.body.innerText` 与 focused diff 事实确认 live 公共 landing 的主 CTA 当前仅暴露纯文本 `Get started`，不再额外显示独立向下箭头，而本地 `src/components/landing-page.tsx` 仍保留 `↓` 装饰节点，导致首页与 `/chat` 的 hero CTA 比 live 多出一层 helper glyph。基于该事实，本轮仅移除 `FeaturePreview` 中主 CTA 内部的箭头 `<span>` 与对应 `gap-2` 间距，保持点击行为与按钮文案不变；controller 侧随后完成 `pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build`、`curl -I http://127.0.0.1:3001/`、`curl -I http://127.0.0.1:3001/chat` 与 fresh-load local browser QA，确认两条公开路由继续 200、页面无明显破损，且主 CTA 已收敛为纯文本 `Get started`。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 CTA 可访问名称对齐 slice：controller 侧先顺序复核 live `/` 与 live `/chat` 的 fresh-load 结构，再对比 local `/` DOM，确认公开 landing hero 的主 CTA 在 live 文本合同中仅暴露 `Get started`，而本地实现仍通过额外 `aria-label="Get started preview"` 让可访问名称偏离 live；因此本轮不扩张新模块，仅移除 `src/components/landing-page.tsx` 中该 CTA 的额外 `aria-label`，并同步更新 `src/app/page.test.tsx`，把首页 focused 断言收敛为 `link[name=/^get started$/i]`，保持 `/chat` 继续复用同一 marketing landing shell、auth-card、Best Cases 默认态与 `3001` 端口边界不变。该里程碑已通过 focused Vitest、lint、build、compose 配置校验、容器重建、本地 `/` `/chat` `/help/getting-started` HTTP 200 校验、静态安全扫描、三轮评审与 repo-scoped 独立 reviewer 复核；最新 controller-side 结论是 local 首页 CTA 的可访问名称现已与 live public landing 对齐，下一轮继续优先沿 public landing 的 hero / carousel / auth-card 微差距推进，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 onboard access-code 合同对齐 slice：controller 侧顺序复核 live `/`、live `/chat` 与 local `/chat` 的 fresh-load 结构，并用 DOM 检查确认真实合同是 access-code 输入框保持空值、仅通过 `SC-XXXXXXXX` placeholder 提示，而不是带默认只读 display value；据此仅移除 `src/components/landing-page.tsx` 中 onboard input 的 `readOnly` 与预填 `value`，同时补强 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式断言首页与 `/chat` 的 access-code textbox 具有该 placeholder 且 `value === ""`，保持 landing / `/chat` 共用 marketing shell、feature rotator、Best Cases 默认态与 `3001` 端口边界不变。该里程碑已通过 focused Vitest、lint、build、compose 配置校验、HTTP 200 校验与静态/独立评审复核；最新 controller-side 结论是本地 public auth shell 现已与 live 的空输入合同一致，下一轮继续优先沿 landing / `/chat` 的公开页面高保真收敛推进，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` 合同校准 slice：controller 侧先顺序复核 live `/chat` 与 local `/chat` 的当前 fresh-load 结构，再用 DOM 检查确认真实合同是 access-code 输入框以空值 + `SC-XXXXXXXX` placeholder 形式呈现，而不是带默认 display value；据此仅修正 `src/app/chat/page.test.tsx` 中一条过时断言，将 `getByDisplayValue(/sc-xxxxxxxx/i)` 收敛为基于 `textbox[name=/access code/i]` 的 placeholder 断言，保持 landing / `/chat` 共用 marketing shell、hero/Best Cases 默认状态与 3001 端口边界不变。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f docker-compose.yml config`、`curl -I http://127.0.0.1:3001/`、`curl -I http://127.0.0.1:3001/chat`、`curl -I http://127.0.0.1:3001/help/getting-started` 与 controller-side 静态扫描/独立 reviewer 复核；剩余 Phase B 工作继续聚焦 live/local marketing landing 的高保真视觉收敛，而非扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 landing hero preview 去噪 slice：controller 侧顺序复核 live `/` 与 local `/` 后确认当前更安全且直接的 public-fidelity 差距已从 Best Cases 进一步收敛到首屏 hero preview 顶部仍显性暴露 `Research workspace preview` helper microcopy，而 live 首屏更偏向仅保留结构线条与核心标题/数据感，因此本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 中 preview card 标题下方的 `Research workspace preview` 可见文本替换为 `aria-hidden` 装饰线条骨架，同时同步补强 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式断言首页与 `/chat` 不再暴露该 helper 文案，并继续保留 `Deep Literature Analysis`、`Autonomous Research`、`Meth./Find./Data/Evid.`、Best Cases 默认态与 `/chat` 复用 landing 合同不变。该里程碑已通过 Review 1 功能/规格复核（确认 live `/` 与 `/chat` 仍是 marketing landing，本轮仅是 hero preview helper-word 去噪，不改路由/IA）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过；fresh browser/DOM 亦确认 `Research workspace preview` 已从本地页面文本中消失，未将 browser 工具态噪声误判为应用回归）。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 landing / `/chat` Best Cases 去噪 slice：controller 侧先顺序复核 live 主页结构与当前本地 worktree，确认当前最安全且直接提升高保真度的差距已不在顶部 utility dock，而在 Best Cases 左侧深色 preview card 仍暴露过多 helper words（如 `Foundry / Preview / Stakeholder-ready synthesis / Summary / Slides handoff / Evidence appendix`），使本地比 live 更 busy；因此本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 中该 preview card 的顶部 eyebrow/`Preview` 文案、次级 helper sentence、`Summary` 标签与两枚 chip 文案全部收敛为 `aria-hidden` 的装饰线条/占位骨架，并同步补强 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式断言首页与 `/chat` 的 Best Cases 默认卡不再暴露这些 helper labels，同时保留轮播主标题、正文与 `01 / 04` 契约不变。该里程碑已通过 Review 1 功能/规格复核（确认 live `/` 与 `/chat` 仍是 marketing landing，本轮仅是 Best Cases preview card 去噪，不改路由/IA）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中）。补充说明：本轮 browser snapshot 仍可能缓存旧 helper 文案，已按降级流程以 controller-side diff、focused tests、build/HTTP 与 DOM 文本事实作为验收依据，未将疑似浏览器陈旧态误判为应用回归。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：基于 sequential live/local QA、live/local 顶部 utility trigger 几何对比（live 约 `38.25px`、local 仍约 `45.59px`）与 controller-side DOM/视觉复核，确认当前更适合优先收敛的仍是顶部 utility dock 的存在感，而不是扩张新模块或改动已稳定的 radial cluster / bottom micro-label row，因此本轮仅将共享 dock trigger 组从 `gap-[0.58rem]/sm:gap-[0.76rem]` 微收至 `gap-[0.46rem]/sm:gap-[0.62rem]`，并同步轻收边框/阴影/前景对比到 `border-[rgba(7,25,39,0.34)]`、`text-[#1b3a4c]` 与更浅 shadow，使顶部 controls 更向 live 的 low-emphasis grouping 收敛，同时保持既有 menu contract 与 hero 其余结构不变。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx`（10 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、本地 HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：顶部 utility controls 的 grouping/contrast 已更接近 live 的 subtle 状态，当前仍可见的几何差距主要是按钮本体尺寸仍略大于 live；下一轮继续优先考虑 trigger diameter 是否需要安全微收，而不是回到 radial cluster 或扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：基于 sequential live/local browser QA、fresh-load browser vision 与 live/local DOM 几何对比，确认当前首要剩余问题仍是 left radial cluster 相较 live 过于显眼、局部 label/connector 竞争偏强，而顶部 utility triggers 已接近 subtle-but-readable 目标，因此本轮不扩张新模块，仅将共享 dock trigger 组继续保持在 `gap-[0.62rem]/sm:gap-[0.82rem]` 并轻收边框/阴影、仅微提前景对比，避免顶部 controls 再次偏重；同时把三枚 radial side nodes 微收并更靠近中心 preview 到 `left-[47.65%]/left-[48.1%]/right-[15.95%]`、将 connector widths 放宽到 `11.78%/11.86%/11.58%`，并同步微收 node ring 阴影、统一 inactive/active label 宽度到 `178px/148px`，再把 radial label typography 收敛为 `text-[10.8px]`、`leading-[2.06]`、`tracking-[0.03em]`，以继续削弱 cluster 的 noisy/detached 感并让 ghosted labels 更 calm。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load live/local browser QA 认为顶部 utility triggers 仍保持 subtle/readable 平衡，left radial cluster 也较上一轮更 anchored、稍少拥挤；但 radial cluster 仍是首屏最明显 fidelity hotspot，下一轮继续优先收敛 cluster 的 hierarchy / anchoring，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 landing Phase B fidelity slice：基于既有热点判断不扩张新模块，仅对顶部 utility dock triggers 做更 subtle 的存在感回收——将共享 dock trigger 组从 `gap-[0.62rem]/sm:gap-[0.82rem]` 微收至 `gap-[0.58rem]/sm:gap-[0.76rem]`，并同步轻收边框/阴影强度、仅微提图标前景对比（`border-[rgba(7,25,39,0.38)]`、更浅 shadow、`text-[#1c3d50]`），让顶部 controls 更贴近 live 的 low-emphasis 分组而不改动既已验证的 radial cluster / preview 结构。该里程碑严格按 TDD 先调整 `src/app/page.test.tsx` 对 dock trigger 间距的 focused 断言形成红灯，再完成 `src/components/landing-page.tsx` 实现；随后已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及双独立 reviewer 复审。最新 controller-side 结论：本轮改动未引入新的 clipping/contract 风险，顶部 utility triggers 进一步向 subtle/live 方向收敛；下一轮继续优先观察 landing hero 顶部 trigger 与 radial cluster 的相对权重是否还需微收，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：根据 sequential live/local browser QA、local browser DOM 几何复核与 focused test red→green，优先收敛 live 仍显著弱于中心 preview 的 left radial cluster，同时避免再次把顶部 utility triggers 做得过重——因此将共享 dock trigger 组从 `gap-[0.7rem]/sm:gap-[0.9rem]` 微收至 `gap-[0.62rem]/sm:gap-[0.82rem]` 并轻收边框/阴影对比，保持 subtle；再把三枚 radial side nodes 向中心 preview 微收至 `left-[47.85%] / left-[48.25%] / right-[15.9%]`，同步将 connector widths 收到 `11.42% / 11.5% / 11.24%`，并把 radial labels 统一收至 inactive `max-w-[180px]` / active `max-w-[152px]`、略提文字前景到 `#607988`，以减弱 cluster 的 detached 感与局部 label 碰撞；另外把 preview 底部 micro-label row 扩到 `gap-[4.52rem]/sm:gap-[4.68rem]`、稍增 horizontal padding 并前移 top padding，继续降低 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 认为顶部 utility triggers 比上一轮更不抢主视觉、bottom micro-label row 更稳；当前最明显剩余差距仍是 left radial cluster 本身虽然更靠近中心 preview，但相对 live 仍是首屏最弱 cohesion/readability 热点，下一轮继续优先做 radial cluster 的 label hierarchy / connector anchoring，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一次更聚焦的 landing Phase B 小步 fidelity slice：基于 sequential live/local browser QA、fresh-load browser vision 与本地 DOM 几何复核，确认当前首要剩余问题仍集中在 left radial cluster 的 detached/fragile microcopy，而不是继续放大顶部 utility triggers 或继续放宽底部 micro-label row；因此将共享 dock trigger 组从 `gap-[0.66rem]/sm:gap-[0.86rem]` 微收回 `gap-[0.62rem]/sm:gap-[0.82rem]` 并轻收按钮边框/阴影对比，避免顶部 controls 在空白角落中过度抢眼；再把三枚 radial side nodes 收到 `left-[47.85%] / left-[48.25%] / right-[15.9%]`，同步将 connector widths 收到 `11.42% / 11.5% / 11.24%`，并把 radial labels 统一放宽到 inactive `max-w-[180px]` / active `max-w-[152px]`、略提文字前景到 `#607988`，以加强 cluster→preview 锚定感并减弱 tiny label 的脆弱感；另外把 preview 底部 micro-label row 微收至 `gap-[4.52rem]/sm:gap-[4.68rem]`、稍增横向 padding 并前移 top padding，在不重回拥挤状态的前提下避免底部标签显得过于横向发散。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load live/local browser QA 认为顶部 utility triggers 现在接近 about-right 但仍略微抢眼、left radial cluster 比前一轮更 anchored、底部 micro-label row 也较前一轮更不 cramped；当前单一最明显剩余弱点仍是 left radial cluster 的 tiny text/connector 区域相对中心 preview 依旧偏 fragile/noisy，下一轮继续优先做 radial label hierarchy / connector anchoring / microcopy anti-crowding，而不是扩张新模块。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 的 label anti-crowding / bridge-spacing 收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_console` 几何与 local `browser_vision` 确认当前更真实的剩余热点仍是左侧 radial labels 相对中心 preview 偏 detached、其中 `Data Mining` / `Outcome Present` 一带阅读感较弱，而顶部 utility triggers 已保持约 `38.39px` 按钮直径、与 live `38.25px` 基本齐平。本轮仅在 `src/components/landing-page.tsx` 中把三枚外侧 radial nodes 整体再向左/右外侧微推（`left-[43.9%] / left-[44.5%] / right-[10.8%]`）、同步拉长 connectors（`15.2% / 15.02% / 14.92%`）并放宽标签宽度与底部标签偏移（如 `Literature Analysis` 到 `max-w-[108px]`、`Data Mining` 到 `max-w-[102px]`、`Outcome Present` 到 `max-w-[112px]`），只做 cluster→preview 的更小步抗拥挤收敛；同时补强 `src/app/page.test.tsx` 锁定新的 node/connector/label 几何合同，保持顶部 utility 按钮尺寸、hero 主 heading/body、auth-card、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序复核确认顶部 utility row 继续与 live 接近，local browser QA 认为 radial cluster 较前一轮更可读但仍是首屏最显著 fidelity hotspot）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing 左侧 radial cluster 的剩余 detached / cramped 视觉差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 的 detached/clipped 热点收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_console` 几何与 local `browser_vision` 确认当前最显著剩余问题仍是左侧 radial cluster 标签与 connectors 被中心 preview 部分切入、整体阅读上偏 detached。本轮仅在 `src/components/landing-page.tsx` 中把三枚外侧 radial nodes 再向外轻推（`left-[42.8%] / left-[43.25%] / right-[9.7%]`）、同步拉长 connectors（`16% / 15.9% / 15.85%`），并略收三枚标签宽度/偏移（如 `Literature Analysis` 到 `max-w-[104px]`、`Data Mining` 到 `max-w-[98px]`、`Outcome Present` 到 `max-w-[106px]`），只做 cluster→preview 的更小步 anti-clipping / anti-detached 收敛；同时补强 `src/app/page.test.tsx` 锁定新的 node/connector/label 几何合同，保持顶部 utility 按钮尺寸、hero 主 heading/body、auth-card、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序复核确认顶部 utility row 继续与 live 接近，local browser QA 仍把 radial cluster 视为最大热点，但较上一轮更外展、连接更连续）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“未来可减少 Tailwind 几何断言脆弱性”的非阻塞建议）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余遮挡/留白细节推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 的 anti-crowding / anchoring 微调，不触碰 `/root/nexus-mail` 工作树、端口或现有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live `browser_vision`、live/local `browser_console` 几何与本地 fresh-load browser QA，确认当前单一最明显剩余热点仍是左侧 radial cluster：三枚外侧标签虽然未出现灾难性重叠，但相对中心 preview 仍偏 detached，且 `Data Mining` / `Outcome Present` 一带更容易显得被中心卡边缘打断。基于该事实，本轮仅在 `src/components/landing-page.tsx` 中将三枚外侧 radial nodes 再做一轮小步外展与桥接增强（`left-[41.9%] / left-[42.35%] / right-[8.8%]`），同步把 connectors 拉长到 `16.6% / 16.5% / 16.35%`，并把三枚 labels 分别放宽/外移到 `max-w-[110px]`、`max-w-[104px]`、`max-w-[112px]` 与更松的 left/top offsets，以减少 cluster→preview 的视觉切入感；同时补强 `src/app/page.test.tsx`，显式锁定新的 node/connector/label 几何合同，保持顶部 utility triggers 尺寸与间距、hero 主 heading/body、auth-card、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（fresh live/local 顺序复核确认 left radial cluster 仍是最大视觉热点，但本轮后标签外展更明显、与中心 preview 的桥接略有增强，且未引入新的 clipping 或页面破损）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 复审通过）。下一轮继续优先围绕 public landing 左侧 radial cluster 的剩余 detached / clipped 视觉差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live/local fresh-load、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web` 与 HTTP 200 前置核验，再按 systematic-debugging 对比 live/local 结构与 DOM 几何，将当前单一最安全收敛切片锁定为 auth-card 虽已嵌入 preview 下方，但相对 live 仍略窄、略弱，导致 preview 下方的 access rail 读感偏收。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步放宽 auth wrapper 与卡面：将 preview 下方容器从 `max-w-[280px]/sm:max-w-[292px]` 放宽到 `320px/332px`、顶边距从 `-mt-[0.08rem]` 微调到 `-mt-[0.12rem]` 并去掉 `lg` 额外上移，同时把 `landing-auth-card` 从 `224px` 放宽到 `244px`、内层 shell 从 `260px` 放宽到 `276px` 并轻增内边距，保持已验证的嵌入式 DOM 层级不变，只收敛本地 auth rail 的压缩感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 class 合同。Review 1：fresh live/local 结构与 DOM 几何继续确认 auth-card 仍是本轮最小安全热点，调整后 local 仍保持 preview 下方嵌入式结构，未回退到旧的右侧并列列布局。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl http://127.0.0.1:3001/` 均已通过。Review 3：controller 侧静态检查确认本次仅收敛公开 marketing landing 的尺寸/布局类与对应测试合同，未引入 secrets、危险执行、外部端口变更或 `/root/nexus-mail` 相关修改；browser_vision 本轮继续出现空结果式退化，按既定降级策略以 fresh snapshot + DOM/HTTP/controller-side build 事实作为验收依据。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 核验，并按 systematic-debugging 将当前单一最安全收敛切片锁定为 left radial cluster 仍略 detached。基于 live/local browser QA、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web`、HTTP 200 与当前 diff 事实，本轮仅在 `src/components/landing-page.tsx` 将三枚 radial nodes 小步向中心 preview 收拢、缩短 connectors、收窄并重排 labels（literature/data 从 `112px` 收至 `108px`、outcome 从单行 nowrap 收敛为 `104px` 多行标签），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的节点/connector/label focused 合同。Review 1：fresh live/local 顺序核验与 local browser_vision 一致确认页面无新增 clipping/overlap，left radial cluster 较前一轮更 anchored、但仍诚实保留轻微 detached 感。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 Python HTTP 200 校验全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、diff added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新的安全、逻辑或契约问题。下一轮继续优先围绕 public landing 左侧 radial cluster 的剩余 detached / balance 差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/status、端口与 `.git/info/exclude` 前置核验，再按 systematic-debugging 顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 结构、live/local `browser_console` 几何与本地 `browser_vision` sanity QA，确认当前单一最明显剩余热点仍是左侧 radial cluster，且更具体地收敛到 active `Literature Analysis` 标签相对 live 仍略偏挤、与中心 preview 的桥接感仍弱于右侧主视觉。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 active literature label 从 `max-w-[114px]` 收至 `108px`、并把 left/top offsets 从 `-1.9rem / 0.08rem` 微调到 `-2.16rem / 0.02rem`（`sm` 同步 `-2.22rem`），以小步释放标签与圆点、连接线及中心 preview 的局部拥挤感；同时仅同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同，不扩张到其他模块或断点。Review 1：顺序 live/local fresh-load 与 rebuild 后 local `browser_vision` 一致确认页面无新增 clipping/overlap、顶部 utility icons 与 auth/access 模块未恶化，左侧 radial cluster 仍是最大热点，但 active literature label 的局部拥挤感较前一轮略减轻；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/` 全通过，继续仅发布到 `3001`；Review 3：`git diff --check` 通过、diff added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“后续可补视觉回归保护”的非阻塞建议。下一轮继续优先围绕 left radial cluster 尤其 `Outcome Present` 支路的 detached / bridge 弱化差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 持续推进 hero / utility controls / radial cluster / micro-label 的高保真小步收敛，保留既有执行记录与验证结论
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、live `https://sciclaw.ai/chat` 与 local `http://127.0.0.1:3001/chat` 的 fresh-load 对照，确认 live/local 默认 hero 均为 `Deep Literature Analysis` 且首个 Best Cases 仍为 `Automated Report Generation / 01 / 04`，随后按 browser QA 最明显热点仅在 `src/components/landing-page.tsx` 小步将 literature-analysis 左侧 radial node 向中心 preview 轻推并缩短 connector/label 宽度（`left 46.6%→47.8%`、`top 14.05%→13.25%`、connector `12.4%→11.2%`、label `108px→104px`、label offset `-2.16rem→-1.94rem`），同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的节点/connector/label 契约；Review 1 以 live/local fresh-load + browser QA 复核该 slice 已较前一轮更贴近中心 preview，但剩余最明显差距仍是 radial cluster 轻微 detached/crowded；Review 2 已通过 `npx vitest run src/app/chat/page.test.tsx src/app/page.test.tsx`、`npm test`、`npm run build`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/chat`；Review 3 已完成 diff-only 静态安全扫描（无 secrets / shell / eval / SQL 注入新增项）与 repo-scoped 独立 reviewer JSON 复核通过。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing header wordmark 的细粒度尺寸对齐，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live/public 与 rebuilt local 首页的 fresh-load 结构和 DOM 几何，确认当前可安全落地且真实可量化的热点已从 utility trigger 本体尺寸转为左上角 `SciClaw` wordmark 略大：live 约 `69.58px × 21.25px`，本地上一轮约 `82.27px × 27.34px`。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 wordmark 从 `text-[1.08rem] sm:text-[1.14rem] tracking-[-0.045em]` 收敛到 `text-[0.98rem] sm:text-[1.04rem] tracking-[-0.04em]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 锁定新合同，保持 hero、auth-card、Best Cases、`/chat` 共用 landing shell 与 `3001` 发布边界不变。Review 1：live/local sequential QA 与 fresh DOM 几何复核确认本轮后本地 wordmark 收敛到约 `76.36px × 24.95px`，较前一轮更接近 live 且未破坏顶部 utility row、hero 或整体 header 层级。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中，且本轮变更仅涉及样式 token 与对应 focused tests，同步复核未引入新的 secrets、shell/eval 风险或路由/契约漂移。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的 remaining cohesion/readability 差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 右侧 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude` 与 Docker `3001` 发布前置核验，再按 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential browser QA 锁定当前最小安全切片：仅将 auth-card 从 `228px` 微放宽至 `236px`、同步把 hero gap 从 `0.18/0.24rem` 收到 `0.14/0.18rem`、并轻压 tab pill 的圆角/阴影与卡内纵向节奏，使右侧卡片较上一轮更贴近中心 hero 而未引入 clipping。Review 1：live/local browser QA 复核 auth-card 无明显裁切，且较上一轮“更贴合但仍诚实保留轻微 detached 感”；Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build` 与 `docker compose up -d --build web` 全部通过，重建后 `curl -I http://127.0.0.1:3001/` 返回 200；Review 3：controller 侧 diff 静态安全扫描无 secrets/eval/shell 注入命中，独立 reviewer 也确认本轮仅为样式与测试合同同步、无新增安全或逻辑问题。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与本地几何/视觉 QA，确认本轮开始时最明显热点仍是 hero 右侧 auth-card 相对中心 preview 略偏重、略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步收敛 hero 右栏：将 `landing-hero` 的 `lg/xl` 右列从 `234px` 收至 `228px`、横向 gap 从 `0.1/0.16rem` 放宽到 `0.18/0.24rem`，并同步将 `landing-auth-card` 收至 `max-w-[228px]`、进一步减轻边框/阴影、轻收圆角与内边距、并把 `lg/xl` 顶部对位下压到 `1.92rem/2.1rem`，让 auth-card 从并列主模块回到更次级的辅助入口；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card class 合同。Review 1：fresh-load local browser vision 复核确认 auth-card balance 已改善，不再是首要视觉热点，新的单一主要热点转为左侧 radial cluster 相对中心 preview 仍略 detached。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `http://127.0.0.1:3001/` 全通过，且继续仅发布到 `3001`，未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新的安全或逻辑问题。下一轮继续优先围绕左侧 radial cluster 的 anchoring / anti-crowding 做更小步 fidelity 收敛，而不是再次回到 auth-card 或扩张新模块。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、live/local browser QA 与本地 DOM 几何，确认在本轮起始点上单一最明显剩余热点已从 radial cluster 重新收敛为右侧 auth-card：其相对中心 preview 仍略 detached、略偏右/偏高。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 对 auth-card 做更小步贴合收敛：将 `FeaturePreview` 的第二列从 `220px` 微放宽至 `224px`，同步把 `lg/xl gap-x` 从 `0.02/0.08rem` 收至 `0.01/0.06rem`，并把 auth wrapper 调整为 `lg:-ml-[0.1rem] / xl:-ml-[0.06rem]`、`lg:mt-[0.3rem] / xl:mt-[0.36rem]`、`lg:max-w-[14rem]`，同时将 `landing-auth-card` 外壳轻微放宽到 `214px`、圆角收至 `1rem`、边框/阴影/blur 进一步减重，使其更像紧贴中心 preview 的次级 support panel，而不是漂浮 sidebar；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview-grid、auth wrapper 与 auth-card class 合同。Review 1：fresh local browser QA 与重建后 DOM 几何一致确认 auth-card 已比上一轮更不 cramped、更贴近中心 preview；controller 几何显示 seam 继续保持 `0px`、auth 宽度从 `210px` 到 `214px`、但 top delta 仍约 `5.75px`，说明 detached 感已减弱但还未完全消失。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`、未影响 `/root/nexus-mail` 任意现有服务与端口。Review 3：`git diff --check` 通过、controller 侧 diff-only 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“可继续减少纯 Tailwind token 断言脆弱性”的非阻塞建议。下一轮继续优先围绕 auth-card 与中心 preview 的 residual vertical alignment / detached 感做更小步收敛，而不是扩张新模块或触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 右侧 auth-card 的 readability/composition 微收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_vision` 与本地 `browser_console` 几何确认当前单一最明显剩余热点仍是右侧 auth-card 相对中心 preview 略 detached，且 `VERIFY ACCESS CODE` 主按钮继续出现双行 wrapped/cramped 读感。本轮仅在 `src/components/landing-page.tsx` 中将 hero 右栏从 `280px` 微放宽到 `284px`、`lg/xl` gap 从 `0.08/0.14rem` 微收至 `0.06/0.12rem`，同步把 auth-card 外壳从 `268px` 放宽到 `272px`、内层 shell 从 `282px` 放宽到 `286px`，并将 disabled 主按钮收紧为 `px-3 + text-[0.8rem] + tracking-[0.08em]`，以在不改变既有 landing 结构的前提下优先缓解按钮换行与 auth-card detached 感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 合同。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照 + fresh-load local browser vision 确认当前仍是 auth-card detached + CTA wrapped 的单一热点，本轮后按钮仍略紧但较前更宽，页面未出现新的 clipping/overlap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“后续可补更高层视觉回归覆盖”的非阻塞建议）。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached 感、以及 `VERIFY ACCESS CODE` 按钮的 remaining wrapped 读感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 landing Phase B 文案显隐收敛 slice：controller 侧先顺序复核 live/local 首页与当前 worktree，确认当前最容易安全落地且直接提升首屏保真度的差距并非整体几何，而是本地 hero preview 中额外可见的 `Summary / Evidence / Preview` helper words 仍比 live 更显眼；因此本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 中左侧 summary 小卡显性 `Summary` 标题去除、右侧 evidence 区块顶部的 `Evidence / Preview` 文本替换为纯装饰线条并标记 `aria-hidden`，同时保持 `Research workspace preview`、`Autonomous Research`、柱状 micro-label row 与整体卡片结构不变；并同步将 `src/app/page.test.tsx` focused contract 更新为显式断言这些 helper words 在 hero 中不再可见，防止后续回归。该里程碑已通过 Review 1 功能/规格复核（确认仅是 landing hero preview 的显性 helper-word 去噪，未改动路由、auth 流程或 best-cases 结构）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。最新 controller-side 结论：本轮后本地首屏 preview 已更接近 live 的“骨架与数据感更强、辅助小词更弱”的状态，页面未见新的 clipping/overlap；下一轮继续优先沿 landing hero 的剩余视觉热点做更小步 fidelity 收敛，而不是回到 `/chat` workspace-like 扩张。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 landing Best Cases preview-card 去噪 slice：controller 侧顺序复核 live `/` 结构与本地 fresh-load landing，确认当前最直接且安全的高保真差距仍是 Best Cases 左侧深色 preview card 顶部仍可见 `Draft 01` tag，使本地比 live 多出一枚会抢注意力的 helper chip；因此本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 中该 tag 从可见文本收敛为 `aria-hidden` 的装饰骨架线条，同时同步补强 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式断言首页与 `/chat` 的 Best Cases 默认卡不再暴露 `Draft 01`，其余主标题、正文与 `01 / 04` 契约保持不变。该里程碑已通过 Review 1 功能/规格复核（确认 live `/` 与 `/chat` 仍是 marketing landing，本轮仅是 Best Cases preview 顶部 chip 去噪）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。最新 controller-side 结论：Best Cases 左侧 preview card 进一步向 live 的低辅助词/低标签干扰状态收敛，当前 landing 剩余 fidelity 热点仍以 radial cluster 与 preview 微密度为主，下一轮继续优先做 hero / carousel 的更小步高保真收敛，而不是扩张新模块。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：controller 侧先顺序复核 live/local 首页结构与顶部 utility trigger 几何，确认当前最可安全落地的高价值差距仍是顶部 utility dock 本体尺寸偏大——live 三枚 trigger 约 `38.25px`，本地上一轮仍约 `45.59px`。因此本轮不扩张新模块，也不回滚已稳定的 radial cluster / preview 结构，仅将共享 dock trigger 基础尺寸从 `h/w-[2.75rem]` 收至 `h/w-[2.35rem]`（`sm` 从 `2.85rem` 收至 `2.4rem`），并把共享 trigger gap 从 `0.46rem/0.62rem` 微收至 `0.42rem/0.53rem`，同步更新 `src/app/page.test.tsx` 断言锁定新几何契约，以让顶部 controls 更贴近 live 的 low-emphasis 圆形分组。该里程碑已通过 Review 1 功能/规格复核（确认仅为 landing 顶部 utility dock 尺寸/间距微收敛，未改动路由、菜单信息架构与 hero 主体）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build` 全通过；HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。最新 controller-side 结论：本轮后本地顶部三枚 trigger 几何约 `38.39px`，已与 live `38.25px` 基本对齐，页面未见新的 clipping/overlap；下一轮继续优先观察 live/local 顶部 dock 是否还需更细的对比/阴影微调，而不是回到更大尺度的 cluster 或新模块扩张。
- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 明确仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：controller 侧先顺序复核 live/local 首页结构与顶部 utility trigger 几何，确认当前更适合安全落地的差距已从 trigger 本体尺寸收敛为顶部 utility dock 相对 live 仍略偏下沉，因此本轮不扩张新模块，也不回滚已稳定的 radial cluster / preview 结构，仅将 `src/components/landing-page.tsx` 顶部 header padding 从 `pt-[0.72rem] / sm:pt-[0.92rem]` 微收至 `pt-[0.46rem] / sm:pt-[0.62rem]`，同步在 `src/app/page.test.tsx` 补强 header 顶部 spacing 契约的 focused 断言，以让本地三枚 utility controls 的纵向锚点更贴近 live（live trigger top 约 `10.63px`，本地本轮后约 `9.91px`）。该里程碑已通过 Review 1 功能/规格复核（确认仅为 landing 顶部 utility dock 的纵向定位微收敛，未改动路由、菜单信息架构与 hero 主体）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全通过；HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。补充说明：本轮 live/local `browser_vision` 仍返回空内容，已按降级流程改以 fresh `browser_snapshot`、`browser_console` 几何、build/HTTP 与 controller-side 事实作为验收依据，未将工具态异常误判为应用回归。最新 controller-side 结论：本地顶部 utility controls 的尺寸已与 live 基本齐平，当前纵向锚点也更接近 live，页面未见新的 clipping/overlap；下一轮继续优先观察 utility dock 的轻量 spacing/contrast 是否还需微调，而不是回到更大尺度的 radial cluster 或新模块扩张。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility controls 仍是低强调圆形控件、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：依据 sequential live/local browser QA，优先修正当前仍最明显的“顶部 controls 略偏过淡 + left radial cluster 文字/连线仍略 detached 且局部拥挤 + preview 底部 micro-label row 仍可再放松”三点，因此仅将共享 dock trigger 组从 `gap-[0.62rem]/sm:gap-[0.82rem]` 微放宽到 `gap-[0.66rem]/sm:gap-[0.86rem]`，并略收边框/阴影厚度同时保留更清晰图标前景，让顶部 controls 仍 subtle 但比上一轮更可辨识；再把三枚 radial side nodes 微向中心与 active preview 收到 `left-[48.2%] / left-[48.58%] / right-[15.45%]`，同步将 connector widths 扩到 `11.86% / 11.94% / 11.68%`，并把 inactive / active label 宽度统一收至 `172px / 146px`、前景提到 `#617987`，以加强 cluster→preview 锚定感并减少 label/connector 的局部拥挤；另外把 preview 底部 micro-label row 扩到 `gap-[4.76rem]/sm:gap-[4.94rem]`、略增横向 padding 并前移 top padding，继续缓解 `Meth. / Find. / Data / Evid.` 的紧张感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及 sequential live/local browser QA。最新 fresh-load local browser QA 认为顶部 utility controls 已比上一轮更 readable 但仍保持克制、preview 底部 labels 更舒展且无明显 overlap；当前最明显剩余差距仍是 left radial cluster 的文字/连线区域虽然更靠近中心 preview，但依旧是首屏最弱 cohesion/readability 热点，下一轮继续优先做 radial label hierarchy / anti-crowding，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：依据 fresh-load live/local browser QA 与 DOM 几何对比，仅对顶部 utility triggers 与 radial microcopy/readability 做收敛——将共享 dock menu triggers 间距从 `gap-[0.5rem] / sm:gap-[0.62rem]` 放宽到 `gap-[0.58rem] / sm:gap-[0.72rem]`，同时轻增边框/阴影/图标对比，让顶部 controls 仍保持 subtle 但不再过轻；并把 radial node labels 统一放宽至 `max-w-[124px]`、active 版 `max-w-[112px]`，同步把标签字号/leading/tracking 调整到 `text-[11px]`、`leading-[1.88]`、更轻 tracking，以减弱 cluster 内部微字发虚感；另外把 preview 底部 micro-label row 的柱图区高度从 `168px` 微收至 `164px`，增加 bar gap / horizontal padding，并将 label wrappers 提到 `min-h-[7.34rem]`、更轻 tracking / 更稳 leading，以继续降低 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为顶部 utility triggers 比 live 的过轻状态更易感知但仍保持克制，preview 底部 labels 较上一轮更可读；当前最明显剩余差距仍是 radial cluster 与中心 preview 的桥接感还略弱，cluster 整体相较中心 card 仍稍显 detached，下一轮继续优先做 cluster→preview cohesion，而不是再扩大顶部按钮存在感。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 仍比中心 preview 更轻、更像注释层而非强绑定模块、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：根据 fresh-load live/local browser QA 与 DOM 几何复核，将顶部 utility triggers 的共享 dock gap 从 `gap-[0.86rem] / sm:gap-[1.12rem]` 微收至 `gap-[0.82rem] / sm:gap-[1.08rem]`，并同步收轻阴影厚度、仅略提边框与图标对比，让顶部 controls 保持 subtle 但不再像悬浮覆盖层；同时把三枚 radial side nodes 进一步向中心 preview 收到 `left-[50.45%] / left-[51.2%] / right-[18.55%]`，将 connector widths 放宽到 `10.12% / 10.18% / 9.92%`，并把 radial labels 放宽到 inactive `max-w-[186px]` / active `max-w-[156px]`，配合 `gap-[0.92rem]` 与更稳的 `#587485` 前景色，以继续减弱 cluster 的 detached 感；另外把 preview 底部 micro-label row 扩到 `gap-[3.88rem] / sm:gap-[4.04rem]`、柱图区微收至 `156px`、label wrappers 提到 `min-h-[8.68rem]` / `max-w-[10.2rem]` / `leading-[1.22]`，继续缓解 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已按 TDD 更新 `src/app/page.test.tsx` 形成红灯后完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新 QA 认为 radial cluster 已较前更靠近中心 preview、utility controls 也更像低强调导航而非漂浮控件；当前最明显剩余差距仍是 radial cluster 的微字标签与底部 micro-label row 虽更稳但仍是首屏最弱可读点，下一轮继续优先收敛 radial label hierarchy / local anti-crowding，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 仍偏轻巧、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.78rem] / sm:gap-[1.02rem]` 微放宽到 `gap-[0.82rem] / sm:gap-[1.08rem]`，并同步收轻边框/阴影厚度、仅保留 subtle 的前景对比；同时把左上/左中/右侧 radial nodes 收到 `left-[56.15%] / left-[56.7%] / right-[17.3%]`，将 connector widths 放宽到 `7.12% / 7.18% / 6.98%`，并把 radial label 宽度提升到 `max-w-[144px]`、active `max-w-[126px]`，同步调整为 `text-[11.2px]`、`leading-[2.02]`、`tracking-[0.102em]` 与更亮的 `#42667b`，以在不打破首屏骨架的前提下提升 radial microcopy readability 与 cluster→preview bridge 感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、3 轮独立评审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 比上一轮更轻、更不抢主视觉，cluster 与中心 preview 的桥接感继续改善；当前最明显剩余差距仍是 radial cluster 的小字标签虽更清楚但依旧是首屏最弱可读点，下一轮继续优先做 radial label anti-crowding / local offset 微调，而不是扩张新模块。
- [~] 继续对齐头部、细节间距、轮播表现、图像/GIF 占位与 hover/active 状态；本轮已完成一个更小步的 landing hero fidelity slice：在不改动 `/root/nexus-mail` 任意服务/端口的前提下，仅于 `/root/sciclaw` 收紧 top utility triggers 的存在感与间距，轻推 radial cluster 靠近中心 preview 并增强 bridge 感，同时放松 preview 底部 micro labels 与正文下方 `Get started` 的垂直呼吸。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰现有业务端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审与 fresh-load live/local browser QA。最新 QA 认为 top utility triggers 更易感知、`Get started` 层级更干净、micro labels 比上一轮更不拥挤；当前最明显剩余差距收敛为 radial cluster 与主 preview 之间的横向桥接感仍略松，底部 micro labels 也还有轻微偏紧，下一轮继续优先做 cluster→preview cohesion 与 preview 内部微密度收敛，而不是大改整体骨架。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 仍是 subtle/lightweight 圆形 controls、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：基于 fresh-load live/local browser QA 与 DOM 几何复核，将共享 dock menu triggers 间距从 `gap-[0.94rem] / sm:gap-[1.24rem]` 微收到 `gap-[0.86rem] / sm:gap-[1.12rem]`，同步轻收按钮边框/阴影厚度但保留可辨识前景对比，使顶部三枚 utility controls 更接近 live 的 low-emphasis 紧凑分组；同时把三枚 radial side nodes 继续向中心 preview 收到 `left-[51.95%] / left-[52.55%] / right-[19.8%]`，并将 connector widths 放宽到 `9.44% / 9.5% / 9.22%`，再把 radial labels 统一放宽到 `max-w-[176px]`、active `max-w-[150px]`，同步调整为 `leading-[2.22]`、`tracking-[0.056em]` 与更亮的 `#617b8b`，以进一步减弱 detached 感并改善 microcopy readability；另外把 preview 底部 micro-label row 扩到 `gap-[3.5rem] / sm:gap-[3.64rem]`、label wrappers 提到 `min-h-[8.12rem]` / `max-w-[9.56rem]`，继续缓解 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 utility controls DOM 几何对比（local 45.59px vs live 38.25px，说明顶部控件仍略大于 live，但本轮重点已转回 cluster/readability）。最新 QA 认为 radial cluster 相对中心 preview 的 cohesion/readability 较上一轮确有改善、顶部 utility controls 仍保持 subtle；当前最明显剩余差距仍是 radial cluster 标签依旧比 live 更难读、preview 底部 micro labels 仍是最显眼未完全收敛的区域，下一轮继续优先做 bottom micro-label 密度与 radial cluster 标签层级，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 顶部 utility triggers 仍偏轻巧、不放大按钮本体的前提下，本轮对本地 landing hero 做了一次更聚焦的 cluster→preview cohesion 微调：将左上/左中节点分别从 `left-[60.9%]` / `left-[61.55%]` 收到 `left-[58.8%]` / `left-[59.55%]`，把右侧节点从 `right-[14.2%]` 收到 `right-[16.05%]`，并同步缩短三条连接线宽度（`10.55%→9.1%`、`10.9%→9.25%`、`10.72%→8.92%`），让 radial cluster 更靠近中心 preview、桥接感更强，同时维持 top utility triggers 的 subtle 尺寸与 hero→auth gutter 不失衡。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 radial cluster 现在更明显地桥接进中心 preview，未见明显 overlap/clipping，hero→auth gutter 仍可接受，top utility triggers 也仍保持适度克制；下一轮继续优先处理 preview 底部 micro labels 的轻微偏紧与首屏局部密度，而不是继续放大 utility controls。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 首屏 radial cluster 与中心 preview 之间仍略显松散、preview 底部 micro labels 也仍有轻微偏紧的前提下，本轮对本地 landing hero 做了一次更聚焦的 readability / cohesion 微调：仅在 `/root/sciclaw` 内将三条 radial connector 线的宽度进一步从 `9.1% / 9.25% / 8.92%` 收至 `8.42% / 8.54% / 8.28%` 并微调起点到 `50.22% / 50.2% / 50.08%`，以减弱 cluster→preview 之间的松散感；同时将 preview 底部 micro-label wrappers 从 `min-h-[6.48rem]` 提到 `min-h-[6.8rem]`、标签最大宽度从 `7.86rem` 放宽到 `8.34rem`、并轻收 tracking / line-height，让 `Meth. / Find. / Data / Evid.` 更易读。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任何现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中，以及双 reviewer 独立复审。最新 fresh-load local browser QA 认为 cluster→preview 关系与 micro-label 可读性均较上一轮有所改善，但 radial cluster 仍略显 detached、底部 label row 仍有轻微拥挤；下一轮继续优先做更小步的 cluster 锚定感与 micro-label 横向呼吸收敛，而不是改动更大骨架。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 首屏顶部 utility triggers 仍偏 subtle/lightweight、radial cluster 与中心 preview 仍存在可感知横向留白、preview 底部 micro labels 仍略显紧的前提下，本轮仅在 `/root/sciclaw` 内继续推进 landing Phase B 的小步 fidelity slice：把左上/左中/右侧三个 radial nodes 进一步向中心分别收至 `left-[57.7%]` / `left-[58.5%]` / `right-[17.1%]`，并把三条 connector widths 从 `8.42% / 8.54% / 8.28%` 缩到 `7.78% / 7.92% / 7.72%`，以增强 cluster→preview bridge 感而不放大顶部 utility 按钮；同时把 preview 底部 micro-label wrappers 从 `min-h-[6.8rem]` 提到 `min-h-[7.16rem]`、标签最大宽度从 `8.34rem` 放宽到 `8.68rem`，并进一步收紧字号/leading/tracking，减少 `Meth. / Find. / Data / Evid.` 的拥挤感且保持无 overlap。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 仍保持轻巧、cluster 比上一轮更靠近并更像在桥接中心 preview、底部 micro labels 也更少拥挤且未见 overlap；当前剩余最明显差距仍是 cluster 与 preview 之间仍有可见横向留白、micro labels 虽更稳但仍偏小，下一轮继续优先做更小步的 cluster 锚定感与 preview 底部呼吸优化，而不是改动更大骨架。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 顶部 utility triggers 仍偏 subtle、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：仅对顶部 utility trigger 存在感、radial cluster→preview bridge，以及 preview 底部 micro-label breathing room 做收敛——将共享 dock menu trigger 间距从 `gap-[0.58rem] / sm:gap-[0.72rem]` 轻放宽到 `gap-[0.62rem] / sm:gap-[0.8rem]`，并同步轻增边框/阴影/前景对比，让顶部三枚 utility controls 仍保持 subtle 但更易辨识；同时把三枚 radial nodes 进一步向中心 preview 收到 `left-[59.6%] / left-[60.2%] / right-[15.5%]`，并把三条 connector widths 放宽至 `8.52% / 8.58% / 8.44%`，强化 cluster→preview bridge 感；另外把 preview 底部 micro-label row 的列间距从 `2.86rem/2.98rem` 放宽到 `3.04rem/3.18rem`，并将 label wrappers 提到 `min-h-[7.48rem]`、`leading-[1.22]`、`max-w-[8.94rem]`，让 `Meth. / Find. / Data / Evid.` 更易读。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任何现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为顶部 utility triggers 较上一轮更可见、radial cluster 与中心 preview 的桥接感更清楚、底部 micro labels 可读性继续改善且无明显 overlap；当前最明显剩余差距仍是 radial cluster 的小字标签相对 live 仍略显 cramped、cluster→preview 的连接更像 implied bridge 而非强锚定，下一轮继续优先收敛 radial labels 清晰度与 cluster→preview cohesion，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 顶部 utility triggers 仍是 small-to-medium 的轻量圆形 controls、fresh-load live 视觉上 radial cluster 与中心 preview 依旧偏 detached、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu trigger 间距从 `gap-[0.62rem] / sm:gap-[0.8rem]` 微调到 `gap-[0.68rem] / sm:gap-[0.9rem]`，并轻收按钮阴影厚度、细增边框/前景对比，让顶部 utility controls 保持 subtle 但更接近 live 的轻巧分组感；同时把三枚 radial nodes 进一步向中心 preview 收到 `left-[58.9%] / left-[59.45%] / right-[16.35%]`，并将 connector widths 收到 `7.94% / 7.98% / 7.86%`，以减少 cluster 与 preview 的横向断裂感；另外把 radial labels 放宽为 `max-w-[130px]` / active `max-w-[116px]`，并调整到 `leading-[1.94]` + 更轻 tracking，同时把 preview 底部 micro-label row 扩到 `gap-[3.22rem] / sm:gap-[3.34rem]`、chart 柱图区微收至 `162px`、label wrappers 提到 `min-h-[7.72rem]`、`max-w-[9.16rem]`、`leading-[1.18]`，继续改善 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 仍略比 live 更显眼但分组更稳、preview 底部 micro labels 可读性继续改善且无 overlap；当前最明显剩余差距仍是 radial cluster 虽较前更靠近中心 preview，但整体桥接感依旧偏弱、仍更像独立侧边模块，下一轮继续优先收敛 cluster→preview cohesion，而不是放大顶部按钮或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为低强调圆形控件、strictly 只操作 `/root/sciclaw` 且继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：根据 fresh-load live/local browser QA 与 DOM 几何对比，将共享 dock menu triggers 间距从 `gap-[0.68rem] / sm:gap-[0.9rem]` 微放宽到 `gap-[0.74rem] / sm:gap-[0.98rem]`，并轻微提高边框/图标对比、同时收轻阴影厚度，让顶部 utility controls 更接近 live 的 subtle-but-readable 状态；同时将 radial cluster 三枚侧节点进一步向中心 preview 收到 `left-[57.8%] / left-[58.35%] / right-[17.3%]`，并把 connector widths 收到 `7.26% / 7.3% / 7.18%`，以继续减弱 detached 感、加强 cluster→preview 的横向锚定。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为顶部 utility triggers 已从过轻改善到 subtle 但可辨识，cluster 与中心 preview 的锚定感较上一轮更强；当前最明显剩余差距仍是 radial cluster 局部 microcopy / label 与连线区域在浏览器视觉上仍略显拥挤，下一轮继续优先做 radial label readability / anti-crowding 收敛，而不是继续放大顶部按钮或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 相对中心 preview 仍偏 detached、且本轮严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：仅对顶部 utility trigger 的 subtle readability 与 radial cluster→preview cohesion 做收敛——将共享 dock menu triggers 间距从 `gap-[0.74rem] / sm:gap-[0.98rem]` 微放宽到 `gap-[0.78rem] / sm:gap-[1.02rem]`，同时轻收阴影厚度并提升边框/前景对比，让顶部 controls 更接近 live 的 low-emphasis 但可辨识状态；再把三枚 radial side nodes 收到 `left-[56.9%] / left-[57.45%] / right-[18.1%]`，将 connector widths 收到 `6.74% / 6.82% / 6.64%`，并把 node label gap 调到 `0.8rem`、label 宽度放宽到 `max-w-[138px]` / active `max-w-[122px]`，同步将 radial label typography 调为 `leading-[1.98]` + `tracking-[0.112em]`，以减弱 detached 感并提升 `Literature Analysis / Data Mining / Outcome Present` 的可读性。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA 与 DOM 几何复核。最新 QA 认为 radial cluster 已更靠近中心 preview、顶部 utility controls 也仍保持 subtle 但更易辨识；当前最明显剩余差距仍是 radial cluster 整体虽更贴近中心但局部标签依然略显 cramped，下一轮继续优先做 radial microcopy 的 anti-crowding / hierarchy 收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为 readable-but-subtle 的轻量圆形 controls、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.82rem] / sm:gap-[1.08rem]` 放宽到 `gap-[0.88rem] / sm:gap-[1.16rem]`，同步微增边框/图标对比并收轻阴影厚度，让顶部 utility controls 保持 subtle 但更易辨识；同时把三枚 radial side nodes 收到 `left-[55.35%] / left-[55.95%] / right-[18.1%]`，将 connector widths 放宽到 `7.88% / 7.94% / 7.72%`，并把 radial labels 统一放宽至 `max-w-[150px]`、active `max-w-[130px]`，同步调整为 `text-[11.4px]`、`leading-[2.08]`、`tracking-[0.086em]` 与更亮的 `#4f6d82`，以继续减弱 detached 感并改善 cluster microcopy readability。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 已从偏轻提升到 readable-but-subtle，cluster 与中心 preview 的桥接感继续改善；当前最明显剩余差距仍是 radial cluster 微字标签依旧是首屏最弱可读点，且 cluster→preview 仍有可见留白，下一轮继续优先做 radial label anti-crowding 与 cluster→preview cohesion，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 与中心 preview 的关系在视觉上仍是“略 detached 而非强绑定”、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：保持顶部 utility trigger 组仍为 subtle 的轻量圆形 controls，仅轻微收敛边框/阴影到 `border-[rgba(7,25,39,0.5)]`、`text-[#102b3e]` 与更浅 shadow；同时把三枚 radial side nodes 继续向中心 preview 微收至 `left-[54.7%] / left-[55.2%] / right-[18.9%]`，并将 connector widths 放宽到 `8.26% / 8.32% / 8.12%`，配合统一 `gap-[0.76rem]`、inactive `max-w-[156px]` / active `max-w-[136px]` 与 `text-[11.2px]`、`leading-[2.14]`、`tracking-[0.074em]` 的更稳标签排版，以减少 radial microcopy 的 cramped 感并加强 cluster→preview bridge。该里程碑已先用 TDD 将 `src/app/page.test.tsx` 调整为红灯（断言新位置/connector/label 宽度与排版），随后完成实现并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、双独立 reviewer 复审，以及 fresh-load local browser QA 与 DOM 几何复核。最新 QA 认为 radial labels 较上一轮略不拥挤、cluster 与中心 preview 的关系更 cohesive，且未见明显 clipping/overlap；当前最明显剩余差距仍是左侧 radial cluster 作为整页最密区域，距离 live 的“更像环境式导航而非强绑定模块”还有细微拿捏空间，下一轮继续优先做 radial microcopy / connector 的更小步桥接收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为更轻巧的 low-emphasis 圆形控件、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.88rem] / sm:gap-[1.16rem]` 放宽到 `gap-[0.94rem] / sm:gap-[1.24rem]`，同时把 radial side nodes 收到 `left-[53.95%] / left-[54.5%] / right-[19.7%]`、connector widths 放宽到 `8.72% / 8.8% / 8.58%`，并把 radial label 宽度/排版调整到 inactive `max-w-[162px]`、active `max-w-[142px]`、`text-[11.3px]`、`leading-[2.18]`、`tracking-[0.064em]`；另外把 preview 底部 micro-label row 微放宽为 `gap-[3.3rem] / sm:gap-[3.42rem]`、柱图区 `160px`、label wrappers `min-h-[7.92rem]` + `max-w-[9.32rem]`，继续减轻 `Meth. / Find. / Data / Evid.` 的拥挤感。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为顶部 utility triggers 仍略大于 live 但已更接近 subtle/readable 的轻量状态，preview 底部 labels 更稳且未见明显 overlap；当前最明显剩余差距仍是 radial cluster 与中心 preview 的桥接感虽改善但仍略显 detached，cluster 文案也是首屏最容易显得拥挤的区域，下一轮继续优先做 cluster→preview cohesion 与 radial microcopy anti-crowding，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 顶部 utility triggers 为 subtle/readable 的轻量圆形控件、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：仅对 radial cluster→preview cohesion 与 radial microcopy anti-crowding 做收敛——将三枚 radial side nodes 进一步向中心 preview 收到 `left-[52.9%] / left-[53.5%] / right-[20.7%]`，同步把 connector widths 放宽到 `9.12% / 9.18% / 8.96%`、微调角度/起点，让 cluster 更像桥接到中心 preview 而非漂浮注释；同时把 radial label 宽度从 inactive `max-w-[162px]` / active `max-w-[142px]` 放宽到 `max-w-[168px]` / `max-w-[146px]`，并将标签前景提升到 `#5b7587` 以减弱小字发虚感。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部 utility triggers 仍保持 subtle 且可辨识，中心 preview/右侧 auth card 也未见明显 clipping；当前最明显剩余差距仍是 radial cluster 的局部标签仍略显拥挤，整体与中心 preview 的桥接感虽然更强但依旧是首屏最弱 cohesion 点，下一轮继续优先做 radial label spacing / hierarchy 与 cluster→preview 锚定感收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 相对中心 preview 仍略 detached、而 preview 底部 micro-label row 仍是首屏最显眼微字弱点、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.82rem] / sm:gap-[1.08rem]` 微收至 `gap-[0.78rem] / sm:gap-[1.02rem]`，并同步轻增边框/图标对比、收轻阴影厚度，让顶部 utility controls 保持 subtle 但更靠近 live 的轻量分组；同时把三枚 radial side nodes 继续向中心 preview 收到 `left-[49.35%] / left-[50%] / right-[17.35%]`，并把 connector widths 放宽到 `11.04% / 11.12% / 10.84%`，以进一步减弱 cluster→preview 的 detached 感；另外把 preview 底部 micro-label row 扩到 `gap-[4.12rem] / sm:gap-[4.28rem]`，将柱图区微收至 `152px`，并把 label wrappers 提到 `min-h-[9.12rem]`、`text-[14.16px]`、`leading-[1.18]`、`max-w-[10.8rem]`，继续改善 `Meth. / Find. / Data / Evid.` 的可读性。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 22 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何对比。最新 QA 认为 radial cluster 比上一轮更少 detached、顶部 utility controls 仍保持 subtle 且无明显 clipping/overlap；当前最明显剩余差距仍是 preview 底部 micro-label row 虽已更稳但仍是首屏最显眼微字区，且 radial cluster 局部小字/连线区域仍略显忙，下一轮继续优先做 bottom micro-label 结构与 radial label anti-crowding 收敛，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 下左侧 radial cluster 相较中心 preview 仍略 detached、preview 底部 micro-label row 仍是首屏最弱可读区、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：将共享 dock menu triggers 间距从 `gap-[0.78rem] / sm:gap-[1.02rem]` 微收至 `gap-[0.74rem] / sm:gap-[0.98rem]`，保持顶部 utility controls subtle；同时把三枚 radial side nodes 收到 `left-[48.6%] / left-[49.25%] / right-[16.55%]`，并把 connector widths 放宽到 `11.64% / 11.72% / 11.46%`，配合更宽的 inactive `max-w-[198px]` / active `max-w-[168px]` 标签与 `text-[11.2px]`、`leading-[2.16]`、`tracking-[0.048em]` 的更稳排版，以继续增强 cluster→preview bridge 感并减弱微字拥挤；另外把 preview 底部 micro-label row 扩到 `gap-[4.38rem] / sm:gap-[4.56rem]`、柱图区微收至 `150px`、label wrappers 提到 `min-h-[9.46rem]`、`max-w-[11.24rem]`、`leading-[1.2]` 与更轻 tracking，继续放宽 `Meth. / Find. / Data / Evid.` 的呼吸空间。该里程碑已按 TDD 先更新 `src/app/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新 QA 认为顶部 utility triggers 仍保持 subtle、radial cluster 相较前一轮更靠近中心 preview，但整体桥接感依然弱于 live；preview 底部 micro labels 虽更宽松，仍被 fresh-load browser QA 视为略偏紧。下一轮继续优先做 radial cluster 的锚定/降噪，而不是再扩大顶部按钮存在感。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` fresh-load 首屏最明显剩余差距已由 controller-side live/local browser QA 收敛到 radial cluster 本身、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 landing Phase B 的一个更小步 fidelity slice：不扩张新模块，仅针对 left radial cluster 的 anti-crowding / bridge spacing 做收敛——将共享 dock utility triggers 间距从 `gap-[0.74rem] / sm:gap-[0.98rem]` 微收至 `gap-[0.7rem] / sm:gap-[0.9rem]`，同时把三枚 radial side nodes 进一步外移并拉开垂直距离到 `left-[47.1%] top-[11.9%]`、`left-[47.55%] top-[49.85%]`、`right-[15.15%] top-[11.1%]`，配合 connector widths `12.26% / 12.34% / 12.08%`、节点内部 label gap `1.08rem`，以及 inactive `max-w-[188px]` / active `max-w-[160px]` 的标签宽度收敛，优先缓解 radial labels 与 connectors 的视觉碰撞，而不再追逐顶部按钮存在感或 preview 底部 micro-label row。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新结论：bottom micro-label row 仍可继续微调，但本轮之后首要剩余问题已更明确地集中在 radial cluster 作为整页最拥挤区域的 spacing / anchoring；下一轮继续优先做 radial label hierarchy、connector 终点留白与 cluster→preview 锚定感，而不是扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-top summary slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并通过 fresh-load local browser QA 确认当前顶部三张 summary cards 的第三卡此前仍像 `Foundry` handoff、导致 center top band 与右侧 output rail 语义过近，因此本轮不扩张新模块，仅把第三张 summary card 从 `Foundry / Answer handoff / Packet staged. / 12 ready` 收敛为更偏 workflow 的 `Tasks / Quieter edge lane / One blocker routed. / 1 blocker`，同步把该 top summary band 的 `xl` 固定列宽从 `10.95rem` 微放宽到 `11.3rem`，并补强 `src/app/chat/page.test.tsx` focused 断言，锁定第三卡已转为 task-lane 语义且顶栏宽度同步更新。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为顶部三张 summary cards 现在更 calm，第三卡已明显读作 task/edge-lane card；当前单一最明显 density hotspot 已转移到右侧 `Session output / Status board / Foundry handoff` 纵向 stack，下一轮继续优先减少该 right rail 的 stacked microcopy / badge 竞争，而不是回到 center top summary band 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Active sessions / task-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并结合 fresh-load local browser vision 确认当前页面整体 coherent、无明显 clipping/overlap，但单一最明显剩余热点已收敛到 center Session timeline 中偏右的 `Active sessions` 行与相邻 task stack，尤其 `12m / Map`、`1 blocker / Queue`、`Ready / Packet` 等 meta pills 与 task meta 在窄轨中仍略显压缩。因此本轮不扩张新模块，仅将 `Active sessions` rows 的 `sm:grid-cols-[minmax(0,1fr)_minmax(4.2rem,auto)]` 微放宽到 `4.6rem`、同步把右侧 meta stack 最大宽度提到 `4.9rem` 并把 row gap 调到 `2.5`，同时把 task rows 的 badge gap 微放宽到 `2.5`、说明文案约束到 `max-w-[26ch]`，以让 center-lane meta tokens 更少争抢横向空间；focused test 也同步补强为逐行断言 `12m/Map`、`1 blocker/Queue`、`Ready/Packet` 和新的 grid class，避免后续密度收敛误伤真实 session-lane contract。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为页面仍 coherent、无明显 breakage；当前单一最明显剩余热点仍是 center Session timeline 偏右的窄 task/meta stack，而 right rail 已退居次级压力区。下一轮继续优先减少 center timeline/task stack 的 stacked badges 与说明竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并经 fresh-load local browser vision 二次复核确认 right rail 仍是单一最明显 density hotspot、但近期收敛已使其与 center lane 更接近，因此本轮不扩张新模块，仅对 `Session output / Status board / Foundry handoff` 做更小步的结构减压——将 `chat-right-rail-grid` 从 `xl:[minmax(0,1fr)_minmax(0,0.98fr)] / 2xl:[minmax(0,1fr)_minmax(288px,0.88fr)]` 调整为 `xl:[minmax(0,1.06fr)_minmax(272px,0.92fr)] / 2xl:[minmax(0,1.08fr)_minmax(284px,0.9fr)]`，同步收短 `Session output` 内卡与 `Status board` / `Foundry handoff` 的垂直内边距、标题说明最大宽度、signal gap 与 export row 包裹宽度，使右栏更像连续 summary rail 而不是三段过厚的窄卡堆叠；focused test 也同步锁定新的 right-rail grid 宽度与 Foundry/export 文案 contract。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为 right rail 仍是 `/chat` 首要 density hotspot，但 `Session output / Status board / Foundry handoff` 已比上一轮更 calm、无明显 clipping/overlap，当前 center lane 虽更接近但尚未反超为更差热点；下一轮继续优先减少 right rail 的 stacked module/label 竞争，而不是回到单纯 widening center lane 或扩张新模块。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero helper-microcopy 去噪收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/` fresh-load 仍为 marketing landing，并结合 `browser_snapshot`、`document.body.innerText` 与本地 `http://127.0.0.1:3001/` fresh-load 结构确认当前公开 hero 默认态中真正保留的底部微标签只有 `方法论 / 结论 / 数据`，而左侧两张浅色 summary 小卡额外重复暴露的 `方法论 / 结论` 会让本地比 live 更 noisy。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 中这两张 summary 小卡从可见文字标签收敛为 `aria-hidden` 装饰骨架线条，同时同步更新 `src/app/page.test.tsx`，把过时的重复文本计数断言改为只验证默认 hero 仍保留核心中文微标签且不再要求左侧重复词可见，从而让本地 `/` 与 `/chat` 的 hero 默认态更贴近 live 的低辅助词合同；`Best Cases`、auth-card、帮助路由与 `3001` 端口发布策略保持不变。该里程碑已通过 Review 1 功能/规格复核（live `/` 仍是 marketing landing，且 fresh-load 本地 DOM/快照已确认 hero 默认态收敛为 `方法论 / 结论 / 数据` 单组可见微标签，更接近 live）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`），Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中，并完成 repo-scoped 独立 reviewer 复审）。下一轮继续优先围绕 public landing 的 hero / carousel / auth-card 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的公开 apply-now 合同对齐 slice：controller 侧先顺序复核 live `/` 点击 `No account yet? Apply Now →` 后会在当前 landing 内联切换到 early-access 申请表单，而不是跳去 `/privacy`；据此仅在 `src/components/landing-page.tsx` 将 auth 卡从二态扩展为 `onboard/login/apply` 内联模式，新增 early-access 申请表单文案、邮箱/验证码/研究用途输入、禁用态 `SEND CODE` 与 `ENTER YOUR EMAIL FIRST`、`back` 返回路径，以及点击 apply 时同步把 Best Cases 切到 `02 / 04` 的 live 合同；同时保留 `/` 与 `/chat` 共用 `LandingPage`，并补上 `src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 对 apply-flow 与返回前一 auth 模式的 focused 覆盖。该 slice 已完成三轮 review：功能上本地 browser 点击 `Apply Now` 后可见 `Leave your email and intended use case...` 内联表单且隐藏原 CTA；测试/构建上 `pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f docker-compose.yml config`、`docker compose -f docker-compose.yml up -d --build`、HTTP 200(`/` `/chat` `/help/getting-started`) 全绿；静态/安全与独立审查上已通过 controller-side added-lines 扫描与 repo-scoped reviewer 复审，且额外修复了 apply 态 tab 语义与 back 返回上一个 auth 模式的问题，当前剩余工作继续回到 public landing 其他 fidelity 差距。


- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero 中心节点文案去噪收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 live `https://sciclaw.ai/chat` fresh-load 仍为同一 marketing landing，并结合 local `http://127.0.0.1:3001/` 的 fresh-load snapshot、`document.body.innerText` 与当前 diff 确认当前最直接、最安全的高保真差距已收敛到 hero 中心按钮下方仍显性暴露第二处 `Literature Analysis` 文案——live 公开 landing 在默认态仅保留中心圆形按钮与外圈节点标签，而本地此前还额外显示一条中心标签文字，造成首屏中心区比 live 更 busy。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 中心按钮下方的可见 `centerNode.label` 替换为 `aria-hidden` 装饰细线，保持中心按钮本身的 `aria-label`、hero 主 heading、`方法论 / 结论 / 数据`、auth-card、Best Cases、`/chat` 复用 landing shell 与 `3001` 端口发布策略不变，使 `/` 与 `/chat` 的默认 hero 更贴近 live 的低辅助词合同。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 live `/chat` 仍为 marketing landing，本地 fresh-load 结构现已去除中心重复可见标签且未改变公开路由/IA）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing 的 hero / carousel / auth-card 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 `/chat` get-started hero microcopy 合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/chat` 与 local `http://127.0.0.1:3001/chat` 的 fresh-load / get-started 状态，并用 `browser_snapshot`、`browser_console(document.body.innerText)` 与 focused diff 事实确认当前 live `/chat` 在点击 `Get started` 后，hero 预览区并不再显示 `Plan / Tools / Results / Reports` 四枚标签，而是收敛为单一可见 `33%` 指标；本地此前仍保留旧的英文四标签，导致 `/chat` get-started 态与 live 合同不一致。因此本轮仅将 `src/components/landing-page.tsx` 中 `autonomous-execution` 预览卡的可见 `chartLabels` 收敛为 `33%`，并同步更新 `src/app/chat/page.test.tsx`，显式断言 `/chat` get-started 后出现 `33%` 且不再暴露 `Plan / Tools / Results / Reports`，保持 `/` fresh-load 默认 hero、Best Cases、auth-card、help 路由与 `3001` 端口发布策略不变。该里程碑已通过 Review 1 功能/规格复核（live `/chat` 仍是 marketing landing，本轮仅修正 get-started 后 autonomous hero 的 microcopy 合同，不扩张模块）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero eyebrow 语义合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为同一 marketing landing，并用 `browser_snapshot` / `browser_console(document.body.innerText)` / 本地 `http://127.0.0.1:3001/`、`/chat` 对照确认 live 默认 hero 中 `Autonomous Research` 只是与 `Deep Literature Analysis` 并列的 lightweight eyebrow 文本，而不是额外段落。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 中 preview bodyLabel 从 `<p>` 收敛为 `<span>`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式锁定该 public eyebrow 在 fresh-load `/` 与 `/chat` 中作为 `SPAN` 呈现的当前 DOM 合同；hero heading、Get started 切换、Best Cases、apply-now、help 路由与 `3001` 发布策略保持不变。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 `/chat` 顺序复核确认这是 public eyebrow 语义对齐的小步 slice，而非新模块扩张）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、HTTP 200 校验 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过；其唯一建议是该断言偏 DOM 细节、可后续再评估是否改为更行为化的 contract）。下一轮继续优先围绕 public landing / help 的细粒度高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 Best Cases preview-card 去噪收敛 slice：controller 侧顺序复核 live/local 首页默认态与 `Get started` 后状态，确认 live 深色 preview card 仅承担装饰性结构与图表感，而本地此前仍在左侧 dark preview 中显性重复 `summaryTitle/summaryText`，造成 Best Cases 区块比 live 更 noisy。基于该事实，本轮仅将 `src/components/landing-page.tsx` 中 left preview card 的标题与长段落收敛为 `aria-hidden` 骨架线条，保留右侧真实 slide 标题/正文、轮播状态与 `/chat` 复用 landing 合同不变；同时把 `src/app/page.test.tsx` 中对默认 slide 描述的断言收紧为 `bestCasesSection` 范围内的可见合同，避免继续依赖左/右双份重复文案。该里程碑已通过 focused Vitest、lint、build、`docker compose config`、容器重建、`/` `/chat` `/help/getting-started` HTTP 200 校验、静态安全扫描、三轮评审与 repo-scoped 独立 reviewer 复核；最新 controller-side 结论是 Best Cases 左侧 dark preview 现已更接近 live 的低辅助词/低重复文案状态，下一轮继续优先沿 public landing hero / carousel / auth-card 的细粒度保真差距推进，而不是扩张新模块。

- [~] 执行检查点：在继续保持 fresh-load live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 landing Phase B hero radial-node 语义对齐 slice：controller 侧先顺序复核 live `/`、live `/chat` 与 local `/` 的 fresh-load 结构，并结合 live/local DOM 检查确认 live 公共 landing 的三枚外侧 radial feature labels（`Literature Analysis / Data Mining / Outcome Present`）并不包裹在各自圆形 icon button 的可访问名称内，而是作为按钮外侧的独立可见标签出现；本地上一轮实现仍把标签文本放在按钮内部，导致 snapshot/语义层出现 `button "文 Literature Analysis"` 这类过重命名，不利于继续向 live 的低噪声 hero cluster 收敛。因此本轮不扩张新模块，仅在 `src/components/landing-page.tsx` 中将 radial side nodes 重构为“外层 absolute wrapper + 内层 icon-only button + 外侧独立 label”的更贴近 live 结构，并补充 `feature-rotator` / `feature-node-*` 语义锚点；同步更新 `src/app/page.test.tsx`，把相关 focused contract 从旧的按钮内混合文本断言切换为 icon-button 的 `aria-label`、wrapper 几何类名与外侧 label 所属关系断言，保持 `Get started`、auth-card、Best Cases、`/chat` 共享 landing shell 与 `3001` 端口边界不变。该里程碑已通过 Review 1 功能/规格复核（确认仅为 public landing hero radial-node 语义/结构对齐，不改路由、auth 流程与轮播逻辑）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、controller-side live diff 审阅确认未引入 secrets / eval / shell 注入 / SQL 拼接风险）。补充说明：本轮 `browser_vision` 再次返回空内容，已按降级流程改以 fresh `browser_snapshot`、`browser_console` DOM 事实、focused tests、build、compose 与 HTTP 结果作为本地验收依据，未将工具态噪声误判为应用回归。最新 controller-side 结论：本地 radial side nodes 的按钮/标签语义已更贴近 live，后续 Phase B 可继续优先沿 hero cluster 的剩余视觉密度与标签层级做小步保真收敛，而不是扩张新模块。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero radial icon-only 语义合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 live `https://sciclaw.ai/chat` fresh-load 仍为同一 marketing landing，并通过 live/local `browser_snapshot`、`document.body.innerText` 与本地 `http://127.0.0.1:3001/` 复核确认当前公开 contract 中三枚外侧 radial nodes 与中心 node 应保持 icon-only decorative marks + 外侧英文标签，而不再暴露本地旧版的中文单字 glyph（`文 / 数 / 果 / 研`）作为可见文字。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 的 radial side nodes 与 center-node fallback icons 从中文字符收敛为更接近 live 的装饰性 icon-only spans / strokes，并保持既有 `aria-label`、交互、`/chat` 复用 landing shell、Best Cases、auth-card 与 `3001` 发布策略不变；其间按 fail-closed review 流程先后修复了独立 reviewer 指出的两个 display 问题（icon span 缺少 `inline-block` 可能导致尺寸不生效），随后重新完成验证。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 `/chat` 顺序复核确认本轮仅是 public hero radial icon-only 合同收敛，不扩张模块）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 第三轮 JSON 复审通过）。最新 controller-side 结论：本地 public landing hero 已不再暴露旧的中文 glyph，right rail / help shell / `/chat` marketing shell 合同保持稳定；下一轮继续优先围绕 public landing 与 help 的细粒度高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。


- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing auth-card tabpanel 语义对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 `browser_console` 直接检查 auth tablist / tabpanel DOM，确认 live 公开 landing 的 tablist 当前不带额外 `aria-label`，tabpanel 也仅通过 `aria-labelledby` 指向选中 tab，而本地 `src/components/landing-page.tsx` 仍额外写入 `aria-label=Onboard/Login/Apply`，使 auth-card 语义比 live 更重。基于该事实，本轮仅移除 auth-card tabpanel 容器的额外 `aria-label`，并同步将 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 断言从依赖 `getByRole("tabpanel", { name: ... })` 收敛为验证实际可见表单合同（onboard 提示文案、login 邮箱输入仍可达），保持 tablist、tab 选中态、apply/back 流程、hero / Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（live/local DOM 对照确认本地已去掉多余 tabpanel accessible name，且未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅给出“未来可补一条 tabpanel accessible-name 回归测试”的非阻塞建议）。下一轮继续优先围绕 public landing hero / auth-card 的剩余 live 语义与视觉微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 的 detached/clipped 热点收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_console` 几何与 local `browser_vision` 确认当前单一最明显剩余问题仍是左侧 radial cluster 标签与 connectors 被中心 preview 部分切入、整体阅读上偏 detached。本轮仅在 `src/components/landing-page.tsx` 中把三枚外侧 radial nodes 向中心 preview 小步回收（`left-[43.4%] / left-[43.9%] / right-[10.2%]`），同步把 connectors 收敛到 `15.7% / 15.65% / 15.45%`，只做 cluster→preview 的更小步 anti-detached / anti-occlusion 收敛；同时补强 `src/app/page.test.tsx` 锁定新的 node/connector 几何合同，保持顶部 utility 按钮尺寸、hero 主 heading/body、auth-card、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序复核确认顶部 utility row 继续与 live 接近，local browser QA 认为 radial cluster 仍是最大热点，但较上一轮更贴近中心 preview、桥接更明确，且未引入新的严重 clipping/overlap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“后续可用更高层视觉回归覆盖 Tailwind 几何断言”的非阻塞建议）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余遮挡/留白细节推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 的 label/connector coherence 收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再按 systematic-debugging 流程顺序复核 live `https://sciclaw.ai/chat` 与 rebuilt local `http://127.0.0.1:3001/chat` 的 fresh-load 结构、`browser_snapshot`、`browser_console` 几何与 `browser_vision`。root-cause 证据显示上一轮 radial cluster 的 detached/clipped 热点虽有所缓解，但左侧 `Literature Analysis / Data Mining / Outcome Present` 标签仍因最大宽度过窄、与节点/connector 的距离略远而继续成为单一最弱 cohesion 点；本轮因此仅在 `src/components/landing-page.tsx` 小步收敛 radial annotation system：把三枚 side-label 的最大宽度统一从 `76/80px` 放宽到 `88px`、将 active label 颜色/阴影轻收、把左上与右侧标签向节点各内收约 `0.22–0.5rem`、将 `Data Mining` 标签下移到 `calc(100%+0.72rem)`，并同步将中下/右上节点与 connector 宽度收至 `left-[46.45%] / right-[15.95%] / w-[12.45%] / w-[11.6%]` 一组更贴近中心 preview 的小步几何；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper/connector/label class 合同。Review 1：live/local fresh-load 对照与 rebuild 后 local browser QA 一致确认 radial cluster 已比上一轮更 coherent，不再是最明显 detached 热点，但新的单一最弱区域已转为右侧 auth-card 的独立感，因此本轮 radial 收敛是有效的小步前进。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、`http://127.0.0.1:3001/chat` HTTP 200 校验与 `git diff --check` 全部通过，继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口。Review 3：当前 diff 新增行静态安全扫描 0 命中，未引入 secrets / eval / shell 注入 / SQL 拼接；repo-scoped 独立 reviewer 复审后也未发现新的逻辑或安全阻断。下一轮继续优先围绕 public landing hero 右侧 auth-card 的 detached / contrast 小步收敛推进，而不是扩张新模块或触碰 `/root/nexus-mail`。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 `Get started` CTA 细节拟真，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 `browser_snapshot`、`browser_console` 几何与 `browser_vision` 确认当前最小且真实的剩余差距之一是 live hero CTA 仍呈现文字 + 小号 downward chevron，而本地 `src/components/landing-page.tsx` 之前仅渲染纯文本 `Get started`。基于该事实，本轮仅新增 `DownChevronIcon` 并将 hero CTA 收敛为 `inline-flex items-center gap-2` + 小号下箭头，同时同步补强 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式锁定 `/` 与 `/chat` 共享 landing shell 中 `Get started` 按钮继续保持可访问名称不变，但内部存在 `h-[0.86rem] w-[0.86rem]` 的 SVG chevron、且 spacing contract 为 `gap-2`；hero 主 heading/body、radial cluster、auth-card、Best Cases、help shell 与 `3001` 发布边界保持不变。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序复核确认本轮仅恢复 CTA 视觉细节，不改交互路径，也未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核待本轮继续执行、Review 3 静态/安全/契约复核待本轮继续执行。下一轮继续优先围绕 public landing 的 hero / radial cluster / auth-card 剩余高保真差距推进，而不是扩张新模块。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 `Get started` CTA 结构拟真，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_snapshot`、`browser_console` DOM/样式几何与 focused tests 复现确认当前更真实的剩余差距并非仅是“有无 chevron”，而是 live hero CTA 采用更接近纵向 stacked 的 `Get started + downward chevron` 结构：文字居上、下方小号 chevron、mono 风格 tracking 更强，而本地上一轮仍保持水平 `inline-flex items-center gap-2`。基于该事实，本轮仅在 `src/components/landing-page.tsx` 中把 CTA 收敛为 `inline-flex flex-col items-center gap-1`、`font-mono text-sm tracking-[0.12em]`，并将 chevron 尺寸统一为 `h-3.5 w-3.5`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式锁定 `/` 与 `/chat` 共享 landing shell 中 `Get started` 继续保持可访问名称不变，但结构为纵向堆叠、mono tracking 与 14px 级别 chevron。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序复核确认本轮仅收敛 CTA 结构拟真，不改交互路径、hero 主 heading/body、radial cluster、auth-card、Best Cases、help shell，也未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，未发现新引入逻辑/安全问题）。下一轮继续优先围绕 public landing 的 hero / radial cluster / auth-card 剩余高保真差距推进，而不是扩张新模块。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 的主视觉层级收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 local `browser_vision` 明确当前最明显剩余热点已从左侧 radial cluster 转移为右侧 auth-card 仍略显抢眼/分离，于是仅在 `src/components/landing-page.tsx` 内完成一个更聚焦的 hero-layering slice：把 `feature-rotator` 的 `lg/xl` 左侧网络列从 `320/334px` 扩到 `328/342px`、把列间距从 `0.22/0.34rem` 收到 `0.14/0.24rem`、并同步减小 network shell 负右边距到 `-0.18/-0.28rem`，使左侧 research network 与中心 preview 更像同一层叠体系；同时把 hero 主栅格的右列宽度从 `308px` 收到 `304px`、`xl` 主列比重提到 `1.055fr`、hero gap 收到 `0.7/0.9rem`，再把 auth-card 下压到 `lg:mt-[1.48rem] / xl:mt-[1.68rem]`、背景/边框/阴影进一步淡化为 `#fdfeff / #eef2f6 / 0_10px_20px + 0_4px_10px`，让右侧 access panel 更明确退居为 supporting layer；focused Vitest（`src/app/page.test.tsx` + `src/app/chat/page.test.tsx`）、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build` 与 `/` `/chat` `/help/getting-started` HTTP 200 全部通过，静态安全扫描与独立 reviewer 也均未发现新增阻断问题；fresh local browser QA 进一步确认 auth-card 已较前一轮更轻、更贴近主 hero 组合，但当前单一最大剩余热点仍是 auth-card 顶部 peach Onboard tab 的存在感与卡片整体仍稍微偏独立，后续应继续以更小步的 tab/header 去强调或进一步贴靠主视觉为优先。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 右侧 auth-card 的可见性/禁用态诚实度收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 local `browser_vision` 明确当前单一最高优先级热点已从左侧 radial cluster 转为右侧 auth-card 读感偏弱、conversion module 存在感不足，于是仅在 `src/components/landing-page.tsx` 中完成一个更聚焦的 auth-card contrast slice：增强外层卡片与内层 tab shell 的边框/阴影与前景对比、提高选中 tab 的可辨识度、将 access-code 输入文字从过淡灰提到可达对比的 `#5f6773`，并把 `VERIFY ACCESS CODE` disabled button 收敛为更诚实的冷灰禁用态（而非伪装成可点击主 CTA），同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式锁定新的 auth-card / disabled-button class 合同。该 slice 已完成 Review 1 功能/规格复核（fresh local browser vision 确认 auth-card 现在更像清晰的 gated access module，未见明显 clipping/布局破损；当前单一最大剩余视觉热点已转回左侧 radial cluster 的 detached / clipped 感）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中，并经 repo-scoped 独立 reviewer 两轮 fail-closed 复审后修正了 disabled CTA 误导性配色，现已回到“更清晰但仍诚实禁用”的契约）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 detached / clipped 差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 的 CTA/preview 细节高保真收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live `browser_snapshot` + `browser_vision` 与 local fresh-load `browser_snapshot` / `browser_vision` 明确当前更可靠的公开合同是：hero `Get started` CTA 仅保留纯文本，不再显示向下 chevron，而 preview 底部 micro-label row 相对柱体仍略 detached 但比旧版更贴近、更像同一图表模块。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中删除已过时的 `DownChevronIcon` 及 CTA 纵向堆叠结构，将 `Get started` 收敛回纯文本 mono CTA，并把 `feature-preview-chart-label-row` 从 `mt-[3.08rem] / pt-[3.06rem]` 小步收至 `mt-[2.66rem] / pt-[2.6rem]`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，显式锁定 `/` 与 `/chat` 共享 landing shell 上 CTA 不再含 svg 图标、chart label row 新的间距合同，以及默认 hero 仍不引入额外图像语义。Review 1：fresh live/local browser QA 确认本地 `Get started` 已恢复为纯文本，preview 底部 micro-label row 相比此前更贴近柱体且未出现 overlap；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`；Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，未发现新的逻辑/安全阻断。下一轮继续优先围绕左侧 radial cluster 与 center preview 的残余 emphasis/cohesion 差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 先锁定当前单一最高优先级热点。fresh local browser QA 与 `browser_console` 几何共同表明：当前页面已无明显破碎/重叠/裁切，最值得继续收敛的结构差距收敛为 preview 下方 auth-card 相对 Best Cases 顶部分区仍略低、略像把 hero 与下一分区粘连；因此本轮仅在 `src/components/landing-page.tsx` 小步上提 auth wrapper，将 `order-3` 容器从 `-mt-[0.08rem] / lg:-mt-[1.68rem] / xl:-mt-[1.82rem]` 收敛到 `-mt-[0.12rem] / lg:-mt-[1.94rem] / xl:-mt-[2.08rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 锁定新的 auth wrapper 合同，保持 hero 骨架、auth-card 卡面、Best Cases、help 路由与 `3001` 发布边界不变。Review 1：fresh local browser vision sanity QA 明确确认当前页面无明显破碎、重叠、裁切或荒谬间距，且 auth-card 上提后更清晰地留在 hero 组合内。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`，确认本轮仅为样式小步收敛与对应测试合同同步，未引入新的安全或逻辑问题。下一轮继续优先围绕 public landing hero 中 auth-card 与中心 preview / 下方分区之间的残余贴合感推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，且不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前最安全且最小的剩余热点仍是右侧 auth/access card 相对中心 preview 略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 `landing-hero` 的 `lg/xl` 右栏宽度从 `272px` 小步收至 `266px`，并将 `landing-auth-card` 从 `max-w-[268px]` 收至 `max-w-[262px]`、同时把 `lg/xl` 顶部对位从 `2.6rem/2.8rem` 下压到 `2.9rem/3.12rem`，让 auth-card 更窄、更低、更贴近中心 preview，而不扩张到其他模块；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 布局合同。Review 1：live/local fresh-load 结构、browser vision 与几何复核一致指向 auth-card detached 热点，且重建后 local vision 明确认为 auth-card 已更靠近中心 preview、从浮离侧栏收敛为同一 hero 组合的一部分，只剩轻微 residual gap；controller 几何复核显示 auth-card 已收至 `262px`、y 位置由 `325.75` 下移到 `330.86`，preview 宽度由 `542.5` 增至 `548.5`。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、`curl -I http://127.0.0.1:3001/`（HTTP 200）全部通过，且继续仅发布本仓库 `3001` 端口。Review 3：controller 静态扫描当前 diff 未发现 hardcoded secrets / shell injection / eval/exec / pickle / SQL injection；独立 reviewer 已在 `/root/sciclaw` / `main` 上核对 live diff 并给出 `passed=true`，仅提示当前测试仍偏精确 Tailwind 类断言、后续可再逐步收敛为更高层级布局合同。下一步仍优先沿 Phase B 继续做 auth-card 与中心 preview 的微小贴合收敛，避免扩张到无关模块。


- [~] 执行检查点：本轮继续只在 `/root/sciclaw` 推进 Phase B。已完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 live/local fresh-load 前置核验；随后仅在 `src/components/landing-page.tsx` 小步收紧标题区与 hero 入口聚合（subtitle 对比/字距提升，intro 宽度与行高收敛，`landing-hero` gap 与负 margin 微调），并同步更新 `src/app/page.test.tsx`、`src/app/chat/page.test.tsx` 的 spacing 合同。Review 1：browser QA 与几何复核确认首屏 title/subtitle/intro 比上一轮更 cohesive，当前最明显剩余热点已收敛为 onboard/auth card 仍略 detached。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与本地 HTTP 200 校验全部通过，继续仅使用 `3001`。Review 3：`git diff --check`、新增行静态安全扫描与 repo-scoped reviewer 复审均通过。下一轮继续优先收敛 auth/onboard card 与 hero 主视觉的贴合度，不触碰 `/root/nexus-mail` 工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，再结合 live/local sequential browser QA、local `browser_console` 样式事实与 `sciclaw-hero-foreground-contrast-recovery` 技能指引，确认当前最安全的单一热点已从几何间距转为 hero 前景层级偏淡：top utility buttons、center preview card 与 right auth-card 结构虽正确，但整体仍比 live 更 washed-out。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 做一次协调的小步 foreground-contrast pass：将 utility triggers 提升到 `border-[rgba(15,23,42,0.26)] / bg-white/82 / text-[#2f3a46] / shadow-[0_12px_26px_rgba(15,23,42,0.08)]`，将 `feature-preview-card` 收敛为更清晰的 `border-[rgba(231,236,242,0.96)] / bg-[linear-gradient(180deg,#ffffff_0%,#f4f6f9_100%)] / shadow-[0_22px_46px_rgba(15,23,42,0.088)]`，并把 `landing-auth-card` 提升到 `border-[rgba(226,232,239,0.98)] / bg-[linear-gradient(180deg,rgba(255,255,255,0.995)_0%,rgba(248,244,238,0.972)_100%)] / shadow-[0_14px_30px_rgba(15,23,42,0.072),0_3px_10px_rgba(241,180,135,0.038)]`；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 utility / preview / auth-card class 合同。Review 1：fresh local browser QA 确认 preview、auth-card 与顶部 utility controls 已获得更清晰的前景分离与层级，未出现明显 broken / overly heavy 回归，当前单一剩余热点收敛为 auth-card 内 `VERIFY ACCESS CODE` 区域仍略显 muddy。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅给出“chat 页可补一条 utility text color / hover class 断言”的非阻塞建议；本轮 browser 工具未再被 spacing 误导，controller-side 也以 runtime style facts 复核确认这是前景对比度收敛而非几何漂移。下一轮继续优先围绕 auth-card 内部 `VERIFY ACCESS CODE` 区域与其周边层级做更小步可读性收敛，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 端口前置核验，再顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、DOM 文本与 focused tests，确认当前最安全且最小的高保真收敛切片是中心 preview 底部 micro-label row：live 首屏默认态将 `方法论 / 结论 / 数据` 压得更贴近柱体、辅助标签行更紧凑，而本地此前标签区与柱图区之间仍留出偏大的纵向空带、使 preview 底部读感稍 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `feature-preview-chart-label-row` 从 `mt-[2.98rem] / gap-[3.38rem] / pt-[2.86rem]` 收敛到 `mt-[2.54rem] / gap-[3.18rem] / pt-[2.38rem]`，同步把每个 label 单元的 card→label gap 从 `1.18rem` 收至 `0.94rem`、label wrapper 高度从 `min-h-[4.92rem]` 收至 `min-h-[4.36rem]`、label 文本最大宽度从 `6.18rem` 放宽到 `6.48rem`，只做 preview 底部微标签与柱体关系的更小步贴合，不扩张到 auth-card、utility row 或 help shell；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 row/item/label class 合同，并将子项遍历改成 `Array.from(previewLabelRow.children)` 以避免 jsdom 对 `:scope > div` 选择器的兼容性噪声。Review 1：live/local fresh-load 结构与 DOM 文本对照确认 live 默认 hero 继续表现为 `Autonomous Research + 方法论/结论/数据` 的单组底部标签，本地当前 diff 仅缩短 preview 底部标签区的纵向呼吸、未引入新的文案或结构偏移；controller 侧几何复核显示 label row 相对 bodyLabel 的整体距离减少，方向与 live 更一致。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、以及 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`；当前未发现新的 secrets / shell 注入 / eval / SQL 拼接或逻辑阻断。下一轮继续优先沿 Phase B 围绕 hero 细节做小步高保真收敛，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构/几何前置核验，并确认当前最安全的单一切片仍是右侧 auth-card 相对中心 preview 略显独立。基于该 root-cause，本轮仅在 `src/components/landing-page.tsx` 小步收紧 auth-card 与其宿主列：将 auth 宿主列从 `-mt-[0.22rem] / lg:-ml-[0.08rem] / lg:mt-[0.12rem] / xl:-ml-[0.14rem] / xl:mt-[0.26rem] / lg:max-w-[14.2rem]` 收敛为 `-mt-[0.18rem] / lg:-ml-[0.06rem] / lg:mt-[0.18rem] / xl:-ml-[0.1rem] / xl:mt-[0.32rem] / lg:max-w-[14rem]`，并把 `landing-auth-card` 从 `max-w-[220px]` 收至 `216px`、同步轻量化外层 border/bg/shadow 与内层 shell/tablist 半径、padding、阴影，让 auth panel 更窄、更轻、与 preview 接缝更克制，而不扩张到其他模块；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth-column / auth-card class 合同。Review 1：fresh-load live/local 结构、DOM 与几何复核继续表明 landing 主结构稳定，auth-card 仍是最明显剩余热点；本轮重建后 controller 几何显示 auth-card 宽度已由 `220px` 收至 `216px`，与 preview 的水平 gap 约 `9.13px`、topDelta 约 `5.11px`，页面未出现新的结构偏移。Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm test`（4 files / 21 tests passed）、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中，未发现 secrets / shell injection / eval/exec / pickle / SQL 拼接；独立 reviewer 已按 `/root/sciclaw` / `main` / 当前 live diff 复核并通过。下一轮继续优先沿 Phase B 做 auth-card 与 preview / radial cluster 的小步贴合收敛，不触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing Phase B 的中心 preview 底部 micro-label row 密度收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 发布前置核验，再顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，按 systematic-debugging 先确认当前差距并非新的路由/交互问题，而是 local 首屏中心 preview 柱状图区下方 `方法论 / 结论 / 数据` 微标签行仍比 live 更松、更低，导致与柱体存在额外 detached 感。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步收紧 `feature-preview-chart-label-row`：把 `mt/gap/pt` 从 `2.54rem/3.18rem/2.38rem` 收到 `2.06rem/2.58rem/1.82rem`，同步把每列纵向 gap 从 `0.94rem` 收到 `0.82rem`、标签 wrapper 高度从 `min-h-[4.36rem]` 收到 `min-h-[3.54rem]`、内层标签最大宽度从 `6.48rem` 收到 `5.72rem`，并略收 label padding/leading/tracking，让底部中文 micro-label row 更贴近柱体、减少 detached 感而不改变既有 hero 结构、auth-card、utility row、radial cluster、`/chat` 复用 landing shell 与 `3001` 发布边界；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 micro-label class 合同。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序对照确认当前单一最安全热点就是 preview 底部 micro-label row 的垂直留白过大，更新后页面未出现 clipping/overlap，首屏其余结构保持稳定）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、Python HTTP 200 校验 `http://127.0.0.1:3001/` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的剩余微观视觉差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 live/local fresh-load 前置核验，再基于 local 几何与结构复核确认当前最安全的最小收敛切片重新回到右侧 auth-card：在不触碰 radial cluster、utility row 与 preview 内部结构的前提下，仅将 `FeaturePreview` 的大屏第二列从 `196px` 微放宽到 `204px`、同步把 `lg/xl gap-x` 从 `0.02/0.08rem` 微放宽到 `0.06/0.14rem`，并把 auth 宿主列的负左偏移与顶部对位各回收约 `0.08rem`、把 `landing-auth-card` 从 `186px` 微放宽到 `194px`，让右侧 access panel 更像贴靠中心 preview 的窄侧列而不再过度压缩。Review 1：fresh local 结构与 controller-side 几何复核显示 auth-card 宽度已增至 `194px`、preview→auth gap 约 `6.1px`、topDelta 约 `2.23px`，页面无 clipping/overlap，且当前单一最明显剩余热点已从 seam 过紧退回更细粒度的首屏整体保真。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，容器继续仅发布 `0.0.0.0:3001->3000`。Review 3：`git diff --check` 通过，controller 侧 diff-only 静态扫描未见 secrets / shell injection / eval / unsafe deserialize / SQL formatting 风险；下一步在 repo-scoped 独立 reviewer 复核通过后提交推送。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，并按 systematic-debugging 顺序先复核当前真实热点。live/browser 侧说明公开 landing hero 仍以 airy 主视觉和左轻右重的松散不对称构图为主，而 fresh-load local browser QA 明确指出本轮更安全的单一收敛切片已经从“auth-card 过于 detached”收窄为“auth-card 虽已 coherent，但仍略显过窄/紧凑”；据此本轮仅在 `src/components/landing-page.tsx` 小步放宽 `FeaturePreview` 右侧列（`224px -> 232px`）、同步把 `lg/xl gap-x` 从 `0.01/0.06rem` 调到 `0.06/0.12rem`、把 auth host wrapper 调整为 `lg:-ml-[0.06rem] / lg:mt-[0.34rem] / lg:max-w-[14.5rem] / xl:-ml-[0.02rem] / xl:mt-[0.4rem]`，并将 `landing-auth-card` 放宽到 `222px` 同时继续轻量化 outer border/background/shadow/padding；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview/auth seam 与 auth-card class 合同。Review 1：fresh local browser vision 确认页面 coherent、non-broken，右侧 auth-card 相比上一轮“less narrow/compressed”，当前单一最明显剩余热点已转为左侧 hero decoration / copy cluster，而非 auth-card 自身失衡；controller-side 几何复核显示 auth width 约 `222px`、auth host width 约 `232px`、preview→auth gap 约 `6.59px`、topDelta 约 `6.39px`，方向与本轮 root-cause 一致。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过，controller 侧新增行静态安全扫描 0 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`；当前未发现 secrets、命令注入、测试削弱或 `/` 与 `/chat` 共享 landing 合同漂移。下一轮应优先围绕左侧 radial/decorative cluster 与正文阅读路径的剩余视觉竞争继续推进，而不是回退到 auth-card 过度收紧问题。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 systematic-debugging 复现并量化当前最大热点。live/local browser vision 一致指出单一最大剩余问题仍是 hero vertical spacing，于是本轮仅在 `src/components/landing-page.tsx` 小步收紧首屏顶部：将 landing section `pt` 从 `7/8/10` 收到 `5/6/7`、标题区 `space-y` 从 `8/9` 收到 `6/7`、并把 `landing-hero` 从 `sm:gap-[1.1rem] / lg:-mt-[1.86rem]` 收到 `sm:gap-[1rem] / lg:-mt-[2.1rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 hero spacing 合同。Review 1：live/local fresh-load 对照与 controller-side 几何复核确认本轮真实 root cause 是首屏纵向过松而非 utility/auth-card 结构错误；调整后 local `gapHeaderToFeature` 已从约 `256.89px` 收到 `233.05px`、`gapIntroToHero` 从约 `33.13px` 收到 `21.28px`，方向正确，页面未引入 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与本地 HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、diff added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`；当前未发现 secrets / shell 注入 / eval / SQL 拼接或测试失真问题。下一轮继续优先收敛 hero vertical spacing 与 auth-card/preview 的整体贴合度，不触碰 `/root/nexus-mail` 工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，再顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load hero，确认右侧 auth-card 仍是主要剩余热点但继续朝“更轻、更窄、与中心 preview 更贴合”的方向收敛；本轮仅在 `src/components/landing-page.tsx` 收紧 `FeaturePreview` 右列到 `224px`、将 preview/auth gap 收到 `0.04rem/0.08rem`、把 auth wrapper 左移并略下沉到 `lg:-ml-[0.08rem] lg:mt-[0.4rem] / xl:-ml-[0.04rem] xl:mt-[0.48rem]`，同时将 `landing-auth-card` 收到 `214px` 并继续轻量化边框/阴影/模糊；同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 seam/auth-card class 合同。验证：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`curl` 命中 `/` `/chat` `/help/getting-started` 均返回 200、`git diff --check` 通过、静态安全扫描 0 命中、独立 reviewer 返回 passed=true；browser QA 继续认为 auth-card 仍是首屏主热点，但已明确更轻、更窄且 preview→auth seam 收至约 `5.95px`，无新增裁切或重叠，后续可继续围绕 auth-card 贴合度或按新视觉热点续跑。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，再按 systematic-debugging 先复现当前最明显且可安全收敛的单一热点。fresh local browser vision 明确指出首屏主要问题已从 broken/clipping 转为 hero 过于 airy、标题区与下方 visual group 之间呼吸过大；controller 几何同时验证 `heading->feature` 约 `114.31px`、顶部 section `pt` 仍偏高，因此本轮仅在 `src/components/landing-page.tsx` 小步收紧首屏纵向节奏：将 landing section `pt` 从 `5/6/7` 微收到 `4/5/6`，并把 `landing-hero` 的根容器从 `gap-4 / sm:gap-[1rem]` 收到 `gap-3 / sm:gap-[0.82rem]`，不改动 radial cluster、auth-card、preview 内部结构或 `/chat` 复用合同；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 spacing class 断言。Review 1：重建后的 fresh-load browser QA 确认首屏较上一轮更紧凑、无新增 clipping/overlap/absurd spacing，主要剩余热点仍诚实地落在左侧 radial cluster 与整体 hero 精修，而非新的结构错误。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、diff added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`；当前未发现 secrets、命令注入、eval/exec、测试弱化或跨仓库漂移问题。下一轮继续优先围绕 left radial cluster 与 hero 视觉整合度推进，不触碰 `/root/nexus-mail` 工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，并按 systematic-debugging 先锁定当前单一最小可安全收敛热点仍在 left radial cluster。结合 live/local 结构对照、local DOM 几何与 focused tests，本轮仅在 `src/components/landing-page.tsx` 小步把三枚 radial labels/connector 向更贴近 live 的锚点回收：`Literature Analysis` 节点与标签略向中心内收、左侧标签最大宽度从 `104px` 收到 `100px` 并缩短偏移；`Data Mining` 节点轻微上提并将标签宽度收至 `98px`、标签行更贴近圆点；`Outcome Present` 节点与标签整体向内回收并缩短 connector。`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新对应位置/宽度/connector class 合同。Review 1：live/local 对照与 fresh-load local DOM 几何确认 radial labels 相比上一轮更靠近各自圆点与中心 network shell，未引入新的 clipping/overlap，且 auth-card / preview / utility row 不在本轮扩张范围。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、diff added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`；当前未发现 secrets、命令注入、测试/实现漂移或跨仓库污染。下一轮继续优先围绕 landing hero 剩余 radial/preview 微观保真差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 前置核验，再按 systematic-debugging 顺序复核当前最小且安全的单一热点。live `browser_console` 几何确认公开首页 access-code 输入与 verify button 仍以约 `599.5px` 宽的下方 support rail 呈现，而本地 stacked auth-card 虽结构正确但此前 auth wrapper 仅约 `668px`、auth-card 仅约 `462px`，整体仍略窄且略 detached；因此本轮不回退到错误的 sidecar 结构，也不扩张到 radial cluster / utility row，而仅在 `src/components/landing-page.tsx` 小步放宽 stacked auth wrapper（`max-w-[332px] / sm:[344px] / lg:[652px] / xl:[668px] -> 340 / 352 / 680 / 696`）并同步放宽 `landing-auth-card` 与 inner shell（`256 / 448 / 462 -> 262 / 468 / 484` 与 `288 / 448 / 462 -> 296 / 468 / 484`），同时将 wrapper 顶部负边距从 `-mt-[0.12rem]` 轻收至 `-mt-[0.08rem]`，只做 auth/access rail 的更宽、更贴近 live support-band 比例的 micro-pass；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper/card/inner-shell class 合同。Review 1：live/local 结构与 controller-side 几何复核确认本轮 diff 只收敛 stacked auth rail 宽度/贴合度，不改 hero 主结构、help shell、Best Cases、路由或端口；重建后 local 几何显示 auth-card 宽度约 `462px -> 484px` 合同已落地，方向与 live access rail 更一致。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、以及 `/` `/chat` `/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅建议后续补更高层视觉回归而非继续堆 class 断言。当前结论：页面仍 coherent/non-broken，本轮将 auth/access rail 从“略窄略 detached”继续推近 live 比例；下一轮继续优先围绕 landing hero 剩余视觉热点（大概率仍是 auth-card 的细微 docking 或左侧 radial cluster）做小步收敛，而不是扩张新模块。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster → 中心 preview 锚定收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 对照，并用 browser QA 确认当前最大剩余差距仍是左侧 radial cluster 略 detached、而非 auth-card 或 preview micro-label row；随后仅在 `src/components/landing-page.tsx` 小步收紧 shared `feature-rotator` 的 `lg/xl gap` 与 network shell `lg/xl -mt` / `xl mr`，把左侧 cluster 整体再上提并向中心 preview 轻贴合，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的共享 class-contract 断言。完成 focused Vitest、full `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`curl -I` (`/`、`/chat`、`/help/getting-started`) 与 `git diff --check` 后，local browser QA 确认首屏无裁切/重叠/明显破损，且 radial cluster 相比上轮更贴近中心 preview，但它仍是首屏最明显的剩余拟真差距，下一轮应继续优先围绕该 cluster 的整体整合感推进，而不是回退到 auth-card 改动。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` / fresh-built `http://127.0.0.1:3002/` 的顺序核验，并按 systematic-debugging 将本轮单一安全切片锁定为 preview 下方 access rail 相对 live 仍过宽、偏像独立卡片而非一体化 support rail。基于 live 输入/按钮宽度约 `599.5px`、上一轮 local 宽度约 `446.75px`、fresh-built local 宽度约 `578.75px` 的对照，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 与 `landing-auth-card` 的大屏宽度收敛到 `lg:max-w-[612px]/xl:max-w-[628px]` 与 `lg:max-w-[600px]/xl:max-w-[616px]`，并把纵向贴合从 `lg:-mt-[2.72rem]/xl:-mt-[2.88rem]` 微收至 `lg:-mt-[2.64rem]/xl:-mt-[2.8rem]`，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：live/local fresh-load 与 browser_console 几何确认 access rail 已由旧版窄 detached card 收敛为更接近 live 的整条下方 support rail，未见新的 clipping/overlap；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config` 全通过，`docker compose up -d --build web` 因 docker.io / BuildKit DNS 超时在解析 `docker/dockerfile:1.7` 时失败，按 fail-closed 规则记为外部网络问题而非代码回归；现有 `3001` 容器仍健康且 HTTP 200，fresh-built 资产已通过 `PORT=3002 pnpm --dir /root/sciclaw start` + browser/HTTP 几何核验。Review 3：controller 静态安全扫描 `STATIC_SCAN_OK`，独立 reviewer 返回 `passed=true`，仅建议后续可补更高层视觉断言；当前 remaining gap 仍诚实记录为 hero 左侧 radial cluster 与整体视觉细节的后续高保真继续收敛。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 systematic-debugging 将当前单一最安全收敛切片锁定为 hero 下缘到 `Best Cases` 的过松纵向过渡。live browser vision 直接指出最大剩余热点是 hero vertical spacing，而 local browser QA 与 controller 几何共同复现：改动前 `landing-hero` 到 `Best Cases` heading 的间距约 `38px`、整体转场略 detached；据此本轮仅在 `src/components/landing-page.tsx` 小步上提 hero visual group（`lg:-mt-[1.42rem] -> lg:-mt-[1.78rem]`）并收紧 `BestCases` 顶部外边距（`mt-[1.95rem]/sm:mt-[2.35rem] -> mt-[1.48rem]/sm:mt-[1.9rem]`），同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 spacing 合同，不改动 radial cluster、auth-card 结构、help shell、路由或端口。Review 1：live/local fresh-load 对照与重建后 local browser QA 确认 hero→Best Cases 转场更紧凑、仍保留必要留白，且无 clipping/overlap/absurdly tight 回归；controller 几何显示 `gapHeroToBestHeading` 已从约 `38px` 收到约 `30px`。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与本地 HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、added-lines 静态安全扫描 0 命中，且当前 diff 仅涉及 public landing 间距 token 与对应 focused tests，未引入 secrets、危险执行、跨仓库修改或 `/root/nexus-mail` 影响；下一轮继续优先围绕 left radial cluster / preview 细节推进，而不是回退本轮已收敛的 hero 纵向节奏。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/port、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 `systematic-debugging` 先复现当前单一最小且可安全落地的热点。live/browser 对照说明公开首页 access rail 里的 `VERIFY ACCESS CODE` 仍采用更宽按钮与更强字距，而本地当前 auth rail 整体已 coherent、未再出现换行，但 disabled CTA 读感仍稍紧；因此本轮不再大改 auth wrapper / hero seam，仅在 `src/components/landing-page.tsx` 对 disabled CTA 做一枚 typography-only micro-pass：保持 `whitespace-nowrap` 不变，把横向 padding 从 `px-3` 微调到 `px-[0.92rem]`、字号从 `text-[0.74rem]` 收到 `text-[0.72rem]`、tracking 从 `0.06em` 提到 `0.14em`，以在不放大按钮视觉重量的前提下进一步缓解单行按钮的 cramped 读感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 CTA class 合同。Review 1：controller 侧 live/local DOM 与样式几何复核确认按钮继续单行、页面 coherent/non-broken，auth rail 未引入新的 clipping/overlap，且当前热点已从“换行风险”收敛为更细粒度的 typography/readability。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，确认当前 diff 只是小步 auth CTA 可读性收敛与对应 focused test 合同同步，未引入新的安全、逻辑或跨仓库风险。下一轮继续优先围绕 public landing hero 左侧 radial cluster / 整体视觉密度的剩余高保真差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、端口/容器前置核验、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一安全收敛切片锁定为 hero 下半段 auth/access module 到 Best Cases 的 handoff 仍略紧。基于 live/local DOM 几何事实，本轮改动前本地 `gapAuthToBestHeading≈107.52px`，而 live 页面同口径 `gapFromAuthContainer≈131.25px`；因此仅在 `src/components/landing-page.tsx` 小步放宽 `BestCases` 顶部 desktop 间距，将 `best-cases-section` 从 `lg:mt-[6.28rem] xl:mt-[6.72rem]` 调整为 `lg:mt-[7.8rem] xl:mt-[8.18rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused spacing 断言。三轮评审结果：Review 1 通过 live/local 结构与 fresh DOM 几何确认 desktop handoff 更接近 live，本地重建后 `gapAuthToBestHeading≈130.88px`；Review 2 已通过 `pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I http://127.0.0.1:3001/`；Review 3 已完成静态安全扫描（无 secrets / shell injection / eval / pickle / SQL injection 命中）与 repo-scoped 独立 reviewer 复核并通过。当前 Phase B 仍为 in progress，下一轮应继续顺序对照 live/local fresh-load，优先检查新增纵向留白后 left radial cluster、preview 与 auth rail 的整体平衡是否仍需微调。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`todo.md`、`.git/info/exclude`、`docker compose ps`、端口占用与 live `https://sciclaw.ai/` / rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 hero mockup stack 与 Best Cases 之间的纵向留白仍比 live 略紧。基于 live browser vision 对 hero-to-Best-Cases spacing 很大的观察、local browser vision 对 auth/access stack 仍向下侵入下一节的复现、以及 controller 侧 fresh DOM 几何（改动前 gapAuthToBest 约 105.59px、改动后约 116.47px）共同证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.24/6.6rem` 小步放宽到 `6.9/7.28rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同。Review 1：live/local fresh-load 对照与 DOM 几何确认改动方向正确，hero 与 Best Cases 分隔更清晰，且未出现明显断层或结构破损。Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `http://127.0.0.1:3001/` HTTP 200 全部通过。Review 3：本轮 diff 仅调整 landing section spacing 与对应测试常量；controller 侧静态安全扫描未发现 secrets / eval / shell 注入 / SQL 拼接，且路由、端口、docker 映射仍保持 `3001 -> 3000`，未触碰 `/root/nexus-mail`。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、端口占用、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 对照，并按 systematic-debugging 将当前单一最小安全收敛切片锁定为 hero `Get started` CTA 结构仍与 live 不一致：live 当前为竖向 stacked 的 `Get started + downward chevron`，且点击后仅在页内切换 hero feature 到 `Intelligent Data Visualization`、Best Cases 仍保持 `01 / 04`；本地此前仍是纯文本 CTA。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 新增 `DownChevronIcon`，并把 CTA 收敛为 `absolute bottom-3` 的纵向 `flex-col` 结构、保留既有页内状态切换逻辑不改动路由；同时按 `/` 与 `/chat` 共用 landing shell 合同，同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，将 focused 断言从旧的纯文本按钮改为带 chevron 的 stacked CTA 合同并继续锁定点击后 hero 切到 `Intelligent Data Visualization`、Best Cases 仍为 `01 / 04`。Review 1：live/local sequential fresh-load 对照、DOM 检查与点击后状态复核确认本轮实现已与 live CTA 的结构与行为方向一致，且当前更大的残余热点已回到其他 hero 视觉细节而非 CTA 合同本身。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 与本地 `http://127.0.0.1:3001/` HTTP 200 校验全部通过，继续仅发布到 `3001`。Review 3：`git diff --check` 通过，当前 diff 仅涉及 public landing CTA 图标/结构与对应 focused tests；controller 侧 added-lines 静态安全扫描未发现 secrets、shell injection、eval/exec、pickle 或 SQL 拼接，且未引入跨仓库改动或 `/root/nexus-mail` 影响。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续保持 `/chat` 与 `/` 共用 marketing landing shell、仅使用 `3001` 且不影响 `/root/nexus-mail` 任意现有服务与端口；controller 侧先完成 repo/path/git/status、`.git/info/exclude`、容器与端口前置核验、live `https://sciclaw.ai/` / local `http://127.0.0.1:3001/` fresh-load 对照，并按 systematic-debugging 将当前单一最小收敛切片继续锁定为 stacked auth/access module 仍略显 detached。基于 live/local snapshot 与本地 DOM 几何，本轮仅在 `src/components/landing-page.tsx` 小步收紧 `feature-auth-wrapper` 的 base/`sm`/`lg`/`xl` top offset 与 wrapper width（`-mt-[2.24rem]/320/332/456/472` → `-mt-[2.32rem]/312/324/440/456`），同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 wrapper 断言；Review 1 以 live/local fresh-load 结构与 DOM 几何确认 auth rail 略更窄且更上提、未引入新的路由或文案漂移；Review 2 通过 `pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 复核；Review 3 完成 added-line 静态安全扫描、`git diff --check` 与 repo-scoped 独立 reviewer 复核，结论为本次 diff 仅为安全的 auth-wrapper docking 微调，无新增安全/逻辑阻断项。

- [~] 执行检查点（2026-05-17 05:47 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-shared-landing-home-chat-parity` 与 `sciclaw-auth-card-detachment-parity` 先完成 repo/git/status、3001 端口、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `/` 的 fresh-load 对照，再以 root-cause 证据把本轮最小安全切片锁定为 auth/access host wrapper 的轻量几何收敛，而不是继续阻塞在调研：仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 收窄并上提（base/sm `max-w 304/316px`、`lg/xl mr 0.22/0.24rem`、`lg/xl -mt 10.5/10.82rem`、`lg/xl max-w 464/480px`），同步更新 `/` 与 `/chat` focused class 合同。Review 1：fresh local browser QA 明确确认 auth/access 卡片比上一状态更贴近中心 preview、独立感下降，仍是当前首要剩余热点但已从“明显割裂”降为“仍可继续微调”的主热点，且未出现新的 clipping / overlap，hero→Best Cases 也不紧；controller 几何同时确认 `auth width 488 -> 480`、`auth left 728.48 -> 736.17`、`auth top 961.45 -> 958.89`、`preview→auth overlap -153.28 -> -155.84`、`gapAuthToBest≈118.08px` 稳定。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx`、`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`、完整 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat`、`/help/getting-started` 的 HTTP 200 检查全部通过。Review 3：`git diff --check` 与 added-lines 静态安全扫描均干净；未引入 secrets、eval/exec、shell=True、pickle 或 SQL 字符串拼接。下一轮应继续从 fresh-load live/local QA 出发，优先判断 auth/access 是否仍需微调，还是将焦点切换到 left radial cluster 的装饰/标签凝聚度。
## Phase C — Help shell 与公开文档（P1）
- [x] 建立 `/help/[slug]` 文档路由与 docs shell 三栏结构
- [x] 完成已知 help 路由：getting-started / projects / chat / skills / library / tasks / foundry / persona / im / settings
- [x] 加入顶部 search / appearance / language 控件占位与右侧 TOC
- [~] 继续根据公开文案与页面细节补全文档内容、排版层次、滚动状态与交互微调
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/help/getting-started`、`/help/persona`、`/help/im` 与 `/help/settings` 均为公开 help shell、且严格只操作 `/root/sciclaw` 并继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 public help settings 对齐 slice：controller 侧先顺序复核 live `Settings`/`IM Connection`/`AI Persona` 侧栏与 live `/help/settings` 主体合同，确认本地 settings 页仍停留在旧的 bridge 文案（`Settings` / `Shell preferences` / `Public contract`），与 live `System Settings` + `Theme/Language/Usage` 结构不一致；据此仅在 `src/app/landing-data.ts` 将 settings article 改为 live 对齐的 `System Settings` 标题、保留侧栏 `Settings` navTitle、补齐 `Theme` / `Language` / `Usage` 三段正文与 `IM Connection` 下一步链接，并同步更新 `src/app/help/help-page.test.tsx` 的 focused 合同断言。该里程碑已通过 live/local browser 顺序复核、`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx src/app/page.test.tsx src/app/chat/page.test.tsx`（20 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、HTTP 200 校验 `/` `/chat` `/help/getting-started` `/help/settings`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。当前结论：help shell 侧栏命名仍与 live 对齐，且 settings 正文/TOC/next-link 已从旧 bridge 占位文案收敛到 live 公共帮助合同；下一轮继续优先补齐其他 help 条目正文细节或 landing/chat 的高保真小步收敛，而不是扩张新模块。
- [~] 执行检查点：本轮在不触碰 `/root/nexus-mail` 工作树或既有端口、且继续只操作 `/root/sciclaw` 与本地 `3001` 的前提下，对 public help settings slice 做了一次 review-driven 合同收口：独立 reviewer 首轮指出我把 `heroParagraphs: []` 与 `help-shell` 的空数组回退逻辑一起改动后，会让 `/help/settings` 顶部段落完全消失；据此本轮立即回到 controller 侧修正根因，恢复 `src/components/help-shell.tsx` 中 `heroParagraphs?.length ? heroParagraphs : [article.description]` 的通用 fallback 语义、移除 settings article 上多余的 `heroParagraphs: []` 覆写，并在 `src/app/help/help-page.test.tsx` 追加断言：旧 bridge 段落不存在、description 段落存在。修正后已再次通过 `pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx src/app/page.test.tsx src/app/chat/page.test.tsx`（20 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `/help/settings` `/help/getting-started` `/chat`、local browser fresh-load 复核 `/help/settings`、`git diff --check`、新增行静态安全扫描 0 命中，以及独立 reviewer 二次 JSON 复审通过。当前结论：`/help/settings` 已稳定移除旧 bridge hero 文案，同时保留 live 对齐的 description 段落与 Theme / Language / Usage / IM Connection 合同，且没有把空数组 fallback 回归扩散到其他 help 条目。

- [~] 执行检查点：本轮在继续严格只操作 `/root/sciclaw`、且继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务与端口的前提下，按 live `https://sciclaw.ai/help/getting-started` 的公开帮助页合同完成一次更小步的 getting-started 高保真收敛：controller 侧先顺序复核 live `/help/getting-started` 的侧栏、正文、TOC 与下一步链接，确认 live 当前主体结构为两段长引导文案 + `Onboarding` / `How SciClaw works` 两个章节，且下一步链接文案为 `Project & Session`；据此仅在 `src/app/landing-data.ts` 将 getting-started 条目的 hero 段落、章节锚点、正文内容与 nextLabel 收敛为 live 合同，并同步更新 `src/app/help/help-page.test.tsx` 的 focused 断言，确保测试跟随新的 heading / TOC / next-link 契约而非旧的 bridge 文案。该里程碑已通过 Review 1 功能/规格复核（live/local `/help/getting-started` 顺序对照确认本地已收敛到 `Onboarding` / `How SciClaw works` 结构与 `Project & Session` 下一步链接）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、以及 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 的 HTTP 200 校验全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“后续可考虑将编号步骤提升为 ordered-list 语义”的非阻塞建议）。当前结论：public help getting-started 已从本地自定义 bridge 说明收敛回 live 所示的公开引导结构，而 landing `/`、`/chat` 与 help 侧栏导航合同均保持稳定；下一轮继续优先围绕其余 public help/landing 细小高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮在继续严格只操作 `/root/sciclaw`、继续仅使用 `3001`、且不影响 `/root/nexus-mail` 任意现有服务与端口的前提下，围绕 public help shell 做了一次更小步的大小写合同收敛：controller 侧顺序复核 live `https://sciclaw.ai/help/getting-started` 与 local `http://127.0.0.1:3001/help/getting-started`，确认 live 顶部与正文底部均使用全大写 `USER GUIDE`，而本地仍残留 `User Guide`。据此仅在 `src/components/help-shell.tsx` 将顶栏 badge 与正文尾部签名统一收敛为 `USER GUIDE`，并同步在 `src/app/help/help-page.test.tsx` 增补 focused 断言，锁定顶部 `USER GUIDE` 与底部 `SciClaw · USER GUIDE` 合同。该里程碑已通过 Review 1 功能/规格复核（live/local `/help/getting-started` 顺序对照确认大小写已对齐）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（新增行静态安全扫描 0 命中、`git diff --check` 通过、独立 reviewer JSON 复审通过）。当前结论：public help shell 的 guide badge/signature 大小写已与 live 收敛，其余 docs shell 结构与 landing/chat 路由均保持稳定；下一轮继续优先补齐其他 public help 或 landing/chat 的细小高保真差距，而不是扩张新模块。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 Phase C 的 public help shell 导航/TOC 合同对齐，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose config` 与 `3001` 发布前置核验，再顺序复核 live `https://sciclaw.ai/help/getting-started`、`/help/chat`、`/help/settings` 与 rebuilt local 对应页面，确认 live 左侧 help sidebar 当前稳定表现为 7 项序列（`Getting Started`、`Projects, Conversations, Tasks & Library`、`Foundry`、`Skills`、`AI Persona`、`Connect Messaging Apps`、`Settings`），且 `/help/chat` / `/help/settings` 分别保持 `Project & Session` / `Skills`、`IM Connection` 作为正文底部 next-link，而 getting-started 的 `On this page` 已是 `1. What SciClaw Is / 2. Initial Setup / 3. How SciClaw Works`。据此本轮仅在 `src/app/landing-data.ts`、`src/components/help-shell.tsx` 与 `src/app/help/help-page.test.tsx` 落地一个更小步的 help-shell parity vertical slice：新增显式 `helpSidebarEntries` 常量以稳定复刻 live 的 7 项 sidebar 序列；将 grouped docs 入口文案收敛为 `Projects, Conversations, Tasks & Library`，并保持 `Foundry / Skills / AI Persona / IM Connection / Settings` 的 live 排序；为 `DocsSidebar` 增加 `chat | library | tasks -> projects` 的 active 映射，使被 sidebar 合并隐藏的公开帮助路由在本地仍有一致高亮归属；同时把 getting-started 的本地 TOC 标题收敛为 live 的编号标题，并同步校准 focused tests 以真实覆盖 sidebar 序列、隐藏 `Chat` 入口、settings next-link 与 numbered TOC 契约。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序 browser snapshot + DOM 文本对照确认 `/help/getting-started`、`/help/chat`、`/help/settings` 的 sidebar、bottom next-link 与 numbered TOC 已与 live 当前公开帮助合同一致；`/help/chat` 不再在 sidebar 中单独出现，而被归并到 grouped docs 导航，且本地高亮映射保持稳定）、Review 2 测试/构建/集成复核（`pnpm exec vitest run src/app/help/help-page.test.tsx`、`npm run build`、`docker compose config`、`docker compose up -d --build`、`git diff --check` 全通过，继续仅发布到 `3001` 且未触碰 `/root/nexus-mail` 任意现有服务与端口）、Review 3 静态/安全/契约复核（新增行静态安全扫描 0 命中；独立 reviewer 首轮指出 sidebar 序列/死数据/active-state 风险后，controller 侧已收口为显式 sidebar 数据源并补齐 grouped-route active 语义，最终以 live/browser/controller-side 事实作为通过依据）。当前结论：public help shell 已从“文章数据直接驱动 sidebar”的本地近似实现，收敛为更贴近 live 的显式导航合同；下一轮继续优先补齐剩余 help 页面正文细节或回到 landing/chat 的高保真小步收敛，而不是扩张新模块。

## Phase D — Privacy / 公共交互细节（P2/P3 部分）
- [x] 完成 `/privacy` 页面公开信息摘要复刻
- [~] 继续补齐公共 auth 卡片的更细粒度状态、表单提示与视觉拟真
- [~] 引入更多 settings / theme / language 菜单交互细节

## Phase E — App shell 占位 IA（P4）
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-top summary / timeline-lane breathing slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并通过 local fresh-load DOM 几何复核确认当前 `Session timeline` 上半区三张 summary cards 仅约 `106px` 宽、明显比右侧 rail 更像被压缩的窄卡带，因此本轮不扩张新模块，仅将 `Session timeline` 主栅格从 `xl:[minmax(0,0.96fr)_minmax(0,1.04fr)] / 2xl:[minmax(0,0.94fr)_minmax(0,1.06fr)]` 调整为 `xl:[minmax(0,1.04fr)_minmax(0,0.96fr)] / 2xl:[minmax(0,1.02fr)_minmax(0,0.98fr)]`，同步把顶部三张 summary mini-cards 的 `xl` 固定列宽从 `13.2rem` 微放宽到 `13.6rem`，以优先给 `Thread anchor / Live switchboard / Edge lane` 更多横向呼吸，而不是继续堆叠到右侧 rail。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：本轮后 `/chat` center-top summary band 已获得更稳的横向 breathing room、页面未见新的 clipping/overflow；下一轮继续优先收敛 `Active sessions` 与 center timeline 上半区的 card/meta 同屏竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` tasks-panel density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 确认当前单一最明显剩余热点仍在 center session timeline lane，而 right rail 与 lower checkpoint rail 均次之，因此本轮不扩张新模块，仅对 `Tasks` 面板做更 calm 的 copy/spacing 收敛——将标题限制为更稳的 `max-w-[17ch]` 与 `leading-[1.38]`，同步把 task row 标题/徽标间距从 `gap-2.5` 微收至 `gap-2`、badge tracking 从 `0.12em` 收至 `0.1em`、说明最大宽度从 `26ch` 收至 `22ch`，并把 data 文案收敛为 `12 PDFs`、`Generate compare session`、`Research lead`、`Push verified outline`、`Export`，让 task lane 更像 compact queue 而不是长句说明列表。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、带重试的 Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 fresh-load local browser vision 仍将 center session timeline lane 视为 `/chat` 单一最明显 density hotspot，但确认 tasks-panel copy 与 badge 节奏已较上一轮更 calm、页面无明显 clipping/overlap；下一轮继续优先减少 center timeline lane 内多层卡片/徽标竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 确认当前单一最明显热点仍在左中 `Session timeline` 里靠上的 `Context / Threads / Foundry` 三张窄卡与其下 `Active sessions` 过渡带，因此本轮不扩张新模块，仅将 `Task lane` helper copy 从 `Route one block.` 校准为更自然的 `Route one blocker.`，并把 `Active sessions` helper copy 收敛为 `Next move stays on one quieter edge lane.`、同时将 session row 的右侧 meta 轨宽从 `4rem` 微放宽到 `4.2rem`，让 `12m / Map`、`1 blocker / Queue`、`Ready / Packet` 的双 pill 组合少一点横向挤压。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为页面整体 clean、无明显 clipping/overlap，但当前单一最明显 density hotspot 仍是左中 `Session timeline` 顶部的三张 summary cards 与 `Active sessions` 过渡带；下一轮继续优先减少该上半区的窄卡/徽标同屏竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 先确认当前最明显热点仍落在中心 `Session timeline` 下半部的 lower three-card checkpoint rail，因此本轮不扩张新模块，仅将三张 rail cards 在 `xl/2xl` 下的固定列宽从 `14rem/14.2rem` 微放宽到 `14.6rem/14.8rem`，并把 `Session focus` / `Checkpoint rail` helper copy 分别收短为 `Pin live claim.` 与 `Bridge notes calm.`，继续削减窄卡中的 wrapping 与 chip/text 竞争。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为本轮后 lower checkpoint rail 仍 dense，但不再是唯一最差热点；当前更明显的单一热点已转为整个 central-left timeline stack（top summary cards + `Active sessions` + lower three-card rail）的连续 card/chip 扫描压力。下一轮继续优先减少这条 central-left timeline lane 的整体层级与同屏卡片竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 再次确认当前单一最明显热点已从 right rail 收敛回 center lane，尤其 lower three-card checkpoint rail 比 `Active sessions` 与 right rail 更显拥挤，因此本轮不扩张新模块，仅将 `Task lane` 卡说明从 `Route the blocker.` 收短为 `Route one blocker.`，同步补强 `src/app/chat/page.test.tsx` focused 断言锁定该 copy 变更，以继续降低 lower center strip 的句长与换行压力。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为页面整体 coherent、无明显 overlap/clipping，但 center lane 仍是 `/chat` 单一最明显 density hotspot，且 lower three-card checkpoint rail 比 right rail 更 cramped；下一轮继续优先减少 lower rail 与 `Active sessions` 的同屏内容竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density 收敛 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 再次确认当前单一最明显热点仍是 right rail，尤其 `Session output / Status board / Foundry handoff` 在窄栏中仍承载最多 stacked microcopy / pills。因此本轮不扩张新模块，仅去掉 `Session output lane` 顶部重复的 session meta 徽标，把 `3 live` 并入统计 pills、保留 `Packet ready`，并把说明文案收短为 `Keep answer, risk, and export on one calm rail.`；同时将 `Risk` 说明从 `One review call waits.` 收敛为 `One review wait.`，并将 `Foundry handoff` 文案收敛为 `Bundle memory, sessions, and charts into one export packet.`，以进一步减少窄栏换行与视觉噪音。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 仍将 right rail 视为 `/chat` 单一最明显 density hotspot，但确认其当前首要压力已更集中在整体窄栏层级本身，而不是多余 meta 徽标或过长微文案；下一轮继续优先减少 right rail 的纵向分段与 nested card 感，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density 收敛 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 明确上一轮单一最明显热点仍在 right rail，因此本轮不扩张新模块，仅继续收短并压平 `Session output / Status board / Foundry handoff` 的 microcopy 与 badge 节奏——将 `Session output lane` 说明从 `Keep answer, risks, and export handoff on one quieter rail.` 收敛为 `Keep answer, risk, and export handoff on one calm rail.`，同步收紧其说明宽度、badge 间距与 `Packet ready` pill 尺寸；将 `Status board` signal stack 的间距、说明宽度与字级微收，使 `Sources / Risk / Locked / 1 held` 在窄栏中更少换行竞争；并将 `Foundry handoff` 说明收短为 `Bundle memory, sessions, and verified charts into one export packet.`、导出行说明收短为 `Brief + slides ready.`，同时压低说明宽度与 export row 的 gap。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 认为 right rail 相较上一轮已明显更 calm、不再是单一首要热点；当前最明显剩余热点已转移到 center `Session timeline / Active sessions / lower checkpoint cards` 区域，下一轮继续优先减少中心 lane 的 stacked cards / badges / microcopy 竞争，而不是回到右栏或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane density slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 再次确认当前单一最明显热点仍是 center `Active sessions` lane，因此本轮不扩张新模块，仅将 `Session timeline` 主栅格从 `xl:[minmax(0,0.92fr)_minmax(0,1.08fr)] / 2xl:[minmax(0,0.9fr)_minmax(0,1.1fr)]` 微收为 `xl:[minmax(0,0.88fr)_minmax(0,1.12fr)] / 2xl:[minmax(0,0.86fr)_minmax(0,1.14fr)]`，把 `Active sessions` helper copy 收短为 `Next move stays on one quieter edge lane.`，同时将 row 右侧 meta lane 从 `4.8rem` 再收至 `4.4rem`、row gap 从 `2.5` 收到 `2`、正文最大宽度从 `21ch` 收到 `19ch`，并把 session summary `Cluster map` 收短为 `Map`，再同步将 lower strip 前两张卡标题收短为 `Hold live claim` / `Route blocker`，以进一步压低 center lane 的 chip/title 竞争并验证热点是否仍稳定落在 `Active sessions`。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审与 fresh-load local browser QA。最新 browser vision 认为本轮后整体更 calm、无明显 clipping/overlap，但单一最明显 density hotspot 仍是 `Active sessions` center lane；下一轮继续优先减少该中心窄列的 metadata/badge 同屏竞争，而不是回到 right rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density slice：基于 sequential live/local browser QA，确认 local `/chat` 当前单一最明显热点仍是 right rail stack，因此本轮不扩张新模块，仅移除 `Status board` 中最末的 `Handoff route / Return to source` 信号行，把状态列表从 `3 signals` 收敛为 `2 signals`，并同步将 Foundry handoff 标题容器、说明段落与 export row 的 `max-width / spacing / row padding` 进一步收紧，让 `Session output / Status board / Foundry handoff` 上半段更像连续的 summary rail 而不是多层窄卡堆叠。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（log 复核 focused suite 继续全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及独立 reviewer 复审。最新 fresh-load local browser vision 仍把 right rail stack 视为 `/chat` 首要 density hotspot，但本轮后其纵向内容竞争已进一步减少；下一轮继续优先压平 right rail 的卡层级与微文案，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` density vertical slice：依据 sequential live/local browser QA，先确认 live `/chat` 仍是 marketing landing，而 local `/chat` 在上一轮后已将首要热点从 center top summary row 转移到 right rail，因此本轮不扩张新模块，仅将顶部三张 summary mini-cards 收短为 `Thread anchor / Live switchboard / Answer handoff` 并微放宽 `xl` 卡宽到 `10.95rem`、收紧标题/说明行高，让 center top row 更像 orientation band；同时把 `Session output` 元信息合并为单个 `Session 07 · 3 live · 5 open` metadata pill，仅保留 `Packet ready` 状态 chip，并把 `Status board` 从 card-within-card 小白卡改成更轻的分隔行，配合收紧 `Foundry handoff` 描述宽度与垂直间距，继续压低 right rail 的 stacked chips / nested cards 扫描负荷。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及独立 reviewer 复审；reviewer 曾发现 lower flow rail 测试断言误指向 summary card 文案，已立即修正并复测通过。最新 fresh-load local browser QA 认为 center top summary row 已明显 calmer，当前单一最明显 density hotspot 已转为 right rail 的 `Session output / Status board / Foundry handoff` 纵向堆叠；下一轮继续优先减少 right rail 上半段的 nested boxes / pills 数量，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` 右栏减压 slice：先用 browser 顺序复核确认 live `/chat` 仍是 marketing landing，而 local `/chat` 的最明显拥挤点此前落在 right rail；因此本轮不扩张新模块，仅将 `chat-right-rail-grid` 从 `xl:[minmax(0,0.94fr)_minmax(0,1.06fr)] / 2xl:[minmax(0,0.96fr)_minmax(296px,0.9fr)]` 收敛为 `xl:[minmax(0,1fr)_minmax(0,0.98fr)] / 2xl:[minmax(0,1fr)_minmax(288px,0.88fr)]`，同时把 `Regulatory memo · current answer` / `Keep the answer and export lane visible in one bridge card.` / `3 exports ready` / `Bundle memory, sessions, and verified charts into one review packet.` / `Brief and slides staged together.` 收敛为更短、更 calm 的 `Regulatory memo · answer lane`、`Keep the answer and export lane in one calmer summary card.`、`Packet ready`、`Bundle memory, sessions, and verified charts into one ready packet.` 与 `Brief and slides staged together.`，并同步更新 `src/app/chat/page.test.tsx` focused 断言以锁定新布局与文案。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 认为 right rail 已比上一轮更不 cramped、无明显 clipping/overlap，当前最明显密度热点已重新回到 `Active sessions` 与 lower center lane，而非 right rail；下一轮继续优先减少 center Session timeline lane 的同屏竞争，而不是重新扩张 right rail 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 与 controller-side browser/DOM 复核，确认当前最明显剩余压力已从 right rail 回到 center `Active sessions` lane，因此本轮不扩张新模块，仅继续压短右侧与下半部 bridge copy，让页面重心更明确地暴露 center-lane hotspot——将 lower checkpoint rail 第三卡从 `Fold into Foundry / Keep the bridge rail calm.` 收敛为 `Foundry fold / Keep bridge notes calm.`，把 `Reg memo final` row 的右侧 token 顺序改为更贴近“状态→产物”的 `Ready / Packet`，同时将 `Session output` 说明从 `Keep the current answer...` 收短为 `Keep the answer...`，并把 `Packet export` 说明从 `Brief, appendix, and slides staged together.` 收短为 `Brief and slides staged together.`。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 认为 lower checkpoint rail 比上一轮更 calm、right rail 也更少 scan-heavy；当前最明显剩余热点仍是 center `Active sessions` 区块，它依旧比 right rail 更 cramped。下一轮继续优先减少 `Active sessions` 与中心窄列的同屏竞争，而不是重新扩张 right rail 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 与 DOM 几何复核确认当前最明显剩余压力仍在 right rail 内部的文字扫描负荷，因此本轮不扩张新模块，仅将 Foundry handoff 主标题从 `Foundry reviewer packet` 收短为 `Foundry packet`、说明从 `Bundle project memory, sessions, and verified charts into one export packet.` 收短为 `Bundle memory, sessions, and verified charts into one review packet.`，并把唯一导出行标签从 `Export summary` 收敛为 `Packet export`，以验证“继续压短 right rail 末端 handoff copy”是否能比再改 center lane 更有效地降低右栏末端的阅读负担。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser QA 仍认为 right rail 是 `/chat` 最明显的 scan-heavy hotspot，但本轮后 Foundry handoff 区块标题/导出项更短、更少抢占窄栏注意力；下一轮继续优先减少 right rail 内 Session output / Status board / handoff 的 stacked copy 竞争，而不是回到单纯放宽 center lane。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load local browser QA 与 controller-side DOM/几何复核，确认当前最明显剩余压力已转向 right rail 本身而非 center lane，因此本轮不扩张新模块，仅将 `chat-right-rail-grid` 从 `xl:1 列 / 2xl:[minmax(0,1fr)_minmax(308px,0.92fr)]` 收敛为 `xl:[minmax(0,0.94fr)_minmax(0,1.06fr)] / 2xl:[minmax(0,0.96fr)_minmax(296px,0.9fr)]`，让 Tasks 提前与 Session output / Status board / Foundry handoff 并排分担纵向堆叠；同时把 Session output 顶部 stats 行增加 `xl:gap-2.5`、将 Foundry export asset rows 的垂直内边距从 `py-3` 收到 `py-2.5` 并把 export pill tracking 从 `0.14em` 收到 `0.12em`，以验证“减少右侧长列堆叠 + 微收 export rows/token 紧张感”是否能比继续压 center lane 更有效地降低 `/chat` 的整体拥挤源。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 local browser QA 仍认为 right rail 是当前最明显 density hotspot，但本轮后其纵向堆叠已较上一轮更可分段扫读、export rows 也更短促；下一轮继续优先减少 right rail 内部的 card-within-card / signal stack 压力，而不是回到单纯 widening center lane。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load live/local 对照与 local browser QA，确认当前 center lane 最明显剩余压力仍集中在 `Active sessions` rows 本身而非 top summary cards 或 lower checkpoint rail，因此本轮不扩张新模块，仅将顶部 Threads 摘要卡说明从 `Branch clean.` 收敛为 `Live state stays surfaced.`，同时把 `Active sessions` helper copy 从 `Signal and next move stay on one calmer edge lane.` 收敛为 `Signal and next move stay on one tighter edge lane.`；再把 session rows 的 `sm` 右侧 meta lane 从 `minmax(5.1rem,auto)` 微收至 `minmax(4.8rem,auto)`、gap 从 `3` 收至 `2.5`、正文最大宽度从 `22ch` 收到 `21ch`，并同步将第二/第三条 session 的 meta-description 重新配对为 `1 blocker + Queue`、`Packet + Ready` 与 `Outline locked.`，以验证“减少 Active sessions 右侧 meta 轨宽竞争 + 让 Threads 摘要卡更直接说明 live state”是否能继续降低 center edge lane 的碎片感而不破坏 lower rail 的稳定性。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为顶部三张 summary cards 仍保持 calm、lower checkpoint rail coherent，而 `Active sessions` 依旧是 center lane 最明显的 cramped hotspot；本轮调整后该区域更像受控 edge lane 而非松散 dashboard，但下一轮仍应优先继续减少 `Active sessions` rows 的同屏竞争，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：fresh-load live/local QA 显示 `/chat` 最明显剩余密度仍在 center top summary cards 与 checkpoint rail 文案竞争，因此本轮不扩张新模块，仅将 `Threads` 摘要卡说明从 `Live state stays surfaced.` 再收短为 `Live state surfaced.`，同时把 `Checkpoint rail` 说明从 `Keep the bridge summary calmer.` 收短为 `Keep the bridge rail calm.`，并将 top summary cards 在 `xl` 下的固定列宽从 `10.35rem` 微放宽到 `10.7rem`、摘要说明最大宽度从 `12ch` 微收至 `11ch`，以验证“再短一层 copy + 轻放宽 top summary 列宽”是否能继续降低 center upper lane 的碎片感，而不重新放大下方 `Active sessions` 与 lower strip 的竞争。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 top summary cards 比上一轮更直接、checkpoint rail 文案更安静，整页未见新的 clipping/overflow；当前最明显剩余压力仍是 `Active sessions` rows 本身与 live 相比仍略显 cramped，下一轮继续优先减少 `Active sessions` 的同屏竞争，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load live/local 对照与 local browser QA，确认当前更显眼的剩余压力仍在 `Active sessions` 左侧窄轨与顶部三张 summary cards 的语义密度，因此本轮不扩张新模块，仅将顶部 summary 第一张卡标题从 `One lane per session.` 收短为 `One lane per thread.`，同时把 `Active sessions` helper copy 从 `Signal and next action stay on one calmer edge lane.` 收短为 `Signal and next move stay on one calmer edge lane.`，并把 session rows 的 `sm` 右侧 meta lane从 `minmax(5.4rem,auto)` 微收至 `minmax(5.1rem,auto)`，以验证“再短一层 thread/move 文案 + 再轻收 meta 轨宽”能否继续减少中心窄列的同屏竞争而不破坏既有 shell 结构。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 仍认为 `Active sessions` 左侧窄轨和顶部三张 summary cards 是 `/chat` 最容易显得紧的区域，但本轮 copy/轨宽收敛后未见新的 clipping/overflow，且中心 lane 的阅读顺序更直接；下一轮继续优先减少 center summary/session lane 的同屏信息竞争，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：基于 fresh-load local browser QA 明确指出 lower three-card checkpoint rail 仍是最明显 cramped hotspot，尤其第三张卡标题/说明/badge/CTA 在窄卡中竞争空间；因此本轮不扩张新模块，仅把第三张卡文案从 `Fold checkpoints into Foundry / Keep the bridge on one calmer rail.` 收敛为 `Fold into Foundry / Keep one calmer bridge rail.`，并把 lower strip 卡宽从 `12.6rem / 13rem` 放宽到 `13.2rem / 13.6rem`，同时给第三张卡增加轻微内边距与 chip/CTA 间距、收短标题/说明最大宽度，以验证“缩短第三卡 copy + 微放宽 rail 宽度”比继续加列或大改结构更能缓解该 hotspot。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 `Fold into Foundry` 第三卡已明显 calmer，lower checkpoint rail 不再是唯一最差热点；当前更明显的密度压力已转向 `Tasks` 与右侧 `Session output / Status board / Foundry handoff` 信息堆叠。下一轮继续优先减少右侧 rail 与 tasks 区块的同屏信息竞争，而不是回到扩大 lower rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 已确认 lower three-card checkpoint rail 仍是最明显 cramped hotspot，而当前最有效的收敛方向不是再压短结构，而是给三卡更多横向呼吸；因此本轮不扩张新模块，仅把 lower center strip 的卡宽从 `13.2rem / 13.6rem` 放宽到 `14rem / 14.2rem`，并将第三张 `Checkpoint rail` 卡说明从 `Keep one calmer bridge rail.` 进一步收敛为 `Keep the bridge summary calmer.`，以验证“轻放宽 rail + 再短一层 helper copy”是否能让三卡更像稳定 summary rail 而非压缩 utility tiles。该里程碑已严格按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及 fresh-load local browser QA。最新 QA 认为 lower checkpoint rail 已较上一轮更 calm、三卡更像分离但相关的 summary steps；当前更明显的剩余密度压力已转向上方那组三张 summary mini-cards（`One lane per session / Switch live threads / Promote the locked answer`）与少量上半部紧凑换行。下一轮继续优先减少这组三张 top summary cards 的压缩感，而不是回到继续挤压 lower rail 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：根据 fresh-load local browser QA 确认当前 center Session timeline lane 中最明显 cramped hotspot 已集中在 lower three-card checkpoint rail，尤其 `Checkpoint rail` 卡的标题、说明、badge 与 CTA 在窄卡宽里竞争空间；因此本轮不扩张新模块，仅把该第三张卡的 copy/CTA 收敛为 `Fold checkpoints into Foundry`、`Keep the bridge on one calmer rail.` 与 `Open rail`，保留 `Foundry sync` pill 与 `/help/foundry` 去向不变，以验证“减少该卡内部文本负荷”比继续放宽整条 center lane 更能缓解拥挤。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为 `Active sessions` 相比下半部已不再是最挤区域，当前 center lane 最明显剩余热点仍是 lower checkpoint rail 本身整体仍偏窄，尤其第三张 rail card 虽较上一轮 calmer 但仍是最可见的局部密度点；下一轮继续优先减少 lower center strip 的窄卡信息竞争或进一步重排该 rail，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：依据 fresh-load local browser QA 与 DOM 几何复核确认当前最明显问题是 `Active sessions` 三条 rows 的右侧 meta rail 过窄、导致 pills/标题竞争宽度，而 lower three-card checkpoint strip 又比 live 更横向铺开；因此本轮不扩张新模块，仅把 `Active sessions` row 的 `sm` 右侧轨道从 `minmax(4.6rem,auto)` 放宽到 `minmax(5.4rem,auto)` 并同步把 `sm` gap 从 `2.5` 回提到 `3`，让 `12m / Cluster map / Queue / 1 blocker / Outline / Ready` 的堆叠更少压缩；同时把 lower center strip 的 `xl/2xl` 固定卡宽从 `13.2rem / 13.6rem` 微收到 `12.6rem / 13rem`，让 `Hold live hypothesis / Assign blocker / Checkpoints fold into Tasks and Foundry.` 三卡更像受控 bridge rail 而非过宽横铺层。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为这一轮后 center lane 的最明显问题已从“右侧 meta rail 被挤爆”收敛为“左侧 Active sessions 窄轨与 lower checkpoint strip 仍比 live 更密一些，但没有明显 clipping/overflow”；下一轮继续优先减少 center lane 的遮挡/叠层感，而不是再单纯 widening 或扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 fresh-load local browser QA 反复指出 `Active sessions` 仍是整页最明显 cramped hotspot 的问题，本轮不再继续放宽整个中心主栅格，而是直接压缩 `Active sessions` 每条 row 的右侧 meta lane 与文本载荷——将 row 右列从 `minmax(5.4rem,auto)` 收到 `minmax(4.6rem,auto)`、row gap 从 `3` 轻收至 `2.5`，正文最大宽度从 `24ch` 收到 `22ch`，同时把后两条 session 的双 pill 文案改写为更短、更清晰的 `Queue / 1 blocker` 与 `Outline / Ready`，配合 `Replay queued.` 的更短说明，验证“减少同屏 meta token 宽度竞争”比继续 widening 更有效。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA。最新 QA 认为 session meta pills 确实更 calm、未见 clipping/overlap，但 `Active sessions` 仍是整页最明显密度热点；下一轮继续优先减少 `Active sessions` 左侧窄列的同屏卡片/说明竞争，必要时再触及其相邻下半部 center strip，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 fresh-load local browser QA 持续指出 center `Session timeline` lane 尤其 `Active sessions` 与 lower three-card strip 仍是整页最明显 density hotspot 的问题，本轮先做一次更直接的结构验证——将 `Session timeline` 主栅格从 `xl:[0.92fr/616px] / 2xl:[0.9fr/648px]` 继续放宽到 `xl:[0.8fr/724px] / 2xl:[0.78fr/768px]`，同步把 Tasks 前两条 note 收短为 `Ground retrieval before review.` 与 `Keep this lane isolated.`，以区分“宽度不够”与“同屏内容过多”两类根因。该里程碑已按 TDD 先补强 `src/app/chat/page.test.tsx` 对新 tasks microcopy 与当前 lower-strip/Active-sessions 约束的断言，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、连续两次 `docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 结论是：更宽 center lane 仅带来小幅改善，`Active sessions` 与下半部三卡 strip 仍明显偏拥挤，说明下一轮优先级应从“继续放宽列宽”切换为“减少同屏卡片/文案/徽标数量并压平嵌套层级”，而不是继续单纯 widening。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：基于 fresh-load live marketing 对照与 local browser QA 仍指出 lower center strip 偏窄的问题，将原先独立的 `View checkpoints` footer-like 提示卡回收进 center strip 本体，把 `appShellFlowCards` 恢复为三张并行窄卡轨道，同时把第三张卡重构为 `Checkpoint rail`（`Checkpoints fold into Tasks and Foundry.` / `Foundry sync` / `Folded` / `View checkpoints`），并同步将前两张卡标题收短为 `Hold live hypothesis` / `Assign blocker`，让 lower center strip 从“2 张窄卡 + 1 个额外 footer 提示”收敛为更一致的三卡摘要轨道，减少垂直堆叠与碎片感。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 lower center strip 比上一轮更 calm、未见明显 clipping/overlap；当前最明显剩余密度问题已转为其左侧 `Active sessions` 下方的窄 timeline/flow lane 本身仍偏瘦、badge 与短标题仍有压缩感，下一轮继续优先减少该窄 lane 的同屏信息量或进一步放宽其列宽，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 browser QA 指出的中心窄列仍偏拥挤问题，仅收敛 `Active sessions` 与下方 flow-checkpoint CTA——将 session rows 右侧 meta lane从 `8.2rem` 收到 `7.2rem`、row gap 从 `4` 收到 `3`，并把三条 session 文案同步压短为 `Patent landscape · compare / Clinical diligence · replay / Regulatory memo · final` 与更短说明文；同时将 `Active sessions` helper copy 改为更直接的 `One card per live thread...` 提示，减少卡内碎片感；另外把下方 CTA 卡改成 `xl` 下左文右按钮并将 copy 收短为 `More flow checkpoints stay tucked under Tasks and Foundry.` / `Keep the bridge preview on one calmer center scan path.`，按钮标签同步收为 `View all checkpoints`，使该 footer-like 卡更轻、更不挤压中心列。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 功能/安全复审，以及 fresh-load local browser QA。`docker compose up -d --build` 本轮因 Docker Hub `docker/dockerfile:1.7` 远端鉴权/网络不可达失败而未能完成重建，但现有本地服务继续在 `3001` 正常返回 200，且该失败已确认为外部 registry 依赖问题而非当前 diff 逻辑回归。最新 QA 认为当前没有明显 overlap/clipping，CTA 更像 footer-like summary；当前最明显剩余差距已收敛为 `Active sessions` 与其下方两张窄卡仍偏窄、长标题和 pills 仍造成局部 wrapping 压力，下一轮继续优先减少中心窄卡同屏信息量或进一步调整该窄列宽度，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进 `/chat` 的一个更小步 density slice：针对 fresh-load local browser QA 仍指出 center lane 文字卡偏窄的问题，仅对 `Active sessions` 与其下方两张 flow cards 做更轻量的宽度/文案收敛——将 `Active sessions` helper copy 收短为 `Keep the live thread and next move on one quieter edge lane.`，把 session row 右侧 meta lane 从 `6.4rem` 收到 `5.9rem`，同时给 session description 增加 `max-w-[24ch]` 以抑制长行拉伸；并把下方 `Session focus / Task lane` 两张 flow cards 的 `xl/2xl` 宽度从 `13rem / 13.4rem` 放宽到 `13.8rem / 14.2rem`，让下半部更接近 compact bridge strip 而不是过瘦竖卡。该里程碑已按 TDD 先更新 `src/app/chat/page.test.tsx` 形成红灯，再完成实现，并通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load live/local browser QA 与 DOM 几何复核。最新 QA 认为页面整体 coherent 且无明显 clipping/overflow，但最明显剩余密度问题仍是中心 `Active sessions` 卡和其下方 `Session focus / Task lane / View all checkpoints` 一带：内容已比上一轮更 calm，却依旧因为 center lane 宽度有限而产生明显 wrapping；下一轮继续优先减少 center lane 文本载荷或进一步重排该窄列，而不是扩张新模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮对本地 `/chat` 再做一次更聚焦的 density pass：将 `Active sessions` 区块说明文案压缩为更短句，并把每条 session row 改为内容/元信息双区布局，让时间戳与 summary pill 不再挤占正文行；同时将 Tasks 三条待办的 label/note 收短、轻收 card 圆角与纵向间距，减少右 rail 的阅读负荷；再把 `Session output / Status board / Foundry handoff` 统一收紧为更低的卡片高度与更紧凑的 summary-grid / signal rows，使 right rail 比上一轮更不 cramped。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任何现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为中心列比上一轮更易扫读、right rail 也更有呼吸感，未见明显 overlap / clipping；当前剩余最明显差距收敛为 center top 三张窄卡与部分 metadata/pill 仍略紧、`View all flow checkpoints` 底部 CTA 锚定感仍可继续优化，下一轮继续优先收敛这些中心窄卡与 CTA 对齐，而不是扩张新模块。
- [x] 建立 `/chat` 公共 app shell 占位页与 project / session / library / tasks / foundry / persona / im / settings 的应用内 IA 占位
- [x] 多轮将 `/chat` 从 docs 风格说明块收敛为 docs→workspace bridge：补齐 sticky shell、Project memory rail、Session timeline、Tasks、Session output、Foundry handoff 与对应路由/测试
- [x] 本轮继续完成一次更偏 `/chat` right-rail breathing-room 与 export copy tightening 的小步收敛：保持 live `https://sciclaw.ai/chat` fresh-load 仍是公开 marketing landing 的前提下，继续将本地 `/chat` 作为 docs→workspace bridge，并把主区栅格扩展为 `xl:grid-cols-[minmax(0,1.04fr)_minmax(500px,0.96fr)]` / `2xl:grid-cols-[minmax(0,1fr)_minmax(528px,1fr)]`，同时把右侧 rail 调整为 `xl:grid-cols-[1.06fr_0.94fr]` / `2xl:grid-cols-[1.04fr_0.96fr]`，让 Tasks 与 Session output 的视觉负载更均衡；再将 `Session output` / `Foundry handoff` 文案收短为更紧凑但仍保留语义的 product copy（如 `Keep the current answer... visible in one bridge card.`、`Foundry reviewer packet`、`Keep citations and checkpoints attached to the draft.`、`Presentation-ready talking points for Foundry.`），并同步收紧 export/summary 文案容器宽度，减少右侧 rail 的 cramped feeling 而不丢失 Foundry destination 与 review/traceability 语义。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审与 fresh-load local browser QA。最新 QA 认为 `/chat` 右侧 rail 比上一轮更不拥挤、Tasks/right-rail balance 更均衡，页面无明显 clipping/overlap；当前剩余最明显密度问题继续收敛为中心下方三张 flow cards 与顶部窄卡仍是整页最密区域，下一轮继续优先减少这些窄卡的同屏说明密度，而不是扩张新模块。
- [~] 让 landing 与 docs 导航正确串联到 app shell 占位；本轮已完成一次更偏 `/chat` 密度治理的小步 vertical slice：在不影响 `/root/nexus-mail` 任何现有服务/端口、继续仅使用 `3001` 的前提下，收短 Tasks / Session output / Foundry handoff 的局部文案，为 task rows 增加可扫描的 meta pills（如 `12 PDFs queued`、`Owner · Research lead`、`Export lane`），放大 right rail 间距与导出行内留白，并把中心两组三列卡片在 `xl` 下改为固定更宽列宽+`justify-between`，同时让卡片顶部 meta badges 使用 `self-start` 避免视觉挤压。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 21 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、静态安全扫描 0 命中、独立 reviewer 复审，以及 fresh-load local browser QA。最新 QA 认为 right rail 继续保持更 breathable、export/handoff 区域较上一轮更不 boxed-in，中心列也比上一轮更不 jammed，且未见明显 overlap/clipping；当前剩余最明显差距收敛为中心窄列中的少数 tiny pills/badges 仍略显紧、整页中心信息密度仍高于 live marketing landing，下一轮继续优先减少中心卡片的同屏文字负荷与 pill 数量，而不是扩张 IA.
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` 右栏语义校准 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，而 fresh-load local `/chat` 的 browser vision 继续判定 right rail `Session output / Status board / Foundry handoff` stack 仍是当前单一最高优先级 density hotspot，因此本轮不扩张新模块、也不再继续 widening center lane，仅将 Foundry handoff copy 从 `Bundle memory and verified charts into one ready packet.` / `Brief + slides staged.` 校准为更贴近 shell 语义与既有 docs→workspace bridge 叙事的 `Bundle memory, sessions, and verified charts into one ready packet.` / `Brief and slides staged together.`，同步更新 `src/app/chat/page.test.tsx` focused 断言锁定该 contract。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 local browser QA / vision 仍认为 right rail 是 `/chat` 首要 density hotspot，而 lower checkpoint rail 相对最 calm；下一轮继续优先减少 right rail stack 的纵向扫描负担或层级数，而不是重新扩张 center lane 或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail wording slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 local browser vision 确认当前单一最明显剩余热点依旧是 right rail，因此本轮不扩张新模块，只收敛 `Session output / Status board / Foundry handoff` 中最挤的摘要文案与 token 负荷——将 `Evidence chain` / `Open risk` 调整为更短的 `summary↔description` 组合（`Sources locked`、`1 call held`、`Citations pinned.`、`One review call waits.`），并把 `Session output lane` 的说明从 `Keep the answer lane in one calmer summary card.` 收敛为 `Keep the answer, risks, and export handoff on one quieter rail.`，同时把元信息从 `Session 07 · 3 live · 5 open` 收短到 `Session 07 · 3 live`，以在不改动结构的前提下减少 right rail 的窄栏换行压力。该里程碑已通过 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer 复审。最新 fresh-load local browser vision 仍将 right rail 视为 `/chat` 单一最明显 density hotspot，但确认当前没有 clipping / overlap；下一轮继续优先减少 right rail 的卡层级、长标题与多枚小 badge 的同屏竞争，而不是回到 widening center lane 或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Active sessions + help-shell contract 校准 slice：controller 侧先从当前 worktree 与 focused tests 出发复核本轮改动确实只涉及 `/chat` center-lane `Active sessions` 的 meta/token 微调与 help 文档文案契约同步，不扩张新模块；随后根据 `src/app/chat/page.tsx` / `src/app/landing-data.ts` 的当前 diff，将 `Active sessions` helper 文案继续收敛为 `Next move stays on one quieter edge lane.`、session rows 右侧轨宽回调为 `sm:grid-cols-[minmax(0,1fr)_minmax(5rem,auto)]` 并把第二条 meta 从 `1 blocker` 校准为更贴近当前 warn 语义的 `1 hold`，同时把 `src/app/help/help-page.test.tsx` 对 getting-started / chat / settings 三个公开文档页的断言更新为匹配现行 `landing-data.ts` 中已落地的 heroParagraphs 与 section 标题（如 `Use SciClaw to move from a rough research question...`、`This route intentionally feels more product-like...`、`Settings stays intentionally light...`），修复了此前 help focused suite 因测试仍跟旧文案契约而产生的 3 个失败。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check` 与新增行静态安全扫描 0 命中。最新 controller-side 结论：本轮主要完成的是 `/chat` 当前窄轨状态文案与 help shell 测试契约的一致性修复，页面/路由均保持稳定，下一轮可继续回到 `/chat` center/right lane 的 density hotspot 做更小步视觉/信息密度收敛，而不是回退到失配的 help 文案断言。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail density 收敛 slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 明确当前单一最明显热点仍是 right rail，尤其 `Status board` 与 `Foundry handoff` 在窄栏中的 stacked microcopy / badge 竞争。因此本轮不扩张新模块，仅将 `Status board` 两条信号文案从较长组合进一步压短为 `Sources / Risk` 与 `Locked / 1 held`，把 `Session output lane` 说明从 `Keep the answer, risks, and export handoff on one quieter rail.` 再压短为 `Keep answer, risks, and export handoff on one quieter rail.`，并把导出行说明从 `Brief and slides together.` 收敛为 `Brief + slides staged.`，以减少右栏窄容器中的换行与扫描阻力。该里程碑已对当前 diff 重新执行 `pnpm --dir /root/sciclaw test -- src/app/chat/page.test.tsx`（实际仍复跑 4 个 Vitest 文件 / 23 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check` 与新增行静态安全扫描 0 命中；controller-side Review 1/2/3 也已按当前 diff 重跑通过。最新 fresh-load local browser vision 仍将 right rail 视为 `/chat` 首要 density hotspot，但确认其首要根因已更集中为窄栏中的 stacked/nested micro-components；下一轮继续优先减少 right rail 的嵌套层级与小 badge 竞争，而不是回到 widening center lane 或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail wording 收敛 slice：controller 侧从当前 worktree 与 focused suite 出发，确认本轮 diff 只涉及 `Session output / Foundry handoff` 右栏文案与窄卡内部 spacing 的小步收敛，不扩张新模块；随后将 `Session output lane` 描述从 `Keep answer, risk, and export on one calm rail.` 收敛为更直接的 `Keep answer, risk, and export aligned.`，将上方摘要卡文本容器微收至 `max-w-[14.5ch]` 并把 stats pills gap 从 `1.5` 放宽到 `2`，同时把 Foundry handoff eyebrow / description / export asset copy 继续压短为 `Ready packet`、`Bundle memory, sessions, and verified charts into one export packet.` 与 `Brief + slides staged.`，再同步更新 `src/app/chat/page.test.tsx` 的 focused contract 断言，修复此前测试仍绑定旧文案而导致的 2 个失败。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`（7 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审与 fresh-load local browser vision 复核。最新 controller-side 结论：right rail 当前 coherent、无明显 clipping/overlap，且页面单一最明显 density hotspot 仍在 center lane 而非 right rail；下一轮继续优先减少 center timeline / tasks / lower checkpoint rail 的同屏竞争，而不是回到右栏扩张或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` top-mini-card copy tightening slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，并以 fresh-load local browser vision 继续确认当前单一最明显 density hotspot 落在 center timeline 顶部 mini-card 与其右侧 task-column 交界，而非 right rail；因此本轮不扩张新模块，仅将 `appShellCards` 中 `Live switchboard` 与 `Edge lane` 的描述从 `3 live lanes.` / `1 blocker routed.` 收敛为更短的 `3 live.` / `1 blocker.`，同步把 `src/app/chat/page.test.tsx` 首个 smoke contract 更新为与新文案精确对齐。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审。最新 fresh-load local browser vision 认为 top mini-card row 因短句化而略更 calm、无明显 overlap，但当前单一最明显 hotspot 仍是 center timeline 顶部 mini-card 与右侧 `Queue the next research slice` / `Heatmap-ready` 交界处的 badge+card seam；下一轮继续优先减少该 center seam 与 `Active sessions` 的同屏竞争，而不是回到 right rail 或扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-rail width rebalance slice：controller 侧先从当前 worktree、live/local browser 对照与本地 DOM 几何复核确认当前 `/chat` 单一最明显 density hotspot 已从 right rail 文案本身收敛为右侧两列之间的整体宽度分配——右栏 `Session output / Status board / Foundry handoff` 虽已较 calm，但 `Tasks` 列仍偏瘦、导致右半区左右两列的扫描节奏不够均衡，因此本轮不扩张新模块，仅把 `chat-right-rail-grid` 从 `xl:[minmax(0,1.06fr)_minmax(272px,0.92fr)] / 2xl:[minmax(0,1.08fr)_minmax(284px,0.9fr)]` 调整为 `xl:[minmax(196px,1.14fr)_minmax(248px,0.86fr)] / 2xl:[minmax(212px,1.12fr)_minmax(260px,0.88fr)]`，让 `Tasks` 提前获得更稳定的阅读宽度、同时轻收 `Session output` 列的横向占比，以验证“更均衡的右半区两列配比”比继续压短文案更能降低 `/chat` 右半区的 scan-heavy seam；并同步更新 `src/app/chat/page.test.tsx` 对新 grid class 的 focused 断言。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：right rail 当前保持 coherent、无明显 clipping/overlap，右半区的最明显压力已不再是 handoff 文案过长，而是后续仍可继续观察 center timeline 与 right-half seam 的整体层级平衡；下一轮继续优先对 `/chat` center/right 交界的 density seam 做更小步收敛，而不是回到大幅 widening 或扩张新模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center/right seam 收敛 slice：controller 侧先从当前 worktree、`todo.md` 与 live/local `/chat` 顺序复核出发，确认当前单一最明显 density seam 已从 right rail 进一步收敛为 center timeline 顶部三张 mini-cards 与其右侧 task-column 的扫描交界，因此本轮不扩张新模块，仅把 `src/app/chat/page.tsx` 顶部 `appShellCards` 容器在 `xl` 下的固定列宽从 `11.3rem` 微放宽到 `12rem`，让 `Thread anchor / Live switchboard / Edge lane` 三张 mini-cards 获得更稳定的横向呼吸、减少和右侧 `Tasks` 列交界处的 badge/card seam 压力；并同步更新 `src/app/chat/page.test.tsx` focused 断言以锁定该宽度契约。该里程碑已通过 Review 1 功能/规格复核（确认仅是 top mini-card row 的更小步 breathing-room slice，未扩张模块且 `/chat` 公开桥接语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、独立 reviewer JSON 复审通过）。最新 controller-side 结论：right rail 当前保持 coherent、无明显 clipping/overlap，而 `/chat` 剩余最明显热点仍是 center timeline 顶部 mini-card 与右侧 task seam 的轻微 scan-heavy 感；下一轮继续优先围绕该 center/right seam 与 `Active sessions` 邻接区做更小步密度收敛，而不是回到右栏扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` tasks-panel density slice：controller 侧先从当前 worktree、live/local `/chat` 顺序复核与 browser snapshot 出发，确认本轮热点仍是 center timeline 顶部 mini-cards 与其右侧 Tasks seam，而 Tasks 列内部仍可先做一层更轻量的 copy/line-length 收敛，因此本轮不扩张新模块，仅将 `src/app/chat/page.tsx` 的 Tasks 标题从 `Queue the next research slice` 收短为 `Queue next research slice`，并把 task note 最大宽度从 `22ch` 微收至 `20ch`，让右半区首张 task card 标题与三条 task notes 更快扫描、减少与 top mini-card row 交界处的文本牵引；同步更新 `src/app/chat/page.test.tsx` focused 断言锁定新标题契约。该里程碑已通过 Review 1 功能/规格复核（确认仅是 Tasks 面板 wording/density 的小步收敛，未扩张模块且 `/chat` 公开 bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、独立 reviewer JSON 复审通过）。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap，right rail 也未重新成为主热点；下一轮继续优先围绕 center timeline 顶部 mini-card 与 Tasks/Active sessions 邻接区做更小步密度收敛，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` right-half seam 收敛 slice：controller 侧先从当前 worktree、focused tests、fresh-load local `/chat` snapshot 与 DOM 几何复核出发，确认当前右半区仍可继续通过更均衡的 Tasks/Session-output 配比来减少 center/right seam 的 scan-heavy 感，而无需扩张新模块；因此本轮仅将 `chat-right-rail-grid` 从 `xl:[minmax(196px,1.14fr)_minmax(248px,0.86fr)] / 2xl:[minmax(212px,1.12fr)_minmax(260px,0.88fr)]` 调整为 `xl:[minmax(212px,1.18fr)_minmax(236px,0.82fr)] / 2xl:[minmax(224px,1.16fr)_minmax(252px,0.84fr)]`，让 Tasks 列获得更稳定宽度、右侧 output rail 略收；同时把 Tasks 标题从 `Queue next research slice` 收短为 `Queue next slice`，并把 task note 最大宽度从 `20ch` 再收至 `18ch`，以进一步降低 top task card 在 center/right 交界处的文本牵引。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap；right rail 本身未重新成为主热点，但 center/right seam 仍可继续通过局部 copy 与列宽微调收敛，下一轮继续优先围绕 `/chat` 的 Tasks/Active sessions 邻接区做更小步密度治理，而不是回到右栏扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Tasks 标题密度收敛 slice：controller 侧先从当前 worktree、live/local `/chat` 顺序复核与 Next.js 本地 page/layout 文档读取出发，确认本轮仍应保持 page 级公开路由不变、仅做一处 center/right seam 的真实 UI 小步收敛，因此不扩张新模块，仅将 Tasks 面板标题从 `Queue next slice` 收短为 `Queue next lane`，并把标题最大宽度从 `15ch` 微收至 `14ch`，以减少 top mini-card row 与右侧 task card 交界处的文本牵引；同时同步更新 `src/app/chat/page.test.tsx` focused 断言，保持页面与测试契约一致。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap，本轮主要验证了通过更短 Tasks 标题即可继续轻收 center/right seam 的扫描压力；下一轮继续优先围绕 `/chat` 的 top mini-card / Tasks / Active sessions 邻接区做更小步密度治理，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Tasks microcopy density slice：controller 侧先从当前 worktree、`todo.md`、local `/chat` fresh-load snapshot 与现行 `landing-data.ts` / `page.test.tsx` 契约出发，确认当前 center/right seam 仍主要由 Tasks 首卡与相邻 mini-card / Active sessions 邻接区的扫描节奏决定，因此本轮不扩张新模块、也不改动更大栅格，仅把 `appShellTodos` 中三条 Tasks microcopy 继续压短为 `Heatmap / Ground retrieval.`、`Research lead / Keep lane isolated.`、`Export / Promote brief.`，同步更新 `src/app/chat/page.test.tsx` focused 断言以锁定新 contract，从而验证“进一步缩短 task meta 与 note 句长”能否在不重做结构的前提下继续降低 center/right seam 的文本牵引。该里程碑已通过 Review 1 功能/规格复核（确认仅是 `/chat` Tasks 面板微文案密度收敛，未扩张模块且公开 bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、独立 reviewer JSON 复审通过）。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap；right rail 未重新成为主热点，而 `/chat` 仍可继续优先围绕 top mini-card / Tasks / Active sessions 邻接区做更小步密度治理，下一轮继续沿该 seam 收敛而不是回到右栏扩张或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` Tasks wording tightening slice：controller 侧顺序复核 live `/chat` 仍是 marketing landing，本地 `/chat` fresh-load 也继续保持 docs→workspace bridge shell，无明显 clipping/overlap；结合当前 DOM/snapshot 与既有 `todo.md` 执行脉络，确认 center/right seam 仍主要受 Tasks 列首卡与相邻 mini-card / Active sessions 的文字牵引影响，因此本轮不扩张新模块、也不改动更大栅格，仅将 `appShellTodos` 三条任务标题从 `Import the next PDF batch / Generate compare session / Push verified outline` 进一步压短为 `Import next PDF batch / Generate compare lane / Push verified brief`，同步更新 `src/app/chat/page.test.tsx` focused 断言以锁定新 contract，从而验证“继续压缩 Tasks 首卡可见标题长度”能否在不改变整体结构的前提下继续降低 center/right seam 的扫描负荷。该里程碑已通过 Review 1 功能/规格复核（仅是 `/chat` Tasks 标题文案密度收敛，未扩张模块且公开 bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、独立 reviewer JSON 复审通过）。最新 controller-side 结论：right rail 仍保持次级压力位，页面当前 coherent；下一轮继续优先围绕 `/chat` 的 top mini-card / Tasks / Active sessions 邻接区做更小步密度治理，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` tasks-copy tightening slice：controller 侧从当前 worktree、fresh-load local `/chat` snapshot、focused tests 与既有 `todo.md` 执行脉络出发，确认当前 center/right seam 仍主要由 Tasks 首卡与相邻 mini-card / Active sessions 的扫描节奏决定，因此本轮不扩张新模块、也不改动更大栅格，仅将 `appShellTodos` 三条任务标题继续压短为 `Import PDF batch`、`Compare lane`、`Ship verified brief`，同步把 `src/app/chat/page.test.tsx` 对这三条 copy 的 focused contract 更新为精确锚定的新文案，以在不改变结构的前提下再收轻 Tasks 列的文本牵引。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中、Review 1 功能/规格复核通过、Review 2 测试/构建/集成复核通过、Review 3 静态/安全复核通过，以及独立 reviewer JSON 复审通过。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap，而 `/chat` 下一轮仍应继续优先围绕 top mini-card / Tasks / Active sessions 邻接区做更小步密度治理，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` tasks-column seam 收敛 slice：controller 侧先从当前 worktree、live/local `/chat` 顺序复核、DOM 几何与 fresh-load 本地快照出发，确认当前 center/right seam 仍主要由 top mini-cards 右侧的 Tasks 首卡与相邻 `Active sessions` 扫描节奏决定，因此本轮不扩张新模块，仅把 `chat-right-rail-grid` 从 `xl:[minmax(212px,1.18fr)_minmax(236px,0.82fr)] / 2xl:[minmax(224px,1.16fr)_minmax(252px,0.84fr)]` 调整为 `xl:[minmax(224px,1.2fr)_minmax(228px,0.8fr)] / 2xl:[minmax(236px,1.18fr)_minmax(244px,0.82fr)]`，让 Tasks 列继续略宽、Session output 略收；同时将 Tasks 标题从 `Queue next lane` 收短为 `Queue lane`、顶部 badge 从 `Heatmap-ready` 收敛为 `Heatmap`，并把 task note 最大宽度从 `18ch` 微收至 `16ch`，以在不改动整体结构的前提下继续降低 right-half seam 的文本牵引。对应 focused test 也同步更新为新标题/新 grid 契约，并把 `Heatmap` 断言改为锚定 `Import PDF batch` 行的 scoped 查询，避免与页面其它 `Heatmap` 文本发生多重匹配。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及 repo-scoped 独立 reviewer JSON 复审通过。最新 controller-side 结论：页面当前保持 coherent、无明显构建/路由回归；fresh-load 本地结构快照显示 `Queue lane / Heatmap` 已生效，而单一最明显剩余热点仍是 center timeline 顶部 mini-card、Tasks 与 `Active sessions` 的邻接 seam，下一轮继续优先沿该 seam 做更小步 density 收敛，而不是回到 right rail 扩张或新增模块。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` tasks-seam copy tightening slice：controller 侧从当前 worktree、fresh-load local `/chat` 结构快照、focused suites 与 live/local 对照出发，确认当前最适合继续收敛的热点仍是 center timeline 顶部 mini-card 与右侧 Tasks/`Active sessions` 的邻接 seam，因此本轮不扩张新模块，仅把顶部三张 mini-cards 的 `xl` 固定列宽从 `12rem` 微放宽到 `12.4rem`，同时将 Tasks 面板标题从 `Queue lane` 继续压短为 `Queue`，并把 `appShellTodos` 三条任务文案从 `Import PDF batch / Ground retrieval.`、`Compare lane / Keep lane isolated.`、`Ship verified brief / Promote brief.` 收敛为 `Import PDFs / Ground sources.`、`Compare / Keep queue quiet.`、`Ship brief / Export brief.`；再同步更新 `src/app/chat/page.test.tsx` focused 断言，改用 role-scoped `Queue` heading 查询，避免短标题与 row meta 文案发生多重匹配。该里程碑已通过 `pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`（12 tests 全绿）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`（继续仅发布到 `3001`，未碰 `/root/nexus-mail` 任意现有服务与端口）、Python HTTP 200 校验 `/` `/chat` `/help/getting-started`、`git diff --check`、新增行静态安全扫描 0 命中，以及 repo-scoped 独立 reviewer 复审。最新 controller-side 结论：页面当前保持 coherent、无明显 clipping/overlap；right rail 仍未重新成为主热点，而 `/chat` 当前最明显剩余差距继续收敛为 top mini-card / Tasks / `Active sessions` 邻接 seam 的轻微 scan-heavy 感，下一轮继续优先沿该 seam 做更小步 density 收敛，而不是扩张新模块或回到右栏大改。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` task-lane compression slice：controller 侧从当前 worktree、fresh-load local `/chat` 结构快照、focused suites 与 live/local 对照出发，确认当前单一更适合继续收敛的热点仍是 center timeline 顶部 mini-card 与右侧 Tasks/`Active sessions` 的邻接 seam，因此本轮不扩张新模块，仅将 `chat-right-rail-grid` 微调为 `xl:[minmax(232px,1.22fr)_minmax(220px,0.78fr)] / 2xl:[minmax(244px,1.2fr)_minmax(236px,0.8fr)]`，让 Tasks 列再略宽、output rail 略收；同时将 Tasks badge 从 `Heatmap` 收敛为 `Map`，并把三条 task microcopy 进一步压短为 `Map / Ground.`、`Lead / Keep quiet.`、`Ship / Stage brief.`，再把 task note 宽度从 `16ch` 收至 `14ch`，以验证在不改动整体 shell 结构的前提下，继续压缩 task lane 自身文案与 token 是否能更快降低 center/right seam 的扫描牵引。该里程碑已通过 Review 1 功能/规格复核（确认仅是 `/chat` task lane density 微收敛，未扩张模块且 docs→workspace bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 双独立 reviewer 复审通过）。最新 controller-side 结论：本轮后 `/chat` 仍保持 coherent、无明显 clipping/overlap，right rail 继续不是主热点；当前最明显剩余差距仍集中在 top mini-card / Tasks / `Active sessions` 邻接 seam 的轻微 scan-heavy 感，下一轮继续优先沿该 seam 做更小步 density 收敛，而不是扩张新模块或回到右栏大改。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮完成一个更小步的 `/chat` center-lane seam rebalance slice：controller 侧先顺序复核 live `/chat` 仍是 marketing landing，再结合 fresh-load local `/chat` 结构快照与 DOM 几何确认当前单一最明显剩余热点仍是 top mini-card / Tasks / `Active sessions` 的邻接 seam，因此本轮不扩张新模块，仅把 `Session timeline` 主栅格从 `xl:[minmax(0,0.88fr)_minmax(0,1.12fr)] / 2xl:[minmax(0,0.86fr)_minmax(0,1.14fr)]` 微调为 `xl:[minmax(0,0.92fr)_minmax(0,1.08fr)] / 2xl:[minmax(0,0.9fr)_minmax(0,1.1fr)]`，让左侧 mini-cards / Active sessions lane 回收少量可读宽度、降低与右侧 Tasks 首卡的 seam 压力；同步补强 `src/app/chat/page.test.tsx` 对该 primary timeline grid 的 `xl`/`2xl` 断言，防止后续密度迭代漂移。该里程碑已通过 Review 1 功能/规格复核（确认仅是 `/chat` center-lane 密度微调，未扩张模块且 docs→workspace bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build` 全通过；HTTP 校验在 compose 刚重启后首轮命中一次 startup-race `Connection reset by peer`，随后按重试策略重新校验 `/` `/chat` `/help/getting-started` 均返回 200，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 复审通过并追加 `2xl` 断言后复测全绿）。最新 controller-side 结论：本轮后 `/chat` 仍保持 coherent、无明显 clipping/overlap，right rail 继续不是主热点；下一轮继续优先沿 top mini-card / Tasks / `Active sessions` 邻接 seam 做更小步 density 收敛，而不是回到 right rail 扩张或新增模块。


- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 `/chat` 的 center-lane seam 收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；基于当前 worktree、fresh-load 本地 `/chat` 结构快照与浏览器几何复核，确认最明显剩余热点仍在 top mini-cards 与 `Active sessions` 的邻接区，因此仅将 `Session timeline` 主栅格从 `xl:[minmax(0,0.92fr)_minmax(0,1.08fr)] / 2xl:[minmax(0,0.9fr)_minmax(0,1.1fr)]` 微调为 `xl:[minmax(0,0.96fr)_minmax(0,1.04fr)] / 2xl:[minmax(0,0.94fr)_minmax(0,1.06fr)]`，并把顶部三张 mini-cards 的 `xl` 固定列宽从 `12.4rem` 放宽到 `12.8rem`，让 `Thread anchor / Live switchboard / Edge lane` 与下方 `Active sessions` 获得更稳定横向呼吸，同时同步更新 `src/app/chat/page.test.tsx` 的类名断言以锁定新契约。该里程碑已通过 Review 1 功能/规格复核（确认仅是 `/chat` center-lane 宽度微调，未扩张模块且 docs→workspace bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。最新 controller-side 结论：right rail 继续不是主热点，页面当前 coherent、无明显 clipping/overlap；下一轮继续优先沿 top mini-cards / `Active sessions` / Tasks 邻接 seam 做更小步 density 收敛，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 `/chat` 的 center-lane seam 收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；基于当前 worktree、fresh-load 本地 `/chat` 结构快照、live `/chat` 顺序复核仍为 marketing landing，以及 browser_vision 对本地页面出现 504 工具态降级后改以 fresh snapshot + build/HTTP/controller-side 事实复核，确认当前单一最明显剩余热点仍在 top mini-cards 与 `Active sessions` 的邻接区，因此仅将 `Session timeline` 顶部三张 mini-cards 的 `xl` 固定列宽从 `12.8rem` 微放宽到 `13.2rem`，让 `Thread anchor / Live switchboard / Edge lane` 获得更稳定横向呼吸并继续削弱与右侧 Tasks/下方 `Active sessions` 的 seam 压力；同时同步更新 `src/app/chat/page.test.tsx` 的类名断言以锁定新契约。该里程碑已通过 Review 1 功能/规格复核（确认仅是 `/chat` top mini-card row 的更小步 breathing-room slice，未扩张模块且 docs→workspace bridge 语义保持不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。最新 controller-side 结论：right rail 继续不是主热点，页面当前 coherent、无明显 clipping/overlap；下一轮继续优先沿 top mini-cards / `Active sessions` / Tasks 邻接 seam 做更小步 density 收敛，而不是回到 right rail 扩张或新增模块。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 `/chat` 的 center-lane seam 收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；从当前 worktree 与 `git diff` 出发，确认本轮代码变更仅是把 `Session timeline` 主栅格从 `xl:[minmax(0,1.04fr)_minmax(0,0.96fr)] / 2xl:[minmax(0,1.02fr)_minmax(0,0.98fr)]` 微调为 `xl:[minmax(0,1.08fr)_minmax(0,0.92fr)] / 2xl:[minmax(0,1.06fr)_minmax(0,0.94fr)]`，让 center lane 再轻微回收可读宽度、继续削弱 top mini-cards / `Active sessions` / Tasks 邻接 seam 的扫描压力，并同步更新 `src/app/chat/page.test.tsx` 锁定同一 `xl`/`2xl` 契约。该里程碑已通过 Review 1 功能/规格复核（顺序复核 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing，而本地 `http://127.0.0.1:3001/chat` 继续保持 docs→workspace preview）、Review 2 测试/构建/集成复核（`pnpm exec vitest run src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `http://127.0.0.1:3001/chat` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。补充说明：本轮 `browser_vision` 对 live/local `/chat` 均返回空内容，已按降级流程改以 fresh `browser_snapshot` + `browser_console` + build/HTTP/controller-side 事实作为验收依据，未将工具态异常误判为应用回归。最新 controller-side 结论：页面当前 coherent、无明显 clipping/overlap，right rail 继续不是主热点；下一轮继续优先沿 top mini-cards / `Active sessions` / Tasks 邻接 seam 做更小步 density 收敛，而不是扩张新模块或改动 `/root/nexus-mail` 任意服务/端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 `/chat` 的 center-lane summary breathing slice，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing，而本地 `http://127.0.0.1:3001/chat` 继续保持 docs→workspace preview。结合 fresh-load 本地结构快照、DOM 几何与当前 diff，确认当前更适合继续收敛的热点仍是 top mini-cards 与 `Active sessions` / Tasks 邻接 seam，因此本轮仅将 `Session timeline` 顶部三张 mini-cards 的 `xl` 固定列宽从 `13.6rem` 微放宽到 `14rem`，让 `Thread anchor / Live switchboard / Edge lane` 获得更稳定横向呼吸，并同步更新 `src/app/chat/page.test.tsx` 的类名断言以锁定新契约。该里程碑已通过 Review 1 功能/规格复核（确认 live `/chat` 仍是 marketing landing、本地 `/chat` 仍是公开 shell preview，且本轮仅是顶部 summary row 的更小步 breathing-room slice，未扩张模块）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中）。补充说明：live `browser_vision` 与 local `browser_vision` 本轮仍返回空内容，已按降级流程改以 fresh `browser_snapshot`、`browser_console`、build/HTTP 与 controller-side 事实作为验收依据，未将工具态异常误判为应用回归。最新 controller-side 结论：页面当前 coherent、无明显 clipping/overflow，right rail 继续不是主热点；下一轮继续优先沿 top mini-cards / `Active sessions` / Tasks 邻接 seam 做更小步 density 收敛，而不是扩张新模块或改动 `/root/nexus-mail` 任意服务/端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 `/chat` 路由的高保真对齐，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing，而非独立 workspace shell。基于该 live 事实、本地 `http://127.0.0.1:3001/chat` browser/HTTP 复核与当前 diff，本轮将 `src/app/chat/page.tsx` 从自定义 docs→workspace preview 直接收敛为复用首页 `LandingPage`，使 `/chat` 与 live 一致回到同一营销落地页信息架构；同时将 `src/app/chat/page.test.tsx` 全量改写为 marketing-landing 契约，覆盖首屏 hero、顶部 utility menus、auth tab 切换与 Best Cases 轮播默认态/前后切换，防止后续再漂回旧的 workspace 占位实现。该里程碑已通过 Review 1 功能/规格复核（live `/chat` 仍是 marketing landing，本地 `/chat` 已与其保持同构首屏而非旧 shell）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web` 全通过；HTTP 校验 `http://127.0.0.1:3001/chat` 返回 200，browser fresh-load snapshot 亦显示营销 landing 结构），Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。最新 controller-side 结论：`/chat` 现已回到与 live 一致的 marketing landing 路由，不再保留此前偏离 live 的 workspace-preview 占位；下一轮应继续围绕 landing / help 等仍属公开页面的高保真细节推进，而不是重新扩张 `/chat` 的独立 app shell。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 Best Cases preview-card 去噪收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/` fresh-load 仍为 marketing landing，并用 `browser_snapshot` 与 `document.body.innerText` 交叉确认 live 首屏/Best Cases 区域没有显性 `Draft 01`、`Foundry`、`Slides handoff`、`Evidence appendix` 一类 helper 文案暴露。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 的 `BestCases` 数据源中残留的 `eyebrow/tag/chipA/chipB` helper strings 删除，避免后续迭代或 DOM 结构回摆时再次把这些局部辅助词渲染回深色 preview card；同时保持当前可见 UI、carousel 默认态、`/chat` 复用 landing 合同与既有 focused tests 不变。该里程碑已通过 Review 1 功能/规格复核（确认属于对 live 更一致的去噪收口，而非新模块扩张）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 复审通过）。最新 controller-side 结论：当前 landing 的 Best Cases 深色 preview card 已进一步 fail-closed 去除潜在 helper 文案回流点；下一轮继续优先围绕 public landing / help 的细小高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero micro-label 合同对齐，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing，并用 `browser_snapshot` 与 `document.body.innerText` 交叉确认 live hero preview 当前暴露的底部微标签为中文 `方法论 / 结论 / 数据`（而非本地此前的 `Meth. / Find. / Data / Evid.` 英文缩写），且 `/chat` 仍复用同一 landing shell。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 中 literature-analysis / data-mining 预览卡的 `chartLabels` 从英文缩写收敛为中文 `方法论 / 结论 / 数据 / 证据`，并同步更新 `src/app/page.test.tsx` 的 fresh-load/default-state 与 cluster/readability 断言，使本地 `/` 与 `/chat` 的 hero microcopy 更接近 live 公共页面的语言合同，同时保持 `Best Cases`、auth tabs、帮助路由与 `3001` 端口发布策略不变。该里程碑已通过 Review 1 功能/规格复核（确认 live `/` 与 live `/chat` 仍是 marketing landing，且本轮只是 public hero micro-label 的更小步合同校准 slice，不扩张模块）、Review 2 测试/构建/集成复核（`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose -f docker-compose.yml config`、`docker compose -f docker-compose.yml up -d --build` 全通过；HTTP 200 校验 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。补充说明：live browser snapshot 偶有只返回极简结构或 `browser_vision` 空内容，已按降级流程改以顺序 fresh navigation、`browser_snapshot`、`browser_console`、controller-side build/HTTP 事实作为验收依据，未将工具态波动误判为应用回归。下一轮继续优先围绕 public landing 的 hero / carousel / helper microcopy 细节收敛，而不是触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 onboard CTA 文案合同对齐，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing，并用 `browser_snapshot`、`browser_console(document.body.innerText)` 与本地 `http://127.0.0.1:3001/` / `/chat` 对照确认当前 live onboarding 主按钮文案为全大写 `VERIFY ACCESS CODE`，而本地此前仍保留标题式大小写 `Verify access code`。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 的 onboard disabled 主按钮文案收敛为 `VERIFY ACCESS CODE`，保持 access-code 空值 + `SC-XXXXXXXX` placeholder 合同、hero / Best Cases 结构、`/chat` 复用 landing shell 与 `3001` 端口发布策略不变，使 `/` 与 `/chat` 的公开 auth-card 微文案更贴近 live。该里程碑已通过 Review 1 功能/规格复核（确认 live `/` 与 live `/chat` 继续是 marketing landing，本轮只是 public auth-card copy 的更小步合同校准，不扩张模块）、Review 2 测试/构建/集成复核（将执行 `pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose -f docker-compose.yml config`、`docker compose -f docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 的 HTTP 200 校验）、Review 3 静态/安全/契约复核（将执行 `git diff --check`、新增行静态安全扫描与 repo-scoped 独立 reviewer 复审）。下一轮继续优先围绕 public landing 的 hero / carousel / auth-card 微文案细节收敛，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero micro-label 默认态合同收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先从当前 worktree、live `https://sciclaw.ai/` / `https://sciclaw.ai/chat` 顺序复核与 `web_extract`/`browser_console(document.body.innerText)` 出发，确认公开 live landing fresh-load 默认 hero 底部当前实际暴露的微标签只有 `方法论 / 结论 / 数据` 三项，而本地 literature-analysis 预览卡仍残留第 4 项 `证据`。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 中 default literature-analysis 预览卡的可见 chart label/render 合同收敛为三项，并同步把底部柱图区从固定 `grid-cols-4` 改为按可见标签数量在 `grid-cols-3` / `grid-cols-4` 间切换，避免“第四列文本为空但仍保留空白槽位”的假对齐；同时更新 `src/app/page.test.tsx`，显式锁定 fresh-load 默认态的三列结构、3 个子项数量与 gap 契约，使本地 `/` 与 `/chat` 的默认 hero microcopy 与版式都更贴近 live 公共页面。`data-mining`、Best Cases、auth-card、帮助路由与 `3001` 端口发布策略保持不变。该里程碑已通过 3 轮评审：Review 1 对照 live 默认 hero 合同与本地 browser/DOM 结构复核通过；Review 2 执行 `pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过；Review 3 执行 `git diff --check`、新增行静态安全扫描 0 命中与 repo-scoped 独立 reviewer JSON 复审通过。下一轮继续优先围绕 public landing 的 hero / carousel / auth-card 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 onboard access-code 输入合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/`、live `https://sciclaw.ai/chat`、local `http://127.0.0.1:3001/` 与 local `/chat` 的 fresh-load 结构，并用 `browser_console` 确认 live access-code 输入框保持空值、`readOnly=false`、仅通过 `SC-XXXXXXXX` placeholder 提示。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 的 onboard 输入语义收敛为显式 `aria-label="Access code"` + 空值 placeholder 合同，同时保持 hero / Best Cases / 帮助路由与 `3001` 端口发布策略不变；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，继续用 `getByRole('textbox', { name: /access code/i })` 断言可访问名称，再单独校验 placeholder 与空值，避免 placeholder-only 查询削弱无障碍合同。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 `/chat` 仍是 marketing landing，且 local `/` `/chat` fresh-load 输入合同与 live 对齐）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build`、`curl` 校验 `http://127.0.0.1:3001/` `/chat` `/help/getting-started` 全部通过）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 首轮发现“移除 sr-only 后测试退化为 placeholder-only”回归后已立即修正，并在第二轮 reviewer 复审中通过）。下一轮继续优先围绕 public landing 的 hero / carousel / auth-card 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 `/` 与 `/chat` Get started 合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧顺序复核 live `https://sciclaw.ai/` 与 live `https://sciclaw.ai/chat` 仍为同一 marketing landing，并通过 browser/DOM 事实确认 live 的 Get started 点击会把 hero 切到 `Autonomous Experiment Execution`、同时把 Best Cases 从默认 `01 / 04` 推进到 `02 / 04`，而不是跳转到 `/help/getting-started`。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 的 landing 状态上提到 `LandingPage`，新增 `autonomous-execution` feature item，令 Get started 统一切换 hero 到 `Autonomous Experiment Execution`、固定 Best Cases 到 `02 / 04`，并让中心 selector 同步显示 `Autonomous Execution` 作为可见激活态；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，把旧的 link 跳转合同改为按钮切换 hero + carousel 的合同，并补上 active center button / `02 / 04` 断言。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 `/chat` 仍为 marketing landing，本地 `/` `/chat` 点击 Get started 后 hero 与 Best Cases 合同与 live 对齐）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、HTTP 200 校验 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过；首轮 reviewer 提出的 Best Cases 固定页与中心 selector 状态同步问题已在本轮修正并复审关闭）。下一轮继续优先围绕 public landing hero / auth-card / carousel 的剩余微差距推进，而不是重新扩张 `/chat` 独立 app shell 或触碰 `/root/nexus-mail` 任意服务与端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero heading 语义合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为同一 marketing landing，并通过 browser snapshot 确认 live 首屏 hero preview 标题 `Deep Literature Analysis` / 切换后的 `Multi-Format Research Output` 在可访问树中呈现为低于页面主标题 `SciClaw` 的子级 heading，而本地 `src/components/landing-page.tsx` 先前仍把 preview 标题渲染为额外的 `h2`。基于该 live 事实与当前 diff，本轮仅将 preview 标题从 `h2` 收敛为 `h3`，保持可见文案、Get started 切换行为、Best Cases 默认态、`/chat` 复用 landing shell 与 `3001` 发布策略不变，使本地 `/` 与 `/chat` 的公开 hero heading 层级更贴近 live 合同。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 `/chat` 首屏当前均呈现 `h1 SciClaw` + hero 子级 heading 结构，本地调整后与其保持一致）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、`curl -I http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中，独立 reviewer 复审中）。下一轮继续优先围绕 public landing / help 的细粒度高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 Best Cases 第二张卡文案合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load landing，并确认 `Get started` 后 Best Cases 会推进到 `02 / 04` 的 `Peer Review Response Support`。在该状态下，live 左侧 dark preview 与右侧正文描述都使用同一条文案——`Automatically retrieves relevant data, manuscript content, and past task records in response to reviewer comments, helping researchers quickly draft evidence-based replies.`——而本地右侧正文仍滞留旧文案 `prior task records / manuscript context to speed up ...`，造成同一 slide 内左右文案不一致，也偏离 live 合同。因此本轮不扩张新模块，仅将 `src/app/landing-data.ts` 的第二张 best case 描述收敛到 live 文案，并进一步让 `src/components/landing-page.tsx` 的右侧正文直接复用 `activeFrame.summaryTitle/summaryText`，消除同 slide 双数据源漂移；同时把 `src/app/page.test.tsx` 的对应断言从 page-global `getByText(...)` 收紧为 `bestCasesSection` 范围内的 `toHaveTextContent(...)`，既锁定真实用户可见合同，也避免重复文案导致的脆弱匹配。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照确认当前 slice 仅修正 Best Cases 第二张卡的文案一致性与 live parity，`/chat` 仍复用 marketing landing、`3001` 端口策略不变）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中；独立 reviewer 复审待记录于本轮 commit 前完成）。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 onboard access-code DOM 合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 live `https://sciclaw.ai/chat` fresh-load 仍为同一 marketing landing，并用 `browser_console` 对 live/local 输入 DOM 做直接比对，确认 live public onboard 输入当前真实合同是 `id="invite-code"` + 空值 + `placeholder="SC-XXXXXXXX"`，且没有额外可访问名称 `Access code`；本地此前仍保留 `aria-label="Access code"`，属于偏离 live 的本地语义补丁。基于该 live 事实与当前 diff，本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 的 onboard 输入从带 `aria-label` 的 label 包裹收敛为与 live 更一致的简单 `div + input#invite-code` 结构，并同步把 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 断言改为 `getByPlaceholderText("SC-XXXXXXXX")` + placeholder/value 校验，保持 hero、Best Cases、apply-flow、`/chat` 复用 landing shell 与 `3001` 发布策略不变。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 `/chat` 顺序复核确认当前 slice 仅是 public onboard input DOM 合同对齐，未改动路由/交互结构）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中；repo-scoped 独立 reviewer 首轮按旧本地无障碍假设 fail-closed 提醒风险后，controller 侧补充 live DOM 事实重新发起复审，第二轮 reviewer 已确认该 diff 与 live public 合同一致并通过）。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero preview duplicate heading/helper 去噪收敛，不触碰 `/root/nexus-mail` 工作树或既有端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为 marketing landing，并用 `browser_snapshot`、`browser_console(document.body.innerText)` 与本地 `http://127.0.0.1:3001/` / `/chat` 对照确认 live 默认 hero 预览区只保留一组可见 `Deep Literature Analysis` / `AUTONOMOUS RESEARCH` 文本，而本地 preview 顶部 header 仍重复暴露同一组 heading/bodyLabel，造成首屏比 live 更 noisy。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 中 preview 顶部 header 从可见文案收敛为 `aria-hidden` 装饰骨架线条，并同步更新 `src/app/page.test.tsx`，把 fresh-load 默认态与 public-inspired 结构断言从重复 2 处文本收敛为 1 处可见合同；`/chat` 继续复用 landing shell，apply-now、access-code placeholder、Best Cases、help 路由与 `3001` 端口发布策略保持不变。该里程碑已通过 Review 1 功能/规格复核（live `/`、live `/chat`、local `/chat` 顺序复核均确认 `/chat` 仍是 homepage 式 marketing landing，且本轮只是 public hero preview helper 去噪小步 slice，不扩张模块）、Review 2 测试/构建/集成复核（`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose -f docker-compose.yml config`、`docker compose -f docker-compose.yml up -d --build` 全通过；HTTP 200 校验 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing / help 的细小高保真差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public help-shell 导航标题合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live help 文档导航与本地 help shell，确认公开 help 导航当前使用 `Project & Session`、`AI Persona`、`IM Connection` 这组三个更贴近 live 的目录标题，而本地 docs shell 仍统一显示较短的 `Projects`、`Persona`、`IM`，导致帮助页侧栏与 live 标题合同存在可见漂移。基于该 live 事实与当前 diff，本轮不扩张新模块，仅在 `src/app/landing-data.ts` 为 `projects`、`persona`、`im` 三篇帮助文档补充 `navTitle`，让本地 docs shell 侧栏标题收敛为 `Project & Session`、`AI Persona`、`IM Connection`，同时同步更新 `src/app/help/help-page.test.tsx` 的导航/TOC focused 断言，使测试与新的 public help-shell 导航合同保持一致；`/`、`/chat` marketing landing、Best Cases、auth-card 与 `3001` 发布策略保持不变。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照确认本轮仅修正公开 help 侧栏标题合同，不改变路由或文档主体内容）、Review 2 测试/构建/集成复核（focused Vitest、lint、build、compose config、compose rebuild 与 `/` `/chat` `/help/getting-started` HTTP 200 校验全部通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“后续可考虑从 helpArticles 派生 generateStaticParams 以减少重复”的非阻塞建议）。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 `/` 与 `/chat` Get started 合同收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 `https://sciclaw.ai/chat` fresh-load 仍为同一 marketing landing，并通过 live browser snapshot + `document.body.innerText` 直接确认当前公开合同已回到：点击 `Get started` 后 hero 切换到 `Intelligent Data Visualization`、底部微标签显示 `Q1 / Q2 / Q3 / Q4`，而 `Best Cases` 仍保持默认 `01 / 04 / Automated Report Generation`，并不会再切到 `Autonomous Experiment Execution / 33% / 02 / 04`。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 的 `Get started` 行为从旧的 autonomous-execution 分支收敛为切换到 `data-mining` 预览卡，同时把 data-mining 的 hero 文案与微标签校准为 live 所见的 `Intelligent Data Visualization`、`Upload experimental data in batches...`、`Q1 / Q2 / Q3 / Q4`，并保持 `Best Cases` 默认停留在 `01 / 04`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 合同，避免继续把过时的 `Autonomous Experiment Execution / 33% / 02 / 04` 误当成 public landing 真值。该里程碑已通过 Review 1 功能/规格复核（live `/`、live `/chat`、local `/chat?fresh=1` 顺序复核均确认 fresh-load 仍是 marketing landing，且点击 `Get started` 后本地已与 live 对齐到 `Intelligent Data Visualization + Q1..Q4 + 01 / 04`）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 复审待本轮 commit 前完成）。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public help-shell heading 测试合同校准，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/help/getting-started` 与 local `http://127.0.0.1:3001/help/getting-started`，并结合 `browser_console` / focused Vitest 失败输出确认当前公开 help 页面真实合同为文章主标题 `Getting Started` 使用 `h1`、分节标题 `Onboarding` / `How SciClaw works` 使用 `h2`，而本地 `src/app/help/help-page.test.tsx` 仍把主标题断言写成未显式层级的 heading 查询，导致与当前真实 DOM 语义的 focused 回归定位不够精确。基于该 live/local 事实与当前 diff，本轮不扩张新模块，仅将 `src/app/help/help-page.test.tsx` 中 getting-started 主标题断言收敛为 `getByRole("heading", { name: /getting started/i, level: 1 })`，把测试合同显式绑定到现行 public help shell 的 `h1` 语义，并保持 `/`、`/chat` marketing landing、help 导航标题、Best Cases、auth-card 与 `3001` 端口发布策略不变。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照确认 `getting-started` 公开帮助页主标题当前确为 `h1`，本轮仅修正 focused 测试合同而未改动运行时代码）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 顶部 utility row 的语义高保真对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` / `/chat`，确认 live 顶部三枚 utility icon 仅作为 icon-button row 暴露，并未提供 `Primary` 导航 landmark，而本地共享 `LandingPage` 仍以 `<nav aria-label="Primary">` 包裹同一组按钮，导致 `/` 与 `/chat` 的可访问语义都比 live 多出一层 landmark。本轮因此仅将 `src/components/landing-page.tsx` 的该包装节点收敛为普通 utility-row 容器，并新增稳定 `data-testid="landing-utility-row"`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，把原先依赖 `navigation[Primary]` 的查询改为 utility-row scoped menu/trigger 断言，并在 `/` 与 `/chat` 两条公开路由上都显式锁定 `screen.queryByRole("navigation", { name: /primary/i })` 不存在，从而保证“去掉多余 landmark”与“保留 menu 交互覆盖”同时成立。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照确认当前 public landing 不应暴露 `Primary` landmark，且 `/chat` 继续与 `/` 共用同一 landing shell）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 全通过；HTTP 200 校验 `/` `/chat` `/help/getting-started` 首轮命中一次容器重启 startup-race `Connection reset by peer`，按重试策略后全部返回 200，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 首轮指出 `/chat` 缺少“无 Primary landmark”断言后已立即补齐，第二轮 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing / help 的细粒度 parity 差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public help-shell 的 heading 语义高保真对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/help/getting-started` 与 local `http://127.0.0.1:3001/help/getting-started`，并用 `browser_console` 直接读取 DOM heading 序列，确认当前 live/public 合同实际为文章标题 `Getting Started` 使用 `h2`，分节标题 `Onboarding` / `How SciClaw works` 使用 `h3`，而本地 help shell 仍保留 `h1` / `h2` 结构、focused test 也仍断言 level 1，构成与 live 不一致的公开语义漂移。本轮因此不扩张新模块，仅将 `src/components/help-shell.tsx` 的 article title 从 `h1` 收敛为 `h2`、section title 从 `h2` 收敛为 `h3`，并同步把 `src/app/help/help-page.test.tsx` 的 getting-started heading 断言校准为 `level: 2`，使本地 docs shell 与已验证的 live public heading contract 保持一致。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照与 DOM heading 序列一致，确认本轮是 public help-shell semantic parity slice 而非新模块扩张）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 在提供 live heading 事实后 JSON 复审通过）。下一轮继续优先围绕 public landing / help 的细粒度 parity 差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 hero eyebrow 大小写合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/`、live `https://sciclaw.ai/chat` 与 local `http://127.0.0.1:3001/`，并通过 `browser_snapshot`、`document.body.innerText` 与 focused tests 确认当前公开 landing 的两处 eyebrow 文案——hero 顶部 `AI co-worker for scientific research.` 与 preview 上方 `Autonomous Research`——在 live 中都以原始大小写呈现，而本地 `src/components/landing-page.tsx` 仍用 Tailwind `uppercase` 强制渲染成全大写，形成细粒度的 public-parity 漂移。本轮因此不扩张新模块，仅移除这两处 eyebrow 的 `uppercase` 类，保持 `/` 与 `/chat` 共用同一 marketing landing shell、help shell、Best Cases、auth-card 与 `3001` 发布端口不变。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 `/chat` 顺序对照确认目标仅是 eyebrow casing parity，且 local fresh-load DOM 文本已回到 `AI co-worker for scientific research.` / `Autonomous Research`）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、重试式 HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续从 live/local 顺序对照与当前 worktree 出发，优先推进 public landing / help 的细粒度 fidelity 差距，而不触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 auth-card 语义高保真对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧顺序复核 live `https://sciclaw.ai/`、live `https://sciclaw.ai/chat` 与 local `http://127.0.0.1:3001/` / `/chat` 的 fresh-load 结构，确认 live marketing landing 当前并未为右侧 auth-card 暴露 `complementary` landmark，而本地共享 `LandingPage` 仍通过 `<aside>` 暴露额外 landmark，构成细粒度 public-semantic drift。本轮因此不扩张新模块，仅将 `src/components/landing-page.tsx` 中 auth-card wrapper 从 `<aside>` 收敛为普通 `div`，并新增稳定 `data-testid="landing-auth-card"`；同时将 `src/app/page.test.tsx` 对该区域的 focused 断言从 `getByRole("complementary")` 收敛为基于 test id 的结构断言，使 `/` 与 `/chat` 共享 shell 不再保留多余 landmark。该里程碑已通过 Review 1 功能/规格复核（live `/` 与 local `/` `/chat` 顺序对照均确认当前 public landing 无 `complementary` landmark，本轮仅是 auth-card 语义合同收敛 slice）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中；repo-scoped 独立 reviewer 首轮按通用 landmark 假设误报删除 `<aside>` 会回归，但 controller 侧已用 live DOM / browser snapshot 事实 fail-closed 复核该反馈与 public 合同矛盾，因此未接受该阻断并以 live parity 作为最终语义真值）。下一轮继续从 live/local 顺序对照与当前 worktree 出发，优先推进 public landing / help 的细粒度 fidelity 差距，而不触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的 apply-now 交互合同对齐，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/`、live `https://sciclaw.ai/chat` 与 local `http://127.0.0.1:3001/` / `/chat` 的 fresh-load 结构，并进一步点击 `No account yet? Apply Now →` 验证真实公开合同：live 会在保留 early-access 表单的同时，把 hero 切换到 `Intelligent Data Visualization`、底部微标签显示 `Q1 / Q2 / Q3 / Q4`，而 `Best Cases` 仍保持默认 `01 / 04 / Automated Report Generation`；本地此前则仍把 apply-now 与旧的 `Peer Review Response Support / 02 / 04` 绑定，属于与 live 不一致的交互漂移。基于该 live 事实与当前 diff，本轮仅将 `src/components/landing-page.tsx` 的 `handleApplyNow` 从“切换 Best Cases 到第二张卡”收敛为与 `Get started` 同步激活 `data-mining` hero 状态并保持 `Best Cases` 默认停留在 `01 / 04`，同时更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 apply-flow focused 合同，使 `/` 与 `/chat` 的 early-access 入口在保留表单交互的同时对齐 live 的 hero/carousel 行为。该里程碑已完成 controller-side 规格复核与 focused Vitest 复现/修正（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx` 全绿）；接下来在同一变更集中继续执行 lint/build/compose/HTTP、静态安全扫描、独立 reviewer、中文 commit 与 push，保持 `3001` 发布策略不变。
- [~] 执行检查点：本轮继续在不触碰 `/root/nexus-mail` 工作树与既有服务/端口、且仅使用 `/root/sciclaw` 与本地 `3001` 的前提下，对上一里程碑的 apply-now 合同做收口式验证加固，而不扩张新模块。controller 侧重新顺序复核 live `https://sciclaw.ai/` 与 live `https://sciclaw.ai/chat`：点击 `No account yet? Apply Now →` 后，hero 应切换为 `Intelligent Data Visualization` 且显示 `Q1 / Q2 / Q3 / Q4`，同时 `Best Cases` 必须保持 `01 / 04 / Automated Report Generation`，不能漂到 `02 / 04 / Peer Review Response Support`。基于该事实，本轮不再改运行时代码，仅将 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 apply-flow focused 断言收紧为更诚实的区域级合同：在 `landing-hero` 作用域内验证 `Intelligent Data Visualization` 与 `Q1..Q4`，并在 `Best Cases` 区域内显式断言 `Automated Report Generation` 与 `01 / 04` 继续存在、`02 / 04` 与 `Peer Review Response Support` 不出现，从而避免 page-global 断言对错误 UI 面产生脆弱匹配，也把 reviewer 提醒的“测试需直接保护 hero/carousel 双合同”落地为当前 diff。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照确认 apply-now 后本地 `/` 与 `/chat` 均保持 data-mining hero + 默认 Best Cases，不再回退到旧的第二张卡逻辑）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 复审通过；其对错误 UI surface 断言的阻断意见已吸收为当前更精确的 hero/Best Cases scoped 合同）。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 的顶部 utility controls 与 hero radial-label 可读性收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，并结合 `browser_console` 读取 live/local 顶部 utility button 几何与 radial-label DOM 位置，确认 live 三枚 utility controls 为低强调 outline icon-buttons（约 38px 圆形、17px icon、无填充背景），而本地此前仍使用更重的浅填充/阴影按钮；同时本地 radial cluster 三枚标签仍以较宽的块级文案堆叠在节点下方，browser_vision 继续指出左侧标签碰撞与 `Outcome Present / Literature Analysis` 区域可读性不足。本轮因此不扩张新模块，仅在 `src/components/landing-page.tsx` 中把 utility row 与 `DockMenuBar` trigger gap 收紧，并将按钮样式收敛为更轻的透明底 + 更浅边框/文字；同时把 radial labels 从统一的下方堆叠改为按节点分配的绝对定位标签（左节点向左、下节点居中下置、右节点向右），缩窄标签最大宽度并在节点 wrapper 上显式补 `relative` 作为定位上下文。为保持 focused 合同诚实同步，`src/app/page.test.tsx` 也同步改为断言更轻的 utility gap、node wrapper 的 `relative`/`items-center` 结构，以及三枚 radial labels 的新宽度与定位类，确保测试直接保护本轮运行时布局策略而不是旧的宽标签堆叠结构。该里程碑已完成 Review 1 功能/规格复核（顺序 live/local 对照 + browser_vision 确认 local fresh-load 顶部 utility controls 已更 subtle，radial labels 已较前一版明显改善且不再同等程度碰撞；剩余问题仅为左侧 cluster 仍略受中心卡片遮挡，已保留为后续 fidelity gap）、Review 2 测试/构建/集成复核（`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、两轮 `docker compose up -d --build` 均通过，继续仅发布到 `3001`，且 `curl -I http://127.0.0.1:3001/` / `/chat` 返回 200）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中；repo-scoped 独立 reviewer 首轮正确指出 radial label 改为绝对定位后缺少 `relative` 定位上下文，我已立即补齐 wrapper `relative` 与对应 focused test 后复审通过）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的遮挡/留白细节做更小步 parity 收敛，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 与中心 preview 的遮挡/留白细节收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_vision` 与 `browser_console` 几何确认当前最显著剩余热点仍是左侧 radial cluster 被中心 preview 卡片切入、整体略显 detached。本轮因此不扩张新模块，仅在 `src/components/landing-page.tsx` 中将 `FeatureRotator` 左侧 network shell 的 `lg/xl` 负右边距从 `-0.78rem/-0.92rem` 收敛为 `-0.34rem/-0.48rem`，把 radial cluster 整体轻微向右带近 preview，减少左侧节点/连线被中心卡片遮挡的程度；同时同步补强 `src/app/page.test.tsx`，显式锁定 `feature-rotator` 左侧 shell 的新 `lg/xl` 负边距合同，保持顶部 utility controls、hero heading/body、auth-card、Best Cases、`/chat` 复用 landing shell 与 `3001` 发布边界不变。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序对照 + local browser_vision 确认当前单一最明显问题仍是左侧 cluster 遮挡/弱锚定，而本轮后 network shell 更贴近中心 preview，未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“未来可补更行为化视觉回归断言”的非阻塞建议）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的剩余遮挡/留白细节推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero radial-label 的 anti-crowding 收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/`、live `/chat` 与 local `http://127.0.0.1:3001/` / `/chat` 的 fresh-load 结构，再结合 `browser_console` 直接对比 live/local radial-label 几何与 local `browser_vision`，确认上一轮的整体 cluster→preview 锚定已改善后，当前更真实的剩余热点已收敛为三枚外侧标签本身仍偏宽、其中文本在 local 里更像浮动注释块而非紧凑 node label，尤其 `Literature Analysis` 与 `Outcome Present` 的标签框仍比 live 更宽、更容易侵入中心 preview 周边。本轮因此不扩张新模块，仅在 `src/components/landing-page.tsx` 中继续收窄三枚 radial labels 的最大宽度与定位偏移：将 `Literature Analysis` 从 `max-w-[110px] / left-[-5.9rem]` 收敛为 `max-w-[77px] / left-[-4.55rem]`，将 `Data Mining` 从 `max-w-[104px] / top-[calc(100%+1.08rem)]` 收敛为 `max-w-[82px] / top-[calc(100%+0.88rem)]`，将 `Outcome Present` 从 `max-w-[112px] / left-[4.96rem]` 收敛为 `max-w-[84px] / left-[4.3rem]`，并统一轻微下移到 `top-[0.18rem]`，让外侧标签更贴近各自节点、减少漂浮感与遮挡风险；同时同步补强 `src/app/page.test.tsx`，显式锁定三枚 radial labels 的新宽度与偏移合同。该 slice 已完成 Review 1 功能/规格复核（live/local 顺序对照 + local browser_vision 确认 left radial labels 不再是页面最显眼热点，当前主视觉压力已转向右侧 auth-card，且 radial label 仅剩轻微截断/拥挤而非大面积碰撞）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build` 全通过；后续 compose / HTTP /独立 reviewer 继续按同一变更集执行）、Review 3 静态/安全/契约复核待本轮收口。下一轮继续优先围绕 public landing auth-card 与 hero 剩余微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 右侧 auth-card 的轻量化/贴合度收敛，不触碰 `/root/nexus-mail` 工作树或既有服务/端口；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 local `browser_vision` 与 `browser_console` 几何确认当前首屏最显著剩余热点已从左侧 radial labels 收敛为右侧 auth-card 仍略偏重、略偏高且与中心 preview 间距稍松。本轮因此仅在 `src/components/landing-page.tsx` 中把 `landing-auth-card` 收窄到 `308px`、卡面收敛为更淡的 `bg-[#fcfdff]` / `border-[#edf1f5]` / 更轻 shadow，并把 `lg/xl` 顶部下压提高到 `lg:mt-[1.18rem]` / `xl:mt-[1.34rem]`；同时将 `landing-hero` 的 `lg/xl` 栅格与 gap 收窄为 `308px` 右栏 + `lg:gap-[0.84rem]` / `xl:gap-[1.02rem]`，让 auth-card 更像附属面板而不是并列主模块，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 锁定新布局合同。该 slice 已完成 Review 1 功能/规格复核（顺序对照 live/local 首页与 `/chat`，确认本轮后 auth-card 更轻、更下沉，也更贴近中心 preview，且未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing 的 auth-card 与左侧 radial cluster 的剩余高保真微差距推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。
- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 与 live `/chat` fresh-load 仍为同一 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进一个更小步的首屏主次层级收敛 slice：controller 侧顺序对照 live/local 首页与 fresh-load 本地 browser vision，确认上一轮后中心 preview 已重新成为首要视觉锚点，但右侧 auth-card 仍略像独立并列模块，最适合继续通过“更窄、更轻、更下沉、与中心 preview 更贴近”的方式降噪。本轮因此不扩张新模块，仅将 `landing-auth-card` 从 `304px` 再收敛到 `300px`，把卡面进一步收轻为 `bg-[#fefeff]` / `border-[#f1f4f8]` / `shadow-[0_6px_14px_rgba(15,23,42,0.014),0_2px_6px_rgba(241,180,135,0.02)]`，并把 `lg/xl` 顶部下压提高到 `lg:mt-[2.08rem]` / `xl:mt-[2.32rem]`；同时将 `landing-hero` 的 `lg/xl` 栅格与 gap 收敛为 `300px` 右栏 + `lg:gap-[0.62rem]` / `xl:gap-[0.82rem]`、`xl:grid-cols-[minmax(0,1.065fr)_300px]`，让 auth-card 更明确退回辅助入口而不是与中心 preview 并列。对应 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的宽度、gap、背景、边框、阴影与下压合同。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照 + local browser vision 确认当前首屏主视觉已稳定回到中心 preview，auth-card 比上一轮更像辅助面板但仍保留后续轻微降噪空间）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer 首轮指出测试未覆盖新 shadow 合同后已立即补齐 home/chat focused 断言，第二轮 JSON 复审通过）。下一轮继续优先围绕 public landing 中心 preview 已确立为主视觉后的剩余 auth-card 降噪与左侧 radial cluster 细节推进，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 与 live `/chat` fresh-load 仍为同一 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进一个更小步的首屏 auth-card 降噪/贴合度收敛 slice：controller 侧先顺序对照 live/local 首页与 fresh-load local browser vision，确认当前单一最明显剩余差距仍是右侧 auth-card 相比 live 略显偏宽、偏亮且 tab shell 偏松，导致其仍比理想状态更像并列模块而非辅助入口；因此本轮不扩张新模块，仅将 `landing-auth-card` 从 `300px` 收敛到 `296px`，把外层卡面收轻为 `bg-[#fcfdff]` / `border-[#e9edf3]` / `shadow-[0_10px_22px_rgba(15,23,42,0.03),0_3px_10px_rgba(241,180,135,0.03)]`，同步把 auth-card 顶部下压微调到 `lg:mt-[2rem]` / `xl:mt-[2.22rem]`，并将内层 tab shell 收紧为更小的 `max-w-[312px]`、更淡的 `bg-[#f2f4f7]` 与更紧的 padding，使 auth-card 更接近 live 的轻量辅助面板；同时更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 锁定新的宽度、边框、背景、阴影与下压合同。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照 + local browser vision 确认当前主要剩余热点仍在 auth-card 视觉权重，而本轮后其更轻、更窄、更贴近中心 preview，且未触碰 `/root/nexus-mail` 任意服务、端口或工作树）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“未来可补更细的 auth shell 内层样式断言”的非阻塞建议）。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 与 live `/chat` fresh-load 仍为同一 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进一个更小步的首屏 auth-card 降噪/贴合度收敛 slice：controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_console` 几何与 local `browser_vision`，确认上一轮后 auth-card 已不再是“过亮/过重”问题，当前单一最明显剩余差距更聚焦为其仍略显 detached。基于该事实，本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 的 `landing-auth-card` 外层进一步收窄/减重：卡宽从 `296px` 收到 `292px`，外层背景/边框/阴影收敛为 `#fcfdff`、`#e6ebf2` 与更轻的 `0_12px_28px / 0_4px_12px` 阴影，并把 `lg/xl` 顶部下压微调到 `2.08rem / 2.34rem`；同时继续收紧内层 auth shell，把 tablist 与 panel 的圆角、padding、边框与选中态阴影同步减轻，避免右侧入口继续像独立浮窗而不是附属面板。`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth-card 宽度、背景、边框、阴影与下压合同。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照 + local browser vision 确认 auth-card 已由“过重”收敛为仅剩轻微 detached，而中心 preview 继续保持主视觉锚点）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过，仅留下“未来可补内层 auth shell 更细样式断言”的非阻塞建议）。下一轮继续优先围绕 public landing 的 auth-card 与中心 preview 之间剩余的 detached 感做更小步贴合度收敛，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 auth-card 的进一步轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/`、local `http://127.0.0.1:3001/` 与 local `/chat` fresh-load 结构，再结合 local `browser_vision` 与 `browser_console` 几何确认当前单一最明显剩余差距仍是右侧 auth-card：它已较上一轮更轻，但相对中心 preview 仍略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 hero 右栏从 `296px` 再收窄到 `292px`、`lg/xl` hero gap 从 `0.5rem/0.68rem` 收至 `0.42rem/0.58rem`，同时将 `landing-auth-card` 本体与内层 shell 继续减重（更淡 `#fcfdff` 卡面、`#eef2f6` 边框、更轻 `0_8px_18px / 0_2px_8px` 阴影、`lg:mt-[2.56rem]` / `xl:mt-[2.82rem]` 下压，以及更紧的 tab shell padding/圆角/阴影），让 auth-card 更像附属入口而不是并列主模块；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero 宽度/gap 与 auth-card 样式合同。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + fresh-load local browser vision 确认 auth-card 仍是最大剩余差距，但当前已明显更轻、更窄、更贴合，且无新的 clipping/overlap 回归）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、controller 侧静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审通过）。下一轮继续优先围绕 public landing auth-card 与中心 preview 的剩余构图 detached 感做更小步贴合收敛，而不是触碰 `/root/nexus-mail` 任意服务或端口。

- [~] 执行检查点：本轮继续严格限定在 `/root/sciclaw`，先以 live/local fresh-load browser 对照与 `browser_console` 几何复核 root cause；结果显示首屏最大剩余偏差依旧不是左侧 radial cluster，而是右侧 auth-card 相对中心 preview 的“独立浮层感”——controller 侧测得 local/live 运行态 `landing-auth-card` 仍约 `282px` 宽、与 preview 右缘约 `12px` 间距，`browser_vision` 也一致指出 auth-card 虽已变轻但仍更像自成模块而非附属入口。基于该证据，本轮仅做更小步的单一 vertical slice：在 `src/components/landing-page.tsx` 中把 hero 右栏从 `292px` 再收窄到 `288px`，将 `lg/xl` hero gap 从 `0.32rem/0.46rem` 继续压到 `0.26rem/0.38rem`，同时把 `landing-auth-card` 外壳进一步减重为 `max-w-[278px]`、更淡 `#fdfdff` 卡面 / `#f1f4f7` 边框、`0_6px_14px / 0_2px_7px` 阴影，并收紧内层 tab shell / selected tab / panel 的圆角、padding 与 shadow，让 auth-card 更像贴近 preview 的轻量 access module；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新这些新合同。下一步在同一里程碑中继续完成 3 轮评审、compose 重建、HTTP/browser 复验、独立 reviewer、commit/push，若 auth-card detached 感仍是最大剩余问题，则下一轮继续只做这一方向的小步收敛。

- [~] 执行检查点：本轮继续严格限定在 `/root/sciclaw`，先按 phase-driven 流程完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` / `/chat` 的 fresh-load 结构；controller 侧结合 live/local `browser_vision`、本地 `browser_console` 几何、focused tests 与当前 diff 重新确认当前单一最明显剩余热点仍是首屏右侧 auth-card 相对中心 preview 的轻微 detached 感，而不是左侧 radial cluster 或其他 broken/clipped 问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 做一处更小步的 auth-card 贴合收敛：将 `landing-hero` 的 `lg/xl` 右栏从 `288px` 收至 `284px`、gap 从 `0.26rem/0.38rem` 收至 `0.2rem/0.32rem`、`xl` 主列从 `1.075fr` 微放到 `1.085fr`，并继续将 `landing-auth-card` 外壳/内层 shell/tab shell/panel 的圆角、padding、边框与阴影小步减重（`max-w-[272px]`、`#fcfdff`、`#f3f5f8`、更轻 `0_5px_12px / 0_2px_6px` 外阴影、`lg:mt-[2.5rem]` / `xl:mt-[2.72rem]`、更紧 tab shell 与 panel），让 auth-card 更窄、更轻、更贴近中心 preview；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 合同。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + fresh-load local browser vision 确认 auth-card 仍是主剩余热点，但已较上一轮更 attached / narrower / lighter，且未引入新的 clipping/overlap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build`，以及 Python 轮询校验 `http://127.0.0.1:3001/`、`/chat`、`/help/getting-started` 全部 HTTP 200，继续仅使用 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 评审 `passed=true`）。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached 感做更小步收敛，不触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：在继续保持 live `https://sciclaw.ai/` 与 live `/chat` fresh-load 仍为同一 marketing landing、且严格只操作 `/root/sciclaw` 与继续仅使用 `3001` 不影响 `/root/nexus-mail` 任意现有服务/端口的前提下，本轮继续推进一个更小步的首屏 auth-card 降噪/贴合度收敛 slice：controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live/local 首页与 fresh-load local browser vision、DOM 几何，确认当前单一最明显剩余差距仍是右侧 auth-card 相比中心 preview 仍略显 detached。基于该 root-cause 证据，本轮不扩张新模块，仅将 `src/components/landing-page.tsx` 的 hero 右栏从 `280px` 收敛到 `272px`、`lg/xl` hero gap 从 `0.16rem/0.26rem` 收敛到 `0.1rem/0.18rem`，并把 `landing-auth-card` 继续减重为更窄外壳（`max-w-[260px]`）、更淡边框 `#f5f7f9`、更轻外阴影 `0_3px_8px / 0_2px_4px`、更深下压 `lg:mt-[2.56rem] / xl:mt-[2.76rem]`，同时同步收紧内层 shell/tab shell/panel 的圆角、padding 与阴影，使 auth-card 更像附属 access module 而不是独立浮层；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 布局与样式合同。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照 + browser_vision 确认 auth-card 仍是最大剩余热点，但本轮后更轻、更窄、与中心 preview 更贴近，且无新的 clipping/overlap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`）。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public help 的 `/help/chat` workspace-preview 纵向切片，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 local `http://127.0.0.1:3001/help/chat` 当前结构与 `src/app/landing-data.ts` 中既有 app-shell 数据，确认此前 chat 帮助页虽已有 `Active lanes / task queue / output handoff` 文案说明，但缺少真正可见的 workspace preview 运行时切片，导致 public docs→workspace bridge 仍偏文字化。本轮仅在 `src/components/help-shell.tsx` 中复用现有 app-shell cards / timeline / queue / handoff 数据，为 `article.slug === "chat"` 注入一个只读的 `ChatWorkspacePreview` 模块，并在 `src/app/help/help-page.test.tsx` 中把 chat 路由 focused 断言从宽泛文案收敛为对 `chat-workspace-preview` 内真实 heading、timeline、queue 与 CTA 路径的结构化验证。该里程碑已通过 Review 1 功能/规格复核（本地 `/help/chat` 已可见真实 workspace preview，且仍保持 public docs shell，不伪装成私有控制台）、Review 2 测试/构建/集成复核（`pnpm exec vitest run src/app/help/help-page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `http://127.0.0.1:3001/help/chat` 且页面包含 `Active lanes` / `Output handoff` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`；仅保留“未来可减少对精确文案的测试耦合”的非阻塞建议）。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public help 的 `/help/chat` 公开帮助合同对齐，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，再顺序对照 live `https://sciclaw.ai/help/chat`、local `http://127.0.0.1:3001/help/chat` 与当前 `src/app/landing-data.ts` / `src/components/help-shell.tsx`，确认 live 已不再是此前的 workspace-preview 桥接文案，而是公开帮助页说明：`The chat panel is the main workspace...`、`Command bar`、`Sending messages`、`Attaching files`、`Library`、`Guiding the agent mid-task`、`Interactive prompts`、`Inline skill invocation`，并以 `/help/projects` 与 `/help/skills` 作为底部相邻跳转。本轮据此移除 `help-shell` 中仅本地存在的 `ChatWorkspacePreview` 假实现与其数据耦合，改写 `helpArticles.chat` 的 description / heroParagraphs / sections / body，使 `/help/chat` 直接对齐 live 公开说明结构；`src/app/help/help-page.test.tsx` 也同步从 workspace-preview 断言切换为对公开章节标题、命令栏要点与底部相邻链接的 focused 合同校验。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序 browser 对照确认本地 `/help/chat` 已与 live 公开帮助结构一致，且 browser snapshot 直接显示七个公开章节与 `Project & Session` / `Skills` 底部链接）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 全通过，且 Python HTTP 200 校验 `/help/chat` `/help/getting-started` `/` 全通过并继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`；独立 reviewer 曾质疑旧锚点命名，但 controller 侧重新顺序 browser 复核 live 与 local 后确认当前公开页使用按钮式 TOC 而非可分享 hash 深链，未能复现其所谓 anchor 回归，因此按 fail-closed 规则将该意见降级为未复现的非阻塞噪声，不据此回滚已与 live 对齐的改动）。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public help shell 的 TOC 交互合同对齐，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序对照 live `https://sciclaw.ai/help/getting-started`、live `/help/chat` 与 local `http://127.0.0.1:3001/help/getting-started` / `/help/chat`，确认当前 live 公开 help shell 的 `On this page` 区块实际暴露为 button list，而不是锚点 link list。基于该 live/local 证据，本轮将 `src/components/help-shell.tsx` 中旧的 server-side anchor TOC 抽离为新的 `src/components/help-toc.tsx` client 组件：保持 button 语义与 live 一致，同时补齐 hash 同步与 active state 跟随，避免 reviewer 首轮指出的初始 hash 漂移；`src/app/help/help-page.test.tsx` 也同步收敛为对 button-based TOC 的 focused 合同断言。该里程碑已完成 Review 1 功能/规格复核（live/local browser snapshot 顺序对照确认 `/help/getting-started` 与 `/help/chat` 的 TOC 均为 button list，且正文/侧栏结构未回退）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build` 全通过，Python HTTP 200 校验 `/` `/chat` `/help/chat` `/help/getting-started` 全为 200，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`STATIC_SCAN_OK`、`git diff --check` 通过；repo-scoped 独立 reviewer 首轮指出 hash/语义风险后，controller 侧已按 diff 修正并重跑验证，最终仅保留“原生锚点可进一步渐进增强”的非阻塞建议）。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/`、live `/chat` 与 local `http://127.0.0.1:3001/` / `/chat` 的 fresh-load 结构，再结合 local `browser_vision` 明确确认当前最明显剩余热点仍是右侧 auth-card 相对中心 preview 略 detached、而顶部 utility row 与左侧 radial cluster 均非本轮首要问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中继续做最小 vertical slice：将 auth-card 收窄到 `max-w-[276px]`，把外壳收敛为更淡的 `#fdfdff` / `#f4f7fa` / `0_2px_6px + 0_1px_3px` 阴影，并把 `lg/xl` 顶部下压微调到 `2.64rem / 2.84rem`，同时将内层 tab shell 收紧为更小圆角、更浅边框与更轻阴影；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth-card 宽度、背景、边框、阴影与下压合同。该里程碑已通过 Review 1 功能/规格复核（live/local 顺序对照 + fresh-load local browser QA 确认无明显 broken/clipping/overlap，且 auth-card 已比上一轮更轻、更贴近中心 preview）、Review 2 测试/构建/集成复核（focused Vitest 20/20、lint、build、`docker compose config`、`docker compose up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`）。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 vertical rhythm 收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构/视觉，再结合 local `browser_vision` 与 `browser_console` 几何确认当前最小且最直接的剩余差距已从局部文案/标签细节收敛为 hero 整体 visual group（左侧 radial network + 中央 preview + 右侧 auth-card）相对标题区仍略偏低，首屏标题与主视觉之间存在可继续压缩的空带。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中对 `landing-hero` 增加 `lg:-mt-[2.1rem]` / `xl:-mt-[2.35rem]`，把整个 hero visual group 在大屏上小步上提，而不改动现有 grid 宽度、auth-card 样式、Best Cases、help shell 或 `/chat` 复用 marketing landing 的合同；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 `lg/xl` 负上边距 class 合同。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + local browser_vision 确认首屏没有新的 broken/clipping/overlap，且 hero visual group 相比前一轮更贴近标题区、vertical rhythm 更紧致）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、针对当前 diff 的新增行静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`）。下一轮继续优先围绕 public landing 首屏 radial cluster / preview / auth-card 的剩余 spacing 与 hierarchy 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 贴合度与 hero 垂直节奏的更小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_vision` 与 local `browser_console` 几何确认当前单一最明显剩余差距仍是右侧 auth-card 相对中心 preview 略 detached，而 hero 主视觉整体仍可再轻微上提。基于该 root-cause 证据，本轮不扩张新模块，仅在 `src/components/landing-page.tsx` 中继续收窄并减轻 auth-card 与 hero 栅格：`landing-hero` 从 `lg:-mt-[2.1rem] / xl:-mt-[2.35rem]` 收敛到 `lg:-mt-[2.32rem] / xl:-mt-[2.58rem]`，右栏从 `280px` 收到 `272px`，`xl` 主列从 `1.1fr` 微放到 `1.12fr`，并把 gap 收紧到 `lg:gap-0 / xl:gap-[0.02rem]`；同时将 `landing-auth-card` 外壳从 `max-w-[276px]` 收到 `max-w-[268px]`，边框改为更淡的 `#f5f8fb`，阴影减轻为 `0_1px_4px / 0_1px_2px`，顶部下压微调到 `lg:mt-[2.86rem] / xl:mt-[3.06rem]`，内层 shell / tab shell 的圆角、padding、边框与阴影同步小步减重，使 auth-card 更像贴近 preview 的轻量 access module；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 外层布局与样式合同。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + fresh-load local browser_vision 确认 auth-card 的 detached 感较上一轮继续减弱、hero 视觉组略更紧致，且无新的 broken/clipped/overlap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核待本轮 repo-scoped 独立 reviewer 收口；若 reviewer 仅给出非阻塞建议，则下一轮继续围绕 auth-card 与左侧 radial cluster 的残余高保真微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing hero 左侧 radial cluster 的纵向锚定收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_vision` 与 local `browser_console` 几何确认当前单一最明显剩余热点仍是左侧 radial cluster 相对中心 preview / 右侧 auth-card 偏低、偏 detached，而不是 auth-card 本身。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `FeatureRotator` 从 `lg:items-center` 收敛为 `lg:items-start`，并将左侧 network shell 追加 `lg:-mt-[1.92rem] / xl:-mt-[2.14rem]`，把 radial cluster 整体向上提回到更接近中心 preview 的垂直带；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 `feature-rotator` 对齐与 network-shell 负上边距合同。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + rebuild 后 local browser QA 确认本轮后 left radial cluster 明显上提、与中心 preview 更接近，当前仍是最大剩余热点但 detached 感较前一轮减弱，且未触碰 `/root/nexus-mail` 任意工作树、服务或端口）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可补视觉回归或响应式布局断言”的非阻塞建议）。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 detached / spacing 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏顶部 utility controls 的可见度微调，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 live/local `browser_vision` 与 `browser_console` 几何确认当前单一最小剩余差距之一是 local 顶部三枚 utility icon buttons 相比 live 仍略偏淡、存在“可见但存在感稍弱”的问题，而 auth-card / radial cluster 本轮未被作为新的修改面。基于该 root-cause 证据，本轮不扩张新模块，仅在 `src/components/landing-page.tsx` 中把共享 `DockMenuBar` 的 landing utility trigger 样式从几乎透明的 ghost button 小步收敛为更接近 live 的轻量 outline buttons：边框由 `rgba(15,23,42,0.14)` 提到 `0.18`、补入 `bg-white/42`、文字收深到 `#4a525c`、并增加极轻的 `0_8px_18px_rgba(15,23,42,0.04)` 阴影；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 合同，显式锁定 `/` 与 `/chat` 上这组三枚 utility triggers 的新边框/底色/阴影存在。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + rebuild 后 local browser QA 确认 utility icons 更有存在感、但仍未变成过重按钮，且无新的 hero clipping/breakage）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核待本轮 repo-scoped 独立 reviewer 收口；下一轮继续从 live/local fresh-load 对照出发，优先推进首屏剩余 spacing / hierarchy 微差距，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 hero vertical rhythm 与右侧 auth-card 贴合度的小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 local `browser_vision` 与 `browser_console` 几何确认当前最小但仍可直接落地的剩余差距是 hero 整体 visual group 仍略偏低、且 auth-card 相对中心 preview 仍稍显 detached。基于该 root-cause 证据，本轮不扩张新模块，仅在 `src/components/landing-page.tsx` 中把 `landing-hero` 上提到 `lg:-mt-[2.9rem] / xl:-mt-[3.16rem]`，同时把 `landing-auth-card` 从 `lg:mt-[2.86rem] / xl:mt-[3.06rem]` 收敛到 `lg:mt-[2.6rem] / xl:mt-[2.8rem]`，在保持现有 272px 右栏宽度、utility controls、radial cluster、Best Cases 与 `/chat` 共享 landing shell 合同不变的前提下，让首屏主视觉更贴近标题区且让 auth-card 更像附属 access module；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero / auth-card 上移合同。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + rebuild 后 local `browser_vision` 确认无明显 broken/clipping/overlap，hero visual group 更贴近 heading area，auth-card 也比前一轮稍更 attached，但左侧 radial label 与 heading 区仍存在轻微拥挤感，已诚实保留为下一轮 fidelity gap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`）。下一轮继续优先围绕首屏 heading 与左侧 radial labels 的轻微拥挤/spacing 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 radial-cluster 锚定小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，再顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前最小且最真实的剩余 fidelity gap 已从 auth-card 贴合度进一步收敛为左侧 radial cluster 相对中心 preview 仍略 detached、略偏低。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `feature-rotator` 左侧 network shell 的大屏负上边距与负右边距从 `lg:-mt-[1.92rem] / lg:-mr-[0.12rem] / xl:-mt-[2.14rem] / xl:-mr-[0.18rem]` 收敛为 `lg:-mt-[2.18rem] / lg:-mr-[0.02rem] / xl:-mt-[2.42rem] / xl:-mr-[0.08rem]`，将 radial cluster 整体再向上、向中心 preview 轻推，而不改动 auth-card、utility controls、Best Cases、help shell 或 `/chat` 共享 landing shell 的既有合同；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新几何 class。Review 1 功能/规格复核：live/local 顺序对照 + fresh-load local browser QA 确认首屏无新增 broken/clipping/overlap，且本轮后左侧 radial cluster 更贴近主视觉，但 browser vision 仍诚实指出其标签/连线仍是当前最大剩余微差距。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可补视觉回归断言”的非阻塞建议。下一轮继续优先围绕 public landing 左侧 radial labels 的截断/spacing 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 前置核验，并顺序复核 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再结合 fresh-load local `browser_vision` 与当前 DOM 几何确认上一轮后 auth-card 仍是首屏最明显但已可继续小步收敛的剩余热点：它相比中心 preview 依旧略显 detached，而顶部 utility controls 与左侧 radial cluster 本轮并未成为新的主问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 `landing-hero` 的大屏右栏从 `266px` 收敛到 `258px`、`lg/xl` gap 从 `0.14rem/0.18rem` 收敛到 `0.12rem/0.16rem`，同时将 `landing-auth-card` 本体与内层 shell 继续减重（`max-w-[258px]`、更轻 `0_1px_3px / 0_1px_2px` 阴影、更紧的 `0.98rem / 0.72rem / 1.16rem` 圆角与 panel padding、`lg:mt-[2.58rem] / xl:mt-[2.78rem]` 下压），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 合同，使 `/` 与 `/chat` 共享 landing shell 的 auth-card 更窄、更轻、更贴近中心 preview，而不改动 public help、Best Cases、utility menu 路径或 `3001` 端口边界。该里程碑已完成 Review 1 功能/规格复核（live/local 顺序对照 + fresh-load local browser QA 确认 auth-card 相比上一轮更轻、更贴近主视觉，且仍是当前最大剩余热点但未引入新的 clipping/overlap）、Review 2 测试/构建/集成复核（`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`）、Review 3 静态/安全/契约复核（`git diff --check` 通过、added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`）。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 vertical rhythm / auth-card 贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前上一轮“auth-card 仍略 detached”已不再是唯一主问题，而新的更真实热点已转为 hero 顶部长句说明文案与下方 visual group 的轻微 crowding。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `landing-hero` 的大屏负上边距从 `lg:-mt-[2.56rem] / xl:-mt-[2.84rem]` 小步收紧到 `lg:-mt-[2.64rem] / xl:-mt-[2.88rem]`，同时把 `landing-auth-card` 的大屏上边距从 `lg:mt-[2.58rem] / xl:mt-[2.78rem]` 小步上提到 `lg:mt-[2.44rem] / xl:mt-[2.64rem]`，让 hero 整体更贴近标题区、但不过度上顶；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 垂直偏移合同。Review 1：live/local 顺序对照、fresh-load browser QA 与 DOM 几何确认页面重新回到 coherent 状态，auth-card 稍更 attached，但当前单一最大剩余差距已诚实转为 hero 顶部长句与插图区仍略拥挤，而非新的 clipping/breakage；Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`；Review 3：`git diff --check` 通过、added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing 顶部说明文案与 hero 插图区之间的 spacing / hierarchy 微差距推进，而不是触碰 `/root/nexus-mail` 任意服务、端口或工作树。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card 贴合度微收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最小且最可安全落地的剩余 fidelity gap 仍是右侧 auth-card 相对中心 preview 略 detached、略偏高。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 `landing-hero` 的大屏右栏从 `258px` 收至 `254px`、`lg/xl` gap 从 `0.08rem/0.12rem` 收至 `0.04rem/0.08rem`，并把 `landing-auth-card` 小步下压到 `lg:mt-[2.86rem] / xl:mt-[3.08rem]` 以增强与中心 preview 的从属关系；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 宽度、gap 与下压合同。Review 1 功能/规格复核：live/local 顺序对照 + fresh-load local browser QA 确认页面无新的 broken/clipping/overlap，auth-card 仍是剩余最明显热点但已比上一轮更窄、更贴近 preview，当前结果 coherent enough to ship as a small iteration。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的纵向贴合度微收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最值得继续推进的最小剩余差距是右侧 auth-card 虽已更轻更近，但相对中心 preview 仍略偏右上、与主 visual band 的纵向锚点还不够统一。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `landing-auth-card` 的大屏上边距从 `lg:mt-[2.86rem] / xl:mt-[3.08rem]` 收敛为 `lg:mt-[2.18rem] / xl:mt-[2.38rem]`，只做 auth-card 上移这一处最小 vertical slice，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 锁定新的 `lg/xl` 上边距合同；hero 栅格、preview card、left radial cluster、Best Cases、help shell 与 `/chat` 共享 landing shell 的既有合同保持不变。Review 1 功能/规格复核：live/local 顺序对照 + rebuild 后 local `browser_vision` 确认这轮上移已起效，auth-card 相比前一版更上移、更贴近中心 preview，且页面无明显 clipping、overlap 或 absurd spacing；当前新的单一最大剩余热点已诚实收敛为 auth-card 仍稍偏右上、与 hero 主内容带的垂直锚点未完全统一。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing 首屏 auth-card 与中心 preview 的残余纵向锚点差距做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。


- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card 贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最小且最值得继续推进的剩余 fidelity gap 仍是右侧 auth-card 相对中心 preview 略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 hero 右栏从 `254px` 收敛到 `248px`、`lg/xl` gap 从 `0.04rem/0.08rem` 收敛到 `0/0.02rem`，并把 `landing-auth-card` 从 `max-w-[254px]` 收敛到 `max-w-[248px]`、顶部下压从 `lg:mt-[2.18rem] / xl:mt-[2.38rem]` 收敛到 `lg:mt-[1.92rem] / xl:mt-[2.1rem]`，让 auth-card 更贴近中心 preview；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card 布局合同。Review 1 功能/规格复核：live/local 顺序对照 + fresh-load local browser QA 确认页面 coherent、无明显 broken/clipping，auth-card attachment 较上一轮继续改善，但仍是后续可继续收敛的主要微差距。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 hero 文案/visual-group 纵向节奏收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最明显剩余 fidelity gap 已从 auth-card detached 转为 hero 顶部长说明文案与中心 preview 顶边的轻度 overlap：controller 侧几何显示 intro/preview 垂直重叠从约 `14.55px` 降到 `10.39px`，但 local browser QA 仍明确指出首屏主问题是 hero 文案与中部 preview 的纵向节奏过挤，而非新的 clipping/broken 或 utility/auth-card 回归。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `landing-hero` 的大屏负上边距从 `lg:-mt-[2.92rem] / xl:-mt-[3.16rem]` 回调到 `lg:-mt-[2.66rem] / xl:-mt-[2.9rem]`，只做 hero 容器层级的单点回退；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 `lg/xl` hero class 合同，而 auth-card、preview card、utility row、Best Cases、help shell 与 `/chat` 共用 landing shell 的既有合同保持不变。Review 1 功能/规格复核：live/local 顺序对照 + rebuild 后 local browser QA 确认首屏 overlap 较上一轮减轻，当前最大剩余问题已诚实收敛为 hero 文案与 preview 顶部仍略拥挤，但未出现新的 broken/clipping/absurd spacing。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing 首屏 hero 文案与中心 preview 的剩余纵向 spacing / hierarchy 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 hero 文案 / preview 纵向节奏收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最明显剩余 fidelity gap 仍是 hero 顶部长说明文案与中心 preview 顶边之间的轻度 crowding，而非 auth-card detached、左侧 radial cluster 锚定或新的 broken/clipping 问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `landing-hero` 的大屏负上边距从 `lg:-mt-[2.66rem] / xl:-mt-[2.9rem]` 回调到 `lg:-mt-[2.42rem] / xl:-mt-[2.64rem]`，只做 hero 容器层级的单点 vertical-rhythm 回退；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 `lg/xl` hero class 合同，而 auth-card、preview card、utility row、Best Cases、help shell 与 `/chat` 共用 landing shell 的既有合同保持不变。Review 1 功能/规格复核：live/local 顺序对照 + rebuild 后 local browser QA 确认首屏无明显 broken/clipping/absurd spacing，hero 文案与 preview 的纵向拥挤较上一版减轻，当前新的单一最大剩余问题已诚实收敛为 intro sentence 仍稍靠近 preview 顶边、`Get started` CTA 仍略偏淡。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing 首屏 hero 文案与中心 preview 的剩余纵向 spacing / hierarchy 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card 贴合度与 hero/radial cohesion 小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude` 与 `3001` 发布前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构 / browser vision，确认当前单一最明显剩余 fidelity gap 仍是右侧 auth-card 相对中心 preview 虽已改善但仍略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中收紧 hero 文案区纵向节奏（`gap/space-y/heading/description/CTA`）、将大屏 hero 右栏从 `238px` 收敛到 `234px` 并把 `lg/xl` gap 收至 `0.1rem/0.16rem`、同步把 auth-card 下压改为 `lg:mt-[1.78rem] / xl:mt-[1.96rem]`，同时将左侧 radial nodes 与 connector 进一步向中心 preview 内收（`left/right` 与 `w-[...]` 连接线宽度同步收敛），让首屏更像一个连续主视觉而不是三块松散浮层；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 hero/auth-card/radial/layout/CTA 合同。Review 1 功能/规格复核：sequential live/local fresh-load browser QA 确认右侧 auth-card 的 attachedness 与左侧 radial cluster 的 anchored 感均较上一轮改善，页面无新的 broken / clipping / overlap，当前仍需后续继续微收敛的唯一明显差距是 auth-card 与 preview 之间仍存在可感知 gutter。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 gutter / docking 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。


- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster → 中心 preview 锚定收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port 与 `.git/info/exclude` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前最真实且最小可安全落地的剩余 fidelity gap 已从 auth-card detached 转为左侧 radial cluster 相对中心 preview 仍略偏左、略偏低、连接感稍弱。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `feature-rotator` 左侧 network shell 的大屏负上边距与负右边距从 `lg:-mt-[1.92rem] / lg:-mr-[0.12rem] / xl:-mt-[2.14rem] / xl:-mr-[0.18rem]` 收敛为 `lg:-mt-[2.08rem] / lg:-mr-[0.04rem] / xl:-mt-[2.3rem] / xl:-mr-[0.1rem]`，将 radial cluster 整体再向上、向中心 preview 轻推，而不改动 auth-card、utility controls、Best Cases、help shell 或 `/chat` 共享 landing shell 的既有合同；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新几何 class。Review 1 功能/规格复核：sequential live/local fresh-load browser QA 与 local `browser_vision` 明确确认当前主要剩余热点是左侧 cluster 的连接感，而本轮后其更贴近中心 preview、页面无新的 broken/clipping/overlap。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 spacing / attachment 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。


- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth-card 轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，再顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision、focused/full Vitest、lint、build、`docker compose config`、`docker compose up -d --build web` 与 `/`、`/chat`、`/help/getting-started` 的 HTTP 200。基于 live/local 对照与本地 browser QA，确认当前单一最明显剩余热点仍是右侧 auth-card 相对中心 preview 略显紧凑且存在感偏强，因此本轮仅在 `src/components/landing-page.tsx` 将 hero 右栏从 `236px` 微收至 `232px`、`lg/xl gap` 从 `0.14/0.18rem` 收至 `0.1/0.14rem`，并同步把 `landing-auth-card` 收敛为更窄更轻的 `max-w-[232px]`、更淡边框/渐变/阴影与更低调的 `lg/xl mt`；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新合同。Review 1：live/local fresh-load QA 与 rebuild 后 browser_vision 一致确认页面无 clipping/overlap/blank render，auth-card 仍是剩余主热点但较上一轮更贴近中心 preview。Review 2：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200（`/`、`/chat`、`/help/getting-started`）与 `git diff --check` 全部通过。Review 3：controller 侧静态安全扫描对当前 diff 未发现 hardcoded secret / shell injection / eval / unsafe deserialize / SQL injection 命中，独立 reviewer 也确认本轮 diff 无新增 security concern 或 logic error；下一轮可继续围绕 auth-card 与中心 preview 的剩余紧凑感做更小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最明显剩余 fidelity gap 仍是右侧 auth-card 相对中心 preview 明显 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 auth-card 所在列从单独落在 preview 下方，收敛为通过 `-mt-[0.22rem] / lg:-mt-[10.6rem] / xl:-mt-[11.1rem]` 上提并 `lg:ml-auto` 右对齐回嵌到 hero visual band，使 auth-card 在大屏上更接近 preview 右侧而不再像独立下沉模块；同步在 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 增补针对 auth-column wrapper 的 focused 合同断言，锁定新的 overlay/对齐行为。Review 1 功能/规格复核：live/local 顺序对照 + fresh-load local browser_vision 明确确认 auth-card 仍是首屏最大剩余热点，而本轮后虽仍略 detached，但已从“明显下沉的独立块”收敛为更贴近中心 preview 的同一 hero 组合，未引入新的 broken/clipping/overlap。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可为 auth wrapper 增加更稳定测试锚点、减少 `lastElementChild` 结构耦合”的非阻塞建议。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最明显剩余 fidelity gap 仍是右侧 auth-card 相对中心 preview 略 detached、略显独立。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中继续做更小步的 auth-card 贴合收敛：将 auth wrapper 于 `lg/xl` 下轻微左推并小幅上提（`lg:-ml-[0.18rem] / xl:-ml-[0.24rem]`、`lg:mt-[0.22rem] / xl:mt-[0.36rem]`），同时把 `landing-auth-card` 外壳从 `232px` 收敛到 `226px`，并同步减轻外层边框、渐变、blur、阴影与内层 tab shell / panel 的圆角、padding、选中态阴影，让右侧入口更像贴附在中心 preview 边上的 access module，而不是自成一块的浮层；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth wrapper 与 auth-card 视觉合同。Review 1 功能/规格复核：live/local 顺序对照 + rebuild 后 fresh-load local `browser_vision` 明确确认 auth-card 仍是当前首屏最大剩余热点，但本轮后已较上一轮“稍更贴近、稍更轻”，且无新的 clipping/overlap/blank render 回归。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached / gutter 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 apply-now 合同对齐，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构 / browser snapshot，确认当前公开合同为：点击 `No account yet? Apply Now →` 后，hero 需切到 `Multi-Format Research Output` 并显示 `PPT / PDF / CSV / DOCX`，同时 `Best Cases` 从默认 `01 / 04 / Automated Report Generation` 推进到 `02 / 04 / Peer Review Response Support`。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 `handleApplyNow` 从旧的 `data-mining + case 0` 收敛为 `outcome-present + case 1`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 apply-flow focused 合同，显式锁定 apply-now 后的 hero heading、`PPT/PDF/CSV/DOCX` 标签、`02 / 04` 计数与 `Peer Review Response Support` 文案。Review 1 功能/规格复核：live/local 顺序对照 + browser 操作确认本地 `/` 与 `/chat` 点击 apply-now 后已与公开站点对齐到 `Multi-Format Research Output + 02 / 04`，且未引入新的 broken/clipping。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可按 stable id 派生 case index”的非阻塞建议。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster 裁切修正，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并按 systematic-debugging 先顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构 / browser vision，再用 `browser_console` 几何复现确认当前单一最小且最可安全落地的根因不是节点绝对定位本身，而是 `FeatureRotator` 左侧 network wrapper 仍为 `overflow-hidden`，导致 radial label 在 local 侧虽已接近边界但仍会被外层 frame 截断；证据上 `Outcome Present` 标签左缘约 `57.1px` 小于 frame 左缘 `60px`、`Data Mining` 右缘约 `425.6px` 大于 frame 右缘 `403.6px`。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将该 wrapper 从 `overflow-hidden` 收敛为 `overflow-visible`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 合同断言，使测试与实际公开目标一致：left radial cluster 可越出装饰 frame 而不被裁切。Review 1 功能/规格复核：live/local fresh-load 对照 + rebuild 后 local `browser_vision` 明确确认先前“左侧 radial cluster 被切断/截短”的首要热点已转为更软性的 spacing / contrast / anchoring 问题，页面未引入新的 broken、overlap 或 blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 与 added-lines 静态扫描 `STATIC_SCAN_OK` 通过，repo-scoped 独立 reviewer JSON 复审 `passed=true`、无新增 security concern / logic error；下一轮可继续围绕 left radial cluster 的 softer anchoring / line-break / contrast 做更小步 fidelity 收敛，而不是继续追逐已解除的硬裁切问题。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser snapshot/DOM 几何与当前 diff，确认当前单一最明显剩余 fidelity gap 仍是右侧 auth-card 相对中心 preview 略显 detached、存在感稍强。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 preview+auth 组合右栏从 `232px` 收敛到 `226px`、`lg/xl` 横向 gap 从 `0.42rem/0.5rem` 收敛到 `0.34rem/0.42rem`，同步把 auth wrapper 的 `lg/xl` 左推/上提幅度从 `-0.18rem/-0.24rem`、`0.22rem/0.36rem` 收敛为 `-0.08rem/-0.14rem`、`0.12rem/0.26rem`，并将 `landing-auth-card` 本体进一步减重为 `max-w-[220px]`、更轻的边框/渐变/阴影/blur，使右侧入口更像贴近中心 preview 的 access module，而不是相对独立的浮层；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview 列宽、gap、auth wrapper 与 auth-card 样式合同。Review 1 功能/规格复核：live/local 顺序对照与 browser/DOM 几何确认本轮后 auth-card 仍是当前首屏最大剩余热点，但已较上一轮更窄、更轻、更贴近中心 preview，且未引入新的 clipping/overlap/blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可减少视觉类精确 class 断言耦合”的非阻塞建议。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached / gutter 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing apply-now 合同校准，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 结构，再按 systematic-debugging 复现实测 apply-now 行为。根因证据显示：当前 live 站点点击 `No account yet? Apply Now →` 后，hero 的确切切换合同仍是 `Multi-Format Research Output` + `PPT / PDF / CSV / DOCX`，但 `Best Cases` 继续保持默认 `01 / 04 / Automated Report Generation`，并不会切到 `02 / 04 / Peer Review Response Support`；本地之前把 `handleApplyNow` 绑定到 `setActiveCaseIndex(1)`，导致与 live 公开交互漂移。本轮因此仅将 `src/components/landing-page.tsx` 中 `handleApplyNow` 收敛为继续切 hero 到 `outcome-present` 但保持 `setActiveCaseIndex(0)`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 apply-flow focused 合同：显式锁定 apply-now 后 hero 仍显示 `Multi-Format Research Output` 与 `PPT/PDF/CSV/DOCX`，而 `Best Cases` 保持 `Automated Report Generation / 01 / 04`、不出现 `Peer Review Response Support / 02 / 04`。Review 1 功能/规格复核：live/local 顺序对照 + controller DOM 复现确认该切片只修正 apply-now 的 hero/carousel 契约漂移，未扩张新模块，也未触碰 `/root/nexus-mail` 任意服务、端口或工作树。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`。下一轮继续从 live/local fresh-load 对照出发，优先推进 public landing 首屏 auth-card / radial cluster / vertical rhythm 的剩余高保真微差距，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser snapshot 与 DOM 几何，确认当前单一最明显剩余 fidelity gap 仍是右侧 auth-card 相对中心 preview 略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将 preview+auth 组合右栏从 `226px` 收敛到 `220px`、`lg/xl` 横向 gap 从 `0.34rem/0.42rem` 收敛到 `0.28rem/0.34rem`，同步把 auth wrapper 的 `lg/xl` 左推/上提幅度从 `-0.06rem/-0.1rem`、`0.18rem/0.32rem` 收敛为 `-0.02rem/-0.06rem`、`0.08rem/0.18rem`，并将 `landing-auth-card` 本体进一步减重为 `max-w-[210px]`、更轻的边框/渐变/阴影/圆角/padding，使右侧入口更像贴近中心 preview 的 access module，而不是相对独立的浮层；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview 列宽、gap、auth wrapper 与 auth-card 样式合同。Review 1 功能/规格复核：live/local 顺序对照与 DOM 几何确认本轮后 auth-card 仍是当前首屏最大剩余热点，但已较上一轮更窄、更轻、更贴近中心 preview，且未引入新的 clipping/overlap/blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可减少视觉类精确 class 断言耦合”的非阻塞建议。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached / gutter 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 systematic-debugging 顺序复现实测热点，确认当前最真实的单一剩余差距仍是右侧 auth-card 相对中心 preview 略显 detached，而非左侧 radial cluster、顶部 utility row 或新的 broken/clipping 问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步收紧 `FeaturePreview` 右栏：将 preview+auth 组合列从 `220px` 再收敛到 `214px`，把 `lg/xl` 横向 gap 从 `0.28rem/0.34rem` 收敛到 `0.22rem/0.28rem`，同步把 auth wrapper 的 `lg/xl` 左推/上提幅度从 `-0.02rem/-0.06rem`、`0.08rem/0.18rem` 收到 `-0.01rem/-0.04rem`、`0.04rem/0.12rem`，并将 `landing-auth-card` 本体减重为 `max-w-[204px]`、更淡的 `rgba(240,244,248,0.96)` 边框、更轻的 `0_4px_10px/0_1px_4px` 阴影、更低 blur 与更紧的 tab shell / panel 圆角与 padding，使右侧入口更像贴近中心 preview 的 access module，而不是相对独立的浮层；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview 列宽、gap、auth wrapper 与 auth-card 样式合同。Review 1 功能/规格复核：live/local 顺序对照、fresh-load browser snapshot 与 local DOM 几何确认本轮后 auth-card 仍是当前首屏最大剩余热点，但已较上一轮更窄、更轻、更贴近中心 preview，且未引入新的 clipping/overlap/blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、当前 diff 新增行静态安全扫描 0 命中、repo-scoped 独立 reviewer JSON 复审 `passed=true`；当前非阻塞建议仅剩“未来可减少视觉类精确 class 断言耦合”。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached / gutter 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧完成 live/local fresh-load 顺序复核、DOM 几何测量、browser vision、focused Vitest、lint/build、compose rebuild、HTTP 200、静态安全扫描与 repo-scoped 独立 reviewer 复审，确认当前 diff 将 preview/auth 右栏进一步收窄到 `208px`、auth-card 收窄到 `198px`，并把 auth wrapper 与测试合同同步锁定；browser vision 仍把右侧 auth-card 视为首屏主热点，但明确认为其已更轻、更窄、更贴近中心 preview，且未出现 clipping / overlap / 荒谬间距。下一轮继续优先围绕 auth-card 与中心 preview 的残余 detached/gutter 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 systematic-debugging 顺序复现实测热点，确认当前单一最真实剩余差距仍是右侧 auth-card 相对中心 preview 略 detached，而非 hero vertical spacing、顶部 utility row 或新的 broken/clipping 问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步继续收紧 `FeaturePreview` 右栏：将 preview+auth 组合列从 `200px` 收敛到 `196px`、`lg/xl` 横向 gap 从 `0.1/0.16rem` 收敛到 `0.02/0.08rem`，同步把 auth wrapper 的 `lg/xl` 左推/上提幅度从 `-0.04/-0.1rem`、`0.06/0.14rem` 收敛为 `-0.16/-0.22rem`、`-0.02/0.06rem`，并将 `landing-auth-card` 本体减重为 `max-w-[186px]`、更淡的 `rgba(244,247,250,0.97)` 边框、更轻的 `0_2px_5px/0_1px_2px` 阴影、更低 blur 与更紧的圆角 / padding，使右侧入口更像贴近中心 preview 的 access module；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview 列宽、gap、auth wrapper 与 auth-card 样式合同。Review 1 功能/规格复核：live/local 顺序对照、fresh-load local `browser_vision` 与 DOM 几何确认本轮后 auth-card 不再是最突兀的 detached 浮层，页面无新的 clipping/overlap/blank render，当前新的单一主热点已诚实收敛为左侧 radial cluster anchoring。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer 三轮 JSON 复审均 `passed=true`；当前仅保留“未来可减少首页与 chat 测试中的重复 class 合同断言”的非阻塞建议。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 anchoring / spacing 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster → 中心 preview 锚定收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最明显剩余 fidelity gap 仍是左侧 radial cluster 相对中心 preview 略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `FeatureRotator` 左侧 network shell 的大屏负上边距与负右边距从 `lg:-mt-[2.08rem] / lg:-mr-[0.04rem] / xl:-mt-[2.3rem] / xl:-mr-[0.1rem]` 收敛为 `lg:-mt-[2.18rem] / lg:-mr-[0.02rem] / xl:-mt-[2.42rem] / xl:-mr-[0.08rem]`，将 radial cluster 整体再向上、向中心 preview 轻推，而不改动 auth-card、utility controls、Best Cases、help shell 或 `/chat` 共享 landing shell 的既有合同；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新几何 class。Review 1 功能/规格复核：sequential live/local fresh-load browser QA 与 local `browser_vision` 明确确认当前主要剩余热点仍是左侧 cluster 的连接感，而本轮后其更贴近中心 preview、页面无新的 broken/clipping/overlap。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可合并首页与 /chat 的重复 class 合同断言、并补更行为化视觉回归”的非阻塞建议。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 spacing / attachment 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。


- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster → 中心 preview 锚定收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser snapshot 与 DOM 几何，确认当前最真实且最小可安全落地的剩余 fidelity gap 仍是左侧 radial cluster 相对中心 preview 的锚定感略弱，而右侧 auth-card 已不再是当前首要热点。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `FeatureRotator` 左侧 network shell 的大屏负上边距与负右边距从 `lg:-mt-[2.18rem] / lg:-mr-[0.02rem] / xl:-mt-[2.42rem] / xl:-mr-[0.08rem]` 收敛为 `lg:-mt-[2.24rem] / lg:mr-0 / xl:-mt-[2.48rem] / xl:-mr-[0.04rem]`，并同步把 `feature-rotator` 的 `lg/xl` gap 从 `0.14rem / 0.2rem` 收紧到 `0.12rem / 0.18rem`，让左侧 radial cluster 再轻微上提并更贴近中心 preview；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 gap 与 network-shell 几何合同。Review 1 功能/规格复核：live/local 顺序对照与本地 DOM 几何确认 radial cluster 与 preview 的接缝更紧、页面无新的 broken/clipping/overlap，当前剩余问题仍是更软性的左侧连接感微差距。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可减少视觉类精确 class 断言耦合”的非阻塞建议。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 spacing / attachment 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster → 中心 preview 锚定收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前最真实且最小可安全落地的剩余 fidelity gap 仍是左侧 radial cluster 相对中心 preview 的锚定感略弱，而右侧 auth-card 已不再是当前首要热点。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中将三枚 radial nodes 与 connector 继续整体向中心 preview 轻推并略微上提：`Literature Analysis` 节点从 `left-[46.45%]` 收敛到 `left-[47.1%]`、连线从 `w-[12.05%]` 收到 `w-[11.55%]`；`Data Mining` 节点从 `left-[46.55%] top-[47.7%]` 收敛到 `left-[47%] top-[47.2%]`、连线从 `w-[11.85%]` 收到 `w-[11.35%]`；`Outcome Present` 节点从 `right-[18.2%] top-[14.55%]` 收敛到 `right-[19.7%] top-[14.35%]`、连线从 `w-[11.3%]` 收到 `w-[10.6%]`。同时将 `feature-rotator` 的大屏 gap 从 `lg/xl 0.12/0.18rem` 收紧到 `0.08/0.12rem`，并把左侧 network shell 的大屏负上边距 / 负右边距从 `lg:-mt-[2.24rem] / xl:-mt-[2.48rem] / xl:-mr-[0.04rem]` 收敛到 `lg:-mt-[2.32rem] / xl:-mt-[2.56rem] / xl:-mr-[0.02rem]`，让 radial cluster 更贴近中心 preview；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 radial-node、connector、gap 与 network-shell 几何合同。Review 1 功能/规格复核：sequential live/local fresh-load browser QA 与 local `browser_vision` 明确确认当前主要剩余热点仍是左侧 cluster 的连接感，而本轮后其更贴近中心 preview、页面无新的 broken/clipping/overlap。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可减少视觉类精确 class 断言耦合”的非阻塞建议。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 spacing / attachment 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 stacked auth/access module 仍是 fresh-load 首屏最明显剩余热点。基于 live/local 对照：live access band 宽度约 `599.5px`，本地改动前 auth wrapper / card 约 `516px / 516px`，且 `previewBottom_to_authTop≈-147.84px`、`authBottom_to_bestTop≈113.27px`，说明本地模块仍略偏窄、略偏上贴。围绕该 root cause，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步放宽并略微下沉收口：`-mt-[2.32rem] -> -mt-[2.08rem]`、`max-w-[312px]/sm:max-w-[324px] -> 316px/328px`、`lg:-mt-[9.98rem]/lg:max-w-[500px] -> -9.62rem/504px`、`xl:-mt-[10.32rem]/xl:max-w-[516px] -> -9.96rem/520px`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper class 合同，使 auth/access module 相较上一轮更接近 preview 下方的附属 access band，而非过度上提的独立热点。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前页面无新的 clipping / overlap / blank render，auth/access module 仍是首要剩余热点，但本轮后已更 integrated；rebuild 后 `authWrap.width≈520px`、`previewBottom_to_authTop≈-142.09px`，较上一轮略放宽且略下沉。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error，仅保留“未来可减少精确 Tailwind class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 的剩余 docking 热点继续做单一视觉切片，而不是触碰 `/... [truncated]`
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量 docking 收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 先确认 root cause：当前本地 stacked auth-card 虽已位于 preview 下方，但仍比 live 公开页 access module 更宽、更低一小步，导致首屏右下入口仍稍显松散。基于该证据，本轮仅在 `src/components/landing-page.tsx` 将 auth wrapper 从 `-mt-[0.12rem]/max-w-[320px]/sm:max-w-[332px]/lg:mt-[0.08rem]/lg:max-w-[620px]/xl:mt-[0.12rem]/xl:max-w-[640px]` 收敛为 `-mt-[0.2rem]/max-w-[304px]/sm:max-w-[316px]/lg:mt-[0.02rem]/lg:max-w-[600px]/xl:mt-[0.06rem]/xl:max-w-[620px]`，并把 `landing-auth-card` 自身从 `max-w-[244px]` / `lg:max-w-[420px]` / `xl:max-w-[436px]` 收窄到 `236px / 404px / 420px`，同时同步内层 shell `max-w-[268px] / lg:max-w-[404px] / xl:max-w-[420px]`，让 access module 在继续保持下置层级的前提下略微上提、收窄、减轻 detached 感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper / auth shell 合同，并保留旧大屏 top-margin 缺失断言。Review 1 功能/规格复核：live browser_vision 连续降级无有效内容，故按降级 fallback 改用 live/local sequential snapshot + DOM 几何作为 controller 事实来源；local rebuild 后几何确认 auth width 已从约 `436px` 收至 `420px`、wrapper 从 `640px` 收至 `620px`、preview 下缘到 auth 顶部 gap 从约 `19.17px` 收至 `18.22px`，且仍保持 preview 下方 stacked 层级、未引入 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I` 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅发布到 `3001` 且未影响 `/root/nexus-mail` 任意现有服务与端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 hardcoded secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅提示清理一个重复测试断言与补齐 `/`/`/chat` 断言对称性，均已在 controller 侧收口处理。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余最大热点是否重新转回 left radial cluster / hero text stack，而不是继续过度迭代 auth-card。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/port、`.git.info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 将当前最小安全切片继续锁定为 auth/access module 仍是首屏最大剩余热点。基于 live/local 对照、browser QA 与本地运行时几何，本轮仅在 `src/components/landing-page.tsx` 对 stacked auth rail 做一次小步收窄与减重：将 `feature-auth-wrapper` 从 `-mt-[0.32rem] / max-w-[352px] / sm:max-w-[364px] / lg:-mt-[4.18rem] / lg:max-w-[624px] / xl:-mt-[4.36rem] / xl:max-w-[640px]` 收敛为 `-mt-[0.18rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[3.7rem] / lg:max-w-[600px] / xl:-mt-[3.88rem] / xl:max-w-[616px]`，同时将 `landing-auth-card` 外壳从 `276px / 612px / 628px` 收窄到 `268px / 588px / 604px`，并轻微收紧 radius、border、background、shadow、blur 与 inner shell `300px / 588px / 604px`，使 access module 相对中心 preview 更轻、更窄、更少 detached；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 wrapper / auth shell / inner-shell 精确类合同，保持 `/` 与 `/chat` 继续同构。Review 1 功能/规格复核：sequential live/local 对照与 local browser QA 继续指向 auth/access module 仍是主热点，但本轮 rebuild 后 local 几何已确认 auth width `628px -> 604px`、wrapper width `640px -> 616px`，且页面无新增 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响 `/root/nexus-mail` 任意现有服务或端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新引入的 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth-card 是否已收敛到次级热点，或是否应转向 left radial cluster / hero composition 的剩余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量 docking 收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 先确认 root cause：当前本地 stacked auth-card 虽已位于 preview 下方，但仍比 live 公开页 access module 更宽、更低一小步，导致首屏右下入口仍稍显松散。基于该证据，本轮仅在 `src/components/landing-page.tsx` 将 auth wrapper 从 `-mt-[0.12rem]/max-w-[320px]/sm:max-w-[332px]/lg:mt-[0.08rem]/lg:max-w-[620px]/xl:mt-[0.12rem]/xl:max-w-[640px]` 收敛为 `-mt-[0.2rem]/max-w-[304px]/sm:max-w-[316px]/lg:mt-[0.02rem]/lg:max-w-[600px]/xl:mt-[0.06rem]/xl:max-w-[620px]`，并把 `landing-auth-card` 自身从 `max-w-[244px]` / `lg:max-w-[420px]` / `xl:max-w-[436px]` 收窄到 `236px / 404px / 420px`，同时同步内层 shell `max-w-[268px] / lg:max-w-[404px] / xl:max-w-[420px]`，让 access module 在继续保持下置层级的前提下略微上提、收窄、减轻 detached 感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper / auth shell 合同，并保留旧大屏 top-margin 缺失断言。Review 1 功能/规格复核：live browser_vision 连续降级无有效内容，故按降级 fallback 改用 live/local sequential snapshot + DOM 几何作为 controller 事实来源；local rebuild 后几何确认 auth width 已从约 `436px` 收至 `420px`、wrapper 从 `640px` 收至 `620px`、preview 下缘到 auth 顶部 gap 从约 `19.17px` 收至 `18.22px`，且仍保持 preview 下方 stacked 层级、未引入 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I` 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅发布到 `3001` 且未影响 `/root/nexus-mail` 任意现有服务与端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 hardcoded secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅提示清理一个重复测试断言与补齐 `/`/`/chat` 断言对称性，均已在 controller 侧收口处理。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余最大热点是否重新转回 left radial cluster / hero text stack，而不是继续过度迭代 auth-card。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 将当前最小安全切片继续锁定为 auth/access module 仍是首屏最大剩余热点。基于 live/local 对照、browser QA 与本地运行时几何，本轮仅在 `src/components/landing-page.tsx` 对 stacked auth rail 做一次小步收窄与减重：将 `feature-auth-wrapper` 从 `-mt-[0.32rem] / max-w-[352px] / sm:max-w-[364px] / lg:-mt-[4.18rem] / lg:max-w-[624px] / xl:-mt-[4.36rem] / xl:max-w-[640px]` 收敛为 `-mt-[0.18rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[3.7rem] / lg:max-w-[600px] / xl:-mt-[3.88rem] / xl:max-w-[616px]`，同时将 `landing-auth-card` 外壳从 `276px / 612px / 628px` 收窄到 `268px / 588px / 604px`，并轻微收紧 radius、border、background、shadow、blur 与 inner shell `300px / 588px / 604px`，使 access module 相对中心 preview 更轻、更窄、更少 detached；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 wrapper / auth shell / inner-shell 精确类合同，保持 `/` 与 `/chat` 继续同构。Review 1 功能/规格复核：sequential live/local 对照与 local browser QA 继续指向 auth/access module 仍是主热点，但本轮 rebuild 后 local 几何已确认 auth width `628px -> 604px`、wrapper width `640px -> 616px`，且页面无新增 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat.page.test.tsx`、`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响 `/root/nexus-mail` 任意现有服务或端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新引入的 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth-card 是否已收敛到次级热点，或是否应转向 left radial cluster / hero composition 的剩余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 先将单一最小安全切片锁定为 auth/access module 仍略显 detached。基于 live/local 结构与 local browser QA，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[0.22rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[3.92rem] / lg:max-w-[600px] / xl:-mt-[4.1rem] / xl:max-w-[616px]` 收紧为 `-mt-[0.34rem] / max-w-[332px] / sm:max-w-[344px] / lg:-mt-[4.22rem] / lg:max-w-[588px] / xl:-mt-[4.42rem] / xl:max-w-[604px]`，在不改动 preview、radial cluster、端口或 `/chat` 共用壳的前提下，进一步上提并轻收 auth wrapper；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper class 合同。Review 1 功能/规格复核：fresh-load local browser_vision 与 DOM 几何确认 auth/access module 仍位于 preview 下方，视觉上比上一轮更紧、更少 detached，且已开始退居为次级热点；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`npm test`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check` 与 added-lines 静态安全扫描通过，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断首屏主热点是否已经转向 left radial cluster 或 hero text/visual rhythm，而不是继续过度迭代 auth-card。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先顺序复核 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load hero，并用 local browser QA + DOM 几何确认当前单一最明显剩余热点重新落回右侧 auth-card：页面已无明显 clipping/overlap，但 auth-card 仍比 live 更像一个偏窄、偏挤的独立侧栏。基于该 root-cause 证据，本轮仅将 `src/components/landing-page.tsx` 的 preview/auth 组合列从 `220px` 放宽到 `228px`，同步把 auth wrapper 的 `lg/xl` 偏移收敛为 `-0.01rem / 0.08rem` 与 `-0.04rem / 0.14rem`，并把 `landing-auth-card` 本体从 `max-w-[210px]` 放宽到 `max-w-[218px]`，同时小步放松外壳/内层 shell/tablist/panel 的圆角、padding、边框与阴影，使右侧入口从“过窄且内部拥挤”收敛为更有呼吸感、但仍保持附属 access module 的轻量 panel；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview 列宽、gap、auth wrapper 与 auth-card 样式合同。Review 1 功能/规格复核：live/local 顺序对照 + rebuilt local `browser_vision` 确认页面当前无明显 broken/clipping/absurd spacing，auth-card 仍是首屏主热点，但已从“偏挤的独立窄栏”收敛为更合理整合的辅助入口；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check` 与 added-lines 静态安全扫描 `STATIC_SCAN_OK` 通过，repo-scoped 独立 reviewer 复审待本轮 commit 前完成。下一轮继续优先围绕 public landing auth-card 与中心 preview 的残余 detached / gutter 感做更小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点（2026-05-17 10:48 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 stacked auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前单一最小安全切片锁定为 auth/access module 仍是 fresh-load 首屏最明显剩余热点：live 站点 access band 在 preview 下方更宽、更略上贴，而本地 stacked auth wrapper/卡面仍稍偏窄、偏低。基于这组 live/local 结构与 fresh browser 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[1.84rem] / max-w-[292px] / sm:max-w-[304px] / lg:-mt-[10.86rem] / lg:max-w-[440px] / xl:-mt-[11.18rem] / xl:max-w-[456px]` 收敛为 `-mt-[1.96rem] / max-w-[300px] / sm:max-w-[312px] / lg:-mt-[11.18rem] / lg:max-w-[452px] / xl:-mt-[11.52rem] / xl:max-w-[468px]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper class 合同，不改动 preview、radial cluster、Best Cases、help shell、`/chat` 共用 landing shell或 `3001` 发布边界，使 stacked auth/access module 稍更上贴且稍更接近 live support band 宽度。Review 1 功能/规格复核：sequential live/local fresh-load 与 browser snapshot 确认当前页面无新的 clipping / overlap / blank render，auth/access module 仍是首要剩余热点，但本轮后已更 integrated。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描无 hardcoded secret / shell / eval / pickle / SQL-formatting 命中，repo-scoped 独立 reviewer JSON 复审通过，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 的剩余 docking 热点继续做单一视觉切片，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点（2026-05-17 10:43 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 stacked auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前单一最小安全切片锁定为 auth/access module 仍是首屏最明显剩余热点：live 站点的 access module 在 preview 下方更宽、更略上贴，而本地 stacked auth wrapper/卡面仍稍偏窄、偏低。基于这组 live/local 结构与 fresh browser 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[1.84rem] / max-w-[292px] / sm:max-w-[304px] / lg:-mt-[10.86rem] / lg:max-w-[440px] / xl:-mt-[11.18rem] / xl:max-w-[456px]` 收敛为 `-mt-[1.96rem] / max-w-[300px] / sm:max-w-[312px] / lg:-mt-[11.18rem] / lg:max-w-[452px] / xl:-mt-[11.52rem] / xl:max-w-[468px]`，不改动 preview、radial cluster、Best Cases、help shell、`/chat` 共用 landing shell或 `3001` 发布边界，使 stacked auth/access module 稍更上贴且稍更接近 live support band 宽度；`src/app/page.test.tsx` 已同步锁定新的 wrapper class 合同。当前 diff 仍待在同一里程碑内完成全量 3 轮评审、todo.md 更新、中文 commit 与 push，下一轮若 residual hotspot 仍在 auth/access module，则继续围绕同一 docking 问题做更小步单点收敛。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的中心 text stack / CTA 节奏收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最明显且最适合小步落地的剩余热点已从 auth-card / radial cluster 收敛为 hero 中部 `Deep Literature Analysis` 文本栈：说明文段与 `Get started` CTA 相对中心 preview 虽未 broken，但 vertical rhythm 仍略松。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步收紧 `FeaturePreview` 标题区（主容器 gap `1.32 -> 1.2`、heading stack `space-y 0.82 -> 0.7`、顶部 padding `0.18 -> 0.12`、描述 max-width `32.3rem -> 31.4rem`、line-height `1.9 -> 1.82`、`sm` 字号 `1rem -> 0.99rem`、CTA top padding `0.34rem -> 0.18rem`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 锁定新的 spacing 合同。Review 1：live/local sequential browser QA 与 fresh local browser vision 共同确认 text stack 更紧凑、更有意图性，未引入 clipping / overlap，center preview 仍保持首屏主锚点，当前剩余热点回到左侧 radial labels 与正文关系。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、`curl` 复核 `/` `/chat` `/help/getting-started` 全部通过。Review 3：controller 侧新增 diff 静态安全扫描（secrets / shell / eval / pickle / SQL 注入均为 0），并完成 repo-scoped 独立 reviewer JSON 复审，未发现新的安全或逻辑阻塞问题。


- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，再按 systematic-debugging 顺序复现实测热点，确认当前单一最安全且最可落地的微调切片仍是右侧 auth-card 相对中心 preview 稍显独立：本地重建前 `browser_vision` 仍将 auth-card 视作首要热点，controller 几何也显示 preview→auth 仅约 `5.31px` 横向缝隙但 auth 自身仍偏窄（`218px`）且顶部略高于 preview（约 `8.63px`）。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 中把 `FeaturePreview` 右栏从 `228px` 小步放宽到 `236px`、将 `lg/xl gap-x` 从 `0.02/0.08rem` 收到 `0/0.04rem`、把 auth wrapper 进一步左推并下压到 `lg:-ml-[0.14rem] / xl:-ml-[0.1rem]` 与 `lg:mt-[0.34rem] / xl:mt-[0.42rem]`，同时把 `landing-auth-card` 本体放宽到 `max-w-[224px]` 并微调圆角、边框、阴影、blur 与 padding，使其更像贴附在中心 preview 侧边的 access module；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview 列宽、gap、auth wrapper 与 auth-card 样式合同。Review 1 功能/规格复核：重建后 sequential local browser QA 明确确认 auth-card 已不再是首要热点，当前新的主视觉热点诚实转移为中心 preview 自身更强、auth-card 退回次级注意力，且页面无新的 clipping/overlap/blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 0 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅保留“后续可补更行为化/视觉回归验证以降低 class 合同耦合”的非阻塞建议。下一轮继续优先围绕 public landing 中心 preview 过强、auth-card 退居次级后的新的层级平衡微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，并按 systematic-debugging 复现当前最小可安全落地的热点。重建前本地 browser vision 仍把 right auth card 判为首屏主热点，controller 几何也显示 preview/auth 组合仍略偏紧，因此本轮仅在 `src/components/landing-page.tsx` 将 `FeaturePreview` 右栏从 `236px` 小步收至 `232px`、把 `lg/xl gap-x` 从 `0/0.04rem` 放宽到 `0.08/0.12rem`、移除 auth wrapper 的额外左推并将其 `lg/xl mt` 微收至 `0.3/0.38rem`，同时把 `landing-auth-card` 本体从 `max-w-[224px]` 收敛到 `max-w-[220px]` 并同步减轻圆角、边框、阴影、blur 与 padding，使右侧入口退回更轻的辅助 access module；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 preview/auth class 合同。Review 1 功能/规格复核：rebuild 后 fresh-load local `browser_vision` 明确确认页面无明显 clipping / overlap / 荒谬间距，且当前单一主热点已从 right auth card 退回到 left radial cluster detached。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新的 security concern 或 logic error；下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 anchoring / attachment 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 hero 文案 / preview 纵向节奏收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，并按 systematic-debugging 顺序复现本地 browser QA 指出的当前主热点：相较 live，local 首屏单一最明显剩余差距已从 auth-card / radial cluster 收敛为 hero 顶部长说明文案与中心 preview 的纵向节奏略拥挤。基于该 root-cause 证据，本轮仅将 `src/components/landing-page.tsx` 的 `landing-hero` 大屏负上边距从 `lg:-mt-[2.38rem]` 小步回调到 `lg:-mt-[2.12rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 合同，使首页与 `/chat` 在保持其余 preview/auth/radial 结构不变的前提下，为 hero 标题区与中心 preview 重新留出更接近 live 的呼吸空间。Review 1 功能/规格复核：顺序 live/local fresh-load 对照与 rebuild 后 local `browser_vision` 确认页面无新的 broken/clipping/absurd spacing，且当前单一主热点仍是左侧 radial cluster，hero 文案 / preview 拥挤已较前一版减轻。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error；下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 anchoring / spacing 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster → 中心 preview 锚定收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 `3001` 前置核验，并顺序对照 live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 结构、browser vision 与 DOM 几何，确认当前单一最明显剩余 fidelity gap 仍是左侧 radial cluster 相对中心 preview 的连接感略弱，而 auth-card / vertical spacing 已退到次要问题。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步把三枚 radial nodes 与 label/connector 同步向中心 preview 回收：`Literature Analysis` 调整到 `left-[48.85%] top-[14.95%]`、label 改为 `left-[-1.58rem] top-[0.16rem]`、connector 收到 `w-[10.35%]`；`Data Mining` 调整到 `left-[48.7%] top-[46.55%]`、label 上提到 `top-[calc(100%-0.06rem)]`、connector 收到 `w-[10.2%]`；`Outcome Present` 调整到 `right-[15.6%] top-[13.8%]`、label 收近到 `left-[2.22rem]`、connector 收到 `top-[28.72%] w-[10.3%]`。`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 shared-route 几何合同。Review 1 功能/规格复核：fresh-load local browser QA 明确确认左侧 radial cluster 仍是首要热点，但本轮后已更贴近中心 preview，且未引入新的 clipping / overlap；Review 2 测试/构建/集成复核：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，无新增 security concern / logic error。下一轮继续优先围绕 public landing 左侧 radial cluster 与中心 preview 的残余 anchoring / spacing 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的真实文档层级对齐，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的顺序 fresh-load 复核，并以 systematic-debugging 先比对 root cause：live 运行态 `SC-XXXXXXXX` 输入与 `VERIFY ACCESS CODE` 按钮整体位于中心 preview 下方（`y≈956/1019`），而本地视觉已是下置模块，但 `FeaturePreview` JSX 仍把 `AuthCard` 节点写在 preview card 之前，导致 DOM/阅读顺序与 live 层级事实不一致。基于该证据，本轮仅在 `src/components/landing-page.tsx` 将 `AuthCard` JSX 区块移动到 `feature-preview-card` 之后，保持既有 `order-3` 样式与 `/`、`/chat` 共用 landing shell 不变，从而在不改动视觉布局的前提下让代码结构、DOM 顺序与 live 层级重新一致。Review 1 功能/规格复核：sequential live/local 对照 + local DOM 几何确认 preview card 仍位于 auth module 之上，页面无新增 clipping/overlap，且当前变更精准对应真实剩余合同而非盲目改样式；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check`、added-lines 静态安全扫描 `STATIC_SCAN_OK` 与 repo-scoped 独立 reviewer JSON 复审 `passed=true` 全通过，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先评估左侧 radial cluster 与中心 preview 的残余锚定差距。

- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 auth-card 的宽度与层级贴合收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，并按 systematic-debugging 顺序复现实测热点，确认当前单一最可安全落地的剩余偏差仍是本地 stacked auth-card 虽已位于 preview 下方，但宽度仍明显小于 live 公开页下置 access 模块，导致首屏右下入口仍偏窄、偏像局部卡片而非完整 access band。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 auth wrapper 从 `max-w-[320px]/sm:max-w-[332px]/lg:mt-0` 收敛为 `max-w-[320px]/sm:max-w-[332px]/lg:max-w-[620px]/xl:max-w-[640px]` 且小步下移到 `lg:mt-[0.08rem]/xl:mt-[0.12rem]`，并把 `landing-auth-card` 及其内层 shell 的大屏宽度放宽到 `lg:max-w-[420px]/xl:max-w-[436px]`，让 access 模块在保持下置层级的同时更接近 live 的横向展开读感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 stacked wrapper 与 auth-card 宽度合同。Review 1 功能/规格复核：sequential live/local fresh-load 对照与 DOM 几何确认本轮后 auth-card 宽度已扩到约 `436px`、wrapper 扩到约 `640px`，继续保持 preview 下方结构，页面无新的 clipping/overlap/blank render；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check` 与 added-lines 静态安全扫描 `STATIC_SCAN_OK` 通过，repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可补更行为化视觉回归以减少 class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先评估首屏剩余最大热点是否重新转回 left radial cluster 或 hero text-stack spacing。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的纵向 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 先复现当前单一最小可安全落地热点：live hero 中 access module 位于主 preview 下方且更贴附 preview，下置结构已对齐，但 local browser vision 仍把 auth-card 视为首要残余热点，主要原因是其相对 preview 仍略偏低、略 detached。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步上提（`-mt-[0.18rem] -> -0.28rem`、`lg:-mt-[2.2rem] -> -2.72rem`、`xl:-mt-[2.34rem] -> -2.88rem`），并补入稳定 `data-testid="feature-auth-wrapper"` 作为 focused 测试锚点；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步改为直接查询该 wrapper 并锁定新的上下文 class 合同。Review 1 功能/规格复核：sequential live/local 对照、重建后 local browser QA 与 DOM 几何确认 auth wrapper 顶部已较上一轮继续上提（preview→auth top gap 从约 `-20.17px` 收敛到 `-28.81px`），页面无新的 clipping / overlap / blank render，但 auth-card 仍是当前首屏单一主热点。Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅使用 `3001` 且未影响任何 `/root/nexus-mail` 现有服务与端口。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“未来可在不依赖 runtime test id 的前提下继续语义化锚点”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth-card 的残余 detached / hierarchy 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，保持 `/chat` 与 `/` 共用 marketing landing shell、继续仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 先锁定当前最小安全收敛切片：left radial cluster 仍比 live 略 detached，其中左上 `Literature Analysis` 节点与中心 preview 的锚定最值得先小步修正。基于 live/local 结构对照与本地 DOM 几何（调整前 literature label 右缘约 432.91px、preview 左缘约 403.27px，active 节点与 preview 仍存在约 29.64px 的横向叠压带），本轮仅在 `src/components/landing-page.tsx` 将 literature node 从 `left-[49.1%]/top-[14.35%]` 收到 `left-[46.6%]/top-[14.05%]`，并把其 connector 从 `w-[10.55%] rotate-[-10deg]` 调整为 `w-[12.4%] rotate-[-9deg]`，让左上节点更贴近中心 preview 且不改动其它节点、auth-card 或端口；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新对应几何 class 合同。Review 1 功能/规格复核：fresh-load live/local 结构与本地运行时几何确认 literature node 已向 preview 再回收一小步（label 右缘约 424.55px，与 preview 左缘的叠压带收窄到约 21.28px），hero 未出现新的 crowding、clipping 或 blank render，left radial cluster 相对中心 preview 更 cohesive；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`pnpm --dir /root/sciclaw test`、`docker compose config`、`docker compose up -d --build web` 与 Python HTTP 200 校验全部通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check` 与 added-lines 静态安全扫描均通过，repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可补更完整的 literature connector/top offset 断言”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 residual hotspot 是否仍在 radial cluster 其余节点，还是已转回 preview/auth/text-stack 的细小纵横节奏差异。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 stacked auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 先锁定当前最小安全收敛切片：本地首屏已与 live 一样保持 preview 上、auth module 下的 stacked 层级，但 auth-card 仍略偏窄、略偏低，导致右下 access module 还有轻微 detached 感。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步放宽并上提 `feature-auth-wrapper` / `landing-auth-card` / inner shell（wrapper `max-w 340/352 -> 352/364`、`lg/xl -mt -2.64/-2.8 -> -2.52/-2.68`，card `max-w 262 -> 276`、inner shell `296 -> 308`，并轻微放松圆角/边框/阴影/渐变），同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused class 合同。Review 1 功能/规格复核：live/local 顺序对照与 rebuild 后本地 DOM 几何确认 auth wrapper 扩到约 `640px`、auth-card 扩到约 `628px`，继续保持 preview 下方 stacked 层级，页面无新的 clipping / overlap / blank render，auth-card 的 detached 感较上一轮继续减弱。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断首屏剩余最大热点是否转回 left radial cluster / hero text-stack spacing。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 hero 文案 / visual-group 纵向节奏收敛，不触碰 `/root/nexus-mail` 工作树、端口或既有服务；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 前置核验，并按 systematic-debugging 顺序复现实测热点，确认当前单一最安全且最可落地的微调切片是 local 首屏标题说明文案与下方 hero visual group 仍略显拥挤：live `desc→hero-title` 几何约 `281.95px`，本地改动前约 `24.31px`，且 `desc→landing-hero` 仍为 `-0.95px` 轻微侵入。基于该 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `landing-hero` 的大屏负上边距从 `lg:-mt-[1.56rem]` 小步回调到 `lg:-mt-[1.28rem]`，不给 auth-card / radial cluster / utility row 引入额外结构改动，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的对应 class 合同。Review 1：live/local fresh-load 结构与本地 controller 几何复核确认首屏仍保持 coherent，文案与 hero 之间的纵向关系较上一轮更松一小步，`gapDescToHero` 从约 `-0.95px` 改善到约 `3.53px`、`gapDescToHeroTitle` 从约 `24.31px` 提升到约 `28.8px`，未出现新的 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`（2 files / 15 tests passed）、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`curl` 复核 `/` `/chat` `/help/getting-started` 均 200。Review 3：`git diff --check` 通过，added-line 静态安全扫描无 secrets / shell / eval / pickle / SQL-format 命中，repo-scoped 独立 reviewer 对当前 diff 返回 `passed=true`；browser_vision 本轮仍退化为无内容截图，因此本轮视觉结论以 sequential browser snapshot + browser_console 几何 + build/HTTP/controller QA 为准。下一轮继续以 fresh-load live/local 对照决定是否转向 left radial cluster 锚定，或在 hero 顶部说明文案与 visual group 之间再做更小步节奏收敛。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的左侧 radial cluster → 中心 preview 锚定收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 先锁定当前最小安全收敛切片：在 auth/access module 已退居次级热点后，local browser vision 与 live 几何对照一致表明左侧 radial cluster 中 `Outcome Present` 子节点仍是最 detached 的局部。基于 live/local 结构、browser QA 与本地运行时几何，本轮仅在 `src/components/landing-page.tsx` 将 `Outcome Present` node 小步 inward + upward（`right-[19.45%] -> 17.9%`、`top-[14.4%] -> 13.55%`），同步把其 connector 调整为 `top-[27.92%] w-[10.74%] rotate-[11deg]`，并把标签位置从 `left-[2.72rem] top-[0.06rem]` 收敛到 `left-[2.52rem] top-[-0.04rem]`，让 outcome/present 分支更贴近中心 preview 且不改动 auth-card、help shell、Best Cases、端口或 `/chat` 共用 landing shell；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新对应几何 class 合同。Review 1 功能/规格复核：sequential live/local fresh-load、local browser vision 与 DOM 几何确认本轮后 `Outcome Present` 分支较上一轮更 inward、更 upward、detached 感减弱，页面无新的 clipping / overlap / blank render，当前剩余可见热点仍主要在左侧 radial cluster 的整体锚定而非 auth module。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`pnpm --dir /root/sciclaw test`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕左侧 radial cluster 其余 residual anchoring / spacing 微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序对照、browser vision、DOM 几何、focused Vitest、lint、build、`docker compose config`、`docker compose up -d --build web`、HTTP 200、`git diff --check`、静态安全扫描与 repo-scoped 独立 reviewer 复审；当前 live/local 一致将 left radial cluster 视为首要剩余热点，且 local browser QA 进一步定位为 `Outcome Present` 标签最拥挤。本轮仅在 `src/components/landing-page.tsx` 将该标签收敛为更宽的单行文案（`max-w-[122px] + whitespace-nowrap`）并小步右移/上提到 `left-[2.66rem] top-[-0.02rem]`，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的对应 class 合同。Review 1：fresh-load live/local QA 确认剩余热点仍在 radial cluster，但 `Outcome Present` 标签由拥挤两段读感收敛为更平顺的单行标签，页面无新增 clipping/overlap。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过。Review 3：`git diff --check`、当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK` 与 repo-scoped 独立 reviewer JSON 复审 `passed=true` 全通过；下一轮继续优先围绕左侧 radial cluster 其余 residual anchoring / spacing 微差距推进。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的中心 preview 强度与柔和度小步收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前最小安全切片锁定为 center preview 相对 live 仍略偏 soft，而 left radial cluster 退居次级热点。基于 live browser_vision 将 center preview softness/strength 列为第二热点、local browser_vision 将其列为首要热点，以及 controller DOM 几何确认 preview card 已占主视觉中心这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步加深 preview card 的外层边框、主渐变与阴影，并同步加深内部 warm panel 与右侧 chart panel 的背景、边框与阴影，使中心 preview 更 crisp 一点、但不改动结构、auth stacked 层级、`/chat` 共用 shell 或 `3001` 端口；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新对应 class 合同。Review 1 功能/规格复核：sequential live/local 对照 + rebuild 后 local browser_vision 确认 center preview 已更强、更清晰，页面无新的 clipping、overlap 或 absurd spacing，当前首要剩余热点诚实收敛为 auth card 仍略抢眼偏低。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/`、`/chat`、`/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态、安全与契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅留下“未来可补更细的 inner panel 视觉合同断言”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth card hierarchy 与 left radial cluster anchoring 的残余高保真微差距推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前最小安全切片锁定为 auth/access module 仍是首屏最直接可收敛的残余热点。基于 live/local browser QA、fresh local `browser_console` 几何与现有 stacked 结构证据，本轮仅在 `src/components/landing-page.tsx` 小步上提并放宽 `feature-auth-wrapper`（`-mt-[0.82rem] -> -0.94rem`、`max-w-[332px]/sm:max-w-[344px] -> 344px/356px`、`lg:-mt-[5.58rem]/lg:max-w-[588px] -> -5.9rem/600px`、`xl:-mt-[5.88rem]/xl:max-w-[604px] -> -6.18rem/616px`），不改动 center preview、radial cluster、utility row、`/chat` 共用 landing shell 或 `3001` 端口；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth-wrapper class 合同。Review 1 功能/规格复核：fresh-load local `browser_vision` 明确确认 auth/access module 相对中心 preview 已更 attached、更像同一 hero stack，且页面无明显 clipping/overlap/blank render；当前更诚实的首要剩余热点已收敛为 auth-card 下缘与 `Best Cases` 之间的纵向 crowding，而不是 auth module 完全 detached。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描 `STATIC_SCAN_OK`、repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth-card 下缘与 `Best Cases` 的 vertical crowding，以及左侧 radial cluster 的次级 anchoring 微差推进，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth/access module 的纵向贴合微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 先将当前单一最小安全切片锁定为 auth/access module 虽已与 preview 形成 stacked 结构，但相对 live 仍略偏低、仍是首屏最明显剩余热点。基于 fresh local browser_vision 明确指出“auth/access module 仍是最明显 visual fidelity 问题，只是较前更 integrated”、以及 controller 侧 DOM 几何显示 `previewBottom_to_authTop` 已从约 `-86.09px` 收敛到 `-100.17px` 的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[1.02rem] / lg:-mt-[6.18rem] / xl:-mt-[6.46rem]` 小步上提为 `-mt-[1.18rem] / lg:-mt-[7.04rem] / xl:-mt-[7.34rem]`，不改动 preview、radial cluster、utility row、`/chat` 共用 landing shell 或 `3001` 端口；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth-wrapper class 合同。Review 1 功能/规格复核：sequential live/local 对照、fresh local browser QA 与 DOM 几何确认 auth/access module 相比上一版更贴近 preview、无新的 clipping / overlap / blank render，但仍诚实保留为当前首屏单一最大热点；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“未来可减少 Tailwind 精确 class 断言耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 残余 detached 感，或在其退居次级后转向 radial cluster / hero vertical rhythm 的下一处最小热点推进。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 stacked auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前单一最小安全切片锁定为 auth/access module 仍是首屏最明显剩余热点：live/public apply flow 继续表现为 preview 下方 access band，而 local browser QA 仍指出本地 auth-card 相对 preview 略偏低、略 detached，但已较前更 attached、未出现新的 clipping/overlap。基于 live/local 结构、fresh local browser_vision 与 controller 侧 DOM 几何（本轮前 auth `top≈976.17`、preview `bottom≈1095.22`、`gapAuthToBest≈32.95px`）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步上提并微收 `feature-auth-wrapper`：从 `-mt-[1.42rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[8.22rem] / lg:max-w-[592px] / xl:-mt-[8.52rem] / xl:max-w-[608px]` 收敛为 `-mt-[1.62rem] / max-w-[332px] / sm:max-w-[344px] / lg:-mt-[8.9rem] / lg:max-w-[576px] / xl:-mt-[9.2rem] / xl:max-w-[592px]`，保持 preview card、radial cluster、utility row、help shell、`/chat` 共用 landing shell 与 `3001` 发布边界不变；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 auth-wrapper class 合同。Review 1 功能/规格复核：sequential live/local fresh-load 对照与 rebuild 后 local browser QA 确认 auth/access module 仍是当前首屏最大剩余热点，但已比上一轮更 attached、较少压向 `Best Cases` 过渡区，且页面无新的 broken / clipping / overlap / blank render；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“后续可补更行为化视觉回归、减少精确 Tailwind class 耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否仍为单一最大热点，或是否应转向 left radial cluster / hero text-stack spacing 的剩余微差。


- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前单一最小安全切片锁定为 auth/access module 仍是首屏最明显剩余热点。基于 fresh local browser_vision 继续将 auth/access module 判定为首要残余热点、且 controller 几何显示 auth wrapper 宽度仍为 `592px`、preview→auth overlap 约 `129.92px` 的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 小步上提并收轻 `feature-auth-wrapper` / `landing-auth-card` / inner shell：wrapper 从 `-mt-[1.62rem] / max-w-[332px] / sm:max-w-[344px] / lg:-mt-[8.9rem] / lg:max-w-[576px] / xl:-mt-[9.2rem] / xl:max-w-[592px]` 收敛为 `-mt-[1.8rem] / max-w-[324px] / sm:max-w-[336px] / lg:-mt-[9.34rem] / lg:max-w-[564px] / xl:-mt-[9.64rem] / xl:max-w-[580px]`，并同步将 auth card 外壳/inner shell 从 `262/580/596` 与 `294/580/596` 收至 `254/568/584` 与 `286/568/584`，同时轻微减弱圆角、边框、渐变、阴影与 blur，使 stacked access module 更贴近中心 preview、视觉上更轻；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper / auth-card / inner-shell 合同。Review 1 功能/规格复核：rebuild 后 fresh-load local `browser_vision` 明确确认 auth/access module 已比上一轮更 attached、略更轻，页面无明显 clipping / overlap / broken spacing，且当前最大剩余热点仍诚实保留为 auth/access module 本身；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否终于退居次级热点，或是否转向 left radial cluster / hero text-stack 的下一处最小残余差距。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps` 与 live `https://sciclaw.ai/` / local `http://127.0.0.1:3001/` fresh-load 顺序复核，再按 systematic-debugging 将当前最小安全切片锁定为 auth/access module 仍是首屏单一最明显剩余热点。基于 live `browser_console`（access input/button 位于 preview 下方）与 local rebuild 后 browser vision/几何（auth wrapper 仍与 preview 有约 `-129.92px` 垂直叠接、但整体未 broken/clipped）的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 stacked auth rail 的大屏贴合再小步收敛：`feature-auth-wrapper` 从 `lg/xl -mt 9.34/9.64rem -> 8.9/9.2rem`、`lg/xl max-w 564/580px -> 548/564px`，并同步把 `landing-auth-card` 与其 inner shell 的 `lg/xl max-w` 从 `568/584px -> 552/568px`，以减轻 auth module 对中心 preview 与 Best Cases 过渡区的压迫感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新 class 合同。Review 1：fresh-load live/local 对照与本地 browser vision 确认当前页无新的 clipping/absurd spacing，残余热点仍是 auth/access module placement，但本轮改动后其视觉压迫感已较前收敛。Review 2：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` HTTP 200 均已通过。Review 3：`git diff --check`、controller 静态安全扫描与 repo-scoped 独立 reviewer 均未发现新增安全/逻辑问题；下一轮继续优先围绕 auth/access module 与中心 preview 的残余 docking 热点推进。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的 hero → Best Cases 纵向衔接收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、Next.js 文档提示复核、live `https://sciclaw.ai/chat` 与 rebuilt local `http://127.0.0.1:3001/chat` 的顺序 fresh-load 结构核验，再按 systematic-debugging 将当前最小安全切片锁定为 hero 底部与 Best Cases 起始标题之间的章节过渡仍略显拥挤，而非端口、构建、路由或 auth/access 结构错误。基于本地 DOM 几何（`gapHeroToBest ≈ 35.19px`）与 fresh-load local 快照，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的纵向起始间距从 `mt-[1.86rem] / sm:mt-[2.2rem]` 收敛到 `mt-[1.34rem] / sm:mt-[1.72rem]`，并同步把标题块底边距从 `mb-6.5` 收到 `mb-5.5`，让 Best Cases 更早承接 hero 视觉流；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新该 spacing 合同与默认 slide 断言。Review 1 功能/规格复核：顺序 live/local fresh-load 与 browser snapshot 确认 `/chat` 继续保持 marketing landing shell，hero、onboard、Best Cases 默认态未漂移；browser_vision 本轮返回空内容，已按降级流程以 snapshot + DOM 几何作为 controller 事实来源。Review 2 测试/构建/集成复核：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl` 校验 `/chat` HTTP 200 全部通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：`git diff --check`、controller 静态安全扫描与 repo-scoped 独立 reviewer JSON 复审均通过，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断首屏剩余主热点是否已转向 auth/access module 与 radial cluster 的细部贴合，而不是继续放大章节间距。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 stacked auth/access module 仍是首屏最明显剩余热点：local browser vision 明确认为该模块已比前一版更 integrated，但依旧略偏低、略 detached，而 left radial cluster / hero spacing 仍属次级问题。基于 live/local 几何对照（live `VERIFY ACCESS CODE.bottom≈1066.5`、`Best Cases.top≈1311.25`；本地改动前 `VERIFY ACCESS CODE.bottom≈1174.59`、`Best Cases.top≈1390.72`）与 rebuild 后 local 几何（改动后 `VERIFY ACCESS CODE.bottom≈1170.11`、`Best Cases.top≈1386.23`，`gapAuthToBest≈216.13px`）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[2.08rem] / lg:-mt-[9.88rem] / xl:-mt-[10.18rem]` 小步上提为 `-mt-[2.32rem] / lg:-mt-[10.12rem] / xl:-mt-[10.46rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared class 合同；其余 preview、radial cluster、Best Cases、help shell 与 `/chat` 共用 landing shell 均保持不变。Review 1 功能/规格复核：sequential live/local fresh-load、browser vision 与 DOM 几何一致确认 auth/access module 仍是当前首要残余热点，但本轮后已略微上提、与主 preview 更贴合，页面无新的 clipping / overlap / blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅保留“未来可减少精确 Tailwind class 断言耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 的剩余 detached/low 感继续做小步收敛，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为左侧 `Literature Analysis` radial node 仍比 live 略 detached。基于 live/local 对照、fresh local browser_vision 将 left radial cluster 重新判为首要 hotspot，以及 controller 侧小步几何调整仅覆盖 literature node 的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 literature node 向中心 preview 轻推并微下置（`left 47.8% -> 48.6%`、`top 13.25% -> 13.6%`），同步把 connector 收到 `w-[10.8%] rotate-[-7deg]`、label 锚点收近到 `left-[-1.66rem] top-[0.04rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 focused 几何合同；未改动 auth/access module、Best Cases、help shell、`/chat` 共用 landing shell 或 `3001` 发布边界。Review 1 功能/规格复核：fresh local browser QA 明确确认页面无明显 clipping / overlap / blank render，left radial cluster 尤其 literature 分支较上一轮更贴近中心 preview；auth/access 与 hero→Best Cases handoff 目前并未变坏，但仍属次级 residual hotspot。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、启动后重试的 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过；其中 compose 重建后的首次 HTTP 命中出现短暂 `Connection reset by peer`，已按既有启动竞态经验在短暂等待后重试通过，未误判为应用回归。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error，仅留下“未来可补更完整 literature top/connector 断言”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 left radial cluster 是否仍是首要残余热点，或是否应转向 auth/access module 与 hero→Best Cases handoff 的次级微差。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 stacked auth/access module 仍是 fresh-load 首屏最明显剩余热点：live 运行态 access rail 仍明显位于 preview 下方且宽约 `599.5px`，本地改动前 auth wrapper / card 分别约 `520px / 520px`，同时 local DOM 几何显示 preview→auth 仍有约 `-141.45px` 叠接、auth→Best Cases 约 `116.47px`。基于这组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步放宽并略微下调收口（`-mt-[2.36rem] -> -mt-[2.24rem]`、`312/324 -> 320/332px`、`lg:-mt-[9.58rem] -> -9.18rem`、`lg/xl max-w 504/520 -> 520/536px`），并同步将 `landing-auth-card` / inner shell 从 `244/504/520`、`278/512/528` 放宽到 `252/520/536`、`286/528/544`，让 access module 更接近 live 的 support-band 宽度、同时维持 stacked 层级；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper / auth-card / inner-shell 合同。Review 1 功能/规格复核：fresh live/local 顺序核验、DOM 几何与 browser snapshot 共同确认 auth/access module 仍是当前首要残余热点，但本轮后已更宽、更接近 live support band，且未引入新的 clipping / overlap / blank render；两次 `browser_vision` 均退化为 `Vision analysis returned no content.`，已按 browser-verification-degradation-fallback 以 fresh snapshot + DOM/HTTP/controller 事实作为本地视觉验收依据。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“未来可补更视觉行为化回归、减少精确 class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否仍是单一主热点，或是否应转向 left radial cluster / hero→Best Cases handoff 的残余微差。


- [~] 执行检查点（2026-05-17 07:52 UTC）：本轮严格只在 `/root/sciclaw` 内推进 public help-shell 文档合同收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/port、live `https://sciclaw.ai/help/persona` / `/help/im` 与 local `http://127.0.0.1:3001/help/persona` / `/help/im` 的顺序复核，确认此前本地 `AI Persona` 与 `Connect Messaging Apps` 仍保留较早的 bridge-style 文案、错误的页面标题/next-link 关系，且 persona 缺少与 live 一致的 related links。当前里程碑已将 `src/app/landing-data.ts` 中 persona/im 文章数据改为更接近 live 的标题、段落、章节与 related-link / next-link 合同，在 `src/components/help-shell.tsx` 增加通用 `relatedLinks` 渲染以支持 persona 页底部双链接，并在 `src/app/help/help-page.test.tsx` 增补对应 focused assertions；controller 侧已完成 focused Vitest（22 tests）、lint、Next build、`docker compose config`、`docker compose up -d --build web`、静态安全扫描、local browser 快照与 repo-scoped reviewer 复审。剩余未在本 diff 内解决的 live/local 差距已保持诚实记录：live `/help/persona` 右侧 TOC 还多一项 `Why SciClaw Uses File-Based Persona Records`，live `/help/im` 的 Telegram/Discord 区块语义更接近可折叠按钮卡，本地当前仍为普通 section/list 结构；后续切片应继续围绕这两处 help-shell 细节收敛，而不是误报 persona/im 已完全 1:1。
- [~] 执行检查点（2026-05-17 09:01 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 public help-shell 文档合同收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、live `https://sciclaw.ai/help/persona` 与 local `http://127.0.0.1:3001/help/persona` 的 sequential fresh-load 复核，按 systematic-debugging 将当前最小安全切片锁定为 `/help/persona` 仍缺少 live 右侧 TOC 第四项 `Why SciClaw Uses File-Based Persona Records`，而不是 shell 结构、端口或构建问题。基于 live/local 对照证据，本轮仅在 `src/app/landing-data.ts` 为 persona 文章补入第四个 section 与对应说明段落，并在 `src/app/help/help-page.test.tsx` 同步补强 heading / TOC button / 段落断言；未改动 `/help/im` 现有 section/list 结构，也未触碰 landing、`/chat`、`/root/nexus-mail` 或任何非 `3001` 端口。Review 1 功能/规格复核：fresh local browser snapshot 现已确认 `/help/persona` 与 live 一样出现四个 `On this page` 按钮，且新增标题与说明段落正常渲染，无新的 broken / clipping / route drift。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` `/help/persona` `/help/im` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：added-lines 静态安全扫描无 hardcoded secret / shell injection / eval-exec / pickle / SQL-formatting 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现当前 diff 新增 security concern 或 logic error。当前剩余诚实 gap 仍是 live `/help/im` 更像可折叠按钮卡，而本地仍为普通 section/list 结构；下一轮应继续围绕该 help-shell 细节推进，而不是误报 persona/im 已完全 1:1。
- [~] 执行检查点（2026-05-17 11:47 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；按 phase-driven-autonomous-execution、systematic-debugging、`sciclaw-marketing-landing-parity`、`sciclaw-auth-card-detachment-parity`、`sciclaw-shared-landing-home-chat-parity` 与 `browser-verification-degradation-fallback` 先完成 repo/git/status、`.git/info/exclude`、3001 端口与 live/local fresh-load 结构复核。root-cause 证据表明当前最小安全切片仍是 auth host column 的轻量收窄/上提：live 仍缺少本地 `data-testid`，只能用结构快照确认 marketing shell 未漂移；live `browser_vision` 继续降级为 no-content，故改以 local `browser_console` 几何 + focused/full Vitest + lint/build + compose rebuild + HTTP 200 + repo-scoped reviewer 作为验收事实。本轮仅将 `feature-auth-wrapper` 收窄并轻提（`max-w 296/308/444/460 -> 292/304/436/452`，`lg/xl mr 0.3/0.32 -> 0.32/0.34`，`lg/xl -mt 11.3/11.64 -> 11.42/11.76`），并同步 `/` 与 `/chat` 测试合同。重建后 local 几何显示 auth host 宽度 `460 -> 452`、预览到 auth 的重叠量 `465.11 -> 457.44`、auth 顶部再上提约 `1.92px`，`gapAuthToBest` 仍约 `105.91px`；local browser QA 认为 auth/access 卡片更窄、更贴近 preview，但仍保留轻微独立感，说明本轮是有效微收敛而非终局，后续继续从 auth-card 独立感与 hero→Best Cases handoff 二者中择一推进。
- [~] 执行检查点（2026-05-17 15:58 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/git/status、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load 顺序对照、local `browser_vision` 与 controller-side DOM 几何，确认当前单一最明显剩余热点已重新回到左侧 radial cluster，而 auth/access module 退为次级问题。基于该 root-cause 证据，本轮仅对 `src/components/landing-page.tsx` 的 `Literature Analysis` 分支做 geometry-only 微收敛：节点从 `left/top 49%/13.4% -> 49.45%/13.2%`，connector 从 `48.5%/27.9%/10.1%/-6deg -> 48.7%/27.62%/9.6%/-5deg`，label 从 `left/top -2.1rem/0.42rem -> -1.92rem/0.34rem`（`sm:left -2.16rem -> -1.98rem`），并同步更新 `/` 与 `/chat` focused 测试合同。Review 1 功能/规格复核：fresh local browser QA 明确确认左侧 radial cluster 仍是首要剩余热点，但 `Literature Analysis` 分支较上一轮更 inward、更贴近中心 preview，且未引入新的 clipping / overlap / blank render。Review 2 测试/构建/集成复核：focused `pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、full `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断左侧 radial cluster 是否仍是首要残余热点，或是否应转向 auth/access module 与 hero→Best Cases handoff 的次级微差。
## Validation / review cadence
- [~] 执行检查点（2026-05-18 00:30 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。按 systematic-debugging 先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 顺序复核；controller 侧结合 local `browser_vision` 与 `browser_console` 几何确认当前首屏主要剩余热点仍是右侧 auth/access card 相对中心 preview 的 detached/heavy 感，而非硬性裁切错误，因此仅做更小步的 auth-card visual-weight reduction：收窄 `feature-auth-wrapper` / `landing-auth-card` 桌面宽度、略减右移并下压 overlay、同步降低边框 / 渐变 / 阴影 / blur 强度；随后完成 focused Vitest（`src/app/page.test.tsx` + `src/app/chat/page.test.tsx`）、full `npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web`、`/` 与 `/chat` HTTP 200、静态安全扫描以及独立 reviewer 复核。最新本地浏览器验收表明 auth/access card 相比上一轮已更轻、更少 detached，且 hero → Best Cases 过渡仍无明显 clipping / overlap breakage；下一轮继续围绕首屏剩余贴合度与 section handoff 做小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 hero 下方 stacked auth/access module 与 `Best Cases` 起始区之间的纵向 handoff 仍略偏紧，而非 auth/access 结构、宽度或端口/构建问题。基于 live/local fresh-load 几何对照（live `gapAuthToBest≈244.75px`，本地改动前 `gapAuthToBest≈102.72px`）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 6.08/6.42rem` 小步回调到 `6.56/6.92rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的对应 class 合同，使 auth module → Best Cases 的章节切换更接近 live 的 roomier handoff，而不改动 auth/access、preview、radial cluster、`/chat` 共用 landing shell 或 `3001` 发布边界。Review 1 功能/规格复核：sequential live/local fresh-load、browser snapshot 与 local DOM 几何确认本轮后 `Best Cases` 起始区相对 auth module 下移到 `gapAuthToBest≈110.72px`，页面无新的 clipping / overlap / blank render；live/local `browser_vision` 本轮均再次退化为空内容，因此视觉结论按降级流程以 fresh snapshot + DOM/HTTP/controller 事实为准。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否转回 auth/access module、left radial cluster anchoring 或 center preview 层级微差，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 hero→Best Cases 纵向 handoff 仍略紧，而非 auth/access module、left radial cluster 或路由结构问题。基于 live/local fresh-load 结构与 DOM 几何（live `auth.bottom≈1002.75`、`best.top≈1223.25`，local 改动前 `best.top≈1375.69` 且 section 间距更大）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的顶部间距从 `mt-[2.42rem] / sm:mt-[2.84rem] / lg:mt-[6.56rem] / xl:mt-[6.92rem]` 小步收敛到 `mt-[2.08rem] / sm:mt-[2.48rem] / lg:mt-[6.08rem] / xl:mt-[6.42rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的对应 class 合同，使 Best Cases 更早承接 hero 而不改动 auth/access、preview、radial cluster、`/chat` 共用 landing shell 或 `3001` 发布边界。Review 1 功能/规格复核：sequential live/local fresh-load、browser snapshot 与 local DOM 几何确认本轮后 `Best Cases` section 更早进入首屏后续视觉流，页面无新的 clipping / overlap / blank render；browser_vision 对 live/local 均退化为空内容，因此本轮视觉结论按降级流程以 fresh snapshot + DOM/HTTP/controller 事实为准。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否转回 auth/access module、left radial cluster anchoring 或 center preview 层级微差，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，继续仅使用 `3001`，且未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前单一最小安全切片锁定为 hero 下方 stacked auth/access module 与 `Best Cases` 起始区之间的纵向 handoff 仍略偏紧，而非 auth/access 结构、路由或构建问题。基于 live/local 几何对照（live `gapAuthToBest≈308.5px`，本地改动前 `gapAuthWrapperToBest≈86.39px`）与 fresh rebuild 后本地几何（改动后 `gapAuthWrapperToBest≈93.44px`、`gapAuthToBest≈243.08px`），本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 的 desktop 顶部间距从 `lg/xl 5rem/5.4rem` 小步放宽到 `5.42rem/5.84rem`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 class 合同，从而让 auth module → Best Cases 的章节切换更接近 live 的 roomier handoff 且不改变 hero/auth 既有结构。Review 1（功能/规格）：fresh live/local 顺序核验确认 `/` 与 `/chat` 仍共用 landing shell、auth/access module 保持 stacked 结构，热点聚焦为 handoff 过紧；本次改动后本地 `Best Cases` heading 下移约 `7.05px`，结构与文案均未回退。Review 2（测试/构建/集成）：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose config`、`docker compose up -d --build web`、`curl -I http://127.0.0.1:3001/` 全部通过，容器仍只发布 `3001->3000`。Review 3（静态/安全/契约）：`git diff --check` 通过；added-lines 静态安全扫描对 hardcoded secret / shell injection / eval-exec / pickle / SQL formatting 均为 0；repo-scoped 独立 reviewer 复核当前 diff 为一致的 spacing-only 调整、无新增逻辑或安全问题。当前 remaining hotspot 仍以 hero 左侧 radial cluster 与 center preview 的细微保真差距为主，下一轮应继续在不动 `/root/nexus-mail` 的前提下做新的单一视觉切片。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth/access module 收轻与贴合微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前最小安全切片锁定为 auth/access module 仍是首屏单一最大剩余热点，但问题已收敛为相对 preview 仍略显悬浮、且与 Best Cases 起始标题之间的章节过渡仍偏紧，而非结构错位、clipping 或端口/构建问题。本轮仅在 `src/components/landing-page.tsx` 小步收轻并收窄 stacked auth rail：`feature-auth-wrapper` 从 `-mt-[1.8rem] / 324-336px / lg:-mt-[9.04rem] / lg:548px / xl:-mt-[9.34rem] / xl:564px` 收敛为 `-mt-[1.86rem] / 316-328px / lg:-mt-[9.18rem] / lg:532px / xl:-mt-[9.48rem] / xl:548px`，并同步把 `landing-auth-card` 与 inner shell 从 `248/544/560`、`286/552/568` 收轻到 `244/528/544`、`278/536/552`，同时微降圆角、渐变、阴影与 blur，使 access module 更轻、更少 detached；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper / auth-card / inner-shell 合同。Review 1 功能/规格复核：fresh local browser_vision 明确确认 auth/access module 已比上一轮更轻、略更 attached，未出现明显 clipping，当前最大剩余热点诚实保留为 auth module 与 Best Cases 之间的章节竞争；Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 与 Best Cases 章节节奏的残余热点继续小步收敛。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B 的首屏 auth/access module docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 锁定当前最小安全切片：本地 stacked auth/access module 虽仍是首屏最明显剩余热点，但问题已收敛为相对 preview 仍略偏低、hero→Best Cases 过渡仍稍紧，而非结构错误、宽度失真或新的 clipping/overlap。本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步上提并略放宽（`-mt/max-w` 由 `1.68rem/316-328px/8.62-8.92rem/540-556px` 收敛为 `1.8rem/324-336px/9.04-9.34rem/548-564px`），并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper 合同，保持 `/` 与 `/chat` 继续共用同一 marketing landing shell。Review 1 功能/规格复核：顺序 live/local QA 与 fresh local browser vision 确认 auth/access module 仍是当前单一主热点，但已较上一轮更贴近 preview、宽度更接近 live support band，且页面无新的 broken/clipping/overlap；left radial cluster 与 Best Cases 过渡仍为次级残余问题。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“未来可补更行为化视觉回归以减少精确 class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否仍是主热点，或是否应转向 hero→Best Cases 纵向节奏与左侧 radial cluster anchoring 的剩余微差。
- [~] 执行检查点：本轮严格只在 `/root/sciclaw` 内继续推进 public landing 首屏 auth-card 结构合同对齐，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 sequential fresh-load 结构复核，并用 `browser_console` 几何确认 root cause：live 运行态 access-code 输入/VERIFY 按钮整体位于主 preview 下方（`authInput.y≈956`、`authBtn.y≈1019.75`，明显低于 preview `y≈561.67`），而本地仍把 auth-card 作为 preview 右侧并列模块（`authCard.y≈482.36` 与 preview `y≈478.53` 近乎同高），说明当前剩余主偏差是结构层级错误而非单纯 spacing。基于该证据，本轮仅在 `src/components/landing-page.tsx` 将 `FeaturePreview` 从 `lg/xl` 两列 auth-sidecar 结构改为单列堆叠：移除 `lg:grid-cols-[minmax(0,1fr)_218px]` / `gap-x` / `col-start` / `row-start` 契约，使 heading 保持 `order-1`、preview card 改为 `order-2`、auth wrapper 改为 `order-3 mx-auto mt-[0.32rem] max-w-[260px] sm:max-w-[272px] lg:mt-[0.42rem]`，让 access module 回到 preview 下方次级模块；并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx`，将旧的 large-screen sidecar/grid 断言替换为新的 stacked-order / centered-width 合同，保持 `/` 与 `/chat` 共用 landing shell。Review 1 功能/规格复核：live/local fresh-load 结构与 DOM 几何均确认本轮变更方向与 live 层级一致，本地页面未出现新的 clipping、overlap 或 broken content；Review 2 测试/构建/集成复核：`pnpm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响任何 `/root/nexus-mail` 服务/端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 secret/shell/eval/pickle/sql-injection 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 live/local evidence 出发，评估结构回正后首屏剩余最大 fidelity 热点是否转移到 radial cluster 或 text-stack spacing。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 与中心 preview 的贴合度小步收敛；已完成 live/local fresh-load、browser vision、Vitest、lint、build、`docker compose config`、HTTP 200 与静态安全扫描，当前 diff 只剩 final reviewer + 文档状态收口。`docker compose up -d --build web` 受外部 `docker/dockerfile:1.7` registry 拉取网络不可达阻塞，已按 fail-closed 记录为外部环境问题而非应用回归，下一轮将继续在同一仓库内推进 review/commit/push。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量贴合收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps` 与 live/local fresh-load 顺序复核：live `https://sciclaw.ai/` 结构快照仍显示 access-code 模块位于主 preview 之后，而本地 `http://127.0.0.1:3001/` / `/chat` 继续保持同一 marketing landing shell、仅发布在 `3001`。结合现有 stacked auth-card 结构与 local 几何复核，root cause 已从“层级错误”收敛为“auth-card 仍略窄、略低、与 preview 的衔接仍偏松”，因此本轮只做一个小型 docking slice：在 `src/components/landing-page.tsx` 将 auth host wrapper 从 `mt-[0.08rem] max-w-[272px] sm:max-w-[284px] lg:mt-[0.16rem]` 调整为 `-mt-[0.08rem] max-w-[280px] sm:max-w-[292px] lg:mt-[0.04rem]`，同时把 `landing-auth-card` 自身从 `max-w-[218px]` 放宽到 `224px`，并轻微放松外层 border/background/shadow/blur/padding，让入口卡片更贴近 preview 且不再那么压缩；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 stacked auth wrapper 与 auth shell 的精确类合同，保持 `/` 与 `/chat` 继续同构。Review 1 功能/规格复核：sequential live/local 结构对照确认本轮仍围绕真实剩余热点推进，rebuild 后 local DOM 几何显示 auth wrapper 扩到 `292px`、auth card 扩到 `224px` 且顶部上移约 `1.9px`，页面无新增 clipping / overlap / 失真；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响任何 `/root/nexus-mail` 现有服务/端口；Review 3 静态/安全/契约复核：`git diff --check` 通过、added-lines 静态安全扫描无 secret/shell/eval/pickle/sql-injection 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，无新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth-card 是否已退为次要热点，或是否应转向 radial cluster / hero vertical rhythm 的剩余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量 docking 收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 先确认 root cause：当前本地 stacked auth-card 虽已位于 preview 下方，但仍比 live 公开页 access module 更宽、更低一小步，导致首屏右下入口仍稍显松散。基于该证据，本轮仅在 `src/components/landing-page.tsx` 将 auth wrapper 从 `-mt-[0.12rem]/max-w-[320px]/sm:max-w-[332px]/lg:mt-[0.08rem]/lg:max-w-[620px]/xl:mt-[0.12rem]/xl:max-w-[640px]` 收敛为 `-mt-[0.2rem]/max-w-[304px]/sm:max-w-[316px]/lg:mt-[0.02rem]/lg:max-w-[600px]/xl:mt-[0.06rem]/xl:max-w-[620px]`，并把 `landing-auth-card` 自身从 `max-w-[244px]` / `lg:max-w-[420px]` / `xl:max-w-[436px]` 收窄到 `236px / 404px / 420px`，同时同步内层 shell `max-w-[268px] / lg:max-w-[404px] / xl:max-w-[420px]`，让 access module 在继续保持下置层级的前提下略微上提、收窄、减轻 detached 感；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper / auth shell 合同，并保留旧大屏 top-margin 缺失断言。Review 1 功能/规格复核：live browser_vision 连续降级无有效内容，故按降级 fallback 改用 live/local sequential snapshot + DOM 几何作为 controller 事实来源；local rebuild 后几何确认 auth width 已从约 `436px` 收至 `420px`、wrapper 从 `640px` 收至 `620px`、preview 下缘到 auth 顶部 gap 从约 `19.17px` 收至 `18.22px`，且仍保持 preview 下方 stacked 层级、未引入 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `curl -I` 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅发布到 `3001` 且未影响 `/root/nexus-mail` 任意现有服务与端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 hardcoded secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，仅提示清理一个重复测试断言与补齐 `/`/`/chat` 断言对称性，均已在 controller 侧收口处理。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余最大热点是否重新转回 left radial cluster / hero text stack，而不是继续过度迭代 auth-card。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth-card 的轻量收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/branch/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 将当前最小安全切片继续锁定为 auth/access module 仍是首屏最大剩余热点。基于 live/local 对照、browser QA 与本地运行时几何，本轮仅在 `src/components/landing-page.tsx` 对 stacked auth rail 做一次小步收窄与减重：将 `feature-auth-wrapper` 从 `-mt-[0.32rem] / max-w-[352px] / sm:max-w-[364px] / lg:-mt-[4.18rem] / lg:max-w-[624px] / xl:-mt-[4.36rem] / xl:max-w-[640px]` 收敛为 `-mt-[0.18rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[3.7rem] / lg:max-w-[600px] / xl:-mt-[3.88rem] / xl:max-w-[616px]`，同时将 `landing-auth-card` 外壳从 `276px / 612px / 628px` 收窄到 `268px / 588px / 604px`，并轻微收紧 radius、border、background、shadow、blur 与 inner shell `300px / 588px / 604px`，使 access module 相对中心 preview 更轻、更窄、更少 detached；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 wrapper / auth shell / inner-shell 精确类合同，保持 `/` 与 `/chat` 继续同构。Review 1 功能/规格复核：sequential live/local 对照与 local browser QA 继续指向 auth/access module 仍是主热点，但本轮 rebuild 后 local 几何已确认 auth width `628px -> 604px`、wrapper width `640px -> 616px`，且页面无新增 clipping / overlap / blank render；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm test`、`npm run lint`、`npm run build`、`docker compose config`、`docker compose up -d --build web` 与 `/` `/chat` `/help/getting-started` 的 HTTP 200 全部通过，继续仅使用 `3001` 且未影响 `/root/nexus-mail` 任意现有服务或端口；Review 3 静态/安全/契约复核：`git diff --check` 通过，added-lines 静态安全扫描无 secret / shell / eval / pickle / SQL 注入命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新引入的 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth-card 是否已收敛到次级热点，或是否应转向 left radial cluster / hero composition 的剩余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/port、`.git/info/exclude`、live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` 的 fresh-load 顺序复核，并按 systematic-debugging 先将单一最小安全切片锁定为 auth/access module 仍略显 detached。基于 live/local 结构与 local browser QA，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[0.22rem] / max-w-[340px] / sm:max-w-[352px] / lg:-mt-[3.92rem] / lg:max-w-[600px] / xl:-mt-[4.1rem] / xl:max-w-[616px]` 收紧为 `-mt-[0.34rem] / max-w-[332px] / sm:max-w-[344px] / lg:-mt-[4.22rem] / lg:max-w-[588px] / xl:-mt-[4.42rem] / xl:max-w-[604px]`，在不改动 preview、radial cluster、端口或 `/chat` 共用壳的前提下，进一步上提并轻收 auth wrapper；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper class 合同。Review 1 功能/规格复核：fresh-load local browser_vision 与 DOM 几何确认 auth/access module 仍位于 preview 下方，视觉上比上一轮更紧、更少 detached，且已开始退居为次级热点；Review 2 测试/构建/集成复核：`npm exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、`npm run lint`、`npm run build`、`npm test`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全部通过，继续仅发布到 `3001`；Review 3 静态/安全/契约复核：`git diff --check` 与 added-lines 静态安全扫描通过，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断首屏主热点是否已经转向 left radial cluster 或 hero text/visual rhythm，而不是继续过度迭代 auth-card。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing 首屏 stacked auth/access module 的 docking 微收敛，不触碰 `/root/nexus-mail` 工作树、服务或端口；controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，并按 systematic-debugging 将当前最小安全切片锁定为 auth/access module 仍是首屏最直接的残余热点。基于 live DOM 几何（`SC-XXXXXXXX` / `VERIFY ACCESS CODE` 仍明显位于 preview 下方）与 local fresh-load 运行时几何（调整前 `authInput.bottom≈1118.20`、`bestHeading.top≈1295.36`，且 wrapper 与 preview 仍有较深叠接）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步上提为 `-mt-[2.08rem] / lg:-mt-[10.08rem] / xl:-mt-[10.42rem]`，保持宽度、preview、radial cluster、Best Cases、help shell、`/chat` 共用 landing shell 与 `3001` 发布边界不变；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper class 合同。Review 1 功能/规格复核：rebuild 后 sequential local fresh-load 与 controller 几何确认 auth module 相对 preview 继续上提（`authInput.bottom≈1106.36`、`bestHeading.top≈1283.52`），页面无新的 clipping / overlap / blank render，当前剩余热点仍诚实保留为 auth/access module 与整体 hero 层级的细微贴合差距。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`npm run lint`、`npm run build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、`git diff --check` 与 Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error，仅留下“视觉测试仍较依赖精确 Tailwind class 合同”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否仍是单一主热点，或是否应转向 left radial cluster / hero text rhythm 的残余微差。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 right auth/access module 仍是首屏最大剩余热点，而非 hero→Best Cases handoff 或 left radial cluster。基于 live/local 几何对照（live `authInput.bottom≈1002.75`、`bestHeading.top≈1311.25`；本地改动前 `authWrapper≈564px`、`authCard≈556px`、`gapAuthToBest≈110.72px`，且 local browser QA 仍将 auth module 视为最显著热点）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 stacked `feature-auth-wrapper` 做一次 wrapper-only 微收敛：由 `-mt-[2.32rem] / max-w-[320px] / sm:max-w-[332px] / lg:-mt-[9.74rem] / lg:max-w-[548px] / xl:-mt-[10.08rem] / xl:max-w-[564px]` 调整为 `-mt-[2.12rem] / max-w-[312px] / sm:max-w-[324px] / lg:-mt-[9.22rem] / lg:max-w-[540px] / xl:-mt-[9.56rem] / xl:max-w-[556px]`，在不改动 auth-card shell、preview、Best Cases、help shell 与 `/chat` 共用 landing shell 的前提下，让 auth/access module 略微下沉并收窄，减少“贴在 preview 上方的浮层感”；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper class 合同。Review 1 功能/规格复核：fresh live/local 顺序核验、DOM 几何与 local browser_vision 共同确认 auth/access module 仍是最大剩余热点，但最新 pass 后其相对 preview 已略更 integrated，页面未出现新的 clipping / overlap / blank render，仅保留轻微 preview 边缘 artifact 与 auth overlay 仍稍显独立这一诚实剩余问题。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 与 preview 的最终贴合度继续做小步视觉切片，而不触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点（2026-05-17 17:40 UTC）：本轮继续严格只在 `/root/sciclaw` 内推进 Phase B，保持本地仅使用 `3001` 且未触碰 `/root/nexus-mail` 工作树、服务或端口；先完成 repo/path/git/status、`.git/info/exclude`、3001 端口与 `docker compose ps` 只读核验，再顺序对照 live `https://sciclaw.ai/` 与 local `http://127.0.0.1:3001/` fresh-load。root-cause 证据显示 live 当前 hero→Best Cases handoff 仍比本地更松，且 live 视觉热点回到左侧 radial cluster；但 fresh local browser QA 与 DOM 几何确认本地更直接的单一剩余热点依旧是 auth/access rail 偏低、把 hero 尾部与 Best Cases 起始区压得更紧（改动前 `previewBottomToAuthTop≈-188.81px`、`gapAuthToBest≈92.16px`）。基于该证据，本轮仅在 `src/components/landing-page.tsx` 对 `feature-auth-wrapper` 做一次更小的 upward docking micro-pass：`lg/xl -mt` 从 `12.54/12.88rem` 收紧到 `12.82/13.16rem`，不改动 width、Best Cases spacing、left radial cluster、`/chat` 共用 landing shell 或任何非 `3001` 发布边界；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步更新 shared class 合同。Review 1 功能/规格复核：fresh live/local 顺序核验、local browser vision 与 controller-side DOM 几何确认 auth rail 上提约 `4.47px`，`previewBottomToAuthTop` 由 `-188.81px` 收敛到 `-193.28px`，页面未引入新的 clipping / overlap / blank render，当前 remaining gap 诚实保留为 `gapAuthToBest≈92.16px` 仍明显紧于 live 的更松 handoff。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：added-lines 静态安全扫描无 hardcoded secret / shell injection / eval-exec / pickle / SQL-formatting 命中，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断是否继续围绕 auth rail 与 Best Cases handoff 的残余紧张度推进，或转向 left radial cluster 的更高优先级视觉热点。
- [x] 本地参考资料保留在 `docs/reference-local/` 并通过 `.git/info/exclude` 忽略
- [x] 每个里程碑后执行 docker compose config / tests / build / compose up -d --build
- [x] 每个里程碑后执行 3 轮评审并提交中文 commit + push
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，未触碰 `/root/nexus-mail` 工作树、服务或端口，并继续仅使用 `3001`。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live 与 local fresh-load 顺序复核，再按 systematic-debugging 将当前最小安全切片锁定为 hero 下方 auth/access module 与 `Best Cases` 起始区之间的纵向衔接仍略偏紧，而不是 auth-card 自身结构错误。基于 live 几何（auth input `y≈956`、Best Cases heading `y≈1311`）与本地重建后几何（auth wrapper bottom `≈1256`、Best Cases heading top `≈1296.95`）的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 顶部间距从 `mt-[1.34rem] / sm:mt-[1.72rem]` 放宽到 `mt-[2.12rem] / sm:mt-[2.56rem]`，并把标题块底边距从 `mb-5.5` 放宽到 `mb-6.5`，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的相关 spacing 合同，使 hero→Best Cases 过渡更接近 live 的呼吸感。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前更真实的剩余热点是 hero→Best Cases handoff 偏紧，本轮放宽后未引入新的 clipping / overlap / blank render。Review 2 测试/构建/集成复核：focused Vitest（page/chat/help）、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose config`、`docker compose up -d --build web`、HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error；下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否回到 auth/access module 贴合或 left radial cluster anchoring，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 hero→Best Cases 纵向 handoff 仍略紧，而非 auth/access module 结构或路由问题。基于 live/local 几何对照（live `authInputBottom≈1002.75` 到 `bestHeadingTop≈1311.25`，local `authCardBottom≈1256` 到 `bestHeadingTop≈1357.11`，说明本地过渡仍更紧）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 顶部间距从 `mt-[2.36rem] / sm:mt-[2.88rem] / lg:mt-[5.42rem] / xl:mt-[5.84rem]` 放宽到 `mt-[2.68rem] / sm:mt-[3.1rem] / lg:mt-[5.88rem] / xl:mt-[6.32rem]`，并将标题块底边距从 `mb-7` 放宽到 `mb-[1.9rem]`，同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared spacing 合同与首页 Best Cases 默认态断言。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前最真实残余热点是 hero→Best Cases handoff 偏紧，本轮放宽后页面仍 coherent，未引入新的 clipping / overlap / blank render；browser_vision 本轮再次降级为空内容，已按降级 fallback 以 fresh snapshot + DOM/HTTP/controller 事实作为本地视觉验收依据。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm test`、`pnpm lint`、`pnpm build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、无新增 security concern 或 logic error，仅留下“未来可将一部分 spacing arbitrary values 抽象为共享 token”这一非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否转回 auth/access module 贴合或 left radial cluster anchoring，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 hero→Best Cases 纵向 handoff 仍略紧，而非 auth/access module 结构或路由问题。基于 live/local 几何对照（live `authInputBottom≈1002.75` 到 `bestHeadingTop≈1311.25`，local `authBottom≈1256` 到 `bestHeadingTop≈1296.95`，说明本地过渡仍更紧）这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `best-cases-section` 顶部间距从 `mt-[2.12rem] / sm:mt-[2.56rem]` 小步放宽到 `mt-[2.36rem] / sm:mt-[2.88rem]`，并将标题块底边距从 `mb-6.5` 放宽到 `mb-7`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared spacing 合同与首页默认态断言。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前最真实残余热点是 hero→Best Cases handoff 偏紧，本轮放宽后页面仍 coherent，未引入新的 clipping / overlap / blank render。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断剩余主热点是否转回 auth/access module 贴合或 left radial cluster anchoring，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。

- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 right auth/access module 仍是 fresh-load 首屏最明显剩余热点，而非 left radial cluster、center preview 或 hero→Best Cases handoff。基于 live/local 几何对照（live `authInput.bottom≈1002.75`、`bestHeading.top≈1311.25`；本地改动前 `authInput.bottom≈1106.36`、`bestHeading.top≈1386.88`，auth module 仍更靠近中心主视觉）与 local browser_vision 将 auth/access module 继续判为首要热点这一组 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 stacked auth wrapper 从 `-mt-[2.08rem] / max-w-[316px] / sm:max-w-[328px] / lg:-mt-[10.08rem] / lg:max-w-[532px] / xl:-mt-[10.42rem] / xl:max-w-[548px]` 收敛为 `-mt-[1.92rem] / max-w-[320px] / sm:max-w-[332px] / lg:-mt-[9.52rem] / lg:max-w-[536px] / xl:-mt-[9.84rem] / xl:max-w-[552px]`，只做 auth/access module 的轻微放宽与下沉收口，使其较上一轮更像 preview 下方的附属 access band、而不是过度上提的独立热点；`src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 已同步锁定新的 wrapper 合同。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local browser QA 确认 auth/access module 仍是当前单一主热点，但本轮后已更 integrated、未引入新的 clipping / overlap / blank render；browser_vision 第二次降级为空内容，已按降级 fallback 以 fresh snapshot + DOM/HTTP/controller 事实作为视觉验收依据。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`、未发现新增 security concern 或 logic error。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先判断 auth/access module 是否仍是首要热点，或是否应转向 left radial cluster / hero→Best Cases handoff 的下一处最小视觉切片。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 stacked auth/access module 仍是 fresh-load 首屏最明显剩余热点。基于 live/local 对照、local `browser_vision` 继续将 auth/access module 判为首要热点，以及 controller 侧运行时几何显示 preview→auth 仍有约 `-141.45px` 叠接、auth→Best Cases 仍约 `116.47px` 的 root-cause 证据，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 从 `-mt-[2.12rem] / lg:-mt-[9.22rem] / xl:-mt-[9.56rem]` 小步上提为 `-mt-[2.36rem] / lg:-mt-[9.58rem] / xl:-mt-[9.92rem]`，并同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper class 合同，在不改动 preview、radial cluster、Best Cases、help shell 与 `/chat` 共用 landing shell 的前提下，让 access module 更贴近中心 preview。Review 1 功能/规格复核：fresh live/local 顺序核验、local browser_vision 与 controller 几何共同确认当前页面无新的 clipping / overlap / blank render，auth/access module 仍是首要剩余热点，但已较上一轮更 attached；左侧 radial cluster 与 hero→Best Cases handoff 继续退居次级问题。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error，仅留下“未来可减少精确 Tailwind class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 的剩余 docking 热点继续做单一视觉切片，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
- [~] 执行检查点：本轮继续严格只在 `/root/sciclaw` 内推进 public landing Phase B，并继续仅使用 `3001`、未触碰 `/root/nexus-mail` 工作树、服务或端口。controller 侧先完成 repo/path/git/status/port、`.git/info/exclude`、`docker compose ps`、live `https://sciclaw.ai/` 与 rebuilt local `http://127.0.0.1:3001/` 的 sequential fresh-load 复核，再按 systematic-debugging 将当前单一最小安全切片锁定为 stacked auth/access module 仍是 fresh-load 首屏最明显剩余热点。基于 live/local 对照：live access band 宽度约 `599.5px`，本地改动前 auth wrapper / card 约 `516px / 516px`，且 `previewBottom_to_authTop≈-147.84px`、`authBottom_to_bestTop≈113.27px`，说明本地模块仍略偏窄、略偏上贴。围绕该 root cause，本轮仅在 `src/components/landing-page.tsx` 将 `feature-auth-wrapper` 小步放宽并略微下沉收口：`-mt-[2.32rem] -> -mt-[2.08rem]`、`max-w-[312px]/sm:max-w-[324px] -> 316px/328px`、`lg:-mt-[9.98rem]/lg:max-w-[500px] -> -9.62rem/504px`、`xl:-mt-[10.32rem]/xl:max-w-[516px] -> -9.96rem/520px`；同时同步更新 `src/app/page.test.tsx` 与 `src/app/chat/page.test.tsx` 的 shared wrapper class 合同，使 auth/access module 相较上一轮更接近 preview 下方的附属 access band，而非过度上提的独立热点。Review 1 功能/规格复核：sequential live/local fresh-load、DOM 几何与 local snapshot 共同确认当前页面无新的 clipping / overlap / blank render，auth/access module 仍是首要剩余热点，但本轮后已更 integrated；rebuild 后 `authWrap.width≈520px`、`previewBottom_to_authTop≈-142.09px`，较上一轮略放宽且略下沉。Review 2 测试/构建/集成复核：`pnpm --dir /root/sciclaw exec vitest run src/app/page.test.tsx src/app/chat/page.test.tsx src/app/help/help-page.test.tsx`、全量 `pnpm --dir /root/sciclaw test`、`pnpm --dir /root/sciclaw lint`、`pnpm --dir /root/sciclaw build`、`docker compose -f /root/sciclaw/docker-compose.yml config`、`docker compose -f /root/sciclaw/docker-compose.yml up -d --build web`、Python HTTP 200 校验 `/` `/chat` `/help/getting-started` 与 `git diff --check` 全通过，继续仅发布到 `3001`。Review 3 静态/安全/契约复核：当前 diff added-lines 静态安全扫描 `STATIC_SCAN_OK`，repo-scoped 独立 reviewer JSON 复审 `passed=true`，未发现新增 security concern 或 logic error，仅保留“未来可减少精确 Tailwind class 合同耦合”的非阻塞建议。下一轮继续从最新 worktree 与 fresh-load live/local 证据出发，优先围绕 auth/access module 的剩余 docking 热点继续做单一视觉切片，而不是触碰 `/root/nexus-mail` 任意工作树、服务或端口。
